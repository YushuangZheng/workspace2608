# 关系—进度信念驱动的少样本闭环对象中心多流操作

# 1. 研究定位

本研究面向少样本、多阶段机器人操作中的**执行时闭环一致性问题**：机器人仅从少量正常成功示范中学习操作策略，但测试环境中的动作执行、物体状态和多智能体协作关系可能受到未见扰动，使实际任务状态逐渐偏离示范策略内部的关系与进度假设。

本工作采用对象中心的多流概率策略作为底层技能表示。机器人末端、任务对象、环境实体以及其他机械臂均可构成参考系流；每条流描述机器人相对于相应实体的局部操作分布，多个流在统一动作空间中融合产生控制指令。采用这一策略表示，是因为相对几何关系比绝对世界坐标轨迹更适合少样本学习和动态场景泛化。

在此基础上，本研究进一步建立一个在线闭环，使策略在每个控制周期回答三个问题：

$$
\text{当前真实交互关系是什么？}
$$

$$
\text{当前实际执行进度在哪里？}
$$

$$
\text{当前是否已经满足进入下一技能的完整场景条件？}
$$

当当前状态仍能由正常示范解释时，系统进行保持、推进或局部重新对齐；当实际关系和对象状态无法由任何正常进度解释时，系统冻结正常任务推进，修复被破坏的交互关系，并从恢复后的真实状态重新进入任务。

本方法以在线关系后验和进度后验作为两个基本闭环变量：前者判断当前真实运动学关系，后者判断机器人实际执行到了哪里。关系推断、进度估计、技能边界放行和异常恢复由此被统一为一套完整的方法。

---

# 2. 核心科学问题

> **如何从少量正常成功示范中学习对象中心的多流操作模型、交互关系先验和技能入口状态，并在执行过程中利用机器人动作响应与当前场景观测，在线推断机器人—物体—其他智能体之间的真实关系与实际技能进度，从而在关系破坏、进度漂移或协作条件未满足时阻止错误技能推进，并恢复任务执行？**

该问题的核心在于判断：

$$
\boxed{
\text{当前完整对象中心状态是否仍能够由正常成功示范解释}
}
$$

其中“完整对象中心状态”由三个互补层次构成：

- 机器人末端相对于任务相关参考系的局部轨迹状态；
- 机器人与参考系之间的实际运动学关系；
- 当前技能相关实体自身及实体之间的稀疏场景构型状态。

如果当前状态可以由某个正常进度解释，则系统根据实际执行情况进行进度重新对齐、保持或推进；如果没有任何正常进度能够同时解释上述状态，则判定为关系—进度失配并进入恢复。

---

# 3. 问题定义与总体框架

## 3.1 少样本示范与匿名技能

设训练数据为：

$$
\mathcal D=\{\mathcal T^{(n)}\}_{n=1}^{N},
$$

其中：

- $N$ 为少量正常成功示范数量；
- $\mathcal T^{(n)}$ 为第 $n$ 条示范；
- 每条示范包含机器人状态、对象状态、控制动作以及各对象参考系的时间序列。

任务被划分为 $K$ 个匿名技能，第 $k$ 个技能包含 $T_k$ 个局部离散状态。完整任务进度表示为：

$$
s=(k,\tau),
\qquad
k\in\{1,\ldots,K\},
\quad
\tau\in\{1,\ldots,T_k\}.
$$

其中：

- $k$ 表示当前技能编号；
- $\tau$ 表示技能内部局部进度。

## 3.2 对象中心参考系

设：

- $e$ 表示当前机械臂末端；
- $f\in\mathcal F$ 表示一个候选参考系；
- $\mathcal F$ 可以包含任务对象、环境实体、目标区域或对侧机械臂末端；
- $T_{e,t}^{W}$ 表示控制周期 $t$ 时末端在世界坐标系中的位姿；
- $T_{f,t}^{W}$ 表示参考系 $f$ 在世界坐标系中的位姿。

末端相对于参考系 $f$ 的局部位姿为：

$$
\xi_{f,t}
=
\left(T_{f,t}^{W}\right)^{-1}
T_{e,t}^{W}.
$$

该相对表示使策略能够在对象或其他智能体发生移动时，依据最新参考系位姿重新实例化动作目标。

当 $f$ 是对侧机械臂末端时，还需要区分“动作参考”与“边界就绪条件”。Eq.6 在同步
双臂示范中选中对侧末端，只能说明它具有统计预测能力，不能单独证明连续控制依赖。方法
因此在每个技能和内部模态上进行结构化执行依赖筛选：本臂与对臂通过同一实体的活动
LINK/`LINK_PENDING` 形成共享或交接关系时保留；没有普通物理任务参考系、且对侧末端
构成跨示范稳定的定向相对几何目标时也保留。若普通物体/目标参考系已经解释动作且不存在
上述关系路径，则把对侧末端从闭环动作和进度流中删除。纯示范同步只可作为候选证据，
不能自动形成控制边或跨臂进度门控；需要等待对臂的条件由后述统一边界关系/场景守卫表达。

## 3.3 在线核心状态

方法在执行时联合维护四类状态。

第一类是参考系 $f$ 与机器人之间的关系后验：

$$
q_t^f(z)
=
P(z_{f,t}=z\mid h_t),
$$

其中：

- $z_{f,t}$ 为当前真实关系；
- $h_t$ 为截至时刻 $t$ 的动作和观测历史；
- 基础关系空间为

$$
z_{f,t}\in
\{\mathrm{external},\mathrm{linked}\}.
$$

`external` 表示参考系仍独立于机器人动作，`linked` 表示参考系已经由机器人动作内生驱动。

第二类是任务进度后验：

$$
\beta_t(s)
=
P(s_t=s\mid o_{1:t},a_{1:t-1}),
$$

其中：

- $o_{1:t}$ 为截至当前的观测序列；
- $a_{1:t-1}$ 为已经执行的动作序列。

第三类是当前控制目标：

$$
s_t^{\mathrm{ref}},
$$

它表示本周期真正从多流策略中查询和伺服的技能状态。

第四类是参考系流的功能角色：

$$
\chi_{f,t}
\in
\{
\mathrm{EXECUTE},
\mathrm{MONITOR},
\mathrm{RECOVER},
\mathrm{DEFER}
\}.
$$

它决定某条流当前用于正常动作融合、关系完整性监测、关系恢复，还是因观测不确定而暂缓决策。

对于多机械臂任务，每只机械臂 $i$ 分别维护自己的进度后验与控制目标：

$$
\beta_t^i(s_i),
\qquad
s_t^{i,\mathrm{ref}}.
$$

各机械臂的进度后验用于估计自身实际进度，并判断自身当前技能是否已经完成。

---

# 4. 示范驱动的对象中心多流任务模型

该模块从少量正常成功示范中建立三个层次的正常任务模型：

$$
\boxed{
\text{多流动作模型}
+
\text{逐进度关系先验}
+
\text{技能条件化场景状态模型}
}
$$

它们分别刻画正常情况下机器人如何运动、各参考系通常与机器人形成什么关系，以及当前技能相关实体的场景构型通常处于什么状态。

## 4.1 对象中心多流技能分布

对每个参考系 $f$ 和进度状态 $s$，学习末端局部轨迹分布：

$$
p_f(\xi\mid s)
=
\mathcal N_{\mathcal M}
\left(
\mu_{f,s},
\Sigma_{f,s}
\right).
$$

其中：

- $\mathcal N_{\mathcal M}$ 表示定义在末端位姿流形 $\mathcal M$ 上的局部分布；
- $\mu_{f,s}$ 为进度 $s$ 下的平均相对末端位姿；
- $\Sigma_{f,s}$ 为相应协方差。

在线执行时，将局部分布利用当前参考系位姿变换到统一动作空间，得到参考系专家：

$$
p_f(a_t\mid s,o_t)
=
\mathcal N
\left(
\mu_{f,t}^{W},
\Sigma_{f,t}^{W}
\right).
$$

多个执行专家通过加权专家乘积融合：

$$
p(a_t\mid s,o_t)
\propto
\prod_{f\in\mathcal F}
p_f(a_t\mid s,o_t)^{w_{f,t}},
$$

其中 $w_{f,t}\geq0$ 是流 $f$ 当前参与正常动作融合的权重。

对于高斯专家，融合后的精度矩阵和均值分别为：

$$
\Lambda_t
=
\sum_f
w_{f,t}
\left(\Sigma_{f,t}^{W}\right)^{-1},
$$

$$
\mu_t
=
\Lambda_t^{-1}
\sum_f
w_{f,t}
\left(\Sigma_{f,t}^{W}\right)^{-1}
\mu_{f,t}^{W}.
$$

其中：

- $\Lambda_t$ 为融合后动作分布的精度矩阵；
- $\mu_t$ 为融合后的动作均值。

## 4.2 示范关系先验

对每个参考系 $f$ 和进度 $s$，根据成功示范中末端—参考系相对轨迹的稳定性，学习示范关系先验：

$$
\pi_{f,s}^{\mathrm{demo}}(z)
=
P(z_f=z\mid s,\mathcal D).
$$

可首先由局部协方差定义连接分数：

$$
M_{f,s}
=
\det(\Sigma_{f,s})^{1/(2d)},
$$

其中：

- $d$ 为局部位姿表示维度；
- $M_{f,s}$ 越小，说明末端与参考系之间的相对运动越稳定。

将连接分数概率化为：

$$
\pi_{f,s}^{\mathrm{demo}}(\mathrm{linked})
=
\sigma
\left[
\alpha
\left(
\log\tau_M-
\log(M_{f,s}+\epsilon)
\right)
\right],
$$

$$
\pi_{f,s}^{\mathrm{demo}}(\mathrm{external})
=
1-
\pi_{f,s}^{\mathrm{demo}}(\mathrm{linked}).
$$

其中：

- $\sigma(\cdot)$ 为 Sigmoid 函数；
- $\tau_M$ 为连接分数参考阈值；
- $\alpha$ 控制由硬阈值向软概率过渡的斜率；
- $\epsilon$ 为数值稳定项。

该先验表示成功示范在进度 $s$ 下参考系通常呈现的运动学关系。

## 4.3 技能条件化的稀疏场景状态分布

仅利用机器人局部轨迹和机械臂—参考系关系，仍不足以完整描述任务执行状态。因此，对每个进度 $s$，进一步建立一个**技能条件化的稀疏场景状态模型**。

设当前可观测实体集合为：

$$
\mathcal V_t
=
\{v_1,\ldots,v_M\},
$$

其中每个 $v$ 表示场景中一个可跟踪实体。

对于进度状态 $s$，根据该技能实际涉及的参考系与任务依赖关系构造活动实体集合：

$$
\mathcal V_s^{\mathrm{act}}
\subseteq
\mathcal V_t.
$$

并在活动实体之间建立稀疏候选因子集合：

$$
\mathcal C_s.
$$

候选状态因子统一采用两类表示。

第一类为**实体构型因子**：

$$
x_{v,t}^{\mathrm{node}}
=
\phi_v(o_{v,t}),
\qquad
v\in\mathcal V_s^{\mathrm{act}},
$$

其中：

- $o_{v,t}$ 为实体 $v$ 当前观测；
- $\phi_v(\cdot)$ 为该实体的可观测状态表示；
- $x_{v,t}^{\mathrm{node}}$ 描述该实体当前自身构型。

第二类为**实体间相对构型因子**：

$$
x_{uv,t}^{\mathrm{edge}}
=
\phi_{uv}(o_{u,t},o_{v,t}),
\qquad
(u,v)\in\mathcal E_s^{\mathrm{cand}},
$$

其中：

- $\mathcal E_s^{\mathrm{cand}}$ 为当前技能活动图中的候选关系边；
- $\phi_{uv}$ 描述两个相关实体之间的可观测相对状态。

对于具有位姿观测的实体，一种具体形式为：

$$
x_{uv,t}^{\mathrm{edge}}
=
\left(T_{v,t}^{W}\right)^{-1}
T_{u,t}^{W}.
$$

从候选因子集合 $\mathcal C_s$ 中选择能够为当前技能状态提供稳定、非冗余解释信息的因素，得到：

$$
\mathcal J_s
\subseteq
\mathcal C_s.
$$

定义候选因素 $j$ 对进度状态 $s$ 的解释增益为：

$$
R_{j,s}
=
\mathcal L_s
\left(
\Xi,Q,X_{\mathcal J_s\cup\{j\}}
\right)
-
\mathcal L_s
\left(
\Xi,Q,X_{\mathcal J_s}
\right),
$$

其中：

- $\Xi$ 表示已有的机器人局部轨迹状态；
- $Q$ 表示机械臂—参考系运动学关系变量；
- $X_{\mathcal J_s}$ 表示当前已选取的场景状态因素；
- $\mathcal L_s(\cdot)$ 表示这些因素对正常示范中进度状态 $s$ 的解释能力；
- $R_{j,s}$ 表示加入因素 $j$ 后带来的额外状态解释信息。

保留满足：

$$
R_{j,s}>\delta_R
$$

且具有可靠观测的因素，得到进度 $s$ 对应的场景状态集合：

$$
X_t^{(s)}
=
\{x_{j,t}\}_{j\in\mathcal J_s}.
$$

对每个被选因素 $j$ 和进度 $s$，从正常示范中学习：

$$
p_j(x\mid s)
=
P(x_j=x\mid s,\mathcal D).
$$

机械臂—参考系运动学关系由关系变量：

$$
q_t^{i,f}(z)
$$

单独表示；每只机械臂的进度状态由其自身的：

$$
\beta_t^i(s_i)
$$

单独表示。因此，机械臂关系和机械臂进度不作为场景构型因子重复进入 $X_t^{(s)}$。

由此，一个正常任务状态由：

$$
\boxed{
\text{机器人局部轨迹}
+
\text{机械臂—参考系关系}
+
\text{稀疏任务相关场景构型}
}
$$

共同描述。

---

# 5. 联合在线关系—进度信念推断

该模块在每一个策略控制周期运行。其核心更新顺序为：

$$
\bar\beta_t
\longrightarrow
q_t
\longrightarrow
\beta_t.
$$

其中，$\bar\beta_t$ 是上一动作完成后的**名义进度先验**；$q_t$ 利用当前动作响应更新实际关系；$\beta_t$ 再根据当前机器人、场景状态与关系观测校正实际进度。

## 5.1 动作后名义进度先验

上一周期的进度后验为：

$$
\beta_{t-1}(s).
$$

在周期 $t-1$ 中，系统执行了动作 $a_{t-1}$。在尚未获得当前观测 $o_t$ 之前，首先默认：

> 上一动作按照正常顺序执行成功，机器人已经进入该动作预期到达的后续状态。

因此，根据上一进度状态和上一动作构造：

$$
\bar\beta_t(s)
=
\sum_{s'}
P_{\mathrm{prog}}
\left(
s\mid s',a_{t-1}
\right)
\beta_{t-1}(s').
$$

其中：

- $P_{\mathrm{prog}}(s\mid s',a_{t-1})$ 为动作后局部进度先验；
- 其最高概率赋给动作 $a_{t-1}$ 正常执行后应到达的名义后继状态；
- 同时为动作未完全完成或稍微提前完成的邻近状态保留少量非零概率。

在 DynaMAC 接口中，`reference_state` 表示上一周期 `query_state` 实际发送的目标状态，而不是动作执行前的源状态。因此动作正常完成分支落在该 `reference_state`，稍微提前完成分支落在其直接后继；未完全完成分支仍保留 $\beta_{t-1}$ 的概率质量。这里的“动作后名义后继”是相对于动作执行前真实进度而言，不应再对已经作为动作目标的 `reference_state` 额外取一次后继。

定义动作正常执行时的名义进度为：

$$
s_t^{\mathrm{nom}}
=
\arg\max_s\bar\beta_t(s).
$$

若上一周期进度后验已经高度集中，则 $\bar\beta_t$ 可以理解为以 $s_t^{\mathrm{nom}}$ 为中心的窄分布：

$$
\bar\beta_t(s)
\propto
\exp
\left[
-\lambda_\tau
d_{\mathrm{prog}}
\left(
s,s_t^{\mathrm{nom}}
\right)
\right],
\qquad
s\in\mathcal W_t.
$$

其中：

- $d_{\mathrm{prog}}(s,s_t^{\mathrm{nom}})$ 为候选状态 $s$ 与名义状态之间的任务进度距离；
- $\lambda_\tau$ 控制先验围绕名义进度的集中程度；
- $\mathcal W_t$ 为名义进度附近的局部候选窗口。

在同一技能内部，候选窗口通常包括：

$$
(k,\tau-1),
\quad
(k,\tau),
\quad
(k,\tau+1),
\quad
(k,\tau+2).
$$

其中主要概率集中在动作正常完成后的状态，邻近候选用于允许当前观测发现：

- 上一动作未完全完成；
- 上一动作按预期完成；
- 上一动作提前完成了少量正常进度。

在技能边界处，只有上一周期的技能入口状态守卫已经放行，动作后名义进度才允许进入：

$$
(k+1,1).
$$

若入口守卫尚未放行，则名义进度保持在当前技能末端：

$$
(k,T_k).
$$

入口守卫一旦放行且边界事务已经提交，跨界是一个不可回退的离散拓扑事实。事务在把 `reference_state` 设置为下一技能入口的同时，将下一周期使用的跨界前进度质量投影到该入口；当前周期已经计算并返回的源技能信念仍作为审计记录保留，关系后验、场景观测和轨迹 mode 不被重置。联合边界只有在全部成员原子提交后才同时执行该投影。

## 5.2 动作条件化的在线关系滤波

对于末端 $e$ 和参考系 $f$，定义相对位姿：

$$
R_{f,t}
=
\left(T_{f,t}^{W}\right)^{-1}
T_{e,t}^{W}.
$$

相邻控制周期的相对运动残差为：

$$
r_{f,t}
=
\operatorname{Log}
\left(
R_{f,t-1}^{-1}R_{f,t}
\right),
$$

其中 $\operatorname{Log}(\cdot)$ 将 SE(3) 相对变换映射到局部切空间。

当机器人发生明显运动，而物体与末端之间的相对位姿基本保持不变时：

$$
r_{f,t}\approx0,
$$

说明该参考系更可能处于 linked 状态。

若机器人产生动作后参考系没有同步响应，或者相对位姿快速变化，则更支持 external 或连接失效。

关系观测定义为：

$$
y_{f,t}
=
\left\{
r_{f,t},
\Delta\xi_{e,t},
a_{t-1},
g_t,
c_t,
\Sigma_{f,t}^{\mathrm{track}}
\right\}.
$$

其中：

- $\Delta\xi_{e,t}$ 为当前周期实际完成的末端运动；
- $a_{t-1}$ 为上一周期发送的动作；
- $g_t$ 为夹爪状态；
- $c_t$ 为可选接触或力觉信息；
- $\Sigma_{f,t}^{\mathrm{track}}$ 为参考系位姿跟踪不确定性。

动作响应使关系判断具备动态可辨识性：当机械臂产生足够运动时，参考系是否随动作同步运动成为判断当前运动学关系的重要证据。

关系后验首先根据上一周期状态进行预测：

$$
\bar q_t^f(z)
=
\sum_{z'}
P_{\mathrm{rel}}(z\mid z')
q_{t-1}^f(z'),
$$

其中 $P_{\mathrm{rel}}(z\mid z')$ 为关系转移先验，用于表达运动学关系具有时间连续性。

依据动作后名义进度先验构造当前示范关系先验：

$$
\pi_{f,t}^{\mathrm{prior}}(z)
=
\sum_s
\bar\beta_t(s)
\pi_{f,s}^{\mathrm{demo}}(z).
$$

该式利用名义时间步附近的关系分布，给出当前控制周期的任务关系先验。

最终关系后验更新为：

$$
q_t^f(z)
=
\eta_{f,t}\,
\bar q_t^f(z)
\left[
\pi_{f,t}^{\mathrm{prior}}(z)
\right]^{\lambda_d}
\left[
p(y_{f,t}\mid z,a_{t-1})
\right]^{X_{f,t}}.
$$

其中：

- $\eta_{f,t}$ 为归一化常数；
- $\lambda_d\in(0,1)$ 控制示范先验强度；
- $p(y_{f,t}\mid z,a_{t-1})$ 为动作条件化关系似然；
- $X_{f,t}\in[0,1]$ 表示当前动作激励与观测可靠性。

当对象遮挡、动作不足或跟踪不可靠时：

$$
X_{f,t}\rightarrow0,
$$

关系更新更多保持历史信念与当前不确定性。

定义关系后验熵：

$$
H(q_t^f)
=
-\sum_z q_t^f(z)\log q_t^f(z).
$$

当：

$$
X_{f,t}<\theta_X
$$

或：

$$
H(q_t^f)>\theta_H,
$$

将该关系标记为 Unknown。Unknown 表示当前关系证据不足，并由后续控制模块暂缓相关关系决策。

## 5.3 对象中心闭环进度校正

动作后名义进度先验 $\bar\beta_t$ 回答：

> 如果上一动作正常执行，机器人当前应该位于哪里？

获得当前观测后，进度校正回答：

> 根据上一动作实际产生的机器人、场景构型和关系结果，机器人当前事实上位于哪里？

对于每个候选状态 $s$，进度校正联合使用三类当前观测证据。

第一类是机器人局部轨迹匹配：

$$
L_t^{\mathrm{robot}}(s)
=
\prod_f
\left[
p_f(\xi_{f,t}\mid s)
\right]^{\rho_{f,t}},
$$

其中：

$$
\rho_{f,t}
=
a_{f,t}v_{f,t}
q_t^f(\mathrm{external}).
$$

这里：

- $a_{f,t}\in\{0,1\}$ 表示参考系当前是否可观测；
- $v_{f,t}\in[0,1]$ 为参考系观测可靠性；
- $\rho_{f,t}$ 根据参考系当前的外生性与可靠性调节其进度证据权重。

`Unknown` 是二元关系后验之上的离散决策，不会把已经可观测、
可靠的连续位姿观测删除。因此物理参考系即使本周期决策为
`Unknown`，仍按 $q_t^f(\mathrm{external})$ 软降权参与连续轨迹匹配；
不可观测或不可靠时权重为零。这不改变离散关系兼容度、动态流角色
或边界关系守卫对 `Unknown` 的严格处理。

第二类是稀疏场景状态匹配。对于当前候选进度 $s$ 的被选状态因子：

$$
j\in\mathcal J_s,
$$

在线观测后验定义为：

$$
r_t^j(x)
=
P(x_{j,t}=x\mid o_t).
$$

其与候选进度状态分布的兼容度为：

$$
L_{j,t}^{\mathrm{state}}(s)
=
\int
r_t^j(x)
p_j(x\mid s)
\,dx.
$$

对于离散变量，上式改为求和。

综合当前技能选出的全部可靠场景因子：

$$
L_t^{\mathrm{state}}(s)
=
\prod_{j\in\mathcal J_s}
\left[
L_{j,t}^{\mathrm{state}}(s)
\right]^{a_{j,t}},
$$

其中：

- $a_{j,t}\in[0,1]$ 为状态因子 $j$ 的观测可靠性；
- $\mathcal J_s$ 为进度状态 $s$ 对应的稀疏场景因子集合。

第三类是关系兼容度：

$$
C_t^{\mathrm{rel}}(s)
=
\prod_f
\left[
\sum_z
q_t^f(z)
\pi_{f,s}^{\mathrm{demo}}(z)
\right]^{
v_{f,t}(1-U_{f,t})
}.
$$

内部项：

$$
\sum_z
q_t^f(z)
\pi_{f,s}^{\mathrm{demo}}(z)
$$

原始关系内积保留在进度后验中。当系统要对事件感知的候选扩展或恢复后重入做绝对可行性判断时，则使用关系状态兼容度：

$$
c_{f,t}^{\mathrm{state}}(s)
=
\frac{\sum_z q_t^f(z)\pi_{f,s}^{\mathrm{demo}}(z)}
{\max_z\pi_{f,s}^{\mathrm{demo}}(z)}
\,\mathbb I\!\left[
\arg\max_z q_t^f(z)=\arg\max_z\pi_{f,s}^{\mathrm{demo}}(z)
\right].
$$

若离线先验持平而未声明关系方向，指示项取1。这一定义使软先验下的同向完全匹配上限为1，同时保证 external 与 linked 方向相反的状态不会因其他项得分较高而获得转移或重入资格。它取代原始内积在这两类绝对阈值中的用法，不叠加新的分类或门控。

衡量当前实际关系是否符合成功示范在候选状态 $s$ 中通常呈现的关系。

最终进度后验为：

$$
\beta_t(s)
=
\eta_t\,
\bar\beta_t(s)
L_t^{\mathrm{robot}}(s)
\left[
L_t^{\mathrm{state}}(s)
\right]^{\lambda_x}
\left[
C_t^{\mathrm{rel}}(s)
\right]^{\lambda_r},
$$

其中：

- $\eta_t$ 为归一化常数；
- $\lambda_x$ 为场景状态证据权重；
- $\lambda_r$ 为关系证据权重。

该公式联合回答：

$$
\underbrace{\bar\beta_t(s)}_{\text{上一动作正常完成时应该在哪里}}
\times
\underbrace{L_t^{\mathrm{robot}}(s)}_{\text{机器人实际在哪里}}
\times
\underbrace{L_t^{\mathrm{state}}(s)}_{\text{任务相关场景构型支持哪里}}
\times
\underbrace{C_t^{\mathrm{rel}}(s)}_{\text{当前关系支持哪个进度}}.
$$

实际进度估计为：

$$
\hat s_t
=
\arg\max_s\beta_t(s).
$$

## 5.4 正常进度解释与关系—进度失配

定义候选状态 $s$ 对当前观测的正常解释分数：

$$
\mathcal E_t(s)
=
L_t^{\mathrm{robot}}(s)
\left[
L_t^{\mathrm{state}}(s)
\right]^{\lambda_x}
\left[
C_t^{\mathrm{rel}}(s)
\right]^{\lambda_r}.
$$

最佳正常解释为：

$$
\mathcal E_t^{\max}
=
\max_{s\in\mathcal W_t}
\mathcal E_t(s),
$$

其中 $\mathcal W_t$ 为当前候选进度集合。

如果：

$$
\mathcal E_t^{\max}>\theta_E,
$$

说明当前状态能够由某个正常进度解释。系统据此将当前进度对齐至与真实机器人、场景和关系状态一致的位置。

当动作提前完成时，未来几帧的正常状态可能同时满足机器人轨迹、场景状态和关系条件，进度后验即可向前移动。

若局部窗口无法解释一个可靠关系变化，可将候选集合扩展到最近的未来关系兼容片段。未来状态只有在机器人状态、任务相关场景构型和关系状态均具有高兼容度时，才能成为合法重新对齐候选。其中关系状态使用 $c_{f,t}^{\mathrm{state}}(s)$ 做绝对阈值判定，而非直接将受软先验峰值限制的原始内积与固定阈值比较。

例如，搬运过程中发生关系由 linked 转为 external 时，未来正常释放状态只有在操作实体、相关目标实体、机器人轨迹和运动学关系共同符合该未来状态时才会获得较高解释分数。若当前整体状态无法满足该正常状态分布，则其解释分数保持较低。

若：

$$
\mathcal E_t^{\max}\leq\theta_E
$$

持续 $H_{\mathrm{mis}}$ 个控制周期，则输出关系—进度失配，转交恢复模块。

---

# 6. 信念驱动的动态多流执行控制

该模块将状态估计转化为控制决策。它明确区分：

$$
\boxed{\text{进度估计转移}}
$$

与

$$
\boxed{\text{控制目标转移}}
$$

前者由动作后名义进度先验和 $\beta_t$ 完成，负责判断实际进度；后者由闭环执行控制器决定，负责更新 $s_t^{\mathrm{ref}}$。

## 6.1 进度校正后的期望关系

完成进度后验更新后，计算控制阶段的期望关系：

$$
\pi_{f,t}^{\mathrm{ctrl}}(z)
=
\sum_s
\beta_t(s)
\pi_{f,s}^{\mathrm{demo}}(z).
$$

它表示根据当前实际进度后验，参考系 $f$ 现在应该呈现何种关系。

在线关系与期望关系的兼容度为：

$$
\kappa_{f,t}
=
\sum_z
q_t^f(z)
\pi_{f,t}^{\mathrm{ctrl}}(z).
$$

该兼容度基于校正后的实际进度，因此正常的执行快慢变化可以先通过进度重新对齐得到解释，再进行流角色决策。

## 6.2 动态流角色

每条参考系流的功能角色由实际关系、期望关系和观测可靠性共同决定。

| 当前条件 | 流角色 | 功能 |
|---|---|---|
| 期望 external，实际 external | `EXECUTE` | 作为正常动作专家参与多流融合 |
| 期望 linked，实际 linked | `MONITOR` | 持续监测连接完整性 |
| 期望 linked，实际 external | `RECOVER` | 阻断正常推进，提出 `LINK(f)` |
| 期望 external，实际 linked | `RECOVER` | 提出 `UNLINK(f)` 并阻断正常推进 |
| 关键关系 Unknown | `DEFER` | 保持上一稳定角色并暂缓相关决策 |

同一对象流在交互过程中可以在动作专家、关系完整性监测和关系恢复几种功能之间动态切换。

角色路由输出：

$$
\{
w_{f,t},
m_{f,t}^{\mathrm{monitor}},
r_{f,t}^{\mathrm{intent}},
b_{f,t}^{\mathrm{advance}}
\},
$$

分别对应流参与权重、监测标志、恢复意图和推进阻断信号，并由统一执行控制器决定任务进度目标。

## 6.3 保持、重新对齐与技能内推进

在正常任务模式下，每个控制周期都重新估计关系与进度，并重新决定控制动作。

若当前观测最符合进度 $\hat s_t$，且后验置信度充分，则内部实际进度重新对齐到：

$$
\hat s_t.
$$

重新对齐修正系统对当前实际进度的估计。

当当前目标尚未完成、关键关系处于 Unknown、进度置信度不足或存在推进阻断时，保持：

$$
s_{t+1}^{\mathrm{ref}}
=
s_t^{\mathrm{ref}}.
$$

保持期间，系统继续依据最新对象和其他机械臂参考系重新实例化当前局部目标并闭环伺服。

当前离散动作目标是否到达，由同一个进度后验 $\beta_t$ 统一解释，而不再叠加一个“当前末端必须通过上一命令完整高斯分布的绝对兼容度阈值”。若可信估计仍落在 $s_t^{\mathrm{ref}}$ 之前，则目标尚未到达并继续 HOLD；若已稳定解释为 $s_t^{\mathrm{ref}}$ 或其同技能直接后继，则允许在关系、执行流和边界条件也通过时推进。上一命令与当前末端的连续几何相容度仍保留为执行审计和控制误差记录，不再作为第二个独立进度门控。

这里需要区分统计进度结论与控制决策差异。阶段二仍按逐状态后验输出 `LOW_CONFIDENCE` 或高置信 `BACKWARD_REALIGNMENT`；阶段三不修改该后验、MAP 和标签，而是仅在相同技能的局部连续状态之间检查它们是否产生相同控制。

对候选状态 $s_a,s_b$，其当前 PoE 动作目标分别为 $(T_a^W,\Sigma_a,g_a)$ 与 $(T_b^W,\Sigma_b,g_b)$。定义动作兼容度：

$$
\kappa_{ab}^{\mathrm{act}}
=
\exp
\left[
-\frac{1}{2d}
\delta_{ab}^{\mathsf T}
(\Sigma_a+\Sigma_b+\epsilon I)^{-1}
\delta_{ab}
\right],
$$

其中：

$$
\delta_{ab}
=
\operatorname{Log}
\left((T_a^W)^{-1}T_b^W\right),
\qquad d=6.
$$

只有两状态的当前 mode 已选流、动态角色、期望与实际关系语义、执行权重、推进阻断、恢复/验证请求和夹爪命令一致，并且动作兼容度通过统一的动作等价阈值时，二者才属于同一个控制等价类。等价类不得跨技能边界或跨越不连续的局部状态。

设包含 MAP 状态 $\hat s_t$ 的控制等价类为 $\mathcal C_t^\star$，则控制层使用：

$$
P_t^{\mathrm{ctrl}}
=
\sum_{s\in\mathcal C_t^\star}\beta_t(s)
$$

作为聚合控制置信度，并以所有等价类的概率质量计算类别熵。只有二者通过原有进度置信度和熵阈值时，才允许控制层消费等价结果。对 `LOW_CONFIDENCE`，这表示统计歧义不改变控制；对高置信 `BACKWARD_REALIGNMENT`，还必须要求 MAP 状态恰为当前引用的同技能直接前驱，且二者同属包含 MAP 的已接受等价类，才可把这一状态离散化滞后按当前引用已完成处理。两种情况仍只推进一个同技能直接后继；原始 $\beta_t(s)$、MAP 状态、进度标签和逐状态审计全部保留。

该处理不适用于 `NO_PLAUSIBLE_STATE`，也不覆盖 MAP 与当前引用没有共同通过控制等价检查的落后状态、高置信多状态落后、关键关系 Unknown、可靠关系失配、无可用执行流或技能入口守卫。由此，静态或弱可辨识轨迹段中的相邻状态差异不会无意义地冻结控制，而真正会改变动作、夹爪、流角色或技能边界的差异仍然 HOLD。

若当前状态稳定达到，且其直接后继仍位于同一技能，则执行技能内推进：

$$
s_{t+1}^{\mathrm{ref}}
=
\operatorname{succ}(s_t^{\mathrm{ref}}),
$$

其中：

$$
\operatorname{succ}(k,\tau)
=
(k,\tau+1).
$$

从名义状态校正到实际状态属于重新对齐；从已经确认达到的状态执行到其后继状态属于推进。

跨技能推进由下一模块的技能入口状态守卫决定。

---

# 7. 对象中心技能入口状态守卫

技能入口状态守卫负责回答：

> 当前技能已经局部完成后，整个任务相关场景是否已经处于下一技能可以正常开始的边界就绪状态？

它统一表示本臂技能完成条件、任务相关机械臂—参考系关系条件和稀疏场景构型条件，使技能切换由当前真实物理状态驱动。

## 7.1 边界就绪状态模型

对于技能边界：

$$
b:
k\rightarrow k+1,
$$

定义第 $n$ 条成功示范中的边界决策时刻 $t_b^{(n)}$：

> 当前技能 $k$ 的最后一个动作已经作用于环境，并获得了更新后的观测；下一技能 $k+1$ 的第一个动作尚未发出。

为了降低单帧观测噪声、控制采样延迟以及技能分段的小范围偏移，在边界观测前定义短终止窗口：

$$
\mathcal B_b^{-,(n)}
=
\left\{
t_b^{(n)}-h_b^-+1,
\ldots,
t_b^{(n)}
\right\},
$$

其中：

- $h_b^-$ 为边界前终止窗口长度；
- $t_b^{(n)}$ 为最接近真实跨界决策的边界观测；
- 窗口中的全部观测均位于下一技能首个动作发出之前。

入口守卫复用三类任务状态信息：

$$
\boxed{
\text{本臂局部完成信息}
+
\text{机械臂—参考系关系信息}
+
\text{稀疏场景构型信息}
}
$$

对于每只机械臂 $i$，其本地完成模型由该机械臂自身的进度后验、技能末端目标及本臂必要关系构成。当前技能的末端状态窗口和末端目标分布由成功示范得到。

边界相关的机械臂—参考系关系条件表示为：

$$
\mathcal R_b
=
\left\{
(r,f,z^\star)
\right\},
$$

其中：

- $r$ 表示与该边界相关的任意机械臂；
- $f$ 表示相关参考系；
- $z^\star\in\{\mathrm{external},\mathrm{linked}\}$ 表示跨界前要求的目标关系。

对于每个关系条件 $(r,f,z^\star)$，根据边界前短终止窗口内的逐进度示范关系先验，构造边界关系支持分布：

$$
\psi_{b,(r,f)}^{\mathrm{rel}}(z)
=
\sum_{\tau\in\mathcal B_b^-}
\omega_{b,\tau}\,
\pi_{r,f,(k,\tau)}^{\mathrm{demo}}(z),
$$

其中：

$$
\sum_{\tau\in\mathcal B_b^-}
\omega_{b,\tau}=1,
$$

且越接近技能边界的状态具有越高权重。

边界相关的场景构型条件从第 4.3 节建立的技能条件化稀疏场景因子中选择。设其候选集合为：

$$
\mathcal C_b^{\mathrm{guard}}.
$$

对于候选场景因素 $j$，从全部成功示范的边界前终止窗口学习边界守卫支持分布：

$$
\psi_{b,j}^{\mathrm{guard}}(x)
=
\frac{1}{N}
\sum_{n=1}^{N}
\sum_{\ell=0}^{h_b^--1}
\omega_\ell\,
r_{t_b^{(n)}-\ell}^{j,(n)}(x).
$$

其中：

- $N$ 为成功示范数量；
- $r_t^{j,(n)}(x)$ 为第 $n$ 条示范在时刻 $t$ 对场景构型因素 $j$ 的观测分布；
- $\omega_\ell\geq0$ 为时间权重；
- $\sum_{\ell=0}^{h_b^--1}\omega_\ell=1$；
- 通常令

$$
\omega_0>\omega_1>\cdots>\omega_{h_b^--1},
$$

使越接近边界决策时刻的状态获得越高权重。

短终止窗口用于抑制边界单帧噪声、容忍技能分段的小范围误差，并判断关键关系和场景状态是否在跨界前稳定进入支持域。

定义场景因素 $j$ 的边界状态一致性：

$$
S_{b,j}
=
1-
\frac{
H\left(\psi_{b,j}^{\mathrm{guard}}\right)
}{
H_j^{\max}
},
$$

其中：

- $H(\cdot)$ 为分布熵；
- $H_j^{\max}$ 为状态因素 $j$ 的最大可能熵。

结合下一技能的活动实体依赖与边界状态一致性，从候选集合中选择场景构型条件集合：

$$
\mathcal J_b^{\mathrm{guard}}
=
\left\{
j:
j\in\mathcal C_b^{\mathrm{guard}},
\quad
S_{b,j}>\delta_S
\right\}.
$$

其中 $\delta_S$ 为边界状态一致性阈值。

因此，技能入口状态守卫学习的是：

$$
\boxed{
\text{下一技能动作发出前，本臂完成状态、任务相关关系和场景构型必须进入的边界就绪支持域}
}
$$

## 7.2 在线边界兼容度

在线时，首先由每只机械臂自身的进度后验判断该机械臂是否已经到达当前技能末端。对于机械臂 $i$，其本地完成判断仅使用：

$$
\beta_t^i(s_i),
$$

以及该机械臂自身的末端轨迹匹配和本臂必要关系。

对于边界关系条件：

$$
(r,f,z^\star)\in\mathcal R_b,
$$

其在线兼容度直接由对应机械臂—参考系关系后验给出：

$$
\gamma_{b,(r,f),t}^{\mathrm{rel}}
=
q_t^{r,f}(z^\star).
$$

若该关系处于 Unknown，则该边界关系条件尚未获得可靠满足证据。

对于场景构型条件：

$$
j\in\mathcal J_b^{\mathrm{guard}},
$$

其在线观测后验为：

$$
r_t^j(x)
=
P(x_{j,t}=x\mid o_t).
$$

当前场景构型与边界守卫支持分布的兼容度定义为：

$$
\gamma_{b,j,t}
=
\int
r_t^j(x)
\psi_{b,j}^{\mathrm{guard}}(x)
\,dx.
$$

对于离散变量：

$$
\gamma_{b,j,t}
=
\sum_x
r_t^j(x)
\psi_{b,j}^{\mathrm{guard}}(x).
$$

每个场景构型因素还具有观测可靠性：

$$
a_{j,t}\in[0,1].
$$

由此，在线边界状态由两类跨界条件共同描述：

$$
\boxed{
\text{机械臂—参考系关系兼容度}
+
\text{稀疏场景构型兼容度}
}
$$

各机械臂的进度后验分别用于各自的本地完成判断，不作为其他机械臂入口守卫中的独立跨臂状态条件。

双臂依赖仍由上述统一关系条件和场景构型条件表达，不另设按任务类型区分的协作守卫。若一只机械臂的本地完成目标经过对侧末端执行依赖筛选后仍以对侧末端为参考系，且正常示范中的两侧边界稳定对应，则该双臂末端相对构型可作为对侧边界的普通候选场景边。它必须通过与其他边界场景因子完全相同的跨示范支持域检查，随后保存在同一个 $\mathcal J_b^{\mathrm{guard}}$ 中并由同一许可公式评估。单纯由同步相关性选中的对侧末端不得借此重新进入边界模型；共享实体、交接关系或其他稳定场景构型仍按统一候选流程表达真实就绪条件。该转换只把示范中已有且可辨识的跨臂物理依赖放到依赖方边界上，不引入对侧 $\beta_t$ 门控、固定时间同步或新的在线分支。

## 7.3 技能入口放行

对于机械臂 $i$ 的边界 $b$，定义本地完成度：

$$
R_{i,b,t}^{\mathrm{local}}
=
P_{i,t}^{\mathrm{end}}(k)
L_{i,t}^{\mathrm{goal}}
C_{i,b,t}^{\mathrm{local}},
$$

其中：

- $P_{i,t}^{\mathrm{end}}(k)$ 为机械臂 $i$ 的进度后验落在当前技能末端窗口的概率；
- $L_{i,t}^{\mathrm{goal}}$ 为机械臂 $i$ 当前世界末端位姿与技能末端联合目标的峰值归一匹配度；该联合目标先将当前 mode 的全部有效最终状态流变换到世界坐标系，再用与动作生成相同的精度加权 PoE 得到单一高斯分布，不对多个坐标表达的边缘支持重复相乘；
- $C_{i,b,t}^{\mathrm{local}}$ 为机械臂 $i$ 在当前技能末端应满足的本地关系兼容度。

例如：

$$
P_{i,t}^{\mathrm{end}}(k)
=
\sum_{\tau=T_k-h_e+1}^{T_k}
\beta_t^i(k,\tau),
$$

其中 $h_e$ 为技能末端窗口长度。

定义本地完成判定：

$$
\mathrm{LocalDone}_{i,b,t}
=
\mathbb I
\left[
R_{i,b,t}^{\mathrm{local}}
>
\theta_{\mathrm{local}}
\right].
$$

机械臂 $i$ 的技能入口许可为：

$$
\begin{aligned}
\mathrm{Permit}_{i,b,t}
={}&
\mathrm{LocalDone}_{i,b,t}
\\
&\land
\bigwedge_{(r,f,z^\star)\in\mathcal R_b}
\left\{
\mathbb I[U_{r,f,t}=0]
\land
\mathbb I
\left[
q_t^{r,f}(z^\star)
>
\theta_{r,f}
\right]
\right\}
\\
&\land
\bigwedge_{j\in\mathcal J_b^{\mathrm{guard}}}
\left\{
\mathbb I
\left[
a_{j,t}>\theta_a
\right]
\land
\mathbb I
\left[
\gamma_{b,j,t}>\theta_{b,j}
\right]
\right\}.
\end{aligned}
$$

其中：

- $U_{r,f,t}$ 表示机械臂 $r$ 与参考系 $f$ 的关系是否处于 Unknown；
- $\theta_{r,f}$ 为边界关系条件的满足阈值；
- $\theta_a$ 为场景状态观测可靠性阈值；
- $\theta_{b,j}$ 为场景构型条件 $j$ 的兼容度阈值。

所有必要条件连续满足 $H_b$ 个控制周期后，机械臂 $i$ 才生成由技能 $k$ 进入技能 $k+1$ 的转换许可。

$H_b$ 只用正常控制周期回放逐边界标定。令 $M_b$ 为正常进度进入边界末端窗口前，
全部必要条件偶发共同满足的最长连续周期数，则取 $H_b=M_b+1$，并再次检查正常末端
保持能够连续维持至少 $H_b$ 个周期。这里不再附加与边界数据无关的全局最短时长；
故障数据不得参与该参数标定。

对于独立技能边界，允许的机械臂单独推进：

$$
s_{t+1}^{i,\mathrm{ref}}
=
(k+1,1).
$$

对于需要多只机械臂联合进入下一技能的边界，设事务组中涉及的机械臂集合为：

$$
\mathcal A_g.
$$

联合提交条件为：

$$
\mathrm{Commit}_{g,t}
=
\bigwedge_{i\in\mathcal A_g}
\mathrm{Permit}_{i,b_i,t}.
$$

当：

$$
\mathrm{Commit}_{g,t}=1,
$$

事务组内所有机械臂在同一控制周期提交技能转换；当任一机械臂尚未完成自身技能，或任一任务相关关系、场景构型尚未满足时，相关机械臂保持各自当前控制目标并继续闭环伺服。

联合边界表示分别提交会产生不合法中间任务状态的对称协作边界，而不只是示范中时间位置接近的两个边界。离线识别除要求所有正常示范中的边界稳定同步外，还要求双方共同维持同一 linked 实体、共同建立一个跨越该边界且随后稳定确认的 linked 关系，或共享完全相同的硬场景构型条件。只由一方等待另一方状态的定向依赖不建立事务组；尚未确认的 `LINK_PENDING` 也不能单独作为原子提交依据。

由此，多机械臂协作通过以下逻辑形成：

$$
\boxed{
\text{各机械臂分别完成自身技能}
+
\text{任务相关多臂关系与场景条件满足}
+
\text{联合边界事务提交}
}
$$

---

# 8. 关系修复与任务重入

当当前状态无法由任何正常候选进度解释，或者进度校正后的期望关系与实际关系持续可靠失配时，系统从正常任务模式进入关系恢复模式。

定义执行模式：

$$
m_t
\in
\{
\mathrm{TASK},
\mathrm{VERIFY\_LINK},
\mathrm{RECOVERY}
\}.
$$

当正常示范中的闭合事件跨示范一致、但闭合后缺少足够运动激励而无法确认关系时，离线保存 `LINK_PENDING`。候选闭合状态及其后仍有跨示范关系支持的连续区间使用可被在线观测推翻的 soft linked 期望，但不建立正式关系来源。若该 linked 关系是后续执行或入口守卫的必要条件，在线先进入 `VERIFY_LINK` 制造有限运动证据；正常任务进度在验证期间冻结，关系后验仍逐控制周期更新。

对于离线已经确认的正式 LINK，本次在线执行仍需由真实运动证据确认关系。事件发生后若后验因 external 持续性而暂时滞后，系统在该事件离线学习的完整连续 `linked_entry_states` 区间内允许自然携带动作继续产生证据，并将相应 linked 流保持为零权重。该窗口不采用全局固定状态数；可靠且信息充分的 external 反证会立即结束自然确认，一旦该 occurrence 已确认 linked，后续断连也不能重新使用宽限。

`VERIFY_LINK/RECOVERY` 是多臂系统的顶层辅助执行模式。任一机械臂进入其中之一时，所有机械臂的正常进度更新、参考状态提交和正常入口事务同时冻结；辅助臂执行验证或恢复，其他机械臂只闭环维持已经提交的当前目标。辅助过程完整返回 `TASK` 后，最新关系结果才重新交由正常执行和入口守卫消费。若主动验证已由原关系滤波器稳定确认 `linked` 或 `external`，系统在完成原路返回时把稳定周期的原始二元后验精确提交回同一个 $q_t^{i,f}$；不转为 one-hot、不额外增强置信度，也不创建新关系状态。这样返回路径产生的短时残差不会覆盖刚获得的有效确认；验证仍为 `Unknown` 时不提交确认结果。

主动验证中的“稳定关系”不由单个控制周期决定。系统排除刚进入 `VERIFY_LINK` 时属于上一个 `TASK` 动作的运动，只累计验证指令真实执行后的成对末端—参考系响应。对验证窗口 $\mathcal W_v$ 定义平移共动残差比：

$$
\rho_{i,f}^{\mathrm{verify}}
=
\sqrt{
\frac{
\sum_{\ell\in\mathcal W_v}
\lVert\Delta p_{f,\ell}-\Delta p_{e,\ell}\rVert^2
}{
\sum_{\ell\in\mathcal W_v}
\lVert\Delta p_{e,\ell}\rVert^2
}
}.
$$

该统计量只检查主动动作是否产生跨周期一致的共动响应，不创建新的关系概率状态。当窗口样本数和实际运动量达到配置下限后，仅在窗口共动方向与原有二元关系滤波器的当前非 Unknown 决策一致时，才认为 linked 或 external 已稳定。这使接触刚建立时的姿态调整或单帧跟踪抖动不会提前触发错误恢复，同时保留“机械臂运动而物体不响应”对 external 的物理证据。

验证得到 linked 时，仅为当前执行激活该关系来源；得到 external 时，若关系所属机械臂已经到达同一 Pending occurrence，则构成“期望 linked、实际 external”的可靠失配，并生成指向该机械臂的 LINK 修复目标。Pending 轨迹此时只作为尚未激活的修复模板，必须在修复后再次由有效运动证据确认 linked，才能建立运行时关系来源。验证仍为 Unknown 时继续阻断必要入口，不凭不充分证据启动关系修复。

## 8.1 恢复触发

定义关系失配集合：

$$
\mathcal V_t
=
\left\{
f:
U_{f,t}=0,
\quad
\kappa_{f,t}<\theta_{\mathrm{mis}}
\right\},
$$

其中：

- $U_{f,t}=0$ 表示该关系观测可靠；
- $\kappa_{f,t}$ 为实际关系与进度校正后期望关系的兼容度；
- $\theta_{\mathrm{mis}}$ 为失配阈值。

当：

$$
\mathcal E_t^{\max}\leq\theta_E
$$

或：

$$
\mathcal V_t\neq\varnothing
$$

持续 $H_{\mathrm{mis}}$ 个周期时：

$$
m_t
\leftarrow
\mathrm{RECOVERY}.
$$

此时冻结正常任务控制目标：

$$
s_t^{\mathrm{ref}}
=
s_{\mathrm{hold}},
$$

关系后验继续更新，用于判断关系恢复状态。

关系与进度模块负责识别失配和阻断正常任务推进，恢复模块负责将实际交互关系重新带入正常任务支持域。

## 8.2 统一关系修复

关系恢复依据期望关系与实际关系之间的方向生成关系目标。

若期望关系为 linked、实际关系为 external，则生成：

$$
\mathrm{LINK}(f).
$$

`LINK(f)` 从正常成功示范中的 external→linked 转移附近提取局部相对轨迹；对已经到达但在线验证为 external 的同一 Pending occurrence，复用其尚未激活的候选局部轨迹作为本次修复模板：

$$
\mathcal A_f
=
\left\{
\mu_{f,s},
\Sigma_{f,s},
g_s
\right\}_{s=s_g-h}^{s_g},
$$

其中：

- $s_g$ 为正常关系建立位置；
- $h$ 为关系建立前的局部轨迹长度；
- $g_s$ 为夹爪状态。

若参考实体仍在滑动或旋转，逐控制周期按其瞬时位姿重新计算整条 LINK 轨迹会形成追逐移动目标的闭环，不能保证局部锚点可达。因此 LINK 修复先等待参考系在连续可靠观测中稳定，并锁定本次恢复实例的参考位姿：

$$
T_{e,t}^{W,\mathrm{target}}(s)
=
T_{f,\mathrm{lock}}^{W}
\mu_{f,s}.
$$

锁定后整条重新接近和预抓取轨迹使用同一个 $T_{f,\mathrm{lock}}^{W}$。夹爪闭合前若参考系相对锁定位姿再次出现显著运动，则废弃该实例、回到稳定等待并重新锁定；闭合后对象随末端运动是验证 linked 的预期响应，不据此重启。这一过程只依赖参考系运动与观测可靠性，因而适用于一般移动对象恢复，不引入任务或状态特例。

恢复分布采用：

$$
\Sigma_{f,s}^{\mathrm{rec}}
=
\Sigma_{f,s}
+
\lambda_{\mathrm{rec}}I,
$$

其中 $\lambda_{\mathrm{rec}}>0$ 为恢复协方差放宽系数。

若期望关系为 external、实际关系为 linked，则生成：

$$
\mathrm{UNLINK}(f).
$$

`UNLINK(f)` 通过释放夹爪和局部脱离，使当前关系恢复为 external。

多个关系失配可以同时产生多个 LINK/UNLINK 关系目标。对于同一夹爪，关系目标满足资源前置约束：

$$
\mathrm{FREE}(\mathrm{gripper})
\prec
\mathrm{LINK}(f).
$$

由此，恢复控制能够将多个局部关系失配统一组织为关系目标序列。

## 8.3 恢复验证与任务重入

关系修复成功后，根据当前完整对象中心状态重新选择合法任务进度。

设：

$$
\mathcal W_{\mathrm{re}}
$$

为恢复后的候选重入集合，重入状态定义为：

$$
s^{\mathrm{re}}
=
\arg\max_{s\in\mathcal W_{\mathrm{re}}}
\mathcal E_t(s).
$$

合法重入状态需要同时满足：

- 当前机器人状态与该进度匹配；
- 当前技能相关场景构型与该进度匹配；
- 当前关系与该进度兼容；
- 涉及跨技能状态时，对应入口守卫满足放行条件。

其中“当前关系与该进度兼容”使用第5章统一定义的 $c_{f,t}^{\mathrm{state}}(s)$：软先验峰值归一后再检查 external/linked 物理方向。这替换了原始内积直接与阈值比较的口径，不改变关系滤波、正常进度后验、候选范围或入口守卫。

正常进度估计中的机器人轨迹匹配仍使用成功示范学习到的原始协方差
$\Sigma_{f,s}$。关系恢复已经使用
$\Sigma_{f,s}^{\mathrm{rec}}=\Sigma_{f,s}+\lambda_{\mathrm{rec}}I$
描述恢复动作允许的局部偏差，因此恢复完成后的重入评分使用相同口径：

$$
\Sigma_{f,s}^{\mathrm{reentry}}
=
\Sigma_{f,s}+\lambda_{\mathrm{rec}}I.
$$

该式只作用于恢复后的机器人轨迹解释项，不改变场景构型和关系兼容度，
也不写回或替换离线任务模型。这样可以避免恢复控制已经按放宽分布完成，
却又被更窄的理想示范分布立即拒绝；正常 TASK 进度推断仍保持原始精度。

随后重新初始化：

$$
\beta_t(s)
\leftarrow
\delta(s=s^{\mathrm{re}}),
$$

$$
s_t^{\mathrm{ref}}
\leftarrow
s^{\mathrm{re}},
$$

$$
m_t
\leftarrow
\mathrm{TASK},
$$

其中 $\delta(\cdot)$ 为离散 Dirac 分布，表示将进度后验重新集中到合法重入状态。

由此，正常进度滤波负责在成功任务轨迹附近持续估计执行位置，恢复模块负责在关系失配后重新建立能够被正常任务模型解释的真实状态。

---

# 9. 完整在线推理闭环

在正常任务模式下，每个策略控制周期执行一次以下顺序：

$$
\boxed{
\text{动作后名义进度先验}
\rightarrow
\text{关系更新}
\rightarrow
\text{进度校正}
\rightarrow
\text{角色路由}
\rightarrow
\text{保持、推进或入口检查}
}
$$

具体而言：

1. 根据上一周期进度后验、上一动作以及动作正常完成的默认假设，构造以当前名义时间步为中心的窄进度先验 $\bar\beta_t$。
2. 根据 $\bar\beta_t$ 对逐进度示范关系先验进行加权，得到当前弱关系先验。
3. 利用当前动作响应、相对运动和观测可靠性更新 $q_t^f$。
4. 对候选进度计算机器人局部轨迹匹配、技能条件化场景状态匹配和关系兼容度。
5. 更新 $\beta_t(s)$，得到实际进度估计 $\hat s_t$。
6. 判断当前状态是否能够由正常成功任务模型解释。
7. 根据更新后的 $\beta_t$ 计算控制阶段期望关系，确定各流的功能角色。
8. 若进度统计为 `LOW_CONFIDENCE`，或为紧邻当前引用的高置信 `BACKWARD_REALIGNMENT`，仅在同技能局部连续候选的角色、关系语义、夹爪和动作目标均控制等价时使用等价结果；原始逐状态后验和标签保持不变。
9. 若当前状态能够解释且执行控制置信度充分，则执行局部重新对齐，并决定保持或技能内推进。
10. 若当前后继跨越技能边界，则由每只机械臂分别根据自身进度后验判断本地技能完成状态，并联合检查边界相关机械臂—参考系关系与稀疏场景构型条件。
11. 对独立边界分别提交技能转换；对联合边界，在同一场景状态下完成所有相关机械臂的入口许可评估，并通过统一事务提交技能转换。
12. 若关系—进度失配持续存在，则冻结正常任务进度并进入恢复。
13. 恢复完成后，根据当前真实状态选择合法重入进度。

顶层策略与 benchmark 执行器之间使用动作事务区分“任务命令已经施加”“物理前缀产生进展”和“绝对策略目标已经到达”。已施加命令的引用和下一周期真实观测进入原有 $\bar\beta_t/\beta_t$ 更新；执行器的 `reached/progressed/stopped` 标签不直接改写进度先验、`StateId` 或 `reference_state`。因此下一周期保持、推进或重新对齐仍由进度后验、关系和边界机制统一决定，而不是由低层执行器建立第二套进度控制。`progressed/stopped` 不提交闭环绝对目标完成。TASK 夹爪命令由任务状态授权：进度后验必须对齐当前引用，非最终技能的末端状态还必须先提交相应边界；边界提交周期可直接执行入口夹爪命令。多臂按每臂授权分别提交夹爪，因此接收臂闭合不要求交出臂同时释放。冻结 DynaMAC 不使用闭环任务授权，但其固定时钟本身就是动作与夹爪的时序来源：每个正式提交的策略事务消费一个固定时钟状态，主动作同时提交相应夹爪值，不能由执行器完成标签另行推进、回滚或延迟。只有没有任务级时序来源的非 TASK 辅助命令才以末端位姿 `reached` 约束夹爪物理顺序。由此，执行器内部路径分段不会被误解释成多个任务状态推进，固定笛卡尔包络也不会取代学习得到的状态和边界条件或改写基线算法。

其中存在三个明确的决策层次：

$$
\boxed{
\beta_t^i
\text{ 判断机械臂 }i\text{ 当前实际在哪里以及自身技能是否完成}
}
$$

$$
\boxed{
\mathrm{Permit}_{i,b,t}
\text{ 判断机械臂 }i\text{ 当前是否可以进入下一技能}
}
$$

$$
\boxed{
\mathrm{RECOVERY}
\text{ 将失配状态恢复至正常任务支持域}
}
$$

三者分别对应状态估计、技能转移和异常恢复，共同形成闭环任务执行过程。

---

# 10. 方法性质

## 10.1 少样本正常示范学习

多流轨迹模型、逐进度关系先验、技能条件化场景状态分布和边界守卫支持域均由少量正常成功示范学习得到。

在线执行中的异常判断建立在“当前状态对正常任务模型的可解释性”之上，因此能够在缺少故障类别示范的条件下识别未见关系—进度失配。

## 10.2 提前完成与真实异常的统一解释

系统以完整状态解释分数：

$$
\mathcal E_t(s)
$$

判断当前状态能否由正常进度解释。

如果机器人轨迹、任务相关场景构型和当前关系共同支持某个正常进度，则将实际进度重新对齐至该状态；当所有候选正常状态均无法形成充分解释时，则进入恢复。

因此，同一套状态推断机制可以统一处理不同的执行速度变化和关系破坏。

## 10.3 单臂与多臂统一

单臂与多臂任务共享相同的：

- 多流轨迹表示；
- 在线关系后验；
- 进度后验；
- 稀疏场景状态因子；
- 技能入口状态守卫。

每只机械臂利用自身的：

$$
\beta_t^i
$$

估计自身实际进度并判断本地技能是否完成。

多机械臂协作条件由任务相关的：

$$
q_t^{r,f}(z)
$$

以及稀疏场景构型共同表达。各机械臂分别生成入口许可，联合技能边界通过统一事务完成同一控制周期的状态转换。

由此，独立技能、定向依赖和联合协作均由同一套本地完成、物理关系、场景构型和事务提交机制表达。

## 10.4 连续动态跟随与离散技能控制并存

任务进度保持期间，离散进度索引保持不变；对象中心多流策略仍使用当前时刻最新参考系位姿不断重新实例化局部动作目标。

因此，系统同时具备：

$$
\boxed{
\text{连续对象中心动态伺服}
+
\text{离散技能进度闭环控制}
}
$$

## 10.5 技能条件化稀疏状态表示

场景状态采用技能条件化的稀疏实体—关系因子表示：

$$
X_t^{(s)}
=
\{x_{j,t}\}_{j\in\mathcal J_s}.
$$

其中：

- 活动实体由当前技能涉及的参考系与任务依赖确定；
- 场景因素由实体构型和实体间相对构型统一表示；
- 只有具有稳定、非冗余状态解释能力的因素进入进度状态模型；
- 机械臂—参考系运动学关系由 $q_t^{i,f}$ 表示；
- 各机械臂的进度由各自的 $\beta_t^i$ 表示，并用于自身进度估计与本地完成判断。

由此，对象中心状态空间能够随任务结构自适应形成稀疏表示。

---

# 11. 主要创新点

## 创新点一：动作条件化的在线交互关系信念

从少量成功示范中学习逐进度关系先验，并结合执行时的相对运动、动作响应、夹爪状态和观测可靠性递推在线关系后验，使策略能够持续估计机器人—对象—其他智能体之间当前真实的运动学关系。

## 创新点二：完整对象中心状态驱动的闭环技能进度

以动作正常完成为默认假设构造动作后名义进度先验，并联合机器人局部轨迹、技能条件化的稀疏场景构型以及交互关系对实际进度进行在线校正，使执行快慢变化和真实关系失配能够在统一概率状态模型中被区分，并据此控制重新对齐、保持和推进。

## 创新点三：信念驱动的动态多流角色切换

根据进度校正后的期望关系与当前实际关系，使同一参考系流在正常动作专家、关系完整性监测、关系恢复和不确定暂缓之间动态切换，使多流策略的功能角色能够随真实交互状态在线变化。

## 创新点四：对象中心技能入口状态守卫与统一关系恢复

从少量成功示范的边界观测及其前方短终止窗口中学习下一技能动作发出前的任务相关边界就绪支持域；每只机械臂利用自身进度信念判断本地技能完成状态，入口守卫联合检查任务相关机械臂—参考系关系与稀疏场景构型，并通过事务式边界提交实现多机械臂技能协作；在关系—进度状态失配后，通过 LINK/UNLINK 关系目标恢复实际交互状态，并依据恢复后的完整对象中心状态重新进入合法任务进度。

---

整项方法最终形成如下逻辑闭环：

$$
\boxed{
\text{少量成功示范}
\rightarrow
\text{对象中心正常任务模型}
\rightarrow
\text{在线关系—进度信念}
\rightarrow
\text{动态多流执行}
\rightarrow
\text{技能入口状态守卫}
\rightarrow
\text{关系修复与任务重入}
}
$$

其核心目标是使少样本对象中心操作策略能够依据机器人、任务相关实体、交互关系和其他智能体的真实执行状态持续闭环更新，从而在动态扰动与执行异常下保持正确的任务进度与技能协作。
