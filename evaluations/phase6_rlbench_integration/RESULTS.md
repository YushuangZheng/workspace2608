# 阶段六 RLBench 受控故障组件级 A/B 结果

## 结果定位

本结果是使用真实 RLBench/CoppeliaSim 物理执行链完成的**组件级 pilot**，用于检查阶段二至五机制接入阶段六后是否真实发挥作用。它不是论文正式成功率结果，也不具备跨任务统计显著性。

固定实验口径如下：

- 封存评测集：`rlbench_eval_v2`；
- 任务：StackWine；
- 正常样本及每个故障单元：封存索引 `0,1,2`，每组3条；
- 执行器：`stage6_hybrid_cartesian_executor_v16`；
- 执行协议：`rlbench-stage6-hybrid-cartesian-continuation-v18`；
- 受控故障协议：`fault_protocol.json`，所有方法共用物理触发条件，注入器不读取或修改策略内部 StateId、信念或任务结果；
- 对比方法：DynaMAC V4、仅闭环进度、闭环进度＋动态角色、完整技术路线；
- 故障数据未用于标定关系、进度、边界、恢复阈值或连续确认周期。

## 正常任务门控

完整方法在3条正常样本上成功 `3/3`，分别使用177、172、179个策略周期。共528个周期全部使用 `TASK/task_poe`，误触发 `VERIFY_LINK`、`RECOVERY`、关系稳定等待或错误重入的周期为0。

## 故障 A/B

每个单元的故障均在全部12个方法×样本回合中有效触发。

| 故障 | DynaMAC V4 | 仅闭环进度 | 进度＋动态角色 | 完整方法 |
|---|---:|---:|---:|---:|
| 时间停滞 | 3/3 | 3/3 | 3/3 | 3/3 |
| 首次抓取失败 | 0/3 | 0/3 | 0/3 | 3/3 |
| 已建立关系后失配 | 0/3 | 0/3 | 0/3 | 3/3 |
| 意外掉落 | 0/3 | 0/3 | 0/3 | 3/3 |

时间停滞持续12个控制周期，四种方法最终都能完成任务，因此成功率不能区分其行为。固定时钟在停滞期间继续推进12个离散状态；三种闭环方法在三条样本中分别推进 `5,4,4` 个状态，平均4.33，随后因观测不再支持后续状态而冻结，运动恢复后重新对齐。该结果证明闭环进度显著减少空推进，但不表述为“第一个静止周期立即完全冻结”。

## 完整恢复链证据

完整方法在三类关系/抓取故障中均真实执行了恢复动作，而不是只改变最终标签：

| 故障 | 稳定等待 | LINK锚点 | 主动验证 | 原路返回 | 重入对齐 |
|---|---:|---:|---:|---:|---:|
| 首次抓取失败 | 6 | 48 | 60 | 27 | 0 |
| 关系失配 | 8 | 48 | 60 | 28 | 3 |
| 意外掉落 | 9 | 64 | 60 | 28 | 14 |

意外掉落的早期试跑暴露了一个执行闭环问题：掉落物体尚在滑动和旋转时，若每周期按当前物体位姿重新实例化 LINK 锚点，机械臂会追逐移动目标，最终在合法锚点处耗尽 IK。最终实现要求参考实体先连续稳定，再锁定本次恢复锚点；预抓取阶段若实体显著移动，则回到稳定等待。该机制不读取任务名或 StateId。修复后3条意外掉落样本均完成“稳定等待→锚点→主动验证→返回→重入→任务成功”。

## 产物

- `results/v1/summary.json`：机器可读完整汇总；
- `results/v1/summary.csv`：16个故障×方法单元；
- `results/v1/time_stall_state_advancement.csv`：时间停滞逐回合状态推进；
- `results/v1/raw_results/`：17个最终正常/故障回合摘要，不依赖 runtime 的忽略目录；
- `results/v1/SHA256SUMS`：原始结果、逐周期诊断、模型、配置及关键源码哈希；
- `diagnostics/*_final_v21/`：39条无损压缩的逐周期 `JSONL.gz` 轨迹。

本 pilot 支持“闭环进度减少时间空推进、动态角色参与关系失配后的执行阻断、完整恢复链使受控抓取/关系故障重新完成任务”的组件级结论。动态角色消融本身在后三类故障中仍为0/3，说明只阻断错误动作并不足以恢复成功，不能把完整方法的成功单独归因于角色切换。要形成论文正式实验，仍需冻结提交后扩大任务、variation、seed 和样本数，并预先规定统计方法与失败分类。

## 正式评测前机制收口

在不改写上述 v16/v18 pilot 身份的前提下，正式评测前又完成三项通用闭环修复：TASK 夹爪改由每臂进度对齐与边界事务授权，执行器 `reached/progressed/stopped` 只保留物理反馈语义；VERIFY_LINK 在原路返回结束时精确提交稳定周期的原二元关系后验；同一目标的碰撞感知线性路径若“生成成功但物理停滞”，后续周期会继续尝试碰撞感知非线性路径和最后的碰撞放宽线性路径。这些规则均不读取任务名、seed、StateId 或对象身份。

快速真实门控使用封存正常索引0：PlaceCups 在190周期成功；Handover 在245周期首次成功、0 InvalidAction，诊断续跑中右臂完成 VERIFY_LINK 并以 `[0.127675, 0.872325]` 的原始在线后验返回 TASK。定向测试79项通过，全仓为 `651 passed, 4 skipped in 323.72s`。正式评测四种方法将统一使用 `stage6_hybrid_cartesian_executor_v17` / `rlbench-stage6-hybrid-cartesian-continuation-v20`；本节只是启动前门控，不与上方 pilot 合并统计。机器可读摘要保存在 `results/preformal_gate_v1/summary.json`。

```text
9009094a2903b447c1d4421e22ae27db1fea4ce5eaadfe53d8b284b753f3418c  integrations/rlbench/results/diagnostics/place_cups_v18_task_gripper_auth_ep0_h300.json
1221a041815ff7cdb9381097ad09095a4e44f3cb1fb2b4f9b40fc3247b8d6676  integrations/rlbench/results/diagnostics/handover_v21_gripper_q_path_fallback_ep0_h500.json
```

## 正式矩阵启动止损与基线语义恢复

首轮正式矩阵尚未形成任何完整有效单元时，PlaceCups 封存索引0的 DynaMAC V4 对照跑满1000周期；冻结发布结果中同一回合应为186周期成功。检查确认共享适配器错误地使用执行器 `progressed/stopped` 回滚冻结基线时钟，并把基线夹爪延迟到笛卡尔 `reached`，从而改变了对照方法本身。该批未完成数据已从正式结果目录移除，不参与任何统计。

修复后，冻结 DynaMAC 每个正式提交事务仍消费一个原固定时钟状态，主动作同时执行固定时钟产生的夹爪命令；执行器标签只作为物理反馈。相同 PlaceCups 索引0重新得到186周期成功、0 InvalidAction，与冻结 V4 发布结果一致。闭环三种方法的进度后验、边界和 TASK 夹爪授权没有改动。事务与适配定向测试为 `67 passed, 1 skipped`，机器可读证据见 `results/preformal_gate_v2/summary.json`。

```text
d167842f5b90533dc28495090526fe65f19e674e8202b411fbec1b70850b3f69  integrations/rlbench/results/diagnostics/place_cups_dynamac_clock_decoupling_ep0.json
```

## 正式矩阵二次止损与边界虚拟帧时序修复

首轮正常矩阵继续运行到 OpenMicrowave 时，三种闭环方法出现可重复的系统性退化，而冻结 V4 仍接近正常水平。逐周期对齐发现，冻结 DynaMAC 在边界周期先执行“源技能终止连续位姿＋目标技能入口夹爪”的混合桥接动作，再从下一次 post-action 观测捕获目标技能虚拟帧；闭环策略却在桥接动作前用 pre-action 末端位姿提前建立了虚拟帧。OpenMicrowave 的一次实测锚点偏差为3.19 mm，足以使后续把手抓取失败并触发正确但非根因的关系阻断。

通用修复删除边界事务中的提前捕获；入口夹爪直接从已提交 `StateNode` 读取，不再为了获得夹爪而提前查询目标技能连续位姿。桥接动作执行后的下一周期由正常观测适配统一捕获虚拟帧。该修复不读取任务名、episode、StateId 特例或故障标签，也不新增门控。

修复后使用 OpenMicrowave 封存正常索引0至9做只读门控：仅闭环进度、进度＋动态角色和完整方法均为9/10；冻结 V4 在同一10条中同样为9/10，四种方法失败的都是索引8。阶段三、四、六、正式运行器与诊断工具相关回归为 `145 passed, 1 skipped`。由旧时序产生的半程正式数据不进入统计，正式矩阵将从干净结果目录重新开始。
