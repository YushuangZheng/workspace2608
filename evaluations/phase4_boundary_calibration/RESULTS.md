# 阶段四边界标定正式结果

当前正式结果位于 `results/v5/`。它只使用冻结 V4 的五条正常训练示范、原始
RLBench 控制周期观测及正常末端保持回放，没有使用故障数据调参。最终目标项继续使用
当前 mode 全部目标流的世界坐标精度加权联合 PoE，只对真实末端位姿评分一次。

连续确认周期不再附加与边界数据无关的全局0.10秒下限。对每个边界，令正常进度进入
末端窗口前最长连续误就绪段为 $M_b$，取 `H=M_b+1`，再验证所有正常末端保持回放均能
连续维持至少 `H` 个周期。当前分布为：30个边界 `H=1`、9个边界 `H=2`、1个边界
`H=3`（WipeDesk `1→2`）。

关键结论：

- 40/40 个技能边界完成 `theta_local` 与 `H` 标定；
- 最终运行配置送入真实 `EntryGuard` 后，200/200 个边界×正常示范通过；
- 真实进度进入终止窗口前提前放行0次；
- StoreBottle 左右臂、WipeDesk 以及其余全部边界通过；
- LiftTray `left:1→2 + right:1→2` 的真实事务组在5/5条正常示范中共同就绪；
- `results/v5/SHA256SUMS` 覆盖配置、表格、压缩逐周期轨迹、环境记录和运行配置。

本结果证明正常入口守卫参数可用，不等同于故障恢复成功率或完整在线仿真任务成功率。
