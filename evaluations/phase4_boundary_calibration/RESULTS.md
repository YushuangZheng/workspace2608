# 阶段四边界标定正式结果

正式结果位于 `results/v3/`，只使用冻结 V4 的五条正常训练示范及其原始 RLBench 控制周期观测，没有使用故障数据调参。

关键结论：

- 40/40 个技能边界完成 `theta_local` 与连续确认周期 `H` 标定；
- 39个边界使用 `H=2`（0.10秒），WipeDesk `1→2` 使用 `H=4`（0.20秒）；
- 最终运行配置重新送入真实 `EntryGuard` 后，200/200 个边界×正常示范通过；
- 真实进度进入终止窗口前提前放行0次，正常末端保持后最终放行200/200；
- StoreBottle 左右臂与 WipeDesk 全部边界通过；
- LiftTray `left:1→2 + right:1→2` 形成一个真实事务组，双方在5/5条正常示范中共同达到入口许可；
- 结果目录中的配置、表格、压缩逐周期轨迹、环境记录及运行配置均通过 `SHA256SUMS`。

本结果证明正常入口守卫的参数可用性、真实 LiftTray 事务组的联合就绪性与代码放行逻辑，不等同于故障恢复成功率或完整在线仿真任务成功率。联合事务的原子批量写入语义由阶段四自动化测试直接覆盖；本次真实回放验证的是事务成员元数据与双方守卫可在同一正常末端保持过程中共同放行。阶段五 `VERIFY_LINK` 和恢复动作尚未实现。
