# 新方法代码开发计划

# 一、开发总原则

现有 GitHub 仓库已经包含 DynaMAC、MiDiGaP、TAPAS 技能分段、单双臂 RLBench 运行时、训练数据适配、评测与测试代码，因此新方法应当直接复用这些能力，不再并行开发第二套 SE(3) 运算、流形高斯、PoE、技能分段或 RLBench 控制器。

已经打好的 DynaMAC tag 作为基线对照即可。在开发分支中：

- 允许对现有 DynaMAC 代码进行必要重构；
- 优先复用现有类、数据结构和数学函数；
- 不要求为了冻结基线建立复杂的副本、golden trace 或重复校验系统；
- 只需保证论文对比时仍能运行原始固定时钟 DynaMAC；
- 新方法关闭时，应有一条清晰的 baseline 执行路径。

开发工作的核心是保留已有多流策略能力，同时把**动作查询、关系估计、进度估计和进度控制**重新组织成闭环。

本计划严格按照当前技术路线实现：

$$
\bar\beta_t
\rightarrow
q_t
\rightarrow
\beta_t
\rightarrow
\text{动态流角色}
\rightarrow
\text{保持/推进/重新对齐}
\rightarrow
\text{入口守卫}
\rightarrow
\text{主动关系验证/关系恢复}.
$$

关系空间只包含：

$$
\{\mathrm{external},\mathrm{linked}\},
$$

Unknown 是证据不足时的决策状态。在线证据使用位姿、动作、夹爪和跟踪可靠性。

顶层执行控制使用独立的：

```text
TASK / VERIFY_LINK / RECOVERY
```

执行模式。它不属于任务进度 $s=(k,\tau)$，也不属于 DynaMAC/MiDiGaP 的轨迹 mode。

---

# 阶段一：统一离线任务模型，并完成策略查询与时钟推进解耦

## 1. 阶段目标

一次性整理后续关系滤波、进度滤波、入口守卫和恢复模块共同需要的示范统计，形成统一的离线任务模型。

同时改造现有 DynaMAC，使多流策略能够在指定技能状态下被查询，而不是每次查询后自动推进时间。

这一阶段完成后，应当具备：

```text
指定任意状态 s=(k,τ)
→ 读取该状态的多流轨迹、关系先验和稀疏场景状态
→ 使用指定流权重查询动作
→ 不自动改变技能和时间索引
```

---

## 2. 开发内容

### 2.1 建立统一状态索引

定义：

```python
StateId = (skill_index, local_index)
```

对每个状态保存：

- 技能编号；
- 技能内进度；
- 该进度状态下的模态先验和各模态分量；
- 前驱状态；
- 后继状态；
- 是否位于技能末端；
- 是否存在跨技能后继。

所有后续模块必须通过同一个 `StateId` 和同一个离线任务模型访问状态，不应分别解析 `SkillModel`、`StreamModel` 和 checkpoint。

模态不是独立的任务进度节点。同一 `(skill_index, local_index)` 的流均值、协方差、关系先验、夹爪命令和场景因子均保留其内部模态维；动作查询需要指定模态时，使用独立的 `mode_index`。因此每只机械臂的状态节点数量为各技能 `duration` 之和，不乘以模态数。

---

### 2.2 建立 `ClosedLoopTaskModel`

建议新增：

```python
@dataclass
class ClosedLoopTaskModel:
    base_policy: DynaMAC
    states: dict[StateId, StateNode]
    skill_states: dict[int, tuple[StateId, ...]]
    boundaries: dict[BoundaryId, BoundaryModel]

    link_anchors: dict[RelationEventId, LinkRecoveryAnchor]
    unlink_events: dict[RelationEventId, UnlinkEventMetadata]
    link_origins: dict[RelationStateKey, RelationEventId]
```

其中每个 `StateNode` 至少包含：

```python
@dataclass
class StateNode:
    state_id: StateId

    mode_priors: np.ndarray

    stream_means: dict[str, np.ndarray]       # 每项 [M, 7]
    stream_covariances: dict[str, np.ndarray] # 每项 [M, 6, 6]

    demo_relation_priors: dict[str, np.ndarray] # 每项 [M, 2]

    selected_frames: tuple[str, ...]
    gripper_commands: np.ndarray # [M, G]

    scene_factor_models: dict[
        FactorId,
        dict[int, FactorDistribution],
    ]
```

这里的 `stream_means` 和 `stream_covariances` 应直接引用或读取现有 DynaMAC/MiDiGaP 拟合结果，不重新拟合底层轨迹模型。

#### 对侧机械臂末端流的执行依赖筛选

DynaMAC Eq.6 对双臂示范产生的 `selected_frames` 是候选动作参考，不足以单独证明
对侧机械臂末端对本臂具有连续控制作用。两臂在少量正常示范中同步运动、共同随任务
根节点平移或同时停留，都可能使对侧末端获得较高的轨迹预测分数，但这种统计同步不应
自动变成 PoE 动作专家、进度轨迹证据或本地完成目标。

双臂任务模型必须对每个 `(skill, mode)` 的已选对侧末端候选执行一次任务名称无关的
执行依赖筛选。满足以下任一条件时才保留：

1. 本臂在该技能和模态中具有某个活动 `link_origin`，且对侧机械臂对同一实体具有正式
   LINK 或 `LINK_PENDING` 事件，形成共享实体或交接关系路径；
2. 当前模态没有已经解释该动作的普通物理任务参考系，对侧末端是唯一非虚拟几何目标，
   且正常示范呈现跨示范一致、非零并相对终点分散度占优的定向相对几何运动。

不满足时，只从闭环 sidecar 的该模态 `selected_frames` 中删除对侧末端，并同步删除其
连续进度轨迹项和本技能 `LocalCompletionModel` 目标；冻结 DynaMAC V4 checkpoint 及其
Eq.6 审计结果不改写。对侧机械臂是否已就绪仍只能由统一 `BoundaryModel` 中可观测、
跨示范稳定的关系条件或场景构型条件表达，不新增对侧进度门控、固定同步时间或任务专用
协作守卫。仅有同步而没有共享实体、交接关系或可辨识连续相对几何时，数据本身不能识别
因果执行依赖，正式实现采取保守删除而不是把同步相关性当作控制关系。

---

### 2.3 从示范中统一提取关系先验

对每个状态 $s$ 和参考系 $f$，由现有连接分数或局部协方差生成：

$$
\pi_{f,s}^{\mathrm{demo}}(z),
\qquad
z\in\{\mathrm{external},\mathrm{linked}\}.
$$

需要保存：

```text
state
frame
link score
P(external)
P(linked)
```

关系先验应随状态索引存入 `StateNode`，供以下模块共享：

- 在线关系滤波；
- 进度关系兼容度；
- 动态流角色；
- 恢复目标判断；
- 技能边界关系条件。

---

### 2.4 统一构造技能条件化稀疏场景因子

对每个进度状态 $s$，从当前技能实际涉及的参考系构建活动实体集合：

$$
\mathcal V_s^{\mathrm{act}}.
$$

候选场景因子只包括两类：

1. 活动实体自身构型；
2. 活动实体图中候选边的相对构型。

候选因子的产生范围限定为：

- 当前技能中被多流策略使用的参考系；
- 当前状态局部邻域中被使用的参考系；
- 仅在边界候选集中引入的下一技能或边界相关参考系；
- 任务数据接口中与这些实体直接绑定的结构实体。

逐进度场景因子的局部邻域只用于离线候选因子生成。对
`StateId=(k,τ)` 的某个内部模态，只在相同 skill、相同 mode 内建立
邻域，两个实体必须在该状态或其局部邻域内共同活动才能生成
候选边。邻域半径按 `r=0,1,2` 依次检查，选择通过跨示范支持率和
分布稳定性检查的最小半径；常规预期为 `r=1`，`r=2` 仍不稳定则
删除，不继续扩大。邻域不得跨技能边界，也不依赖人工实体对白名单。下一技能入口
相关实体只进入边界候选集，不扩散到当前技能的所有逐进度状态。

对每个候选因子，使用正常示范进行留一验证。只有同时满足以下条件才保留：

- 所有或绝大多数示范中可观测；
- 分布能够稳定估计；
- 加入该因子后，当前局部状态识别或边界状态识别得到可重复提升；
- 与已有机器人轨迹或关系变量不完全重复。

逐进度因子的按示范留一评分以现有 DynaMAC 机器人轨迹作为局部状态识别基线，并叠加已经保留的场景因子，不加入名义时间进度先验或关系证据。候选因子加入前后均以真实状态相对局部错误候选的 `logsumexp` 间隔计算增益，并按每轮最大可重复增益进行前向选择。五条正常示范时，软进度因子至少需要四个留一折为正增益，且平均增益超过统一最低阈值。与机器人轨迹或关系变量不重复通过候选因子的定义和排除规则保证，不通过向识别评分额外加入关系兼容项实现。

最终为每个状态保存：

$$
\mathcal J_s
$$

以及：

$$
p_j(x\mid s),
\qquad
j\in\mathcal J_s.
$$

关系后验 $q_t^{i,f}$ 由关系模型单独表示，各机械臂的进度后验 $\beta_t^i$ 由各自进度模型单独表示。

---

### 2.5 从同一示范数据构建技能边界守卫模型

对每个机械臂和每个技能边界：

$$
b:k\rightarrow k+1,
$$

在当前技能末端定义边界前短终止窗口：

$$
\mathcal B_b^-
=
\left\{
(k,T_k-h_b^-+1),
\ldots,
(k,T_k)
\right\},
$$

其中 $h_b^-$ 为终止窗口长度。

技能边界守卫复用统一任务模型中已有的三类信息：

1. **本地技能完成信息**；
2. **机械臂—参考系关系信息**；
3. **技能条件化稀疏场景构型信息**。

#### 本地技能完成定义

为边界 $b$ 保存当前技能完成所需的状态统计，包括：

- 技能末端状态窗口；
- 当前技能末端目标分布；
- 本臂在技能末端应满足的关系条件；
- 本地完成度计算所需的阈值或支持范围。

其中，本臂必要关系集合只包含当前技能由本臂直接建立或解除、且在技能完成时必须满足的关系：

$$
\mathcal R_{i,b}^{\mathrm{own}}.
$$

对每个 $c=(i,f)\in\mathcal R_{i,b}^{\mathrm{own}}$，由边界前终止窗口中的正常示范关系先验拟合目标关系分布：

$$
\psi_{b,c}^{\mathrm{own}}(z).
$$

阶段一保存整个终止窗口、最终状态 $T_k$ 的正常示范目标分布、本臂必要关系集合及其目标关系分布。阶段一不最终标定本地完成阈值 $\theta_{i,b}^{\mathrm{local}}$ 和连续确认周期 $H_{i,b}$；二者在阶段二、三的 $q_t$、$\beta_t$ 和执行链完成后，于阶段四仅使用正常示范回放或留出正常轨迹统一标定，不得使用故障测试集调参。

该信息在运行时用于计算：

$$
R_{i,b,t}^{\mathrm{local}}.
$$

#### 边界关系条件

从边界相关的活动关系图中选择需要在跨界前成立的机械臂—参考系关系，形成：

$$
\mathcal R_b
=
\{(i,f,z^\star)\}.
$$

其中：

- $i$ 表示相关机械臂；
- $f$ 表示相关参考系；
- $z^\star\in\{\mathrm{external},\mathrm{linked}\}$ 表示边界所要求的目标关系。

对每个关系条件，根据终止窗口内的示范关系先验构造边界关系支持分布：

$$
\psi_{b,(i,f)}^{\mathrm{rel}}(z)
=
\sum_{\tau\in\mathcal B_b^-}
\omega_{b,\tau}\,
\pi_{i,f,(k,\tau)}^{\mathrm{demo}}(z),
$$

其中：

$$
\sum_{\tau\in\mathcal B_b^-}\omega_{b,\tau}=1,
$$

并令越接近技能末端的状态具有越高权重。

边界关系条件按以下离线流程构建：

1. 收集下一技能所有 `affected_arms` 的 `selected_frames`；
2. 仅为这些 selected frames 生成 arm-frame 关系候选；
3. 从各臂边界前短终止窗口拟合关系支持分布；
4. 选择最大支持关系作为目标关系；
5. 检查总体支持度、全部正常示范的一致性和观测可用率；任一正常留一折不一致均删除；
6. 删除已由本臂 `LocalCompletionModel`、同一事务中其他机械臂 `LocalCompletionModel` 保证的条件，以及与已有条件完全重复的关系；
7. 将剩余关系及其目标支持分布、可靠性统计保存到 `relation_conditions`。

单臂边界先生成本臂候选。双臂边界先利用未去重的硬依赖和正常示范边界同步性确定事务组，再合并事务中所有 affected arms 的候选，最后执行跨臂 LocalDone 去重并保存最终 `BoundaryModel`。

#### 边界场景构型条件

从第 2.4 节建立的稀疏场景因子库中，选取由下一技能所使用的参考系、边界关系条件涉及的实体及其直接结构关联实体构成的候选因子；再保留在边界前短终止窗口中具有稳定支持分布且能够可靠观测的因子，形成：

$$
\mathcal J_b^{\mathrm{guard}}.
$$

对每个场景构型条件 $j$，由全部成功示范拟合：

$$
\psi_{b,j}^{\mathrm{guard}}(x)
=
\frac{1}{N}
\sum_{n=1}^{N}
\sum_{\tau\in\mathcal B_b^-}
\omega_{b,\tau}\,
r_{b,\tau}^{j,(n)}(x),
$$

其中：

- $N$ 为示范数量；
- $r_{b,\tau}^{j,(n)}(x)$ 为第 $n$ 条示范中条件 $j$ 的状态分布；
- $\omega_{b,\tau}$ 为终止窗口内的时间权重。

边界场景条件是硬入口条件，必须通过全部按示范留一折。连续因子的在线兼容度使用归一化马氏兼容度 `exp(-d²/2)`；每个留一折取边界前短终止窗口内的最低兼容度，正式阈值取全部正常留一折下界并减去仅用于数值容差的微小余量。

双臂模型仍使用同一个候选生成、留一筛选和 `BoundaryModel.scene_conditions` 接口。若机械臂 $r$ 的本地完成目标在上述执行依赖筛选后仍由机械臂 $i$ 的末端参考系表达，且两条正常示范边界在全部示范中稳定对应，则该跨臂相对构型也是机械臂 $i$ 跨界前必须等待的物理就绪候选。构建器将同一规范化双臂末端边因子加入机械臂 $i$ 的边界候选，并仍要求全部正常留一折覆盖后才保存。被判定为单纯同步的对侧末端流不得通过这条路径重新进入边界模型；真正的对侧就绪要求仍可由共享/交接关系和普通边界场景因子进入同一个守卫。该因子只进入对应边界的 $\mathcal J_b^{\mathrm{guard}}$，不扩散到逐进度 $\mathcal J_s$，也不新增独立协作守卫、对侧进度条件、固定时间差或第二套阈值。若双方互相具有同一硬条件，再按统一事务规则判断是否需要原子提交；单向条件只让依赖方等待。

最终 `BoundaryModel` 保存：

```python
@dataclass
class BoundaryModel:
    boundary_id: BoundaryId
    source_skill: int
    target_skill: int

    local_completion_model: LocalCompletionModel

    relation_conditions: dict[
        RelationConditionId,
        RelationGuardDistribution,
    ]

    scene_conditions: dict[
        FactorId,
        FactorDistribution,
    ]

    scene_condition_thresholds: dict[FactorId, float]

    condition_reliability: dict[ConditionId, ReliabilityStatistics]

    affected_arms: tuple[str, ...]
    transaction_group: TransactionGroupId | None
```

其中：

- `local_completion_model` 描述本臂当前技能是否完成；
- `relation_conditions` 描述跨界前需要成立的机械臂—参考系关系；
- `scene_conditions` 描述跨界前需要进入支持域的稀疏场景构型；
- `affected_arms` 和 `transaction_group` 用于多机械臂边界的统一提交。

`transaction_group` 只标记必须在同一控制周期原子提交的联合边界。离线构建首先要求左右边界在全部正常示范中稳定同步，再要求双方具有对称的共同硬依赖，具体包括以下任一情况：

- 双方各自与同一个物理实体保持必须成立的 `linked` 关系；
- 双方对同一个物理实体的正式 `LINK` 建立过程跨越该边界：保存的短局部轨迹从源技能开始，首次确认的连续 linked 区间位于目标技能，并且该事件得到全部正常示范支持；
- 双方具有完全相同的硬场景构型条件。

这里只观察其他机械臂状态的单向守卫不构成双方共同物理依赖；`LINK_PENDING` 也不能单独创建事务组。独立边界、单向等待边界以及只有时间同步而没有共同硬依赖的边界保持 `None`。

---

### 2.6 离线构建关系转移模板与任务重入信息

对正常示范中每个任务相关的：

$$
\mathrm{external}\rightarrow\mathrm{linked}
$$

关系事件，建立事件索引：

$$
e=(i,f,k,m,r),
$$

其中：

- $i$ 为执行该关系建立的机械臂；
- $f$ 为被连接的参考系；
- $k$ 为关系事件所在的技能上下文；
- $m$ 为对应操作模态；
- $r$ 用于区分同一技能中可能出现的多个关系事件。

从全部对应示范中提取关系建立前的短局部轨迹：

```text
接近
预抓取
闭合夹爪
关系建立
```

拟合并保存：

```python
@dataclass
class LinkRecoveryAnchor:
    event_id: RelationEventId
    arm_id: str
    frame_id: str
    context_state: StateId

    local_means: np.ndarray
    local_covariances: np.ndarray
    gripper_commands: np.ndarray

    linked_entry_states: tuple[StateId, ...]
```

`LINK` 锚点以机械臂—参考系—关系事件为单位构建。在线发生关系失配时，根据当前需要恢复的关系事件查询对应锚点，并使用参考系当前位姿重新实例化。

离线关系事件首先由同一 mode 的全部正常示范共同学习逐状态关系先验 $\pi_{\mathrm{demo}}$，再在完整的联合关系先验序列上使用 `external≤0.3`、`linked≥0.7` 的迟滞阈值和前后最短稳定窗口检测 LINK/UNLINK 候选。候选事件使用按示范留一一致性检查：每一折用其余 $N-1$ 条正常示范重新拟合联合关系先验，并检查同类型事件能否在统一位置容差内重现；五条示范时至少要求四折支持。验证通过后，事件位置取全部示范联合模型的检测结果，并重新使用全部对应正常示范拟合最终 LINK 锚点或 UNLINK 脱离信息。LINK 建立后到对应 UNLINK 前的 linked 状态保存 `link_origin` 映射，以便执行中关系破坏时追溯正确的事件级恢复锚点。

正式事件还用于校正部署时的关系期望连续性：一旦正式 LINK 建立，在对应正式 UNLINK 前不能因逐状态跨示范协方差的局部抖动退回 external；正式 UNLINK 后再恢复 external。原始 GMSD/协方差分数继续保留用于审计，`LINK_PENDING` 不参与该传播。事件状态写回 `StateNode.demo_relation_priors` 时仍保存为可被在线观测推翻的软先验，当前统一 linked 为 `[0.3,0.7]`、external 为 `[0.7,0.3]`，不得写成 one-hot；边界支持分布也复用这一软先验，并将总体支持阈值与稳定二元关系阈值 `0.7` 对齐，全部正常留一折一致性仍单独严格要求。

没有形成正式 LINK/UNLINK、也不处于 `LINK_PENDING` 候选支持区间的孤立低协方差 linked 脉冲，只保留在 `demo_relation_scores` 中用于审计，不得写成控制期 linked 期望。部署关系先验继续保持事件状态机的 soft external 状态。已经通过跨示范一致性检查的 Pending 闭合候选状态本身写入 `[0.3,0.7]` 的 soft linked 期望；其后只在原始跨示范协方差继续支持 linked 的连续区间保持该软期望，支持结束即恢复 soft external。该写回不要求闭合当帧已经具有事后运动激励，并作为独立覆盖层与同一 arm-frame 更早的正式 LINK/UNLINK 事件状态机共存；它只表达正常任务在该 occurrence 建立连接的可推翻期望，不建立或传播正式 `link_origin`。

正式 LINK 与 LINK_PENDING 的局部轨迹均使用固定最大窗口 `link_anchor_horizon`，当前默认值为16个对齐后的离散进度状态。设当前事件终点的全局状态索引为 $e$，并在该机械臂所有可恢复参考系的稳定关系序列中查找 $e$ 之前最近一次 linked→external 的位置 $u$，则轨迹起点为：

$$
s_{\mathrm{start}}
=
\max\left(0,e-H_{\mathrm{anchor}}+1,u+1\right).
$$

若不存在先前 UNLINK，则忽略 $u+1$。截断后不向更早状态回填到固定长度；轨迹允许跨技能边界，但不得跨过同一机械臂最近一次 UNLINK，从而避免把上一段抓取或释放轨迹混入本次模板。正式 LINK 以第一个稳定 linked 状态为终点，LINK_PENDING 以跨示范对齐的本次闭合状态为终点，两者共用同一起点计算与轨迹拟合实现。

对于正常示范中的 LINK 假设，如果跨示范 GMSD、抓取闭合模板和事件位置留一一致性均支持 external→linked，但闭合后最短稳定窗口完全缺少有效运动激励，无法离线确认也没有运动反证，则不将它强判为 LINK 或 external，而保存：

```python
@dataclass
class LinkPendingCandidate:
    event_id: RelationEventId
    arm_id: str
    frame_id: str
    candidate_state: StateId

    context_state: StateId
    local_means: np.ndarray
    local_covariances: np.ndarray
    gripper_commands: np.ndarray

    support_fraction: float
    demonstration_indices: tuple[int, ...]
    event_local_indices: tuple[int, ...]
```

`LINK_PENDING` 是离线事件候选状态，不是第三种关系概率状态，也不等同于在线决策中的 `Unknown`。它保存“这次 LINK 事件可重复出现，但示范缺少确认运动”，并使用与正式 LINK 锚点相同的拟合口径，保存候选建立前的短局部轨迹、协方差和夹爪命令。该轨迹只是未激活的恢复模板：离线阶段不生成正式 `LinkRecoveryAnchor`、不传播 `link_origin`、不计入阶段一 `LocalCompletionModel` 的已完成必要关系，也不执行 `VERIFY_LINK`。若同一闭合事件在 LINK 锚点窗口内稍后已经获得充分共动证据并通过留一检查，则只保留正式 LINK，不重复保留 Pending。

该表示支持同一关系事件在不同 episode、不同世界位置和不同对象姿态下重复使用。

对于正常示范中的：

$$
\mathrm{linked}\rightarrow\mathrm{external},
$$

保存：

```python
@dataclass
class UnlinkEventMetadata:
    event_id: RelationEventId
    arm_id: str
    frame_id: str

    release_state: StateId
    legal_reentry_states: tuple[StateId, ...]

    local_detachment_target: np.ndarray | None
```

其中主要记录：

- 正常关系解除位置；
- 关系解除后的合法任务状态；
- 必要的局部脱离信息。

在线 `UNLINK` 使用统一关系解除过程，并使用上述元数据完成关系验证与任务重入。

---

### 2.7 重构 DynaMAC 查询接口

在现有 DynaMAC 基础上增加一个指定状态查询接口，例如：

```python
query_state(
    observation,
    state_id,
    stream_weights=None,
    *,
    mode_index=None,
) -> DynaMACAction
```

该接口负责：

- 读取指定状态的多流均值和协方差；
- 将局部 marginal 变换到世界坐标；
- 按给定流权重执行 PoE；
- 输出位姿、协方差、夹爪和诊断信息。

该接口不自动执行：

```text
time_index += 1
skill_index += 1
```

原始 `act()` 可以重构为：

```text
读取当前 cursor
→ query_state()
→ 按原 DynaMAC 逻辑推进 cursor
```

从而复用同一查询代码。

---

## 3. 阶段交付物

建议形成：

```text
source/policy/closed_loop/
├── task_model.py
├── task_model_builder.py
├── state_index.py
├── scene_factors.py
├── boundary_model.py
├── relation_events.py
└── query_adapter.py
```

以及对应测试。

离线模型应支持保存和加载，并记录其依赖的基础 DynaMAC 模型标识。附加模型只保存新统计量，不重复保存已有大规模流均值和协方差。

---

## 4. 验收标准

阶段一完成需满足：

1. 所有技能状态都有唯一 `StateId`；
2. 状态数量等于各技能 duration 总和；
3. 每个状态能正确访问原有 MiDiGaP 流分布；
4. 每个参考系的关系先验归一化；
5. 每个状态只有稀疏场景因子，不生成全对象两两组合；
6. 每个技能边界均生成完整的 `BoundaryModel`；
7. 边界窗口为当前技能末端的 $h_b^-$ 个状态；
8. 任务相关 external→linked 假设在有充分运动证据时生成正式 `LINK` 锚点，无反证但激励不足时保存带未激活事件前轨迹模板的 `LINK_PENDING`，有充分运动反证时不保留；
9. 每个 linked→external 事件保存关系解除位置和合法重入信息；
10. `query_state()` 可查询任意状态；
11. `query_state()` 不自动推进任务时钟；
12. 原始 DynaMAC 执行路径仍可运行；
13. 所有 SE(3)、四元数、流形高斯与 PoE 计算复用现有代码。

---

# 阶段二：建立统一在线观测，并实现关系—进度联合信念推断

## 1. 阶段目标

完成技术路线的核心状态估计链：

$$
\bar\beta_t
\rightarrow
q_t
\rightarrow
\beta_t.
$$

该阶段先确保关系和进度估计正确、可解释。它可以先以旁路方式运行，不立即改变机器人动作。

---

## 2. 开发内容

### 2.1 建立统一在线观测

定义一个新方法使用的运行观测：

```python
@dataclass
class RuntimeObservation:
    tick: int

    ee_pose: np.ndarray
    frame_poses: dict[str, np.ndarray]

    gripper_state: np.ndarray

    previous_command_pose: np.ndarray | None
    previous_ee_pose: np.ndarray | None

    tracking_reliability: dict[str, float]
    frame_visibility: dict[str, bool]
```

在线关系与进度模块共享该数据结构。

当前 RLBench 实现以低维仿真状态作为训练和推理输入，可以继续沿用这一观测来源，不增加新的感知系统。

---

### 2.2 统一计算运行特征

每个控制周期一次性计算：

- 实际末端运动；
- 上一命令对应的目标运动；
- 各参考系世界位姿变化；
- 末端—参考系相对位姿；
- 相对运动残差；
- 夹爪当前状态及变化；
- 参考系可见性；
- 跟踪可靠性；
- 动作激励程度。

这些特征由统一 `RuntimeFeatureBuilder` 生成，关系滤波和进度滤波共享同一组结果。

---

### 2.3 构造动作后名义进度先验

根据上一周期进度后验和上一动作正常完成的默认假设，构造：

$$
\bar\beta_t(s).
$$

主要概率集中在上一动作实际发送的目标状态，同时为邻近的：

- 未完全完成状态；
- 正常完成状态；
- 稍微提前完成状态；

保留非零概率。

在技能内部只生成局部候选。

在技能末端，只有入口守卫已经放行，名义进度先验才允许进入下一技能首状态；否则保持在当前技能末端。

在阶段三执行控制器接入后，上一动作 $a_{t-1}$ 的离散动作锚点由上一周期实际查询的 `reference_state` 给出。DynaMAC 的 `query_state(reference_state)` 直接生成该状态对应的目标位姿，因此“动作正常完成”质量落在 `reference_state`，“略微提前完成”质量落在其直接后继；“动作未完全完成”分支仍必须保留上一周期进度后验 $\beta_{t-1}$ 的概率质量，不得用 one-hot `reference_state` 替换整个进度后验。在固定离线示范回放中，若控制器查询的反事实动作没有真正施加到环境，则必须使用真正产生当前观测的示范动作目标及其 StateId 作为动作证据，不得混用未执行的控制器输出。

边界事务提交后，跨界前的进度质量必须在下一周期投影到已提交的下一技能入口，正常执行不得再次越过该已提交边界返回上一技能。当前周期已返回的源技能信念只保留为审计记录；关系后验、场景观测和 mode 不重置。多臂联合事务必须在全部成员通过并原子提交后同步投影，等待中的事务不得提前修改任何成员进度。

---

### 2.4 实现在线关系滤波

对每个机械臂—参考系关系维护：

$$
q_t^f(\mathrm{external}),
\qquad
q_t^f(\mathrm{linked}).
$$

关系更新使用：

- 上一周期关系后验；
- 名义进度先验加权得到的示范关系先验；
- 当前相对运动残差；
- 上一命令动作；
- 实际末端运动；
- 夹爪状态；
- 跟踪可靠性和可见性。

动作条件化观测似然对平移和旋转分量分别计算相对残差比，只让实际得到动作激励的分量参与。设分量动作幅值为 $a_c$、残差幅值为 $r_c$，使用 $w_c=a_c/\sum_{c'}a_{c'}$ 和 $d_c=r_c/\max(a_c,\epsilon_a)$ 构造有限分量似然 $\ell_c(z)$，并统一采用：

$$
p(y_{f,t}\mid z,a_{t-1})
=
\sum_c w_c\widetilde\ell_c(z),
$$

其中：

$$
\widetilde\ell_c(z)
=
(1-\epsilon_{\mathrm{out}})\ell_c(z)
+
\frac{\epsilon_{\mathrm{out}}}{2}.
$$

这里的加权混合表示对当前可靠运动分量进行边缘化，状态无关的离群分量使单帧跟踪抖动或瞬态接触证据保持有限，防止其通过乘积否决另一分量中的明确共动证据。夹爪只可作为弱的有界似然偏置，不能在没有运动信息时建立关系。不得为特定任务增加分量开关或阈值；单分量动作退化为普通似然，所有被激励分量持续失配时仍应确认 external。

关系信息权重必须统一包含实际末端运动激励、上一命令与实际响应的一致性、相邻两帧的成对跟踪可靠性，以及末端运动相对参考系独立运动的占优程度。没有可靠成对观测或没有实际响应时，$X_{f,t}=0$。

实现：

$$
\bar q_t^f(z)
=
\sum_{z'}
P_{\mathrm{rel}}(z\mid z')
q_{t-1}^f(z'),
$$

$$
\pi_{f,t}^{\mathrm{prior}}(z)
=
\sum_s
\bar\beta_t(s)
\pi_{f,s}^{\mathrm{demo}}(z),
$$

$$
q_t^f(z)
\propto
\bar q_t^f(z)
\left[
\left(
\pi_{f,t}^{\mathrm{prior}}(z)
\right)^{\lambda_d}
p(y_{f,t}\mid z,a_{t-1})
\right]^{X_{f,t}}.
$$

示范关系先验是解释当前动作响应的弱上下文，必须与观测似然一起由 $X_{f,t}$ 调温。$X_{f,t}=0$ 时更新严格退化为持久性预测 $\bar q_t^f$，不得在 HOLD 或静止周期中反复注入示范先验并据此改写已经观测到的物理关系。

Unknown 通过以下信息判断：

- 动作激励不足；
- 参考系不可见；
- 跟踪可靠性过低；
- 关系后验熵过高。

Unknown 不改变 external/linked 概率空间，而作为：

```python
informative: bool
```

或：

```python
decision_state = UNKNOWN
```

传给后续控制模块。后续控制直接联合读取同一 `RuntimeFeatures` 中已有的参考系可见性、跟踪可靠性和动作激励程度，与 `decision_state=UNKNOWN` 一起判断是否适合主动验证。只有参考系可见、跟踪可靠且实际动作激励不足时才能生成主动关系验证请求。

动作激励不足不应无条件抹去此前已经确认的关系。若该 arm-frame 关系先前已经由实际有效运动证据得到稳定 external/linked 判定，当前参考系仍可见且跟踪可靠，则离散决策可继续保留最近一次有效证据的方向，同时软后验继续按持久性模型预测；该周期仍标记为 `informative=False`，不得伪装成新增运动证据。新的、方向相反的可靠动作响应仍通过同一二元滤波器正常改写该记忆。没有先前稳定判定的初始静止或 `LINK_PENDING` 仍输出 `Unknown`，参考系不可见或跟踪不可靠时也必须输出 `Unknown`。

在后续 `VERIFY_LINK` 执行模式中，同一关系滤波器继续读取实际末端与参考系运动，并给出稳定的 `linked`、`external` 或仍为 `Unknown` 的决策结果。主动验证不增加第三种关系概率状态；`LINK_PENDING` 表示当前事件上下文命中了一个尚未获得关系判定的离线 LINK 候选。

其中，阶段一保存的 `LinkPendingCandidate` 是按事件 occurrence 持久化的离线候选；`Unknown` 是当前在线周期的关系决策。在线进入 Pending 对应事件阶段后，稳定 `linked` 表示本次 LINK 获得确认，稳定 `external` 表示本次 LINK 未建立，而 `Unknown` 表示事件仍未解析。

---

### 2.5 实现候选进度统一评分

对：

$$
s\in\mathcal W_t
$$

一次性计算：

#### 机器人局部轨迹匹配

$$
L_t^{\mathrm{robot}}(s).
$$

只让可靠且更可能 external 的参考系主要参与：

$$
\rho_{f,t}
=
a_{f,t}v_{f,t}
q_t^f(\mathrm{external}).
$$

其中 $a_{f,t}$ 是可观测指示，$v_{f,t}$ 是跟踪可靠性。轨迹证据直接使用
归一化二元后验中的 $q_t^f(\mathrm{external})$ 进行连续降权，不再用
离散 `Unknown` 决策删除可靠位姿证据。参考系不可观测或跟踪不可靠时仍必须
为零权重。这项口径只用于连续机器人轨迹匹配；离散关系兼容度、动态角色、
关系守卫和事务提交仍保留各自的 `Unknown` 语义。

机器人轨迹和连续场景因子在线评分统一使用峰值归一马氏支持：

$$
S(\delta;\Sigma)
=
\exp\left(-\frac12\delta^{\mathsf T}\Sigma^{-1}\delta\right),
$$

不得让高斯密度中的协方差行列式奖励更窄的状态。原始完整密度、马氏距离和行列式只作为诊断分项保存。

当前集成策略为每个技能固定一个实际轨迹分量。多个对象中心流的边缘均值一般不能由同一个世界末端位姿同时达到，因此机器人绝对支持还必须减去同一 PoE 在该候选状态下的联合可达峰值：

$$
\log L_t^{\mathrm{robot}}(s)
=
\min\left\{
0,
\sum_f\rho_{f,t}\log S_{f,t}(s)
-
\max_T\sum_f\rho_{f,t}\log S_f(T\mid s)
\right\}.
$$

联合峰值必须复用动作生成的参考系变换与 PoE 运算，不能建立第二套 SE(3) 或融合口径。这样绝对支持相对于真正可达到的多流最优状态归一，而不是相对于一组互不相容的边缘峰值归一。

#### 稀疏场景状态匹配

$$
L_t^{\mathrm{state}}(s)
=
\prod_{j\in\mathcal J_s}
\left[
S_j(x_{j,t}\mid s)
\right]^{a_{j,t}}.
$$

若场景观测本身为分布，则对峰值归一支持取期望；离散变量使用离散重叠。

#### 关系兼容度

$$
C_t^{\mathrm{rel}}(s)
=
\prod_f
\left[
\sum_z
q_t^f(z)
\pi_{f,s}^{\mathrm{demo}}(z)
\right]^{
v_{f,t}(1-U_{f,t})
}.
$$

上述原始离散关系兼容度继续原样参与进度后验。但在判断“是否存在任何可解释状态”的统一绝对支持度时，每个关系项必须消除离线软先验自身的峰值上限：

$$
\widetilde c_{f,t}^{\mathrm{rel}}(s)
=
\frac{
\sum_z q_t^f(z)\pi_{f,s}^{\mathrm{demo}}(z)
}{
\max_z \pi_{f,s}^{\mathrm{demo}}(z)
}.
$$

因此绝对解释度中，轨迹使用相对联合可达 PoE 峰值的支持度，连续场景高斯使用相对各自密度峰值的支持度，离散关系使用相对该软先验可达最佳重叠的支持度。各类证据再按各自有效权重和取几何平均，使完全匹配上限统一为 1，随后使用单一最低解释度判定 `NO_PLAUSIBLE_STATE`。该绝对解释度不替换进度后验中的原始 $C_t^{\mathrm{rel}}(s)$，也不改变关系滤波、动态角色或动作 PoE。

当候选状态需要通过“关系是否足够兼容”的绝对阈值才能进入事件感知扩展或恢复后重入时，使用统一的关系状态兼容度：

$$
c_{f,t}^{\mathrm{state}}(s)
=
\begin{cases}
\widetilde c_{f,t}^{\mathrm{rel}}(s),
& \arg\max_z q_t^f(z)=\arg\max_z\pi_{f,s}^{\mathrm{demo}}(z),\\
0,&\text{关系方向相反}.
\end{cases}
$$

离线关系先验近似持平、不对状态关系方向作出区分时，不施加方向限制。多个必要关系中任一可靠关系方向相反，该候选的关系状态兼容度为0，不允许其他关系补偿。该值**替换**原始内积在事件候选扩展和重入绝对阈值中的用法，不是叠加一道新门控；原始内积仍参与进度后验，峰值归一兼容度仍参与统一绝对解释度。两种分项均保留供审计。

所有评分应在 log-space 中计算并分别保存，不能只输出最终总分。

---

### 2.6 更新进度后验

实现：

$$
\beta_t(s)
\propto
\bar\beta_t(s)
L_t^{\mathrm{robot}}(s)
\left[
L_t^{\mathrm{state}}(s)
\right]^{\lambda_x}
\left[
C_t^{\mathrm{rel}}(s)
\right]^{\lambda_r}.
$$

输出：

```python
@dataclass
class ProgressEstimate:
    prior: dict[StateId, float]
    posterior: dict[StateId, float]

    nominal_state: StateId
    estimated_state: StateId

    confidence: float
    entropy: float

    best_explanation_score: float
    status: ProgressStatus
```

其中 `ProgressStatus` 至少包括：

```text
ALIGNED
FORWARD_REALIGNMENT
BACKWARD_REALIGNMENT
LOW_CONFIDENCE
NO_PLAUSIBLE_STATE
```

---

### 2.7 实现关系事件感知的候选扩展

默认只在局部窗口内估计进度。

当出现可靠关系变化，且局部窗口没有可解释状态时：

1. 根据当前实际关系查询最近的未来关系兼容状态段；
2. 将其加入候选集合；
3. 仍要求机器人轨迹、场景构型和关系三者均匹配；
4. 跨越技能边界的候选还必须通过入口守卫。

第1、3步中的绝对关系阈值使用上文 $c_{f,t}^{\mathrm{state}}(s)$：软先验峰值归一后再检查 external/linked 物理方向，不使用最大值只有 0.7 的原始重叠直接与固定阈值比较。

该机制用于正确处理动作提前完成，同时避免把搬运中掉落解释为未来正常释放。

---

### 2.8 建立固定的单周期更新器

建立统一的 `BeliefUpdater`，严格执行一次：

```text
progress prior
→ relation posterior
→ progress posterior
```

同一个控制周期内不循环重新更新关系和进度。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── runtime_observation.py
├── runtime_features.py
├── progress_prior.py
├── relation_filter.py
├── state_evaluator.py
├── progress_filter.py
└── belief_updater.py
```

并输出统一的：

```python
ClosedLoopBelief
```

包含所有关系后验、进度先验、进度后验和候选评分。

---

## 4. 验收标准

阶段二完成需满足：

1. external/linked 后验始终归一化；
2. 机械臂和对象同步运动时 linked 后验上升；
3. 机械臂运动、对象不随动时 external 后验上升；
4. 机械臂和对象均静止时，不因残差接近零而强判 linked；
5. 参考系不可见或不可靠时进入 Unknown；
6. 名义进度先验主要集中在上一动作实际发送的目标状态；
7. 动作未完成时，进度后验可回到较早状态；
8. 动作提前完成时，进度后验可前移；
9. linked 流不会凭极小协方差独占进度判断；
10. 搬运中掉落不能仅凭 external 关系匹配未来释放状态；
11. 同一周期更新严格执行一次；
12. 所有候选状态均保留可审计的分项评分。

---

# 阶段三：实现动态流角色与闭环正常执行控制

## 1. 阶段目标

让在线关系和进度信念真正参与动作生成与任务时间控制。

建立三个明确进度变量：

```text
nominal_state
estimated_state
reference_state
```

并实现：

```text
HOLD
REALIGN
ADVANCE
```

三类正常执行决策。

---

## 2. 开发内容

### 2.1 实现动态流角色

对每条离线已建模且已被任务参数选择保留的流，分配：

```text
EXECUTE
MONITOR
RECOVER
DEFER
```

角色判断使用：

- 当前在线关系后验；
- 进度校正后的期望关系；
- 关系观测可靠性；
- 原有任务参数选择结果。

角色语义为：

| 条件 | 角色 | 执行作用 |
|---|---|---|
| 期望 external，实际 external | EXECUTE | 参与 PoE |
| 期望 linked，实际 linked | MONITOR | 不参与正常 PoE，继续关系监测 |
| 期望与实际可靠失配 | RECOVER | 阻断正常推进，生成恢复意图 |
| 关键关系不确定 | DEFER | 保持上一可信角色并暂缓切换 |

必须区分两种不确定：一是进度后验跨越正常关系切换区间时，$\pi_{f,t}^{\mathrm{ctrl}}$ 暂时没有达到期望方向阈值；二是在线关系决策 $q_t^f$ 本身为 `Unknown`。前者只表示正常期望方向尚模糊，应保持 `DEFER` 和当前安全权重，不生成虚假失配，也不单独阻断进度；后者仍作为关键关系证据不足处理。

对无任何历史稳定关系的启动周期，若示范期望明确为 external、二元后验也达到 external 支持阈值，且参考系当前可见、跟踪可靠，则允许使用 $q_t^f(\mathrm{external})$ 安全降权执行以产生自然动作激励；此时角色仍是 `DEFER`，不得把 Unknown 改写成 external。已有可信关系后再次变为 Unknown、期望 linked、期望方向模糊、不可见或跟踪不可靠时，均不适用该启动例外。

运行期角色只作用于离线任务模型中已经建模并保留的参考系流。

已经由正式 LINK 建立且当前进度仍具有 `link_origin` 的物理关系属于持续任务状态，即使对应专家在当前 StateId/mode 中不再被 Eq.6 选中，仍必须进入关系监控：关系符合时记为零权重 `MONITOR`，可靠断连时记为 `RECOVER` 并阻断推进，不可靠时记为零权重 `DEFER`。这类未选关系决策的 `selected_offline=False`，不写入动作执行权重，也不能重新启用原本未选的 PoE 专家；没有正式 `link_origin` 的普通未选关系仍不参与角色路由。

正式 LINK 的离线事件只表示“正常示范在此建立连接”，不等于本次在线执行已经确认 linked。若事件刚发生时关系后验仍受前一 external 状态的持续性影响，允许在该事件阶段一学习得到的完整 `linked_entry_states` 区间内继续执行自然携带动作：相应物理流以零权重 `DEFER`，不因后验暂时滞后而冻结产生确认运动。该区间由每个事件的离线连续 linked 来源决定，不设置统一固定状态数，而且只有从该正式 LINK 的直接前驱沿任务拓扑进入时才激活；reset、恢复重入或任意索引跳转到 linked 状态不能取得宽限。事件直接前驱上的 external/Unknown 不得阻断建立 LINK 所必需的直接后继命令；进入区间后的观测跨越抓取、夹爪闭合和初始携带，不能用尚在建立关系期间的 external 持续性反过来否决因果动作。路由当前查询状态时，即使其对应物理参考系不属于该 StateId/mode 的 Eq.6 动作流，只要该状态仍在本次已因果进入且未确认/否决的正式 LINK 区间内，也必须把参考系作为零权重监控流送入关系判断；事件 occurrence 自身给出 linked 目标语义，不依赖可能落后一个周期的 beta 先行确认。该规则不得恢复物理流的 PoE 权重。末入口状态在 beta 尚未到达时可以继续完成自然动作；当 beta 已确认末状态且下一周期仍查询同一状态时，区间才视为已经消费，可靠 external 转入正常失配/恢复。同一 occurrence 一旦在线确认过 linked，后续掉落也不得重新使用该宽限。

当当前事件上下文命中阶段一的 `LinkPendingCandidate`，该关系是后续正常执行的必要条件，关系决策为 `Unknown`，且同一 `RuntimeFeatures` 表明参考系可见、跟踪可靠但实际动作激励不足时，动态角色层在 `DEFER` 和 `HOLD` 之外生成：

```python
RelationVerificationRequest(
    arm_id,
    frame_id,
    relation="linked",
    pending_event_id=pending_event_id,
)
```

本阶段只生成可审计的主动验证请求，不执行验证移动，也不改变任务进度。

---

### 2.2 扩展 PoE 流权重

在现有 PoE 计算中支持非负精度权重：

$$
\tilde{\Lambda}_{f,t}
=
w_{f,t}\Lambda_{f,t}.
$$

建议运行权重由以下因素共同确定：

- 离线是否被选中；
- 当前角色；
- external 后验；
- 观测可靠性。

满足：

- `EXECUTE`：具有正常或连续权重；
- `MONITOR`：正常 PoE 权重为 0；
- `RECOVER`：正常 PoE 权重为 0；
- `DEFER`：保持上一可信执行权重或安全降权。

PoE 权重只缩放精度，不直接缩放专家均值。

---

### 2.3 实现闭环任务游标

定义：

```python
@dataclass
class ClosedLoopCursor:
    nominal_state: StateId
    estimated_state: StateId
    reference_state: StateId
```

其中：

- `nominal_state`：上一动作正常完成后应该到达的状态；
- `estimated_state`：当前观测推断的实际状态；
- `reference_state`：当前真正用于查询动作的状态。

---

### 2.4 实现保持、重新对齐和技能内推进

#### HOLD

当出现：

- 当前目标尚未完成；
- 进度置信度不足；
- 关键关系 Unknown；
- 当前存在可靠关系失配；
- 后继条件尚未满足；

保持：

$$
s_{t+1}^{\mathrm{ref}}
=
s_t^{\mathrm{ref}}.
$$

HOLD 继续调用 `query_state()` 伺服当前参考状态，不等于发送关节静止命令。

#### REALIGN

当：

$$
\hat s_t\neq s_t^{\mathrm{nom}}
$$

且后验可信时：

$$
s_t^{\mathrm{ref}}
\leftarrow
\hat s_t.
$$

重新对齐只修改任务游标，不执行正常轨迹的反向回放。

#### ADVANCE

当前状态可靠完成，且后继位于同一技能时：

$$
s_{t+1}^{\mathrm{ref}}
=
\operatorname{succ}(\hat s_t).
$$

一个控制周期最多推进一个正常后继。

#### 动作目标是否到达的统一口径

动作是否已经到达不再由“当前末端与上一条命令的完整高斯相容度是否超过一个独立阈值”重复判定。该相容度受每个状态自身协方差尺度影响，把它作为绝对硬门控会在当前观测已可由正常进度状态稳定解释时造成第二次、且量纲不一致的拒绝。

统一使用阶段二的进度后验判断离散动作目标是否到达：

- 若可信 `estimated_state` 仍位于当前 `reference_state` 之前，通常认为当前目标尚未到达，继续 HOLD 并伺服原目标；仅当控制等价检查已经通过，且 `estimated_state` 与当前 `reference_state` 同属包含 MAP 的等价类时，才允许把离散落后视为不改变动作语义。其中 `LOW_CONFIDENCE` 继续处理局部连续等价类；高置信 `BACKWARD_REALIGNMENT` 只允许吸收紧邻当前引用的一个状态，不允许用较长静止段掩盖多状态真实落后。通过后仍只推进一条同技能拓扑边；
- 若可信 `estimated_state` 已等于当前 `reference_state`，则认为该离散目标已到达，允许按其他既有条件推进到同技能直接后继；
- 若当前观测已可信解释为旧引用的直接后继，则作为提前到达提交该后继；
- 初始 reset 周期没有上一条已执行动作，仍然 HOLD，不会无条件跳过首状态。

上一命令与当前末端的连续几何跟踪分数仍由 `RuntimeFeatureBuilder` 计算并保存，用于执行审计和连续控制误差分析；它不再独立改写进度决策。这不删除动作响应字段，也不影响阶段二在关系滤波和进度观测中使用实际运动证据。

#### 控制等价状态聚合

阶段二的 `LOW_CONFIDENCE` 和高置信 `BACKWARD_REALIGNMENT` 均保留其原始统计含义，不直接修改进度后验、MAP 或状态标签。阶段三在执行决策前额外判断局部离散进度差异是否真的会改变当前控制。

只在相同技能内，将后验支持中的局部连续状态按以下条件划分为控制等价类：

- 当前 mode 的离线已选流集合一致；
- 动态流角色、期望/实际关系方向、推进阻断、恢复/验证请求及运行权重一致；
- 夹爪命令一致；
- 使用当前同一观测查询得到的 PoE 位姿目标相互兼容。

对于两个候选动作 $a,b$，定义：

$$
\delta_{ab}
=
\operatorname{Log}
\left(
(T_a^W)^{-1}T_b^W
\right),
$$

$$
\kappa_{ab}^{\mathrm{act}}
=
\exp
\left(
-\frac{1}{2d}
\delta_{ab}^{\mathsf T}
(\Sigma_a+\Sigma_b+\epsilon I)^{-1}
\delta_{ab}
\right),
\qquad d=6.
$$

当 $\kappa_{ab}^{\mathrm{act}}$ 不低于统一的 `minimum_action_equivalence_compatibility` 时，才认为两状态动作等价，不新增任务专用阈值。等价关系采用类内两两检查；局部索引有缺口、跨技能边界、任一状态无正权重动作、夹爪不同或角色语义不同都会切断等价类。

设包含 MAP 状态 $\hat s_t$ 的等价类为 $\mathcal C_t^\star$，聚合后的控制置信度为：

$$
P_t^{\mathrm{ctrl}}
=
\sum_{s\in\mathcal C_t^\star}\beta_t(s).
$$

再将所有等价类的概率质量作为类别分布计算归一化熵。只有聚合置信度和类别熵同时通过阶段二已经配置的 `minimum_confidence` 与 `maximum_normalized_entropy` 时，执行层才可继续使用原有 HOLD/REALIGN/ADVANCE 规则。对于 `LOW_CONFIDENCE`，这表示不确定性不会改变控制；对于 `BACKWARD_REALIGNMENT`，还必须要求 MAP 状态恰好是当前引用的同技能直接前驱，且二者同属包含 MAP 的已接受等价类，才可把这一状态离散化滞后按当前引用已完成处理。两种情况每周期都最多推进一个同技能直接后继，原始 `ProgressEstimate.status`、逐状态后验和诊断分数均不改变。

该聚合不得覆盖：

- `NO_PLAUSIBLE_STATE`；
- 进度后验落在当前 `reference_state` 之前、但没有与当前引用共同通过上述控制等价检查的情形；
- 高置信 `BACKWARD_REALIGNMENT` 落后两个或更多状态的情形，即使更长静止段给出相似动作也不得吸收；
- 关系 Unknown 或可靠失配阻断；
- 无正权重执行流；
- 技能入口守卫和多臂事务；
- 跨技能或会产生不同控制动作的进度歧义。

---

### 2.5 实现正常状态可解释性与失配累计

根据：

$$
\mathcal E_t^{\max}
$$

以及关系兼容度，累计：

- 无正常候选状态；
- 关系持续失配；
- 持续 HOLD；
- 进度长期无法推进。

统一生成：

```python
MismatchEvent
```

本阶段只阻断正常推进并输出恢复意图，具体恢复动作在后续阶段实现。

对“期望 linked，但因运动证据不足而不确定”的情形，应优先输出 `RelationVerificationRequest`，不将证据不足直接当作可靠关系失配。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── frame_roles.py
├── weighted_poe.py
├── execution_cursor.py
├── execution_controller.py
└── mismatch.py
```

---

## 4. 验收标准

阶段三完成需满足：

1. `EXECUTE` 流正常参与 PoE；
2. `MONITOR` 流不参与正常 PoE，但关系滤波仍读取它；
3. `RECOVER` 流不参与正常 PoE；
4. Unknown 不造成单帧角色剧烈切换；
5. PoE 权重为 1 时与现有 PoE 语义一致；
6. PoE 权重为 0 时等价于移除该专家；
7. HOLD 继续闭环伺服当前状态；
8. REALIGN 只修改内部状态索引；
9. ADVANCE 每次只进入直接后继；
10. 关系失配时不继续盲目推进；
11. 角色路由不直接修改任务游标；
12. 所有进度变更只由统一执行控制器提交。
13. 当前事件命中 `LinkPendingCandidate`、后续必要 linked 关系仅因动作激励不足而 Unknown 时，生成主动验证请求而不直接判定关系失配。
14. `LOW_CONFIDENCE` 只在同技能局部连续状态形成控制等价类、聚合置信度与类别熵通过原进度阈值时解除该项 HOLD；高置信 `BACKWARD_REALIGNMENT` 仅在 MAP 是当前引用的同技能直接前驱且二者属于同一已接受等价类时，允许把当前引用按控制等价完成。两者都只推进一个直接后继；多状态落后、不同动作、夹爪、流角色、关系语义、无动作或跨技能歧义仍必须 HOLD，且原始进度后验与状态不被改写。
15. 当前离散动作目标是否到达由可信进度后验与 `reference_state` 的拓扑关系统一判定；上一命令连续几何相容度保留审计，不作为另一个独立进度硬门控。
16. 正式 LINK 的自然确认只覆盖该 occurrence 离线学习的完整 `linked_entry_states`，不使用固定状态数；仅从事件直接前驱沿拓扑进入时激活，建立关系的直接后继命令不能被 pre-LINK external/Unknown 阻断；当前查询处于该区间时，即使对应实体不是 Eq.6 动作流也必须以零权重监控，且 occurrence 语义不依赖落后一周期的 beta；末状态由 beta 确认到达后才消费完入口区间，区间结束后仍可靠 external 才进入失配，reset/重入和已确认事件均不能重复取得宽限。

---

# 阶段四：实现对象中心技能入口状态守卫与多臂事务推进

## 1. 阶段目标

在技能阶段跨越之前，检查下一技能所需的完整边界就绪状态。

该模块统一处理：

- 本臂技能完成条件；
- 任务相关机械臂—参考系关系条件；
- 稀疏场景构型条件。

每只机械臂利用自身进度后验判断本地技能是否完成。多臂协作通过同一边界中涉及的多机械臂关系和场景条件表达，并通过统一事务提交技能转换。

当前仓库中的双臂策略分别维护自身技能索引和局部时钟。新入口守卫在保留双臂本地进度的基础上，对必要边界进行状态驱动的放行。

---

## 2. 开发内容

### 2.1 在线计算边界条件

复用阶段一生成的：

```text
BoundaryModel
local completion model
guard relation conditions
guard scene conditions
affected arms / transition transaction group
```

对每个边界条件计算：

- 当前状态兼容度；
- 当前观测可靠性；
- 是否满足阈值；
- 是否已经连续满足指定周期。

场景状态和关系状态直接使用阶段二已有的状态后验。

---

### 2.2 计算本臂局部完成度

每只机械臂分别使用自身的：

$$
\beta_t^i
$$

以及：

- 进度后验是否位于技能末端；
- 当前机器人状态是否达到末端目标；
- 本臂必要关系是否满足；

计算：

$$
R_{i,b,t}^{\mathrm{local}}
=
P_{i,b,t}^{\mathrm{end}}
L_{i,b,t}^{\mathrm{goal}}
C_{i,b,t}^{\mathrm{own-rel}}.
$$

其中：

$$
P_{i,b,t}^{\mathrm{end}}
=
\sum_{\tau=T_k-h_e+1}^{T_k}\beta_t^i(k,\tau),
$$

$$
L_{i,b,t}^{\mathrm{goal}}
=
\exp\!\left(
-\frac{1}{2}
\operatorname{Log}\!\left(
(\mu_{i,b,m}^{W})^{-1}T_{e,t}^{W}
\right)^{\!\top}
(\Sigma_{i,b,m}^{W})^{-1}
\operatorname{Log}\!\left(
(\mu_{i,b,m}^{W})^{-1}T_{e,t}^{W}
\right)
\right),
$$

其中 $(\mu_{i,b,m}^{W},\Sigma_{i,b,m}^{W})$ 不是任意一条局部流的边缘分布，而是将当前 mode 在最终状态 $T_k$ 的全部有效目标流先变换到世界坐标系，再按与 DynaMAC 动作 PoE 相同的精度加权口径合成的**单一联合末端目标分布**。$L^{\mathrm{goal}}$ 只对当前真实世界末端位姿评分一次；不得将多个坐标表达的边缘支持度相乘或平均，否则同一个几何误差会被重复惩罚。该式使用峰值归一的马氏支持，不让协方差行列式改变绝对匹配口径。

且对每个 $c=(i,f)\in\mathcal R_{i,b}^{\mathrm{own}}$：

$$
\kappa_{c,t}
=
\sum_z q_t^{i,f}(z)\psi_{b,c}^{\mathrm{own}}(z),
$$

$$
C_{i,b,t}^{\mathrm{own-rel}}
=
\min_{c\in\mathcal R_{i,b}^{\mathrm{own}}}\kappa_{c,t}.
$$

若本臂必要关系集合为空，则定义 $C_{i,b,t}^{\mathrm{own-rel}}=1$。任一必要关系为 Unknown 或观测不可靠时，本周期 `LocalDone=False`，但不直接判为异常。最终：

$$
\mathrm{LocalDone}_{i,b,t}
=
\mathbb I\left[R_{i,b,t}^{\mathrm{local}}>\theta_{i,b}^{\mathrm{local}}\right],
$$

并要求连续满足 $H_{i,b}$ 个周期。$\theta_{i,b}^{\mathrm{local}}$ 与 $H_{i,b}$ 只能使用正常示范回放或留出正常轨迹标定，不得使用故障测试集调参；标定完成后保存到 `LocalCompletionModel` 或运行配置。

连续周期按边界自身的正常反例标定：令 $M_{i,b}^{\mathrm{pre}}$ 为正常进度进入末端
窗口前，完整入口条件偶发满足的最长连续周期数，取

$$
H_{i,b}=M_{i,b}^{\mathrm{pre}}+1.
$$

随后必须用正常末端保持回放确认条件能够稳定维持至少 $H_{i,b}$ 个真实控制周期。
不得附加与该边界正常数据无关的统一最短秒数，也不得使用故障样本压低或抬高 $H$。

本地参数标定只读取 $R_{i,b,t}^{\mathrm{local}}$ 的正常边界前分布和正常末端保持分布。
边界关系/场景守卫是否已经放行是另一项结果，不得作为筛除本地正样本的条件。特别是
Handover 一类单向等待边界允许出现“本臂 `LocalDone=True`、跨臂关系可观测但尚未满足、
因此继续 HOLD”；这表示守卫正确工作，不表示 $\theta_{i,b}^{\mathrm{local}}$ 或 $H_{i,b}$
无法标定。

只有机械臂 $i$ 完成当前技能后，才为该机械臂生成跨界请求。

---

### 2.3 实现入口放行逻辑

对机械臂 $i$ 的边界 $b$，入口许可为：

$$
\mathrm{Permit}_{i,b,t}
=
\mathrm{LocalDone}_{i,b,t}
\land
\bigwedge_{(r,f,z)\in\mathcal R_b}
\left[
q_t^{r,f}(z)>\theta_{r,f}
\right]
\land
\bigwedge_{j\in\mathcal J_b^{\mathrm{guard}}}
\left[
a_{j,t}>\theta_a
\land
\gamma_{b,j,t}>\theta_{b,j}
\right].
$$

其中：

- $\mathcal R_b$ 为边界相关的机械臂—参考系关系条件集合；
- $r$ 可以是当前机械臂，也可以是其他机械臂；
- $\mathcal J_b^{\mathrm{guard}}$ 为稀疏场景构型条件集合；
- $a_{j,t}$ 为条件观测可靠性；
- $\gamma_{b,j,t}$ 为当前状态与边界支持域的兼容度。

所有必要条件使用逻辑与。

任何必要条件：

- 不满足；
- 处于 Unknown；
- 可靠性不足；

均保持在当前技能末端。

若阻断条件是期望为 `linked` 的必要关系，当前事件上下文命中阶段一的 `LinkPendingCandidate`，关系决策为 `Unknown`，且同一 `RuntimeFeatures` 表明参考系可见、跟踪可靠但实际动作激励不足，入口守卫同时生成 `RelationVerificationRequest`。该请求只表示需要制造可观测运动，不等价于边界放行，也不改变当前的 `LocalDone=False` 结果。

---

### 2.4 实现多臂边界的统一评估与事务提交

每个控制周期首先获取一次联合 pre-action 场景快照，并基于该快照分别完成所有机械臂的：

- 在线关系后验更新；
- 本地进度后验更新；
- 本地技能完成判断；
- 技能入口状态守卫评估。

每个机械臂生成独立的转换请求：

```python
@dataclass
class TransitionRequest:
    arm_id: str
    boundary_id: BoundaryId

    permitted: bool
    source_state: StateId
    target_state: StateId

    local_done: bool
    condition_results: dict[ConditionId, ConditionResult]
    verification_requests: tuple[RelationVerificationRequest, ...]
```

所有转换请求先完成评估，再统一提交：

```text
读取联合场景快照
→ 更新所有机械臂信念
→ 评估所有技能入口守卫
→ 生成全部转换请求
→ 校验事务组条件
→ 统一提交允许的技能转换
```

不同边界依赖结构由各自条件集合自然表达：

- 条件只涉及本臂时，机械臂独立推进；
- 某一机械臂的守卫包含其他机械臂产生的关系或场景条件时，依赖方保持等待；
- 多个机械臂的守卫共同依赖同一组关系和场景条件时，在条件同时满足后于同一控制周期完成技能转换。

等待期间，各机械臂保持自身已经估计出的实际进度和控制目标，并继续基于当前对象与其他机械臂参考系动态伺服。

在线入口判断直接使用当前控制周期的真实场景状态。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── entry_guard.py
├── boundary_runtime.py
├── transition_transaction.py
└── bimanual_controller.py
```

---

## 4. 验收标准

阶段四完成需满足：

1. 当前技能未完成时不生成跨界许可；
2. 必要边界条件未满足时不跨界；
3. 必要条件 Unknown 时保持等待；
4. 条件稳定满足后能够正常放行；
5. 每只机械臂的本地完成判断只使用自身进度后验；
6. 多臂协作条件通过机械臂—参考系关系和稀疏场景构型表达；
7. 所有机械臂基于同一 pre-action 场景快照评估；
8. 所有转换请求在评估结束后统一提交；
9. 事务组内的联合转换在同一次状态提交中完成；
10. 不相关边界能够保持异步推进；
11. 等待期间已就绪机械臂仍能动态伺服；
12. 在线边界判断直接使用当前真实状态；
13. 必要 linked 关系命中 `LinkPendingCandidate`、且仅因动作激励不足而 Unknown 时，保持边界阻断并生成主动验证请求。

---

# 阶段五：实现主动关系验证、统一关系恢复与任务重入

## 1. 阶段目标

实现：

$$
\mathrm{ExecutionMode}
\in
\{\mathrm{TASK},\mathrm{VERIFY\_LINK},\mathrm{RECOVERY}\}
$$

顶层执行模式切换，以及通用：

$$
\mathrm{LINK}(f),
\qquad
\mathrm{UNLINK}(f)
$$

关系恢复。

`ExecutionMode` 与任务进度 StateId 及 DynaMAC/MiDiGaP 轨迹 mode 彻底分离。`VERIFY_LINK` 只为不可观的必要 linked 关系制造有限运动证据；关系判定结果仍只是 `linked`、`external` 或 `Unknown`。

恢复过程根据期望关系和实际关系生成关系目标。

---

## 2. 开发内容

### 2.1 恢复触发与模式切换

当：

- 所有正常候选状态均无法解释当前状态；
- 期望关系与实际关系持续可靠失配；
- 入口条件长期无法满足且本臂存在关系异常；

进入：

```text
RECOVERY
```

此时：

- 冻结正常 `reference_state`；
- 正常进度先验不再驱动动作；
- 关系后验继续更新；
- 恢复动作使用独立内部状态；
- 正常入口守卫暂停任务推进。

该模式是整个多臂任务的顶层执行模式，而不是只冻结执行辅助动作的单臂。任一机械臂处于 `VERIFY_LINK/RECOVERY` 时，所有机械臂的正常进度后验、`reference_state` 提交和正常入口事务均冻结；执行辅助动作的机械臂继续验证或恢复，其余机械臂只闭环伺服各自已经提交的当前参考目标。所有辅助动作完成并返回 `TASK` 后，才恢复正常进度更新和入口守卫评估。恢复自身的合法重入评估不属于正常入口推进，仍可在冻结期间执行。

---

### 2.2 实现 `VERIFY_LINK`

当阶段三或阶段四生成的 `RelationVerificationRequest` 满足以下条件时，从 `TASK` 切换到 `VERIFY_LINK`：

- 请求的关系是后续正常执行或技能入口的必要 linked 关系；
- 请求引用当前事件上下文中尚未解析的 `LinkPendingCandidate`；
- 当前关系决策为 `Unknown`，且同一 `RuntimeFeatures` 表明参考系可见、跟踪可靠但实际动作激励不足；
- 最近 `TASK` 实际末端轨迹已产生可定义固定反向探测模板的非零接近方向；
- 同一关系事件尚未执行过本次验证。

进入时记录当前末端位姿，并从 `TASK` 模式最近的实际末端轨迹中估计抓取接近方向。验证统一使用固定模板：

```text
冻结正常任务时钟和进度提交
→ 保持夹爪命令和末端朝向
→ 沿抓取接近方向的反方向低速移动
→ 关系滤波继续使用实际末端和参考系运动更新
→ 停止外移
→ 沿原路径返回记录位姿
→ 统一返回 TASK
```

接近方向可用性是该反向探测模板的定义前提，不是新关系状态或额外任务门控。若最近实际轨迹静止、样本不足或净位移近似为零，本周期不切换模式、不把该情况作为异常，而是保持 `TASK/HOLD`，由已有正常动作或失配—恢复链继续处理。只有后续真实 `TASK` 轨迹使同一请求的接近方向可估计时，才能使用原模板进入 `VERIFY_LINK`。

不使用“根据累计运动逐级扩大幅度”机制。反向探测阶段持续移动，直到首个退出条件成立：

$$
\mathrm{StableRelationDecision}
\lor
\Delta t\ge t_{\max}
\lor
\mathrm{SafetyConstraintTriggered}.
$$

`StableRelationDecision` 必须建立在验证动作已产生满足关系滤波激励条件的实际末端运动之上，不能只因离线先验或指令目标已发送就提前结束。其中 $t_{\max}$ 只计算反向探测阶段；超时后停止继续外移，但仍执行原路径返回。关系滤波在外移和返回期间都继续更新。若安全约束表明返回路径本身不安全，则现有安全控制优先，不强制返回。

为使“稳定”确实来自主动验证而非进入模式前的 TASK 运动或单帧接触调整，只收集已发出的 `VERIFY_LINK` 动作在下一控制周期实际产生的成对响应。对第 $\ell$ 个可观测且可靠响应，令 $\Delta p_{e,\ell}$ 和 $\Delta p_{f,\ell}$ 分别为末端与参考系的实际世界平移，定义窗口共动残差比：

$$
\rho_{i,f}^{\mathrm{verify}}
=
\sqrt{
\frac{
\sum_{\ell}\lVert\Delta p_{f,\ell}-\Delta p_{e,\ell}\rVert^2
}{
\sum_{\ell}\lVert\Delta p_{e,\ell}\rVert^2
}
}.
$$

当前默认至少需要3个实际动作响应，且累计平移达到既有 `minimum_probe_motion`；$\rho_{i,f}^{\mathrm{verify}}\le0.85$ 支持 linked，否则支持 external。该窗口结果不创建第三种关系状态，也不替代 $q_t^{i,f}$；只有它与同一关系滤波器的当前非 Unknown 决策方向一致，才认为 `StableRelationDecision` 成立。方向冲突时继续探测，直到一致、超时或安全约束触发，从而避免一帧转移瞬态将真实 LINK 提前否定。

返回完成后不在 `VERIFY_LINK` 内直接执行恢复动作，而是统一切回 `TASK`。若验证窗口已经由原二元关系滤波器稳定确认 `linked` 或 `external`，返回结束时将该稳定周期的原始二元后验精确写回同一个在线关系滤波状态；不转成 one-hot、不固定增强置信度，也不新建第三种关系状态。这样返回运动中的短时残差不会抹掉刚确认的结果，下一周期正常执行、入口守卫和恢复触发逻辑可直接消费同一 $q_t$。仍为 `Unknown` 时不写入确认结果。跨臂定向守卫通常只表示等待；只有关系所属机械臂已经到达同一 `LINK_PENDING` occurrence、守卫要求 linked 且在线稳定判定为 external 时，才将该守卫失配路由为关系所属机械臂的 `LINK` 恢复意图。由此不会把“对臂尚未执行到抓取位置”误判为恢复故障。

若该 Pending occurrence 在本次在线执行中被稳定验证为 `linked`，则只对当前 episode 激活阶段一随候选保存的恢复轨迹模板，并建立指向该 occurrence 的运行时 `link_origin`；之后同一连接若发生关系失配，恢复模块能够找到正确的事件前轨迹。若验证为 `external` 且该 linked 关系是当前入口守卫的必要条件，则允许把同一 occurrence 保存的轨迹作为**尚未激活的临时 LINK 修复模板**执行重新接近和闭合；这不表示关系已经建立，也不提前创建运行时来源。只有恢复动作随后通过有效运动证据确认 linked，才激活该 Pending 并建立运行时 `link_origin`。验证后仍为 `Unknown` 时不激活模板，也不生成可靠二元失配恢复意图。任何在线激活均不回写或篡改离线任务模型。

为每次关系事件保存验证尝试状态。同一未决关系从 `VERIFY_LINK` 返回 `TASK` 后不得立即重复触发；只有关系状态、技能状态或新的抓取事件发生变化时，才清除对应尝试记录并允许重新触发。

---

### 2.3 实现 `LINK(f)`

根据当前需要建立的机械臂—参考系关系，查询阶段一保存的事件级 `LINK` 锚点：

$$
e=(i,f,k,m,r).
$$

锚点包含：

- 关系建立前短局部轨迹；
- 当前参考系坐标下的相对目标；
- 夹爪闭合命令；
- 关系建立后的合法任务状态。

执行 LINK 恢复时，不能在参考系仍明显滑动或旋转的每个周期重新计算整条锚点。先要求参考系在连续控制周期内处于可观测、可靠且稳定的状态，随后锁定本次恢复使用的世界位姿，并一次性实例化完整恢复目标：

$$
T_{e,t}^{W,\mathrm{target}}(s)
=
T_{f,\mathrm{lock}}^{W}
\mu_{f,s}.
$$

在重新接近和预抓取的开夹爪阶段，若参考系相对 $T_{f,\mathrm{lock}}^{W}$ 的移动超过配置阈值，则废弃本次实例、回到稳定等待并重新锁定；夹爪闭合后对象随末端运动属于预期现象，不再据此重启。该规则只依赖可观测参考系运动，不读取任务名、StateId 或故障类型，用于避免恢复控制追逐尚未静止的移动目标。

恢复流程为：

```text
重新接近
→ 预抓取
→ 闭合夹爪
→ 通过在线关系后验验证 linked
```

恢复分布采用：

$$
\Sigma_{f,s}^{\mathrm{rec}}
=
\Sigma_{f,s}
+
\lambda_{\mathrm{rec}}I.
$$

锚点按照当前关系事件上下文选择。同一事件锚点可以在参考系不同世界位姿下重复实例化。

上述锚点轨迹是达到目标关系的动作手段，不是关系之外的额外完成条件。若当前目标关系
已经由可靠运动证据稳定判定为 linked，则当前 `LINK(f)` 立即完成并进入下一关系目标或
任务重入，不得仅为走完离线均值轨迹而继续移动。若当前 LINK 主动探测已经使机械臂离开
起点，仍必须按主动验证规则完成原路返回；关系提前稳定不能跳过这一安全返回义务。

---

### 2.4 实现 `UNLINK(f)`

统一执行：

```text
停止继续搬运
→ 打开夹爪
→ 执行局部脱离
→ 通过在线关系后验验证 external
```

阶段一保存的 `UnlinkEventMetadata` 用于确定：

- 正常关系解除位置；
- 可选局部脱离目标；
- 关系解除后的合法重入状态。

`UNLINK(f)` 同样以稳定且具有有效运动证据的 external 判定为完成条件；局部脱离轨迹只
用于产生该关系结果，不作为 external 已确认后的附加位姿目标。

---

### 2.5 实现多个关系目标排序

当同时需要：

$$
\mathrm{UNLINK}(B)
$$

和：

$$
\mathrm{LINK}(A),
$$

使用夹爪资源前置条件：

$$
\mathrm{FREE}(\mathrm{gripper})
\prec
\mathrm{LINK}(A).
$$

恢复控制器接受一组关系目标，并生成符合资源约束的执行顺序。

---

### 2.6 实现恢复验证与任务重入

关系恢复成功后：

1. 重新更新在线关系后验；
2. 对合法重入候选计算完整状态解释分数；
3. 选择：

$$
s^{\mathrm{re}}
=
\arg\max_{s\in\mathcal W_{\mathrm{re}}}
\mathcal E_t(s);
$$

其中，正常进度估计始终使用离线示范学习到的原始轨迹协方差
$\Sigma_{f,s}$。只有关系恢复已经使用
$\Sigma_{f,s}^{\mathrm{rec}}=\Sigma_{f,s}+\lambda_{\mathrm{rec}}I$
生成动作、随后评估恢复后的合法重入状态时，机器人轨迹项才复用同一个
$\lambda_{\mathrm{rec}}$：

$$
\Sigma_{f,s}^{\mathrm{reentry}}
=
\Sigma_{f,s}+\lambda_{\mathrm{rec}}I.
$$

该放宽只统一“恢复动作允许的偏差”和“恢复完成后的机器人兼容度”口径；
不覆盖离线模型，不用于正常进度评分、场景因子、关系滤波或动作 PoE，也不改变合法重入候选、入口守卫和既有兼容度阈值。重入候选的关系项按阶段二统一的 $c_{f,t}^{\mathrm{state}}(s)$ 计算：使用原有阈值，但不再让软先验自身的 0.7 峰值压低同向关系，并且关系方向相反时不得被平均分数补偿。

若某个合法候选已通过场景和关系检查，但当前机器人位姿尚未达到正式重入阈值，恢复管理器可先输出指向该候选的对齐动作。这个动作必须使用候选状态自身的关系期望和流角色路由，不得继续使用故障发生时冻结状态的关系语义否决该动作。候选条件视图只用于生成对齐动作；真实 $\beta_t$、`StateId`、`reference_state`、mode 和任务时钟仍保持冻结，只有同一候选随后通过完整重入阈值才允许原子提交。这是恢复动作与重入判定的上下文一致性要求，不新增候选、阈值或任务分支。

4. 检查涉及的技能入口守卫；
5. 重置进度后验；
6. 设置新的 `reference_state`；
7. 返回 TASK 模式。

不得直接恢复到故障发生前的旧时钟索引。

恢复尝试需要具有明确上限，并在无法恢复时输出结构化失败结果。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── relation_verification.py
├── recovery.py
├── link_anchors.py
├── unlink_metadata.py
├── relation_goals.py
└── reentry.py
```

---

## 4. 验收标准

阶段五完成需满足：

1. `TASK`、`VERIFY_LINK`、`RECOVERY` 是与 StateId 和轨迹 mode 分离的顶层执行模式；
2. 任一机械臂处于 `VERIFY_LINK` 或 `RECOVERY` 时，整个多臂系统的正常任务进度与正常入口事务均被冻结，其他机械臂只伺服各自当前参考目标；
3. 两种非 TASK 模式中关系后验仍持续更新；
4. `VERIFY_LINK` 引用当前未解析的 `LinkPendingCandidate`，并使用最近实际接近轨迹的反方向低速移动，不逐级扩大幅度；
5. 稳定关系判定、反向探测超时或安全约束任一成立时停止外移；
6. $t_{\max}$ 只限制反向探测，超时后仍执行可安全完成的原路径返回；
7. 返回完成后无论关系结果如何均统一切回 `TASK`；
8. 同一未决关系不会在 `TASK → VERIFY_LINK → TASK` 之间立即重复触发；
9. 每个 `LINK` 请求能够查询正确的事件级恢复锚点；
10. `LINK` 锚点在参考系连续稳定后锁定单一位姿并重新实例化；预抓取阶段参考系显著移动时能够返回稳定等待；
11. `UNLINK` 能通过统一过程验证关系已恢复 external；
12. linked→external 元数据能够提供合法重入状态；
13. 多关系目标遵守夹爪资源顺序；
14. 恢复期间不把 linked 近零协方差流直接重新加入正常 PoE；
15. 恢复完成后重新估计任务状态；
16. 重入状态必须满足轨迹、场景和关系兼容性；
17. 跨技能重入必须通过入口守卫；
18. 恢复失败有明确终止状态，不无限循环。

---

# 阶段六：完整策略集成、诊断信息与验收

## 1. 阶段目标

将前五阶段整合为一个可以直接接入当前 RLBench runtime 的新策略类，同时保留原 DynaMAC 对比路径。

---

## 2. 开发内容

### 2.1 实现顶层策略

建议新增：

```python
class ClosedLoopMultiStreamPolicy:
    def reset(...)
    def act(...)
    def summary(...)
    def save(...)
    def load(...)
```

每个 TASK 控制周期严格执行：

```text
1. 构造 RuntimeObservation
2. 计算 RuntimeFeatures
3. 构造动作后名义进度先验
4. 更新在线关系后验
5. 对候选状态评分
6. 更新实际进度后验
7. 计算进度校正后的期望关系
8. 路由动态流角色
9. 若原始进度为 LOW_CONFIDENCE，或为紧邻当前引用的高置信 BACKWARD_REALIGNMENT，评估局部候选的控制等价类
10. 决定 HOLD / REALIGN / ADVANCE
11. 若跨技能，执行入口守卫并生成转换请求
12. 多臂情况下统一提交技能转换事务
13. 若存在可执行的主动关系验证请求，切换 VERIFY_LINK
14. 若持续可靠失配，切换 RECOVERY
15. 仍处于 TASK 时，根据 reference_state 和流权重查询多流动作
16. 验证并提交本周期状态
```

主动关系验证模式执行：

```text
冻结所有机械臂的正常任务进度、引用提交和正常入口事务
→ 沿最近接近轨迹的反方向低速移动并持续更新关系后验
→ 关系稳定判定 / 超过反向探测时限 / 安全约束触发
→ 停止外移并沿原路径返回
→ 返回 TASK
```

恢复模式执行：

```text
关系目标执行
→ 关系验证
→ 合法状态重入
→ 返回 TASK
```

---

### 2.2 采用事务式状态更新

每个控制周期使用：

```text
prepare
→ validate
→ commit
```

关系、进度、游标、角色、边界转换和恢复状态只有在当前周期全部计算成功后才统一提交。

计算异常时不留下部分更新状态。

---

### 2.3 建立 benchmark 隔离的通用末端执行器

阶段二至五只输出本周期原始绝对末端目标、夹爪命令和模式意图，不绑定
RLBench 的具体 IK/规划实现。阶段六在 benchmark 适配层提供可替换的通用
末端执行器；更换 benchmark 时允许替换该适配器，但不得改变核心策略输出的
目标或为任务名、固定 StateId 增加执行特例。

RLBench 默认执行器采用以下有界混合链：

```text
原始绝对末端目标
→ 连续性约束的 CoppeliaSim 当前种子伪逆
→ 精确实时运动链上的 current-seeded bounded TRAC-IK Distance
→ 有界 SE(3) 小步延续与逐段物理反馈
→ 碰撞感知优先、必要时碰撞放宽的采样 IK
→ 碰撞感知线性路径
→ 远目标直接使用、近目标仅在同一目标真实停滞且局部层级耗尽后使用碰撞感知 RRTConnect
→ 最后的碰撞放宽线性路径
→ 所有前述层均失败或物理停滞后的碰撞放宽 RRTConnect
```

其中小步延续只分解执行器内部的同一个策略目标。默认平移步长为 5 mm、
旋转步长为 2°，按 `1, 0.5, 0.25, 0.125` 依次回退；每段执行后重新读取真实
末端位姿、重建当前运动链并重新求解。任何内部子目标都不能被提交为新的
`reference_state`。当前 RLBench 适配层对 bounded TRAC-IK 请求与执行后物理完成
使用 0.5 mm / 0.05°，外层策略目标接受使用 1 mm / 0.1°；只有原始策略目标
进入外层控制包络才返回 `reached`。前者保证求解和物理执行精度，后者吸收仿真
读回与重复求解的微小抖动，二者均为通用执行口径，不读取任务名、StateId 或
边界身份。更宽的 2 mm / 1° 只保留为外部运动链 FK 对齐审计上限，不能作为
策略动作完成口径。

碰撞放宽只属于 RLBench 接触丰富场景的后备执行口径：先尝试碰撞感知采样和
线性路径；若同一近目标已由物理反馈确认停滞并耗尽本地求解层级，则在放宽线性
碰撞检查前先尝试有界的碰撞感知 RRTConnect。路径“成功生成”不等于该路径已经
产生有效物理运动：碰撞感知线性路径若对同一目标实际停滞，下一周期必须跳过该
路径族并尝试碰撞感知 RRTConnect；后者仍停滞时才进入碰撞放宽线性路径。若该线性后备同样失败或没有产生真实改善，才允许使用最终的碰撞放宽 RRTConnect。该最终层继续复用统一的规划时间、采样和物理执行预算，不新增任务、状态或故障特例。
首次近目标求解不得直接调用该全局规划器，远目标仍可直接进入碰撞感知 RRTConnect。该顺序、
使用次数和结果必须进入诊断，不能隐式改变或冒充真实机器人安全控制策略。

双臂命令必须先完成全部机械臂的候选准备再推进共享物理时钟。执行后使用各臂
真实笛卡尔残差做不可补偿的进展判断：至少一臂改善且没有任何一臂退化才算
`progressed`。执行器返回值严格区分：

- `reached`：原始目标进入统一笛卡尔控制容差；
- `progressed`：本周期产生了朝向原始目标的物理进展，但尚未到达；
- `stopped`：求解得到合法命令但真实末端没有继续改善；
- 无效动作：在产生任何有效物理进展前，所有有界求解与路径后备均耗尽。

执行结果与策略提交必须是同一动作事务，但执行器状态不得成为第二套任务进度
控制器。只要任务命令已实际施加，其命令引用和下一周期真实观测就进入既有
$\bar\beta_t/\beta_t$ 更新；`reached/progressed/stopped` 标签本身不改写进度先验、
`StateId` 或 `reference_state`，后续保持、推进或重新对齐仍完全由阶段二、三的
进度后验、关系和边界机制决定。`progressed/stopped` 不把绝对策略目标标记为
完成；`reached` 只提交执行器侧的绝对目标完成。夹爪提交也不能反过来由固定
笛卡尔 `reached` 包络建立第二套任务完成判据。事务接口只保留一套显式语义：
主动作真实施加时 `primary_action_applied=true`；若主动作在有效进展前失败、随后
以当前关节保持完成本周期，则提交 `primary_action_status=stopped` 和
`primary_action_applied=false`。不得再用旧布尔字段把“动作是否施加”和
`reached/progressed/stopped` 混为一件事。
对于 `TASK/task_poe` 命令，只有
进度后验已对齐当前 `reference_state` 时才允许执行该状态的夹爪命令。若 beta 已确认当前引用，但运行观测中的夹爪状态尚未体现该 `StateNode` 的离散命令，则当前周期保持同一 `reference_state` 并提交该夹爪命令；下一周期仍由原 beta、关系和边界逻辑决定是否推进。该离散动作完整性检查不读取 `reached/progressed/stopped`，不另建任务进度变量。若当前引用
是非最终技能的末端状态，还必须先由对应边界事务提交。边界提交周期可立即执行
入口状态夹爪命令。多臂命令按每臂各自的任务授权分别屏蔽或执行，因此交接任务
可以先闭合接收臂而继续保持交出臂，不强制把所有双臂夹爪动作捆成同一事务。
冻结 DynaMAC 对照的夹爪由其自身固定策略时钟显式授权：每次正式提交的策略事务
消费一个固定时钟状态，主动作同时执行对应夹爪值，不能再由执行器 `reached`
包络延迟，也不能根据
`progressed/stopped` 回滚固定时钟。否则共享执行器会把冻结基线改造成另一种
策略。只有没有任务级授权来源的非 TASK 辅助命令，才保留 `reached` 后执行夹爪
的物理顺序。该授权只控制物理夹爪提交，不直接推进闭环 `StateId`。

合法关节命令产生后若真实末端对同一原始笛卡尔目标停滞，不能仅因为“已经有
一个合法关节解”就永久重复该解；应在同一有界混合链中跳过已停滞的求解层，
依次尝试后续求解器族。任一备用层产生真实物理进展后立即向策略返回
`progressed`，由下一控制周期重新观测并求解；不得在一个控制周期内隐藏地串联
多次策略状态推进。全部后备层仍无进展时才返回 `stopped`。该升级只针对同一个
策略目标的 benchmark 执行，不改写目标、`reference_state` 或阶段二至五状态。
物理停滞升级中的采样候选还必须满足与 bounded TRAC-IK 相同的当前关节连续性
上限，防止末端近似不变但机械臂切换到远距离逆解分支；这不取消“所有局部 IK
初始不可行时”的普通全局采样后备，后者仍按关节限位和有限性筛选后选择离当前
关节状态最近的合法候选。两种入口及其否决计数必须在诊断中明确区分。

固定最大分段数、单段物理步数和规划预算，禁止无限求解或对同一状态进行无信息
重试。求解器、路径和逐段反馈的全部分项计数、残差、耗时及碰撞口径必须保存
在结果元数据中。执行器只能报告真实执行结果；`stopped` 或耗尽后的处理继续由
阶段二至五的进度、关系、验证和恢复闭环决定。

### 2.4 建立完整逐周期诊断

每个 tick 至少记录：

```text
nominal_state
estimated_state
reference_state

progress prior
progress posterior
progress confidence
control-equivalence class / aggregated confidence / class entropy / action compatibility

每条流：
    demo relation prior
    relation posterior
    informative / Unknown
    role
    execution weight

每个候选状态：
    robot score
    scene score
    relation score
    total score

执行决策：
    HOLD / REALIGN / ADVANCE
    reason

边界守卫：
    local completion
    active scene conditions
    active relation conditions
    condition reliability
    compatibility
    transition request
    transaction commit result

恢复：
    mode
    relation event id
    relation goals
    recovery phase
    reentry candidates

主动关系验证：
    relation candidate
    recent approach direction
    outbound elapsed time / t_max
    action-response sample count / co-motion residual ratio / response direction
    relation decision
    exit reason
    return progress
    verification attempted

动作：
    participating streams
    PoE weights
    pose
    covariance
    gripper
```

诊断记录必须能够在不重新运行仿真的情况下解释一次状态切换的原因。

---

### 2.5 接入当前 RLBench runtime

复用当前：

- observation adapter；
- 双臂同步 pre-action snapshot；
- IK 与控制器；
- evaluation launcher；
- result schema；
- replay 系统。

新增策略只替换策略推理与进度控制部分。

配置中应允许：

```text
policy_type = dynamac
policy_type = closed_loop_multistream
```

用于同一评测管线中的对比。

---

### 2.6 建立测试层次

#### 单元测试

覆盖：

- 离线状态模型；
- 关系后验；
- 进度后验；
- 稀疏状态因素；
- 边界前短终止窗口；
- 边界关系条件；
- 边界场景构型条件；
- 事件级 LINK 锚点；
- UNLINK 元数据；
- 流角色；
- PoE 权重；
- 执行状态机；
- 主动关系验证；
- 入口守卫；
- 多臂事务提交；
- LINK/UNLINK；
- 任务重入。

#### 轨迹回放测试

使用已经采集的 observation/action 序列，在不运行物理仿真的情况下验证：

```text
关系后验
进度后验
角色变化
边界判断
转换事务
失配触发
```

#### 仿真集成测试

验证新策略能够：

- reset；
- 单臂 act；
- 双臂 act；
- 正常完成 episode；
- 进入和退出 RECOVERY；
- 进入和退出 VERIFY_LINK；
- 保存和加载模型；
- 输出完整 diagnostics。

---

### 2.7 当前阶段六集成验证口径

阶段六接通后必须先用封存正常样本和定向受控故障确认：正常任务不会误触发辅助模式，时间停滞不会导致固定时钟式空推进，关系故障能够真实执行稳定等待、LINK锚点、主动探测、原路返回和重入对齐。该检查只作为进入正式矩阵前的机制门控，不生成长期保留的旧执行器 pilot 版本；门控过程和中间产物在当前正式协议生效后清理。

论文结论只使用冻结提交和预注册统计口径下的完整正式矩阵，不使用小样本门控估计成功率或显著性，也不得用故障样本重新标定关系、进度、边界或恢复阈值。

### 2.8 阶段六正式评测冻结口径

正式评测协议、调度器和统计脚本统一位于 `evaluations/phase6_formal_evaluation/`，并要求在任何正式结果产生前提交冻结。正常回归使用封存 `rlbench_eval_v2` 的全部8任务、全部200条 episode 和4种方法，共32个单元、6400回合；受控故障使用同一封存集预先固定的连续索引 `[0,49]`，覆盖8任务、4类故障和4种方法，共128个单元、6400回合。不得在运行后替换 seed、episode、故障机械臂或触发参数。

四种方法共用 `stage6_hybrid_cartesian_executor_v18`，使消融变量严格限定为固定时钟、闭环进度、动态角色、入口守卫及完整恢复链，而不是 IK 执行器质量。执行器对碰撞感知规划路径只拥有当前有界物理执行段：完整路径结束时由 PyRep 释放运动句柄；若因单周期物理预算、停滞或异常提前返回，则必须在返回闭环观测前释放尚未完成的 RML 句柄，不能把仿真资源泄漏到后续动作或 episode。该资源生命周期规则不参与 StateId、进度或关系判断。故障注入器只能读取动作和仿真 attachment 等物理条件，不读取或改写 StateId、关系/进度后验、执行模式或最终任务结果。主分析采用全部预注册回合的 intention-to-treat 成功率；故障是否触发及触发后的条件恢复率作为次要指标，不得据此删除未触发或提前失败回合。

每个任务/方法报告 Wilson 95% 区间；同 episode 的方法结果使用 exact McNemar 成对比较并进行 Holm 校正；跨任务总体采用任务宏平均和10000次成对 bootstrap。正常完整方法相对 DynaMAC V4 的非劣效界限在结果产生前固定为5个百分点。故障结果不得用于重新标定任何阶段一至五参数。

本机调度最多并行8个隔离 CoppeliaSim 单元，GPU 0至7分别作为可见设备槽，并按 NUMA 划分全部64个物理核及其 SMT 线程。2026-08-29 的同提交、同 StackWine episode 0准入探测中，surfaceless EGL 与 Xvfb 均成功，但分别使用178和177个控制周期，未满足严格轨迹等价，因此正式后端固定为已验证的 Xvfb 软件 OpenGL。不能把 `CUDA_VISIBLE_DEVICES` 误报为 GPU 已承担渲染；后续只有原生支持 CUDA 的训练/评测组件才使用真正的多卡并行。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── policy.py
├── config.py
├── state.py
├── diagnostics.py
└── serialization.py
```

以及：

```text
integrations/rlbench/
└── closed_loop policy adapter / launcher support
```

---

## 4. 最终验收标准

整个开发完成需要满足：

1. 新方法完整使用现有 DynaMAC/MiDiGaP 多流策略；
2. 不存在第二套重复的 SE(3)、四元数或 PoE 实现；
3. 离线关系、场景因子、边界状态和关系转移数据由同一示范数据管线生成；
4. 关系空间严格为 external/linked；
5. Unknown 作为不确定决策正常工作；
6. 单周期更新顺序严格为：

$$
\bar\beta_t
\rightarrow
q_t
\rightarrow
\beta_t;
$$

7. `nominal_state`、`estimated_state`、`reference_state` 职责清晰；
8. HOLD 继续闭环伺服当前状态；
9. REALIGN 不执行任务轨迹反向回放；
10. ADVANCE 每次只进入合法直接后继；
11. 动态流角色能够实际改变 PoE 参与方式；
12. 每只机械臂使用自身进度后验判断本地技能完成；
13. 技能边界由任务相关关系和稀疏场景构型条件决定；
14. 多臂边界基于同一 pre-action 场景快照评估，并通过事务统一提交；
15. 当前事件命中 `LinkPendingCandidate`、必要 linked 关系因动作激励不足而 Unknown 时能生成主动验证请求；
16. `VERIFY_LINK` 冻结任务进度、持续更新关系后验，并按固定反向探测模板返回 `TASK`；
17. 同一未决关系的主动验证不会立即重复触发；
18. 关系失配能生成 LINK/UNLINK 恢复目标；
19. LINK 使用正确的事件级关系恢复锚点；
20. UNLINK 使用统一关系解除过程及合法重入元数据；
21. 恢复后根据当前真实状态重新进入任务；
22. 原 DynaMAC 对比路径仍然可运行；
23. 新方法模型可保存、加载并重复执行；
24. 每个控制周期均有完整、可解释的诊断记录；
25. 所有新增测试和现有相关测试通过。
26. 任一机械臂进入 `VERIFY_LINK/RECOVERY` 后，所有正常进度和入口事务冻结，其他机械臂只伺服已提交目标；返回 TASK 后才恢复正常提交。
27. 控制等价聚合只消除不改变动作、夹爪、动态角色和关系语义的局部低置信歧义；跨技能或控制相关歧义仍 HOLD，且原始进度信念保持可审计。
28. 动作目标到达口径不重复叠加上一命令高斯相容度硬门控；进度后验、关系阻断、无可用流和边界守卫仍按原机制共同决定 HOLD/ADVANCE。
29. RLBench 执行器的 `progressed/stopped` 不提交闭环绝对目标完成，也不直接推进或回滚任何方法的任务进度；闭环进度仍由后验决定，冻结 DynaMAC 则对每个已施加动作消费一个原固定时钟状态。TASK 夹爪由进度对齐和边界事务授权，冻结基线夹爪由其固定时钟命令授权；多臂按每臂授权提交而不强制捆绑无关夹爪。
30. LINK 恢复对移动参考系先稳定、再锁定锚点；预抓取阶段目标显著移动会重新等待，而不是逐周期追逐移动目标。
31. 技能边界提交周期保持 DynaMAC 的混合桥接语义：连续位姿来自源技能终止状态，夹爪来自已提交的目标技能入口状态；目标技能虚拟帧在桥接动作执行后的下一次真实观测中捕获，不能提前使用 pre-action 末端位姿建立。
32. 关系滤波的示范先验和动作响应似然由同一个信息权重调温；无信息周期退化为持久性预测，平移/旋转证据按实际激励幅值稳健混合，单个异常分量不能通过乘积否决另一分量中的明确共动证据。
33. 机器人与场景高斯使用峰值归一支持，机器人多流支持再相对于联合可达 PoE 峰值归一；进度后验保留原始关系重叠，事件候选扩展和重入的绝对关系阈值则统一使用峰值归一且方向一致的 `relation_state_compatibility`。
34. 恢复重入对齐动作使用候选状态自身的关系期望和流角色，但只形成只读动作查询视图；真实进度后验、引用状态、轨迹分量、角色历史和任务时钟在完整重入条件通过前保持冻结。
35. 有界规划路径若在完整结束前返回策略闭环，必须在返回观测前释放其未完成运动句柄；该资源生命周期只属于 benchmark 执行层，不得进入任务状态、关系或进度判断。

---

# 建议拆给 Codex 的六个主任务

可以直接将开发工作拆成以下六个连续 Codex 任务：

1. **统一离线任务模型、边界数据、关系事件模板与任意状态策略查询**
2. **运行观测、在线关系滤波与进度后验**
3. **动态流角色与闭环执行状态机**
4. **技能入口状态守卫与多臂事务提交**
5. **主动关系验证、事件级 LINK、通用 UNLINK 与任务重入**
6. **RLBench 集成、序列化、诊断与完整测试**

每个主任务都要求同时提交：

```text
实现代码
单元测试
集成测试
配置定义
数据结构说明
关键公式到代码的映射说明
验收命令及输出
```
