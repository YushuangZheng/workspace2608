# 新方法代码开发计划

# 一、开发总原则

现有 GitHub 仓库已经包含 DynaMAC、MiDiGaP、TAPAS 技能分段、单双臂 RLBench 运行时、训练数据适配、评测与测试代码，因此新方法应当直接复用这些能力，不再并行开发第二套 SE(3) 运算、流形高斯、PoE、技能分段或 RLBench 控制器。

已经打好的 DynaMAC tag 作为基线对照即可。在开发分支中：

- 允许对现有 DynaMAC 代码进行必要重构；
- 优先复用现有类、数据结构和数学函数；
- 不要求为了冻结基线建立复杂的副本、golden trace 或重复校验系统；
- 只需保证论文对比时仍能运行原始固定时钟 DynaMAC；
- 新方法关闭时，应有一条清晰的 baseline 执行路径。

开发工作的核心是保留已有多流策略能力，同时把**动作查询、关系估计、进度估计和进度控制**重新组织成闭环。

本计划严格按照当前技术路线实现：

$$
\bar\beta_t
\rightarrow
q_t
\rightarrow
\beta_t
\rightarrow
\text{动态流角色}
\rightarrow
\text{保持/推进/重新对齐}
\rightarrow
\text{入口守卫}
\rightarrow
\text{主动关系验证/关系恢复}.
$$

关系空间只包含：

$$
\{\mathrm{external},\mathrm{linked}\},
$$

Unknown 是证据不足时的决策状态。在线证据使用位姿、动作、夹爪和跟踪可靠性。

顶层执行控制使用独立的：

```text
TASK / VERIFY_LINK / RECOVERY
```

执行模式。它不属于任务进度 $s=(k,\tau)$，也不属于 DynaMAC/MiDiGaP 的轨迹 mode。

---

# 阶段一：统一离线任务模型，并完成策略查询与时钟推进解耦

## 1. 阶段目标

一次性整理后续关系滤波、进度滤波、入口守卫和恢复模块共同需要的示范统计，形成统一的离线任务模型。

同时改造现有 DynaMAC，使多流策略能够在指定技能状态下被查询，而不是每次查询后自动推进时间。

这一阶段完成后，应当具备：

```text
指定任意状态 s=(k,τ)
→ 读取该状态的多流轨迹、关系先验和稀疏场景状态
→ 使用指定流权重查询动作
→ 不自动改变技能和时间索引
```

---

## 2. 开发内容

### 2.1 建立统一状态索引

定义：

```python
StateId = (skill_index, local_index)
```

对每个状态保存：

- 技能编号；
- 技能内进度；
- 该进度状态下的模态先验和各模态分量；
- 前驱状态；
- 后继状态；
- 是否位于技能末端；
- 是否存在跨技能后继。

所有后续模块必须通过同一个 `StateId` 和同一个离线任务模型访问状态，不应分别解析 `SkillModel`、`StreamModel` 和 checkpoint。

模态不是独立的任务进度节点。同一 `(skill_index, local_index)` 的流均值、协方差、关系先验、夹爪命令和场景因子均保留其内部模态维；动作查询需要指定模态时，使用独立的 `mode_index`。因此每只机械臂的状态节点数量为各技能 `duration` 之和，不乘以模态数。

---

### 2.2 建立 `ClosedLoopTaskModel`

建议新增：

```python
@dataclass
class ClosedLoopTaskModel:
    base_policy: DynaMAC
    states: dict[StateId, StateNode]
    skill_states: dict[int, tuple[StateId, ...]]
    boundaries: dict[BoundaryId, BoundaryModel]

    link_anchors: dict[RelationEventId, LinkRecoveryAnchor]
    unlink_events: dict[RelationEventId, UnlinkEventMetadata]
    link_origins: dict[RelationStateKey, RelationEventId]
```

其中每个 `StateNode` 至少包含：

```python
@dataclass
class StateNode:
    state_id: StateId

    mode_priors: np.ndarray

    stream_means: dict[str, np.ndarray]       # 每项 [M, 7]
    stream_covariances: dict[str, np.ndarray] # 每项 [M, 6, 6]

    demo_relation_priors: dict[str, np.ndarray] # 每项 [M, 2]

    selected_frames: tuple[str, ...]
    gripper_commands: np.ndarray # [M, G]

    scene_factor_models: dict[
        FactorId,
        dict[int, FactorDistribution],
    ]
```

这里的 `stream_means` 和 `stream_covariances` 应直接引用或读取现有 DynaMAC/MiDiGaP 拟合结果，不重新拟合底层轨迹模型。

---

### 2.3 从示范中统一提取关系先验

对每个状态 $s$ 和参考系 $f$，由现有连接分数或局部协方差生成：

$$
\pi_{f,s}^{\mathrm{demo}}(z),
\qquad
z\in\{\mathrm{external},\mathrm{linked}\}.
$$

需要保存：

```text
state
frame
link score
P(external)
P(linked)
```

关系先验应随状态索引存入 `StateNode`，供以下模块共享：

- 在线关系滤波；
- 进度关系兼容度；
- 动态流角色；
- 恢复目标判断；
- 技能边界关系条件。

---

### 2.4 统一构造技能条件化稀疏场景因子

对每个进度状态 $s$，从当前技能实际涉及的参考系构建活动实体集合：

$$
\mathcal V_s^{\mathrm{act}}.
$$

候选场景因子只包括两类：

1. 活动实体自身构型；
2. 活动实体图中候选边的相对构型。

候选因子的产生范围限定为：

- 当前技能中被多流策略使用的参考系；
- 当前状态局部邻域中被使用的参考系；
- 仅在边界候选集中引入的下一技能或边界相关参考系；
- 任务数据接口中与这些实体直接绑定的结构实体。

逐进度场景因子的局部邻域只用于离线候选因子生成。对
`StateId=(k,τ)` 的某个内部模态，只在相同 skill、相同 mode 内建立
邻域，两个实体必须在该状态或其局部邻域内共同活动才能生成
候选边。邻域半径按 `r=0,1,2` 依次检查，选择通过跨示范支持率和
分布稳定性检查的最小半径；常规预期为 `r=1`，`r=2` 仍不稳定则
删除，不继续扩大。邻域不得跨技能边界，也不依赖人工实体对白名单。下一技能入口
相关实体只进入边界候选集，不扩散到当前技能的所有逐进度状态。

对每个候选因子，使用正常示范进行留一验证。只有同时满足以下条件才保留：

- 所有或绝大多数示范中可观测；
- 分布能够稳定估计；
- 加入该因子后，当前局部状态识别或边界状态识别得到可重复提升；
- 与已有机器人轨迹或关系变量不完全重复。

逐进度因子的按示范留一评分以现有 DynaMAC 机器人轨迹作为局部状态识别基线，并叠加已经保留的场景因子，不加入名义时间进度先验或关系证据。候选因子加入前后均以真实状态相对局部错误候选的 `logsumexp` 间隔计算增益，并按每轮最大可重复增益进行前向选择。五条正常示范时，软进度因子至少需要四个留一折为正增益，且平均增益超过统一最低阈值。与机器人轨迹或关系变量不重复通过候选因子的定义和排除规则保证，不通过向识别评分额外加入关系兼容项实现。

最终为每个状态保存：

$$
\mathcal J_s
$$

以及：

$$
p_j(x\mid s),
\qquad
j\in\mathcal J_s.
$$

关系后验 $q_t^{i,f}$ 由关系模型单独表示，各机械臂的进度后验 $\beta_t^i$ 由各自进度模型单独表示。

---

### 2.5 从同一示范数据构建技能边界守卫模型

对每个机械臂和每个技能边界：

$$
b:k\rightarrow k+1,
$$

在当前技能末端定义边界前短终止窗口：

$$
\mathcal B_b^-
=
\left\{
(k,T_k-h_b^-+1),
\ldots,
(k,T_k)
\right\},
$$

其中 $h_b^-$ 为终止窗口长度。

技能边界守卫复用统一任务模型中已有的三类信息：

1. **本地技能完成信息**；
2. **机械臂—参考系关系信息**；
3. **技能条件化稀疏场景构型信息**。

#### 本地技能完成定义

为边界 $b$ 保存当前技能完成所需的状态统计，包括：

- 技能末端状态窗口；
- 当前技能末端目标分布；
- 本臂在技能末端应满足的关系条件；
- 本地完成度计算所需的阈值或支持范围。

其中，本臂必要关系集合只包含当前技能由本臂直接建立或解除、且在技能完成时必须满足的关系：

$$
\mathcal R_{i,b}^{\mathrm{own}}.
$$

对每个 $c=(i,f)\in\mathcal R_{i,b}^{\mathrm{own}}$，由边界前终止窗口中的正常示范关系先验拟合目标关系分布：

$$
\psi_{b,c}^{\mathrm{own}}(z).
$$

阶段一保存整个终止窗口、最终状态 $T_k$ 的正常示范目标分布、本臂必要关系集合及其目标关系分布。阶段一不最终标定本地完成阈值 $\theta_{i,b}^{\mathrm{local}}$ 和连续确认周期 $H_{i,b}$；二者在阶段二、三的 $q_t$、$\beta_t$ 和执行链完成后，于阶段四仅使用正常示范回放或留出正常轨迹统一标定，不得使用故障测试集调参。

该信息在运行时用于计算：

$$
R_{i,b,t}^{\mathrm{local}}.
$$

#### 边界关系条件

从边界相关的活动关系图中选择需要在跨界前成立的机械臂—参考系关系，形成：

$$
\mathcal R_b
=
\{(i,f,z^\star)\}.
$$

其中：

- $i$ 表示相关机械臂；
- $f$ 表示相关参考系；
- $z^\star\in\{\mathrm{external},\mathrm{linked}\}$ 表示边界所要求的目标关系。

对每个关系条件，根据终止窗口内的示范关系先验构造边界关系支持分布：

$$
\psi_{b,(i,f)}^{\mathrm{rel}}(z)
=
\sum_{\tau\in\mathcal B_b^-}
\omega_{b,\tau}\,
\pi_{i,f,(k,\tau)}^{\mathrm{demo}}(z),
$$

其中：

$$
\sum_{\tau\in\mathcal B_b^-}\omega_{b,\tau}=1,
$$

并令越接近技能末端的状态具有越高权重。

边界关系条件按以下离线流程构建：

1. 收集下一技能所有 `affected_arms` 的 `selected_frames`；
2. 仅为这些 selected frames 生成 arm-frame 关系候选；
3. 从各臂边界前短终止窗口拟合关系支持分布；
4. 选择最大支持关系作为目标关系；
5. 检查总体支持度、全部正常示范的一致性和观测可用率；任一正常留一折不一致均删除；
6. 删除已由本臂 `LocalCompletionModel`、同一事务中其他机械臂 `LocalCompletionModel` 保证的条件，以及与已有条件完全重复的关系；
7. 将剩余关系及其目标支持分布、可靠性统计保存到 `relation_conditions`。

单臂边界先生成本臂候选。双臂边界先利用未去重的硬依赖和正常示范边界同步性确定事务组，再合并事务中所有 affected arms 的候选，最后执行跨臂 LocalDone 去重并保存最终 `BoundaryModel`。

#### 边界场景构型条件

从第 2.4 节建立的稀疏场景因子库中，选取由下一技能所使用的参考系、边界关系条件涉及的实体及其直接结构关联实体构成的候选因子；再保留在边界前短终止窗口中具有稳定支持分布且能够可靠观测的因子，形成：

$$
\mathcal J_b^{\mathrm{guard}}.
$$

对每个场景构型条件 $j$，由全部成功示范拟合：

$$
\psi_{b,j}^{\mathrm{guard}}(x)
=
\frac{1}{N}
\sum_{n=1}^{N}
\sum_{\tau\in\mathcal B_b^-}
\omega_{b,\tau}\,
r_{b,\tau}^{j,(n)}(x),
$$

其中：

- $N$ 为示范数量；
- $r_{b,\tau}^{j,(n)}(x)$ 为第 $n$ 条示范中条件 $j$ 的状态分布；
- $\omega_{b,\tau}$ 为终止窗口内的时间权重。

边界场景条件是硬入口条件，必须通过全部按示范留一折。连续因子的在线兼容度使用归一化马氏兼容度 `exp(-d²/2)`；每个留一折取边界前短终止窗口内的最低兼容度，正式阈值取全部正常留一折下界并减去仅用于数值容差的微小余量。

最终 `BoundaryModel` 保存：

```python
@dataclass
class BoundaryModel:
    boundary_id: BoundaryId
    source_skill: int
    target_skill: int

    local_completion_model: LocalCompletionModel

    relation_conditions: dict[
        RelationConditionId,
        RelationGuardDistribution,
    ]

    scene_conditions: dict[
        FactorId,
        FactorDistribution,
    ]

    scene_condition_thresholds: dict[FactorId, float]

    condition_reliability: dict[ConditionId, ReliabilityStatistics]

    affected_arms: tuple[str, ...]
    transaction_group: TransactionGroupId | None
```

其中：

- `local_completion_model` 描述本臂当前技能是否完成；
- `relation_conditions` 描述跨界前需要成立的机械臂—参考系关系；
- `scene_conditions` 描述跨界前需要进入支持域的稀疏场景构型；
- `affected_arms` 和 `transaction_group` 用于多机械臂边界的统一提交。

`transaction_group` 只标记必须在同一控制周期原子提交的联合边界。离线构建时，只有左右边界在全部正常示范中稳定同步，并且双方共享同一个必须保持的 linked 物理实体或完全相同的硬场景条件时，才建立事务组；独立边界和单向等待边界保持 `None`。

---

### 2.6 离线构建关系转移模板与任务重入信息

对正常示范中每个任务相关的：

$$
\mathrm{external}\rightarrow\mathrm{linked}
$$

关系事件，建立事件索引：

$$
e=(i,f,k,m,r),
$$

其中：

- $i$ 为执行该关系建立的机械臂；
- $f$ 为被连接的参考系；
- $k$ 为关系事件所在的技能上下文；
- $m$ 为对应操作模态；
- $r$ 用于区分同一技能中可能出现的多个关系事件。

从全部对应示范中提取关系建立前的短局部轨迹：

```text
接近
预抓取
闭合夹爪
关系建立
```

拟合并保存：

```python
@dataclass
class LinkRecoveryAnchor:
    event_id: RelationEventId
    arm_id: str
    frame_id: str
    context_state: StateId

    local_means: np.ndarray
    local_covariances: np.ndarray
    gripper_commands: np.ndarray

    linked_entry_states: tuple[StateId, ...]
```

`LINK` 锚点以机械臂—参考系—关系事件为单位构建。在线发生关系失配时，根据当前需要恢复的关系事件查询对应锚点，并使用参考系当前位姿重新实例化。

离线关系事件首先由同一 mode 的全部正常示范共同学习逐状态关系先验 $\pi_{\mathrm{demo}}$，再在完整的联合关系先验序列上使用 `external≤0.3`、`linked≥0.7` 的迟滞阈值和前后最短稳定窗口检测 LINK/UNLINK 候选。候选事件使用按示范留一一致性检查：每一折用其余 $N-1$ 条正常示范重新拟合联合关系先验，并检查同类型事件能否在统一位置容差内重现；五条示范时至少要求四折支持。验证通过后，事件位置取全部示范联合模型的检测结果，并重新使用全部对应正常示范拟合最终 LINK 锚点或 UNLINK 脱离信息。LINK 建立后到对应 UNLINK 前的 linked 状态保存 `link_origin` 映射，以便执行中关系破坏时追溯正确的事件级恢复锚点。

正式 LINK 与 LINK_PENDING 的局部轨迹均使用固定最大窗口 `link_anchor_horizon`，当前默认值为16个对齐后的离散进度状态。设当前事件终点的全局状态索引为 $e$，并在该机械臂所有可恢复参考系的稳定关系序列中查找 $e$ 之前最近一次 linked→external 的位置 $u$，则轨迹起点为：

$$
s_{\mathrm{start}}
=
\max\left(0,e-H_{\mathrm{anchor}}+1,u+1\right).
$$

若不存在先前 UNLINK，则忽略 $u+1$。截断后不向更早状态回填到固定长度；轨迹允许跨技能边界，但不得跨过同一机械臂最近一次 UNLINK，从而避免把上一段抓取或释放轨迹混入本次模板。正式 LINK 以第一个稳定 linked 状态为终点，LINK_PENDING 以跨示范对齐的本次闭合状态为终点，两者共用同一起点计算与轨迹拟合实现。

对于正常示范中的 LINK 假设，如果跨示范 GMSD、抓取闭合模板和事件位置留一一致性均支持 external→linked，但闭合后最短稳定窗口完全缺少有效运动激励，无法离线确认也没有运动反证，则不将它强判为 LINK 或 external，而保存：

```python
@dataclass
class LinkPendingCandidate:
    event_id: RelationEventId
    arm_id: str
    frame_id: str
    candidate_state: StateId

    context_state: StateId
    local_means: np.ndarray
    local_covariances: np.ndarray
    gripper_commands: np.ndarray

    support_fraction: float
    demonstration_indices: tuple[int, ...]
    event_local_indices: tuple[int, ...]
```

`LINK_PENDING` 是离线事件候选状态，不是第三种关系概率状态，也不等同于在线决策中的 `Unknown`。它保存“这次 LINK 事件可重复出现，但示范缺少确认运动”，并使用与正式 LINK 锚点相同的拟合口径，保存候选建立前的短局部轨迹、协方差和夹爪命令。该轨迹只是未激活的恢复模板：离线阶段不生成正式 `LinkRecoveryAnchor`、不传播 `link_origin`、不计入阶段一 `LocalCompletionModel` 的已完成必要关系，也不执行 `VERIFY_LINK`。若同一闭合事件在 LINK 锚点窗口内稍后已经获得充分共动证据并通过留一检查，则只保留正式 LINK，不重复保留 Pending。

该表示支持同一关系事件在不同 episode、不同世界位置和不同对象姿态下重复使用。

对于正常示范中的：

$$
\mathrm{linked}\rightarrow\mathrm{external},
$$

保存：

```python
@dataclass
class UnlinkEventMetadata:
    event_id: RelationEventId
    arm_id: str
    frame_id: str

    release_state: StateId
    legal_reentry_states: tuple[StateId, ...]

    local_detachment_target: np.ndarray | None
```

其中主要记录：

- 正常关系解除位置；
- 关系解除后的合法任务状态；
- 必要的局部脱离信息。

在线 `UNLINK` 使用统一关系解除过程，并使用上述元数据完成关系验证与任务重入。

---

### 2.7 重构 DynaMAC 查询接口

在现有 DynaMAC 基础上增加一个指定状态查询接口，例如：

```python
query_state(
    observation,
    state_id,
    stream_weights=None,
    *,
    mode_index=None,
) -> DynaMACAction
```

该接口负责：

- 读取指定状态的多流均值和协方差；
- 将局部 marginal 变换到世界坐标；
- 按给定流权重执行 PoE；
- 输出位姿、协方差、夹爪和诊断信息。

该接口不自动执行：

```text
time_index += 1
skill_index += 1
```

原始 `act()` 可以重构为：

```text
读取当前 cursor
→ query_state()
→ 按原 DynaMAC 逻辑推进 cursor
```

从而复用同一查询代码。

---

## 3. 阶段交付物

建议形成：

```text
source/policy/closed_loop/
├── task_model.py
├── task_model_builder.py
├── state_index.py
├── scene_factors.py
├── boundary_model.py
├── relation_events.py
└── query_adapter.py
```

以及对应测试。

离线模型应支持保存和加载，并记录其依赖的基础 DynaMAC 模型标识。附加模型只保存新统计量，不重复保存已有大规模流均值和协方差。

---

## 4. 验收标准

阶段一完成需满足：

1. 所有技能状态都有唯一 `StateId`；
2. 状态数量等于各技能 duration 总和；
3. 每个状态能正确访问原有 MiDiGaP 流分布；
4. 每个参考系的关系先验归一化；
5. 每个状态只有稀疏场景因子，不生成全对象两两组合；
6. 每个技能边界均生成完整的 `BoundaryModel`；
7. 边界窗口为当前技能末端的 $h_b^-$ 个状态；
8. 任务相关 external→linked 假设在有充分运动证据时生成正式 `LINK` 锚点，无反证但激励不足时保存带未激活事件前轨迹模板的 `LINK_PENDING`，有充分运动反证时不保留；
9. 每个 linked→external 事件保存关系解除位置和合法重入信息；
10. `query_state()` 可查询任意状态；
11. `query_state()` 不自动推进任务时钟；
12. 原始 DynaMAC 执行路径仍可运行；
13. 所有 SE(3)、四元数、流形高斯与 PoE 计算复用现有代码。

---

# 阶段二：建立统一在线观测，并实现关系—进度联合信念推断

## 1. 阶段目标

完成技术路线的核心状态估计链：

$$
\bar\beta_t
\rightarrow
q_t
\rightarrow
\beta_t.
$$

该阶段先确保关系和进度估计正确、可解释。它可以先以旁路方式运行，不立即改变机器人动作。

---

## 2. 开发内容

### 2.1 建立统一在线观测

定义一个新方法使用的运行观测：

```python
@dataclass
class RuntimeObservation:
    tick: int

    ee_pose: np.ndarray
    frame_poses: dict[str, np.ndarray]

    gripper_state: np.ndarray

    previous_command_pose: np.ndarray | None
    previous_ee_pose: np.ndarray | None

    tracking_reliability: dict[str, float]
    frame_visibility: dict[str, bool]
```

在线关系与进度模块共享该数据结构。

当前 RLBench 实现以低维仿真状态作为训练和推理输入，可以继续沿用这一观测来源，不增加新的感知系统。

---

### 2.2 统一计算运行特征

每个控制周期一次性计算：

- 实际末端运动；
- 上一命令对应的目标运动；
- 各参考系世界位姿变化；
- 末端—参考系相对位姿；
- 相对运动残差；
- 夹爪当前状态及变化；
- 参考系可见性；
- 跟踪可靠性；
- 动作激励程度。

这些特征由统一 `RuntimeFeatureBuilder` 生成，关系滤波和进度滤波共享同一组结果。

---

### 2.3 构造动作后名义进度先验

根据上一周期进度后验和上一动作正常完成的默认假设，构造：

$$
\bar\beta_t(s).
$$

主要概率集中在正常后继状态，同时为邻近的：

- 未完全完成状态；
- 正常完成状态；
- 稍微提前完成状态；

保留非零概率。

在技能内部只生成局部候选。

在技能末端，只有入口守卫已经放行，名义进度先验才允许进入下一技能首状态；否则保持在当前技能末端。

---

### 2.4 实现在线关系滤波

对每个机械臂—参考系关系维护：

$$
q_t^f(\mathrm{external}),
\qquad
q_t^f(\mathrm{linked}).
$$

关系更新使用：

- 上一周期关系后验；
- 名义进度先验加权得到的示范关系先验；
- 当前相对运动残差；
- 上一命令动作；
- 实际末端运动；
- 夹爪状态；
- 跟踪可靠性和可见性。

实现：

$$
\bar q_t^f(z)
=
\sum_{z'}
P_{\mathrm{rel}}(z\mid z')
q_{t-1}^f(z'),
$$

$$
\pi_{f,t}^{\mathrm{prior}}(z)
=
\sum_s
\bar\beta_t(s)
\pi_{f,s}^{\mathrm{demo}}(z),
$$

$$
q_t^f(z)
\propto
\bar q_t^f(z)
\left[
\pi_{f,t}^{\mathrm{prior}}(z)
\right]^{\lambda_d}
\left[
p(y_{f,t}\mid z,a_{t-1})
\right]^{X_{f,t}}.
$$

Unknown 通过以下信息判断：

- 动作激励不足；
- 参考系不可见；
- 跟踪可靠性过低；
- 关系后验熵过高。

Unknown 不改变 external/linked 概率空间，而作为：

```python
informative: bool
```

或：

```python
decision_state = UNKNOWN
```

传给后续控制模块。后续控制直接联合读取同一 `RuntimeFeatures` 中已有的参考系可见性、跟踪可靠性和动作激励程度，与 `decision_state=UNKNOWN` 一起判断是否适合主动验证。只有参考系可见、跟踪可靠且实际动作激励不足时才能生成主动关系验证请求。

在后续 `VERIFY_LINK` 执行模式中，同一关系滤波器继续读取实际末端与参考系运动，并给出稳定的 `linked`、`external` 或仍为 `Unknown` 的决策结果。主动验证不增加第三种关系概率状态；`LINK_PENDING` 表示当前事件上下文命中了一个尚未获得关系判定的离线 LINK 候选。

其中，阶段一保存的 `LinkPendingCandidate` 是按事件 occurrence 持久化的离线候选；`Unknown` 是当前在线周期的关系决策。在线进入 Pending 对应事件阶段后，稳定 `linked` 表示本次 LINK 获得确认，稳定 `external` 表示本次 LINK 未建立，而 `Unknown` 表示事件仍未解析。

---

### 2.5 实现候选进度统一评分

对：

$$
s\in\mathcal W_t
$$

一次性计算：

#### 机器人局部轨迹匹配

$$
L_t^{\mathrm{robot}}(s).
$$

只让可靠且更可能 external 的参考系主要参与：

$$
\rho_{f,t}
=
v_{f,t}(1-U_{f,t})
q_t^f(\mathrm{external}).
$$

#### 稀疏场景状态匹配

$$
L_t^{\mathrm{state}}(s)
=
\prod_{j\in\mathcal J_s}
\left[
\int
r_t^j(x)p_j(x\mid s)\,dx
\right]^{a_{j,t}}.
$$

#### 关系兼容度

$$
C_t^{\mathrm{rel}}(s)
=
\prod_f
\left[
\sum_z
q_t^f(z)
\pi_{f,s}^{\mathrm{demo}}(z)
\right]^{
v_{f,t}(1-U_{f,t})
}.
$$

所有评分应在 log-space 中计算并分别保存，不能只输出最终总分。

---

### 2.6 更新进度后验

实现：

$$
\beta_t(s)
\propto
\bar\beta_t(s)
L_t^{\mathrm{robot}}(s)
\left[
L_t^{\mathrm{state}}(s)
\right]^{\lambda_x}
\left[
C_t^{\mathrm{rel}}(s)
\right]^{\lambda_r}.
$$

输出：

```python
@dataclass
class ProgressEstimate:
    prior: dict[StateId, float]
    posterior: dict[StateId, float]

    nominal_state: StateId
    estimated_state: StateId

    confidence: float
    entropy: float

    best_explanation_score: float
    status: ProgressStatus
```

其中 `ProgressStatus` 至少包括：

```text
ALIGNED
FORWARD_REALIGNMENT
BACKWARD_REALIGNMENT
LOW_CONFIDENCE
NO_PLAUSIBLE_STATE
```

---

### 2.7 实现关系事件感知的候选扩展

默认只在局部窗口内估计进度。

当出现可靠关系变化，且局部窗口没有可解释状态时：

1. 根据当前实际关系查询最近的未来关系兼容状态段；
2. 将其加入候选集合；
3. 仍要求机器人轨迹、场景构型和关系三者均匹配；
4. 跨越技能边界的候选还必须通过入口守卫。

该机制用于正确处理动作提前完成，同时避免把搬运中掉落解释为未来正常释放。

---

### 2.8 建立固定的单周期更新器

建立统一的 `BeliefUpdater`，严格执行一次：

```text
progress prior
→ relation posterior
→ progress posterior
```

同一个控制周期内不循环重新更新关系和进度。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── runtime_observation.py
├── runtime_features.py
├── progress_prior.py
├── relation_filter.py
├── state_evaluator.py
├── progress_filter.py
└── belief_updater.py
```

并输出统一的：

```python
ClosedLoopBelief
```

包含所有关系后验、进度先验、进度后验和候选评分。

---

## 4. 验收标准

阶段二完成需满足：

1. external/linked 后验始终归一化；
2. 机械臂和对象同步运动时 linked 后验上升；
3. 机械臂运动、对象不随动时 external 后验上升；
4. 机械臂和对象均静止时，不因残差接近零而强判 linked；
5. 参考系不可见或不可靠时进入 Unknown；
6. 名义进度先验主要集中在正常后继；
7. 动作未完成时，进度后验可回到较早状态；
8. 动作提前完成时，进度后验可前移；
9. linked 流不会凭极小协方差独占进度判断；
10. 搬运中掉落不能仅凭 external 关系匹配未来释放状态；
11. 同一周期更新严格执行一次；
12. 所有候选状态均保留可审计的分项评分。

---

# 阶段三：实现动态流角色与闭环正常执行控制

## 1. 阶段目标

让在线关系和进度信念真正参与动作生成与任务时间控制。

建立三个明确进度变量：

```text
nominal_state
estimated_state
reference_state
```

并实现：

```text
HOLD
REALIGN
ADVANCE
```

三类正常执行决策。

---

## 2. 开发内容

### 2.1 实现动态流角色

对每条离线已建模且已被任务参数选择保留的流，分配：

```text
EXECUTE
MONITOR
RECOVER
DEFER
```

角色判断使用：

- 当前在线关系后验；
- 进度校正后的期望关系；
- 关系观测可靠性；
- 原有任务参数选择结果。

角色语义为：

| 条件 | 角色 | 执行作用 |
|---|---|---|
| 期望 external，实际 external | EXECUTE | 参与 PoE |
| 期望 linked，实际 linked | MONITOR | 不参与正常 PoE，继续关系监测 |
| 期望与实际可靠失配 | RECOVER | 阻断正常推进，生成恢复意图 |
| 关键关系不确定 | DEFER | 保持上一可信角色并暂缓切换 |

运行期角色只作用于离线任务模型中已经建模并保留的参考系流。

当当前事件上下文命中阶段一的 `LinkPendingCandidate`，该关系是后续正常执行的必要条件，关系决策为 `Unknown`，且同一 `RuntimeFeatures` 表明参考系可见、跟踪可靠但实际动作激励不足时，动态角色层在 `DEFER` 和 `HOLD` 之外生成：

```python
RelationVerificationRequest(
    arm_id,
    frame_id,
    relation="linked",
    pending_event_id=pending_event_id,
)
```

本阶段只生成可审计的主动验证请求，不执行验证移动，也不改变任务进度。

---

### 2.2 扩展 PoE 流权重

在现有 PoE 计算中支持非负精度权重：

$$
\tilde{\Lambda}_{f,t}
=
w_{f,t}\Lambda_{f,t}.
$$

建议运行权重由以下因素共同确定：

- 离线是否被选中；
- 当前角色；
- external 后验；
- 观测可靠性。

满足：

- `EXECUTE`：具有正常或连续权重；
- `MONITOR`：正常 PoE 权重为 0；
- `RECOVER`：正常 PoE 权重为 0；
- `DEFER`：保持上一可信执行权重或安全降权。

PoE 权重只缩放精度，不直接缩放专家均值。

---

### 2.3 实现闭环任务游标

定义：

```python
@dataclass
class ClosedLoopCursor:
    nominal_state: StateId
    estimated_state: StateId
    reference_state: StateId
```

其中：

- `nominal_state`：上一动作正常完成后应该到达的状态；
- `estimated_state`：当前观测推断的实际状态；
- `reference_state`：当前真正用于查询动作的状态。

---

### 2.4 实现保持、重新对齐和技能内推进

#### HOLD

当出现：

- 当前目标尚未完成；
- 进度置信度不足；
- 关键关系 Unknown；
- 当前存在可靠关系失配；
- 后继条件尚未满足；

保持：

$$
s_{t+1}^{\mathrm{ref}}
=
s_t^{\mathrm{ref}}.
$$

HOLD 继续调用 `query_state()` 伺服当前参考状态，不等于发送关节静止命令。

#### REALIGN

当：

$$
\hat s_t\neq s_t^{\mathrm{nom}}
$$

且后验可信时：

$$
s_t^{\mathrm{ref}}
\leftarrow
\hat s_t.
$$

重新对齐只修改任务游标，不执行正常轨迹的反向回放。

#### ADVANCE

当前状态可靠完成，且后继位于同一技能时：

$$
s_{t+1}^{\mathrm{ref}}
=
\operatorname{succ}(\hat s_t).
$$

一个控制周期最多推进一个正常后继。

---

### 2.5 实现正常状态可解释性与失配累计

根据：

$$
\mathcal E_t^{\max}
$$

以及关系兼容度，累计：

- 无正常候选状态；
- 关系持续失配；
- 持续 HOLD；
- 进度长期无法推进。

统一生成：

```python
MismatchEvent
```

本阶段只阻断正常推进并输出恢复意图，具体恢复动作在后续阶段实现。

对“期望 linked，但因运动证据不足而不确定”的情形，应优先输出 `RelationVerificationRequest`，不将证据不足直接当作可靠关系失配。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── frame_roles.py
├── weighted_poe.py
├── execution_cursor.py
├── execution_controller.py
└── mismatch.py
```

---

## 4. 验收标准

阶段三完成需满足：

1. `EXECUTE` 流正常参与 PoE；
2. `MONITOR` 流不参与正常 PoE，但关系滤波仍读取它；
3. `RECOVER` 流不参与正常 PoE；
4. Unknown 不造成单帧角色剧烈切换；
5. PoE 权重为 1 时与现有 PoE 语义一致；
6. PoE 权重为 0 时等价于移除该专家；
7. HOLD 继续闭环伺服当前状态；
8. REALIGN 只修改内部状态索引；
9. ADVANCE 每次只进入直接后继；
10. 关系失配时不继续盲目推进；
11. 角色路由不直接修改任务游标；
12. 所有进度变更只由统一执行控制器提交。
13. 当前事件命中 `LinkPendingCandidate`、后续必要 linked 关系仅因动作激励不足而 Unknown 时，生成主动验证请求而不直接判定关系失配。

---

# 阶段四：实现对象中心技能入口状态守卫与多臂事务推进

## 1. 阶段目标

在技能阶段跨越之前，检查下一技能所需的完整边界就绪状态。

该模块统一处理：

- 本臂技能完成条件；
- 任务相关机械臂—参考系关系条件；
- 稀疏场景构型条件。

每只机械臂利用自身进度后验判断本地技能是否完成。多臂协作通过同一边界中涉及的多机械臂关系和场景条件表达，并通过统一事务提交技能转换。

当前仓库中的双臂策略分别维护自身技能索引和局部时钟。新入口守卫在保留双臂本地进度的基础上，对必要边界进行状态驱动的放行。

---

## 2. 开发内容

### 2.1 在线计算边界条件

复用阶段一生成的：

```text
BoundaryModel
local completion model
guard relation conditions
guard scene conditions
affected arms / transition transaction group
```

对每个边界条件计算：

- 当前状态兼容度；
- 当前观测可靠性；
- 是否满足阈值；
- 是否已经连续满足指定周期。

场景状态和关系状态直接使用阶段二已有的状态后验。

---

### 2.2 计算本臂局部完成度

每只机械臂分别使用自身的：

$$
\beta_t^i
$$

以及：

- 进度后验是否位于技能末端；
- 当前机器人状态是否达到末端目标；
- 本臂必要关系是否满足；

计算：

$$
R_{i,b,t}^{\mathrm{local}}
=
P_{i,b,t}^{\mathrm{end}}
L_{i,b,t}^{\mathrm{goal}}
C_{i,b,t}^{\mathrm{own-rel}}.
$$

其中：

$$
P_{i,b,t}^{\mathrm{end}}
=
\sum_{\tau=T_k-h_e+1}^{T_k}\beta_t^i(k,\tau),
$$

$$
L_{i,b,t}^{\mathrm{goal}}
=
p_{i,b,m}^{\mathrm{goal}}(x_{i,t}^{\mathrm{goal}}),
$$

且对每个 $c=(i,f)\in\mathcal R_{i,b}^{\mathrm{own}}$：

$$
\kappa_{c,t}
=
\sum_z q_t^{i,f}(z)\psi_{b,c}^{\mathrm{own}}(z),
$$

$$
C_{i,b,t}^{\mathrm{own-rel}}
=
\min_{c\in\mathcal R_{i,b}^{\mathrm{own}}}\kappa_{c,t}.
$$

若本臂必要关系集合为空，则定义 $C_{i,b,t}^{\mathrm{own-rel}}=1$。任一必要关系为 Unknown 或观测不可靠时，本周期 `LocalDone=False`，但不直接判为异常。最终：

$$
\mathrm{LocalDone}_{i,b,t}
=
\mathbb I\left[R_{i,b,t}^{\mathrm{local}}>\theta_{i,b}^{\mathrm{local}}\right],
$$

并要求连续满足 $H_{i,b}$ 个周期。$\theta_{i,b}^{\mathrm{local}}$ 与 $H_{i,b}$ 只能使用正常示范回放或留出正常轨迹标定，不得使用故障测试集调参；标定完成后保存到 `LocalCompletionModel` 或运行配置。

只有机械臂 $i$ 完成当前技能后，才为该机械臂生成跨界请求。

---

### 2.3 实现入口放行逻辑

对机械臂 $i$ 的边界 $b$，入口许可为：

$$
\mathrm{Permit}_{i,b,t}
=
\mathrm{LocalDone}_{i,b,t}
\land
\bigwedge_{(r,f,z)\in\mathcal R_b}
\left[
q_t^{r,f}(z)>\theta_{r,f}
\right]
\land
\bigwedge_{j\in\mathcal J_b^{\mathrm{guard}}}
\left[
a_{j,t}>\theta_a
\land
\gamma_{b,j,t}>\theta_{b,j}
\right].
$$

其中：

- $\mathcal R_b$ 为边界相关的机械臂—参考系关系条件集合；
- $r$ 可以是当前机械臂，也可以是其他机械臂；
- $\mathcal J_b^{\mathrm{guard}}$ 为稀疏场景构型条件集合；
- $a_{j,t}$ 为条件观测可靠性；
- $\gamma_{b,j,t}$ 为当前状态与边界支持域的兼容度。

所有必要条件使用逻辑与。

任何必要条件：

- 不满足；
- 处于 Unknown；
- 可靠性不足；

均保持在当前技能末端。

若阻断条件是期望为 `linked` 的必要关系，当前事件上下文命中阶段一的 `LinkPendingCandidate`，关系决策为 `Unknown`，且同一 `RuntimeFeatures` 表明参考系可见、跟踪可靠但实际动作激励不足，入口守卫同时生成 `RelationVerificationRequest`。该请求只表示需要制造可观测运动，不等价于边界放行，也不改变当前的 `LocalDone=False` 结果。

---

### 2.4 实现多臂边界的统一评估与事务提交

每个控制周期首先获取一次联合 pre-action 场景快照，并基于该快照分别完成所有机械臂的：

- 在线关系后验更新；
- 本地进度后验更新；
- 本地技能完成判断；
- 技能入口状态守卫评估。

每个机械臂生成独立的转换请求：

```python
@dataclass
class TransitionRequest:
    arm_id: str
    boundary_id: BoundaryId

    permitted: bool
    source_state: StateId
    target_state: StateId

    local_done: bool
    condition_results: dict[ConditionId, ConditionResult]
    verification_requests: tuple[RelationVerificationRequest, ...]
```

所有转换请求先完成评估，再统一提交：

```text
读取联合场景快照
→ 更新所有机械臂信念
→ 评估所有技能入口守卫
→ 生成全部转换请求
→ 校验事务组条件
→ 统一提交允许的技能转换
```

不同边界依赖结构由各自条件集合自然表达：

- 条件只涉及本臂时，机械臂独立推进；
- 某一机械臂的守卫包含其他机械臂产生的关系或场景条件时，依赖方保持等待；
- 多个机械臂的守卫共同依赖同一组关系和场景条件时，在条件同时满足后于同一控制周期完成技能转换。

等待期间，各机械臂保持自身已经估计出的实际进度和控制目标，并继续基于当前对象与其他机械臂参考系动态伺服。

在线入口判断直接使用当前控制周期的真实场景状态。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── entry_guard.py
├── boundary_runtime.py
├── transition_transaction.py
└── bimanual_controller.py
```

---

## 4. 验收标准

阶段四完成需满足：

1. 当前技能未完成时不生成跨界许可；
2. 必要边界条件未满足时不跨界；
3. 必要条件 Unknown 时保持等待；
4. 条件稳定满足后能够正常放行；
5. 每只机械臂的本地完成判断只使用自身进度后验；
6. 多臂协作条件通过机械臂—参考系关系和稀疏场景构型表达；
7. 所有机械臂基于同一 pre-action 场景快照评估；
8. 所有转换请求在评估结束后统一提交；
9. 事务组内的联合转换在同一次状态提交中完成；
10. 不相关边界能够保持异步推进；
11. 等待期间已就绪机械臂仍能动态伺服；
12. 在线边界判断直接使用当前真实状态；
13. 必要 linked 关系命中 `LinkPendingCandidate`、且仅因动作激励不足而 Unknown 时，保持边界阻断并生成主动验证请求。

---

# 阶段五：实现主动关系验证、统一关系恢复与任务重入

## 1. 阶段目标

实现：

$$
\mathrm{ExecutionMode}
\in
\{\mathrm{TASK},\mathrm{VERIFY\_LINK},\mathrm{RECOVERY}\}
$$

顶层执行模式切换，以及通用：

$$
\mathrm{LINK}(f),
\qquad
\mathrm{UNLINK}(f)
$$

关系恢复。

`ExecutionMode` 与任务进度 StateId 及 DynaMAC/MiDiGaP 轨迹 mode 彻底分离。`VERIFY_LINK` 只为不可观的必要 linked 关系制造有限运动证据；关系判定结果仍只是 `linked`、`external` 或 `Unknown`。

恢复过程根据期望关系和实际关系生成关系目标。

---

## 2. 开发内容

### 2.1 恢复触发与模式切换

当：

- 所有正常候选状态均无法解释当前状态；
- 期望关系与实际关系持续可靠失配；
- 入口条件长期无法满足且本臂存在关系异常；

进入：

```text
RECOVERY
```

此时：

- 冻结正常 `reference_state`；
- 正常进度先验不再驱动动作；
- 关系后验继续更新；
- 恢复动作使用独立内部状态；
- 正常入口守卫暂停任务推进。

---

### 2.2 实现 `VERIFY_LINK`

当阶段三或阶段四生成的 `RelationVerificationRequest` 满足以下条件时，从 `TASK` 切换到 `VERIFY_LINK`：

- 请求的关系是后续正常执行或技能入口的必要 linked 关系；
- 请求引用当前事件上下文中尚未解析的 `LinkPendingCandidate`；
- 当前关系决策为 `Unknown`，且同一 `RuntimeFeatures` 表明参考系可见、跟踪可靠但实际动作激励不足；
- 同一关系事件尚未执行过本次验证。

进入时记录当前末端位姿，并从 `TASK` 模式最近的实际末端轨迹中估计抓取接近方向。验证统一使用固定模板：

```text
冻结正常任务时钟和进度提交
→ 保持夹爪命令和末端朝向
→ 沿抓取接近方向的反方向低速移动
→ 关系滤波继续使用实际末端和参考系运动更新
→ 停止外移
→ 沿原路径返回记录位姿
→ 统一返回 TASK
```

不使用“根据累计运动逐级扩大幅度”机制。反向探测阶段持续移动，直到首个退出条件成立：

$$
\mathrm{StableRelationDecision}
\lor
\Delta t\ge t_{\max}
\lor
\mathrm{SafetyConstraintTriggered}.
$$

`StableRelationDecision` 必须建立在验证动作已产生满足关系滤波激励条件的实际末端运动之上，不能只因离线先验或指令目标已发送就提前结束。其中 $t_{\max}$ 只计算反向探测阶段；超时后停止继续外移，但仍执行原路径返回。关系滤波在外移和返回期间都继续更新。若安全约束表明返回路径本身不安全，则现有安全控制优先，不强制返回。

返回完成后不在 `VERIFY_LINK` 内根据结果分支，而是统一切回 `TASK`。最新的 `linked`、`external` 或 `Unknown` 结果由原有正常执行、入口守卫和恢复触发逻辑消费。

若该 Pending occurrence 在本次在线执行中被稳定验证为 `linked`，则只对当前 episode 激活阶段一随候选保存的恢复轨迹模板，并建立指向该 occurrence 的运行时 `link_origin`；之后同一连接若发生关系失配，恢复模块才能找到正确的事件前轨迹。验证为 `external` 或仍为 `Unknown` 时均不激活模板，也不建立运行时来源。此在线激活不回写或篡改离线任务模型。

为每次关系事件保存验证尝试状态。同一未决关系从 `VERIFY_LINK` 返回 `TASK` 后不得立即重复触发；只有关系状态、技能状态或新的抓取事件发生变化时，才清除对应尝试记录并允许重新触发。

---

### 2.3 实现 `LINK(f)`

根据当前需要建立的机械臂—参考系关系，查询阶段一保存的事件级 `LINK` 锚点：

$$
e=(i,f,k,m,r).
$$

锚点包含：

- 关系建立前短局部轨迹；
- 当前参考系坐标下的相对目标；
- 夹爪闭合命令；
- 关系建立后的合法任务状态。

根据参考系当前位姿重新实例化恢复目标：

$$
T_{e,t}^{W,\mathrm{target}}(s)
=
T_{f,t}^{W}
\mu_{f,s}.
$$

恢复流程为：

```text
重新接近
→ 预抓取
→ 闭合夹爪
→ 通过在线关系后验验证 linked
```

恢复分布采用：

$$
\Sigma_{f,s}^{\mathrm{rec}}
=
\Sigma_{f,s}
+
\lambda_{\mathrm{rec}}I.
$$

锚点按照当前关系事件上下文选择。同一事件锚点可以在参考系不同世界位姿下重复实例化。

---

### 2.4 实现 `UNLINK(f)`

统一执行：

```text
停止继续搬运
→ 打开夹爪
→ 执行局部脱离
→ 通过在线关系后验验证 external
```

阶段一保存的 `UnlinkEventMetadata` 用于确定：

- 正常关系解除位置；
- 可选局部脱离目标；
- 关系解除后的合法重入状态。

---

### 2.5 实现多个关系目标排序

当同时需要：

$$
\mathrm{UNLINK}(B)
$$

和：

$$
\mathrm{LINK}(A),
$$

使用夹爪资源前置条件：

$$
\mathrm{FREE}(\mathrm{gripper})
\prec
\mathrm{LINK}(A).
$$

恢复控制器接受一组关系目标，并生成符合资源约束的执行顺序。

---

### 2.6 实现恢复验证与任务重入

关系恢复成功后：

1. 重新更新在线关系后验；
2. 对合法重入候选计算完整状态解释分数；
3. 选择：

$$
s^{\mathrm{re}}
=
\arg\max_{s\in\mathcal W_{\mathrm{re}}}
\mathcal E_t(s);
$$

4. 检查涉及的技能入口守卫；
5. 重置进度后验；
6. 设置新的 `reference_state`；
7. 返回 TASK 模式。

不得直接恢复到故障发生前的旧时钟索引。

恢复尝试需要具有明确上限，并在无法恢复时输出结构化失败结果。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── relation_verification.py
├── recovery.py
├── link_anchors.py
├── unlink_metadata.py
├── relation_goals.py
└── reentry.py
```

---

## 4. 验收标准

阶段五完成需满足：

1. `TASK`、`VERIFY_LINK`、`RECOVERY` 是与 StateId 和轨迹 mode 分离的顶层执行模式；
2. `VERIFY_LINK` 与 `RECOVERY` 中正常任务进度均被冻结；
3. 两种非 TASK 模式中关系后验仍持续更新；
4. `VERIFY_LINK` 引用当前未解析的 `LinkPendingCandidate`，并使用最近实际接近轨迹的反方向低速移动，不逐级扩大幅度；
5. 稳定关系判定、反向探测超时或安全约束任一成立时停止外移；
6. $t_{\max}$ 只限制反向探测，超时后仍执行可安全完成的原路径返回；
7. 返回完成后无论关系结果如何均统一切回 `TASK`；
8. 同一未决关系不会在 `TASK → VERIFY_LINK → TASK` 之间立即重复触发；
9. 每个 `LINK` 请求能够查询正确的事件级恢复锚点；
10. `LINK` 锚点能够根据参考系当前位姿重新实例化；
11. `UNLINK` 能通过统一过程验证关系已恢复 external；
12. linked→external 元数据能够提供合法重入状态；
13. 多关系目标遵守夹爪资源顺序；
14. 恢复期间不把 linked 近零协方差流直接重新加入正常 PoE；
15. 恢复完成后重新估计任务状态；
16. 重入状态必须满足轨迹、场景和关系兼容性；
17. 跨技能重入必须通过入口守卫；
18. 恢复失败有明确终止状态，不无限循环。

---

# 阶段六：完整策略集成、诊断信息与验收

## 1. 阶段目标

将前五阶段整合为一个可以直接接入当前 RLBench runtime 的新策略类，同时保留原 DynaMAC 对比路径。

---

## 2. 开发内容

### 2.1 实现顶层策略

建议新增：

```python
class ClosedLoopMultiStreamPolicy:
    def reset(...)
    def act(...)
    def summary(...)
    def save(...)
    def load(...)
```

每个 TASK 控制周期严格执行：

```text
1. 构造 RuntimeObservation
2. 计算 RuntimeFeatures
3. 构造动作后名义进度先验
4. 更新在线关系后验
5. 对候选状态评分
6. 更新实际进度后验
7. 计算进度校正后的期望关系
8. 路由动态流角色
9. 决定 HOLD / REALIGN / ADVANCE
10. 若跨技能，执行入口守卫并生成转换请求
11. 多臂情况下统一提交技能转换事务
12. 若存在可执行的主动关系验证请求，切换 VERIFY_LINK
13. 若持续可靠失配，切换 RECOVERY
14. 仍处于 TASK 时，根据 reference_state 和流权重查询多流动作
15. 验证并提交本周期状态
```

主动关系验证模式执行：

```text
冻结正常任务进度
→ 沿最近接近轨迹的反方向低速移动并持续更新关系后验
→ 关系稳定判定 / 超过反向探测时限 / 安全约束触发
→ 停止外移并沿原路径返回
→ 返回 TASK
```

恢复模式执行：

```text
关系目标执行
→ 关系验证
→ 合法状态重入
→ 返回 TASK
```

---

### 2.2 采用事务式状态更新

每个控制周期使用：

```text
prepare
→ validate
→ commit
```

关系、进度、游标、角色、边界转换和恢复状态只有在当前周期全部计算成功后才统一提交。

计算异常时不留下部分更新状态。

---

### 2.3 建立完整逐周期诊断

每个 tick 至少记录：

```text
nominal_state
estimated_state
reference_state

progress prior
progress posterior
progress confidence

每条流：
    demo relation prior
    relation posterior
    informative / Unknown
    role
    execution weight

每个候选状态：
    robot score
    scene score
    relation score
    total score

执行决策：
    HOLD / REALIGN / ADVANCE
    reason

边界守卫：
    local completion
    active scene conditions
    active relation conditions
    condition reliability
    compatibility
    transition request
    transaction commit result

恢复：
    mode
    relation event id
    relation goals
    recovery phase
    reentry candidates

主动关系验证：
    relation candidate
    recent approach direction
    outbound elapsed time / t_max
    relation decision
    exit reason
    return progress
    verification attempted

动作：
    participating streams
    PoE weights
    pose
    covariance
    gripper
```

诊断记录必须能够在不重新运行仿真的情况下解释一次状态切换的原因。

---

### 2.4 接入当前 RLBench runtime

复用当前：

- observation adapter；
- 双臂同步 pre-action snapshot；
- IK 与控制器；
- evaluation launcher；
- result schema；
- replay 系统。

新增策略只替换策略推理与进度控制部分。

配置中应允许：

```text
policy_type = dynamac
policy_type = closed_loop_multistream
```

用于同一评测管线中的对比。

---

### 2.5 建立测试层次

#### 单元测试

覆盖：

- 离线状态模型；
- 关系后验；
- 进度后验；
- 稀疏状态因素；
- 边界前短终止窗口；
- 边界关系条件；
- 边界场景构型条件；
- 事件级 LINK 锚点；
- UNLINK 元数据；
- 流角色；
- PoE 权重；
- 执行状态机；
- 主动关系验证；
- 入口守卫；
- 多臂事务提交；
- LINK/UNLINK；
- 任务重入。

#### 轨迹回放测试

使用已经采集的 observation/action 序列，在不运行物理仿真的情况下验证：

```text
关系后验
进度后验
角色变化
边界判断
转换事务
失配触发
```

#### 仿真集成测试

验证新策略能够：

- reset；
- 单臂 act；
- 双臂 act；
- 正常完成 episode；
- 进入和退出 RECOVERY；
- 进入和退出 VERIFY_LINK；
- 保存和加载模型；
- 输出完整 diagnostics。

---

## 3. 阶段交付物

```text
source/policy/closed_loop/
├── policy.py
├── config.py
├── state.py
├── diagnostics.py
└── serialization.py
```

以及：

```text
integrations/rlbench/
└── closed_loop policy adapter / launcher support
```

---

## 4. 最终验收标准

整个开发完成需要满足：

1. 新方法完整使用现有 DynaMAC/MiDiGaP 多流策略；
2. 不存在第二套重复的 SE(3)、四元数或 PoE 实现；
3. 离线关系、场景因子、边界状态和关系转移数据由同一示范数据管线生成；
4. 关系空间严格为 external/linked；
5. Unknown 作为不确定决策正常工作；
6. 单周期更新顺序严格为：

$$
\bar\beta_t
\rightarrow
q_t
\rightarrow
\beta_t;
$$

7. `nominal_state`、`estimated_state`、`reference_state` 职责清晰；
8. HOLD 继续闭环伺服当前状态；
9. REALIGN 不执行任务轨迹反向回放；
10. ADVANCE 每次只进入合法直接后继；
11. 动态流角色能够实际改变 PoE 参与方式；
12. 每只机械臂使用自身进度后验判断本地技能完成；
13. 技能边界由任务相关关系和稀疏场景构型条件决定；
14. 多臂边界基于同一 pre-action 场景快照评估，并通过事务统一提交；
15. 当前事件命中 `LinkPendingCandidate`、必要 linked 关系因动作激励不足而 Unknown 时能生成主动验证请求；
16. `VERIFY_LINK` 冻结任务进度、持续更新关系后验，并按固定反向探测模板返回 `TASK`；
17. 同一未决关系的主动验证不会立即重复触发；
18. 关系失配能生成 LINK/UNLINK 恢复目标；
19. LINK 使用正确的事件级关系恢复锚点；
20. UNLINK 使用统一关系解除过程及合法重入元数据；
21. 恢复后根据当前真实状态重新进入任务；
22. 原 DynaMAC 对比路径仍然可运行；
23. 新方法模型可保存、加载并重复执行；
24. 每个控制周期均有完整、可解释的诊断记录；
25. 所有新增测试和现有相关测试通过。

---

# 建议拆给 Codex 的六个主任务

可以直接将开发工作拆成以下六个连续 Codex 任务：

1. **统一离线任务模型、边界数据、关系事件模板与任意状态策略查询**
2. **运行观测、在线关系滤波与进度后验**
3. **动态流角色与闭环执行状态机**
4. **技能入口状态守卫与多臂事务提交**
5. **主动关系验证、事件级 LINK、通用 UNLINK 与任务重入**
6. **RLBench 集成、序列化、诊断与完整测试**

每个主任务都要求同时提交：

```text
实现代码
单元测试
集成测试
配置定义
数据结构说明
关键公式到代码的映射说明
验收命令及输出
```
