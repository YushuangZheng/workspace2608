# 新方法开发日志

本日志持续记录“关系—进度信念驱动的少样本闭环对象中心多流操作”方法的关键开发过程、机制决策、验收结果和异常处理。日志不替代代码注释、配置说明或正式阶段汇报。

## 2026-08-24：阶段一启动

### 本阶段范围

- 建立统一 `StateId` 和 `ClosedLoopTaskModel`；
- 从正常成功示范构建逐状态关系先验、稀疏场景因子、边界模型和关系事件模板；
- 将 DynaMAC 的指定状态策略查询与固定时钟推进解耦；
- 保存新增统计并绑定基础 DynaMAC 指纹，不重复保存原有流模型；
- 保持原始 DynaMAC baseline 执行路径可运行。

### 已确认的实现口径

- 关系空间严格为 `external/linked`，Unknown 仅作为证据不足的决策状态；
- 虚拟技能帧继续作为固定执行参考系，不参与关系滤波、关系失配或 LINK/UNLINK；
- 不增加 `progress_conditions`，多臂边界以代码开发计划中的本地完成度、关系条件、场景条件和事务组为准；
- 优先复用现有 RLBench 低维真值、DynaMAC/MiDiGaP、SE(3)、流形高斯和 PoE 能力；
- 开发计划要求的可靠性和可见性字段必须保留，但不因当前评测平台缺少遮挡而提前建设新的感知系统；
- 只保留必要校验和事务门控，不增加偏离主线的重复防御层。

### 阶段依赖解释

阶段一让 `query_state()` 能按调用方给定的精度权重执行 PoE；阶段三再根据动态流角色和在线信念计算这些权重。二者是底层计算能力与运行决策逻辑的分工，不是不同方法版本。

### 已完成开发

- 建立统一 `StateId` 拓扑和 `ClosedLoopTaskModel`，每个状态直接引用基础 DynaMAC 的流均值和协方差；
- 从同一批正常成功示范提取逐状态 GMSD 关系连接分数及 `external/linked` 软先验；
- 构建技能条件化稀疏场景因子，使用留一示范的局部状态或边界识别提升进行筛选；
- 为每个相邻技能边界构建本地完成、关系条件、场景条件、可靠性与事务元数据；
- 为任务相关物理参考系构建事件级 LINK 局部恢复锚点和 UNLINK 合法重入信息；
- 将 `DynaMAC.query_state()` 与固定时钟推进解耦，增加非负精度权重，原 `act()` 继续按冻结 baseline 逻辑推进；
- 新增附加模型 sidecar 保存/加载，只保存新统计量并绑定基础 DynaMAC 指纹与选择语义。

### 异常与修复

- 首轮全任务检查发现，若仅依关系先验把所有相关候选帧加入活动实体，会让交接任务的干扰物产生过大候选图。已按开发计划收敛为“当前/相邻技能使用帧 + 当前技能中稳定 linked 的可恢复帧”，只在该稀疏集合内产生节点和单向标准边。
- 发现边界因子不应只从已通过逐状态筛选的因子中取交集，否则会漏掉“仅对技能末端有识别力”的条件。已在同一候选因子库上独立进行边界窗口留一判别，未改变因子定义。
- 代码复核发现逐状态 GMSD 已用于先验转换但未单独保存，已补齐 `demo_relation_scores` 字段、sidecar 和验收测试。
- 真实任务验收脚本曾因调用双臂数据适配器时漏传 `TaskSpec` 停止；确认为一次性脚本参数错误后从未完成任务续跑，模型代码无需修改。

### 真实 V4 数据验收

使用已发布 V4 checkpoint 及其对应的 5 条训练示范构建阶段一模型，并对每个模型执行 sidecar 保存/加载摘要一致性校验。临时验收文件已由临时目录自动清理。

| 任务/机械臂 | 状态 | 边界 | 状态场景因子 | 边界场景条件 | 边界关系条件 | LINK | UNLINK |
|---|---:|---:|---:|---:|---:|---:|---:|
| stack_wine | 181 | 3 | 105 | 3 | 1 | 1 | 0 |
| place_cups | 187 | 4 | 45 | 2 | 1 | 1 | 1 |
| open_microwave | 168 | 2 | 0 | 0 | 1 | 1 | 0 |
| wipe_desk | 255 | 3 | 9 | 0 | 1 | 1 | 0 |
| store_bottle/left | 275 | 2 | 73 | 9 | 3 | 1 | 1 |
| store_bottle/right | 275 | 2 | 3 | 0 | 0 | 1 | 1 |
| handover_item/left | 262 | 6 | 30 | 8 | 4 | 1 | 0 |
| handover_item/right | 262 | 6 | 10 | 3 | 0 | 3 | 2 |
| lift_tray/left | 116 | 2 | 75 | 10 | 2 | 2 | 0 |
| lift_tray/right | 116 | 2 | 42 | 3 | 0 | 2 | 0 |
| sweep_to_dustpan/left | 144 | 4 | 169 | 11 | 3 | 2 | 0 |
| sweep_to_dustpan/right | 144 | 4 | 172 | 9 | 1 | 2 | 0 |

`open_microwave` 没有保留场景因子，原因是门在局部状态/边界窗口中未获得可重复的留一识别增益；门的状态字段、关系先验、本地完成模型和 linked 边界条件均完整保留。

### 阶段一最终校验

- 阶段一专用测试：`7 passed`；
- 涉及 DynaMAC 查询重构的专项回归：`100 passed`；
- 全仓回归：`437 passed, 4 skipped`，条件跳过为仓库既有测试设置；
- `compileall`、`git diff --check`、新增模块 Flake8 和 Black 检查通过；
- 全仓集成测试需以 `PYTHONPATH=.:source` 包含仓库顶层 namespace；首次未带该路径时的 collection error 已确认为测试启动环境问题。

阶段一未启动训练、仿真评测或回放，未修改或删除已发布 V4 数据和产物。

## 2026-08-24：阶段一计划一致性复核与第一批修复

### 复核结论

对照《新方法代码开发计划》及后续确认口径重新审计后，撤销此前“阶段一完成”的结论。此前场景因子、边界场景条件和关系事件的部分实现只满足代码自洽，没有严格满足计划机制；上表中的真实 V4 场景因子和边界场景条件统计由旧机制生成，不能继续作为阶段一验收结论，待修复完成后重新生成。

### 本批已修复的明确偏差

- 场景活动实体改为按 `StateId=(skill, local)` 的内部 mode 分量构造，局部邻域严格限制在同一技能、同一模态内，不跨技能边界；
- 邻域从 `r=0` 开始，最多尝试到 `r=2`，保存通过跨示范支持覆盖和稳定性检查的最小半径，`r=2` 仍不通过则删除；
- 删除“普通刚体相对技能入口末端”的错误节点因子。节点因子只读取实体自身附带的内部欧氏构型字段；
- 为示范和观测补充可选的实体内部构型、附加场景实体位姿和直接结构绑定接口。现有 baseline 数据不提供这些字段时保持兼容，不伪造节点因子；
- 场景边只来自局部共同激活的任务参考系，以及任务数据接口已有的直接结构绑定；直接结构实体不会诱导新的完全图；
- 虚拟技能帧和对侧机械臂末端不进入普通场景节点或场景边；
- 逐状态 LODO 改为比较加入候选因子前后的局部进度识别 margin，`loo_gain` 保存平均增益，`loo_accuracy` 保存正增益折比例；
- `FactorDistribution` 同时支持 SE(3) 边因子和欧氏节点因子，并保存可观测率、稳定支持率和所选邻域半径；
- 边界场景条件复用第 2.4 节统一的节点/边因子定义，并根据下一技能活动参考系、边界关系实体及其直接结构实体限定候选范围；只对当前技能边界前短终止窗口拟合和筛选，不要求候选因子已经进入某个 `StateNode`，也不将边界专用因子扩散到当前技能逐进度状态；
- “直接结构关联”严格限定为任务参考系的一跳绑定，不因结构实体之间继续存在绑定而做传递扩展；
- 边界 LODO 改为检查其余示范学习的支持分布能否覆盖留出示范的正常边界前终止窗口，不再要求终止窗口与更早窗口可区分；
- 撤销人工配置 `tasks.json.scene_factor_edges` 的解释，代码未增加该字段或实体对白名单。

跨示范支持域当前使用配置化的高斯置信域，默认置信度为 `0.99`；仅使用标准库 Wilson–Hilferty 近似计算卡方阈值，不增加 SciPy 强制依赖。

### 新增机制测试

- 普通刚体不会自动生成节点因子；
- 关节等内部构型字段生成欧氏节点因子；
- 直接结构绑定只产生直接边，不诱导结构实体与其他参考系的完全图；
- 当两个参考系分别在相邻状态激活时，候选边从 `r=0` 失败并选择最小可用半径 `r=1`；
- 只在下一技能入口相关的边界候选因子能够进入 `BoundaryModel`，即使它不具备逐进度识别增益、没有进入任何 `StateNode`；
- 在边界前保持稳定、但与更早窗口没有差异的正常因子可以通过边界覆盖验证。

### 本批校验

- 阶段一专项测试：`11 passed`；
- 全仓回归：`441 passed, 4 skipped`；
- `py_compile` 与 `git diff --check` 通过；当前环境未安装 `ruff`，未把该缺失误报为代码失败；
- 使用认证 V4 StoreBottle 语义适配器完成只读构建，确认训练参考系 `fridge` 与通用旧接口 `fridge_root` 的命名差异无需修改模型或任务配置；StoreBottle 和 SweepDust 的双臂模型均可从已发布 checkpoint 与五条训练示范完成内存构建，未生成或覆盖发布产物。

### 本批结束时尚未修复的机制

- 多模态状态数量与“duration 总和”验收条款之间的口径冲突；
- 双臂边界 `transaction_group` 的生成规则；
- LINK/UNLINK 的逐示范联合检测条件、事件对齐、合法重入范围；
- 任意状态查询中非当前模态路径的前驱诊断语义。

因此，本批修复后阶段一仍处于“未完成、不得进入阶段二”状态。

## 2026-08-24：阶段一计划一致性复核与第二批修复

### 规范依据与实现取舍

- 《技术路线报告》和《新方法代码开发计划》为规范依据；外部 GPT 附件只作为待验证建议，没有直接转写为机制。
- 进度状态统一为 `StateId=(skill_index, local_index)`，mode 保存在节点内部；每臂状态数严格为各 skill `duration` 之和。
- 逐进度场景因子使用归一化的机器人、关系和已选场景证据块，不加名义时间先验；以真实状态相对局部竞争状态的 `logsumexp` margin 计算增益，并做确定性前向选择。
- 五条正常示范时，进度软因子至少要求 4/5 折正增益；边界硬条件要求 5/5 折全部覆盖。
- 边界连续因子兼容度使用 `exp(-d²/2)`，每折取边界前短终止窗口的最低兼容度，正式阈值取所有折下界减去纯数值容差。
- 双臂事务组只在全部正常示范中边界稳定同步，且双方共享必须保持的 linked 实体或完全相同的硬场景条件时生成；不因“双臂任务”这一标签强制同步所有边界。

### 本批修复

- 统一状态拓扑、`StateNode` 内部 mode 维度、sidecar 序列化和 `query_state(..., mode_index=...)` 语义，消除 mode 被错误展开成进度节点的偏差。
- 场景因子的 LODO 机器人分布、关系先验和场景分布均严格由 N-1 条示范拟合，修复留出示范泄漏；邻域半径只由支持率和稳定性选择最小值，因子若无独立增益则直接删除，不用更大半径变相放宽。
- 边界专用因子候选范围改为下一技能参考系、边界关系实体及其一跳直接结构实体，不建立额外“实体依赖模型”，不向当前技能所有进度状态扩散。
- `BoundaryModel` 保存每个场景硬条件的兼容度阈值；连续因子兼容度支持叠加观测协方差，为后续真实跟踪不确定性保留完整接口。
- 实现双臂边界的稳定同步与共享硬依赖配对，只对符合条件的边界设置同一 `transaction_group`和 `affected_arms=(left,right)`。
- 离线关系事件改为逐示范动作响应证据：比较末端运动与末端在参考系中的相对运动残差；无动作激励时保持中性，不将“双方都静止”误判为 linked。
- 打开夹爪作为可恢复抓取关系的 external 证据；闭合夹爪不能单独生成 LINK。迟滞状态沿完整任务序列连续运行，再将事件映射回 skill/local/mode，避免跨无激励技能时遗忘已有关系。
- LINK 锚点能从任意靠近技能起点的事件跨回上一技能提取局部轨迹；新增事件示范支持率、逐示范对齐位置和 `link_origin` 持久化。

### 异常发现与处理

- 初次改造曾用单条示范的短时相对位姿协方差生成事件证据，真实 V4 上出现“静止机器人和静止物体被误判为 linked”以及正常抓取 LINK 消失。没有通过降低 4/5 支持阈值规避，而是按报告的动作响应定义修正证据。
- StoreBottle 正常释放末段只有两个状态且几乎无位移激励，单靠相对运动不足以通过最短稳定窗口。利用报告已定义的夹爪状态作为 external 证据后，左臂 UNLINK 在 5/5 示范中均对齐到局部索引 0。
- HandOver 中间多个技能没有充分动作激励，原按 skill 重置迟滞状态会遗忘左臂已建立的 item0 关系。改为全任务连续检测后，左臂 UNLINK 在 5/5 示范中均对齐到最后技能局部索引 1。
- HandOver 右臂末段虽有闭合命令，但只有 1/5 示范提供可确认的 item0 同步运动响应，因此按计划不生成 LINK，没有将“夹爪闭合”替换为关系建立条件。

### 修复后真实 V4 只读验收

对全部 8 个 V4 任务、12 个机械臂模型重新构建阶段一任务模型，并在临时目录中执行 sidecar 保存/加载摘要一致性校验。临时文件随临时目录自动清理。

| 任务/机械臂 | 状态 | 边界 | 逐状态因子分量 | 边界场景条件 | 边界关系条件 | LINK | UNLINK | link_origin | 事务边界 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| stack_wine | 181 | 3 | 0 | 0 | 3 | 1 | 0 | 96 | 0 |
| place_cups | 187 | 4 | 0 | 0 | 4 | 1 | 1 | 109 | 0 |
| open_microwave | 168 | 2 | 0 | 0 | 1 | 1 | 0 | 53 | 0 |
| wipe_desk | 255 | 3 | 0 | 0 | 1 | 1 | 0 | 180 | 0 |
| store_bottle/left | 275 | 2 | 0 | 0 | 5 | 1 | 1 | 156 | 0 |
| store_bottle/right | 275 | 2 | 0 | 0 | 1 | 0 | 0 | 0 | 0 |
| handover_item/left | 262 | 6 | 0 | 0 | 4 | 1 | 1 | 134 | 0 |
| handover_item/right | 262 | 6 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| lift_tray/left | 116 | 2 | 0 | 0 | 2 | 2 | 0 | 60 | 0 |
| lift_tray/right | 116 | 2 | 0 | 0 | 0 | 2 | 0 | 58 | 0 |
| sweep_to_dustpan/left | 144 | 4 | 1 | 1 | 3 | 1 | 0 | 56 | 1 |
| sweep_to_dustpan/right | 144 | 4 | 1 | 0 | 1 | 0 | 0 | 0 | 1 |

“逐状态因子分量”统计实际保存的 `StateNode × FactorId × mode` 分布数，仅用于审计稀疏度，不是任务复杂度或模型质量指标。当前 RLBench 示范没有提供额外实体内部构型字段，普通刚体不会被强制生成节点因子；大多数局部边候选又没有在已有机器人轨迹和关系证据之外提供稳定非冗余增益，因此只有 SweepDust 两臂各保留 1 个逐状态因子分量，左臂边界保留 1 个场景条件。这是筛选结果，场景因子字段、候选构建、可观测率、稳定率、LODO 增益和边界阈值接口均保留完整。

LiftTray 没有生成事务组，因为当前两臂同步边界没有同时具备“共享 linked 实体或完全相同硬场景条件”；SweepDust 有一对边界同时满足稳定同步和共享硬依赖，因此两侧各标记 1 个同一事务组边界。

### 阶段一最终校验

- 阶段一专项测试：`13 passed`；
- 全仓回归：`443 passed, 4 skipped`，条件跳过为仓库既有测试设置；
- 全部 V4 任务的 sidecar 往返摘要一致；
- `compileall`、`git diff --check` 和新增模块 Black 检查通过。

阶段一修复后已达到开发计划的当前交付范围，未进入阶段二。本轮未启动训练、仿真评测或回放，未修改或删除已发布 V4 数据和产物。

## 2026-08-25：关系事件学习口径纠正

### 纠正原因

上一批实现把“逐示范动作响应证据”扩展成了 LINK/UNLINK 的离线硬判据，并进一步使用夹爪打开状态强制 external。该规则不是原开发计划规定的关系先验学习机制，也把“多条正常示范共同学习关系规律”错误拆成了彼此独立的逐示范事件检测，因此本次明确撤销。上一节基于该旧机制给出的 V4 LINK/UNLINK 数量和“阶段一完成”结论不再作为当前验收结论；阶段一恢复为逐项确认状态。

### 本次修改

- 每个 `StateNode` 的关系先验继续由同一 mode 的全部正常示范共同拟合。
- LINK/UNLINK 候选直接在全部正常示范形成的联合关系先验序列上检测；仍保留 `0.3/0.7` 迟滞阈值和最短稳定窗口。
- 按示范留一改为稳定性检查：每折用其余 `N-1` 条示范重新拟合联合关系先验，检查全部示范模型发现的同类事件能否在位置容差内重现。五条示范仍要求至少四折支持。
- 留一验证通过后，事件位置使用全部示范联合模型的检测位置；LINK 锚点和 UNLINK 脱离目标均重新使用该 mode 的全部正常示范拟合，不只使用通过验证的折。
- 删除夹爪状态对关系事件的硬门控。夹爪命令只作为 LINK 恢复轨迹的一部分保存。
- 逐示范动作响应仅保留为场景因子 LODO 中留出示范关系观测的代理，不再参与离线关系事件检测。

### 本次校验范围

- 新增定向回归：人为使一个留一折不能重现事件，确认事件以 `4/5` 支持通过，但最终锚点仍由全部五条示范拟合；同时确认把夹爪观测全部改为打开不会改变事件集合。
- 关系事件定向测试：`2 passed`。
- 阶段一专项回归：`14 passed`。
- 本次没有重新运行真实 V4 全任务统计、全仓回归、训练、仿真评测或回放，也不据此重新宣告阶段一收口。后续继续按用户确认顺序复核其他实现。

## 2026-08-25：撤出阶段一中的动作响应代理

用户复核确认，“逐示范动作响应”属于阶段二在线关系滤波的技术内容，不是阶段一离线任务模型的组成部分。上一节虽然已将它移出 LINK/UNLINK 判定，但仍把它保留为场景因子 LODO 的留出关系观测代理，依然跨越了阶段边界。本次修改取代该保留决定。

### 本次修改

- 完整删除 `_held_out_relation_observations()` 及其调用链。
- 删除阶段一配置中的 `relation_minimum_excitation` 和 `relation_response_tolerance_ratio`；这两个参数只服务于已撤销的动作响应代理。
- 场景因子 LODO 的关系兼容块改为严格的阶段一计算：每一折只用其余 `N-1` 条正常示范，分别拟合真实状态和各局部候选状态的示范关系先验，并以两个 `external/linked` 概率分布的重叠度评分。
- 该关系兼容评分不构造单条示范关系观测，不计算动作激励、相对运动残差或在线关系后验，也不读取留出示范的动作响应。
- 同步更新阶段一配置、开发计划相关计算口径和闭环模块 README。阶段二规定的在线动作条件化关系滤波仍留待阶段二实现，本次没有提前开发。

### 本次校验范围

- 新增定向回归：大幅修改留出示范的末端和参考系位姿后，`N-1` 联合关系先验兼容评分保持完全不变。
- 定向测试：`3 passed`。
- 阶段一专项回归：`15 passed`。
- 本次没有运行真实 V4 全任务统计、全仓回归、训练、仿真评测或回放，不据此宣告阶段一收口。

## 2026-08-25：移除场景因子筛选中的关系兼容机制

进一步对照开发计划第 2.4、2.5 节确认：第 2.4 节“与已有机器人轨迹或关系变量不完全重复”是候选因子的去重要求，不表示在场景因子留一识别评分中加入关系证据；第 2.5 节的边界关系条件与边界场景条件也是并列、独立构建的条件。上一节把 `N-1` 关系先验重叠度保留为场景因子评分块，仍然混淆了两类模型，本次明确撤销并取代该实现。

### 本次修改

- 删除 `_relation_candidate_scores()`，场景因子 LODO 不再计算任何关系兼容度。
- 删除 `progress_relation_weight` 配置及其校验和配置文件字段。
- 修正机器人轨迹基线：只对当前 mode、候选状态中由 DynaMAC 已激活的流计算留出示范轨迹似然并取平均，不再使用示范关系先验中的 `P(external)` 对流加权。
- 逐进度场景因子的增益比较统一为“机器人轨迹基线＋已选场景因子”与“机器人轨迹基线＋已选场景因子＋当前候选因子”的局部状态识别间隔差。
- 场景因子与机器人轨迹、关系变量不重复由候选定义和排除规则保证：不把末端—参考系技能轨迹或机械臂—参考系 linked/external 关系重复构造成场景因子。
- 边界关系条件仍独立使用示范关系先验构建；该修改不删除阶段一合法的 `StateNode` 关系先验、边界关系支持分布或 LINK/UNLINK 离线关系事件。

### 本次校验范围

- 定向回归确认机器人轨迹基线不随关系先验 Sigmoid 斜率变化，且阶段一跟踪配置与代码默认值一致。
- 定向测试：`3 passed`。
- 阶段一专项回归：`15 passed`。
- Black、JSON 解析和 `git diff --check` 通过；本次仍未进行真实 V4 全任务效果验收、训练、仿真评测或回放，不据此宣告阶段一收口。

## 2026-08-25：修复阶段一离线边界完成模型与关系守卫

### 最终确认口径

- 阶段一第 2.5 节只完成候选筛选、支持分布拟合和 `BoundaryModel` 保存，不读取在线 $\beta_t$、$q_t$，不执行技能切换。
- 本地完成模型保存整个终止窗口、最终状态 $T_k$ 的目标分布，以及当前技能由本臂直接 LINK/UNLINK 且完成时必须满足的关系集合与目标支持分布。
- 边界关系硬条件只来自下一技能所有 affected arms 的 selected frames，必须通过总体支持度、观测可用率和全部正常留一折一致性；五条示范要求 5/5。
- 已由本臂或同一事务其他机械臂 LocalDone 保证的关系不再重复保存为边界关系守卫。
- 阶段一不标定本地完成阈值和连续确认周期；后续阶段四只能使用正常示范回放或留出正常轨迹标定，不得使用故障测试集调参。

### 本次修改

- `LocalCompletionModel` 新增 `own_relation_conditions`，保存本臂必要关系集合及每个关系在成功终止窗口中的 `external/linked` 目标分布；sidecar 保存/加载同步支持该字段。
- 调整构建顺序为先生成 LINK/UNLINK 关系事件，再构建本地完成模型和边界条件，使本臂必要关系能够由当前技能直接建立或解除的事件确定。
- 边界关系候选从“当前技能与下一技能 selected frames 并集”修正为“下一技能 selected frames”；双臂事务配对后合并双方候选，形成所有 affected arms 的候选集合。
- 新增边界关系按示范留一一致性检查：总体支持达到阈值后，每一折均用其余 $N-1$ 条正常示范重新拟合终止窗口关系支持分布；目标关系或支持阈值任一折不一致即删除。
- 本臂必要关系先从本臂关系守卫中去重；事务配对后再用双方 `LocalCompletionModel` 的必要关系集合执行跨臂去重，并让事务两侧保存一致的剩余关系守卫。
- 事务依赖识别同时读取本地完成必要 linked 关系与未去重边界关系，避免去重后丢失建立事务组所需的共享物理依赖。
- `BoundaryModel` 增加必要一致性检查，禁止同一 arm-frame 同时出现在本地完成必要关系和边界关系守卫中。
- 开发计划补充阶段四本地完成度的最终公式、必要关系不可补偿最小值聚合、Unknown 处理和正常数据标定限制；本次没有实现这些在线计算。

### 本次校验范围

- 定向测试覆盖：本臂必要关系保存与去重、下一技能关系候选范围、5/5 留一一致性、sidecar 往返、双臂事务候选合并及跨臂 LocalDone 去重。
- 阶段一专项回归：`17 passed`。
- 本次未运行真实 V4 全任务效果验收、全仓回归、训练、仿真评测或回放，不据此宣告阶段一收口。

## 2026-08-25：按阶段一 2.1—2.7 逐项复核并清除阶段混淆

### 复核边界

本轮以《新方法代码开发计划》阶段一的开发内容、交付物和 13 条验收标准为唯一实现基准。阶段一允许构建并保存后续模块需要的离线统计、边界模型、关系事件锚点和重入元数据，也允许提供只读 `query_state()` 与流精度权重底层能力；不实现阶段二在线观测/信念更新、阶段三动态流角色、阶段四在线入口判断与事务提交、阶段五在线恢复控制。

### 已确认并修复的偏差

- 删除 `query_state()` 对三元 `(skill, local, mode)` 状态的兼容入口。进度状态严格保持 `StateId=(skill_index, local_index)`，mode 只能通过独立 `mode_index` 传入；原始 `act()` 同步改用二元状态加独立 mode，固定时钟路径不变。
- 撤回零散加入基础 `DynaMACObservation` 的场景实体和内部构型字段。阶段一只在离线 `DynaMACDemonstration` 中保留构建节点/边因子所需的数据接口；统一在线 `RuntimeObservation` 仍留待阶段二完整实现，避免提前形成半套在线观测。
- 场景因子候选中，若关节实体已经提供自身内部构型字段，则以节点因子作为规范表示，不再同时生成与其结构主体运动学等价的直接结构边；普通刚体仍可使用直接结构边。
- 删除开发计划未规定且与跨示范稳定性、留一识别增益重复收窄的固定 `scene_max_gmsd` 门槛。场景因子继续依靠可观测率、跨示范支持稳定性和局部识别增益筛选。
- 机器人轨迹留一基线明确只读取当前 mode 下 Eq.6 已选且当前状态 active 的 DynaMAC 流，不把未选流加入基线。
- 边界场景候选严格读取下一技能的 selected frames；即使某参考系在旧 DynaMAC 固定参与掩码中因 linked 而暂时 inactive，也不会因此从下一技能入口候选实体中消失。仅属于本地完成、且不属于下一技能或剩余边界关系条件的实体不再额外扩入边界场景候选。
- LINK/UNLINK 候选的前后稳定窗口改为读取完整任务关系先验序列，允许稳定窗口跨技能边界。此前仅读取到当前技能末端，会漏掉在技能最后一个状态发生、但在下一技能继续稳定的关系事件。
- `link_origin` 改为沿正常示范路径传播，而不是按相邻技能中可能改变含义的数值 mode 索引传播；最终 LINK 的 linked 状态、UNLINK 合法重入状态和局部脱离目标均可跨技能延续。
- `BoundaryModel.condition_reliability` 现在与实际保存的本臂必要关系、边界关系和边界场景条件严格一一对应；双臂事务合并、跨臂 LocalDone 去重后会同步移除失效的可靠性条目。

### 本次校验范围

- 阶段一专项回归：`19 passed`；新增覆盖二元 StateId、关节节点因子规范表示、跨技能关系事件稳定窗口、跨技能 LINK 来源和 UNLINK 合法重入。
- DynaMAC 原始 PoE、`act()`/reset/虚拟帧事务回滚及双臂原路径定向回归：`10 passed`。
- 本次未运行真实 V4 全任务统计、全仓回归、训练、RLBench 仿真评测或回放，也不据此宣告阶段一最终收口。

## 2026-08-25：阶段一正式验收

### 验收范围与数据身份

- 严格按《新方法代码开发计划》第 536—552 行的 13 条阶段一验收标准执行。
- `integrations/rlbench/data/training/manifest.json` 完整认证通过：9 个 cohort、45 条正常示范、125 个文件的 SHA256 均匹配。
- 对 8 个冻结 V4 策略任务、12 套机械臂模型，使用各自冻结 checkpoint 和对应的 5 条正常成功示范重新构建阶段一离线任务模型。
- StoreBottle 使用其冻结训练身份规定的专用语义 `TaskSpec`（`bottle/fridge`），没有退回旧通用 `bottle/fridge_root` 口径。
- 全部 sidecar 保存/加载均在临时目录完成，验收结束后自动清理；没有写入训练数据、模型或正式结果目录。

### 真实 V4 验收结果

下表中的“场景因子分布条目”是 `StateNode × FactorId × mode` 的实际保存条目数，只用于核验稀疏性；它不是任务复杂度指标。

| 任务/机械臂 | 状态 | 边界 | 场景因子分布条目 | LINK | UNLINK | link_origin | 实际只读查询 |
|---|---:|---:|---:|---:|---:|---:|---:|
| stack_wine | 181 | 3 | 0 | 1 | 0 | 112 | 181 |
| place_cups | 187 | 4 | 0 | 1 | 1 | 129 | 187 |
| open_microwave | 168 | 2 | 0 | 1 | 0 | 72 | 168 |
| wipe_desk | 255 | 3 | 0 | 1 | 0 | 193 | 255 |
| handover_item/left | 262 | 6 | 0 | 1 | 0 | 186 | 262 |
| handover_item/right | 262 | 6 | 0 | 3 | 2 | 57 | 262 |
| lift_tray/left | 116 | 2 | 0 | 2 | 0 | 114 | 116 |
| lift_tray/right | 116 | 2 | 0 | 2 | 0 | 108 | 116 |
| store_bottle/left | 275 | 2 | 0 | 1 | 1 | 170 | 275 |
| store_bottle/right | 275 | 2 | 0 | 1 | 1 | 4 | 275 |
| sweep_to_dustpan/left | 144 | 4 | 2 | 2 | 0 | 175 | 144 |
| sweep_to_dustpan/right | 144 | 4 | 2 | 1 | 0 | 80 | 144 |
| **合计** | **2385** | **40** | **4** | **17** | **5** | **1400** | **2385** |

SweepDust 左右臂实际保留的唯一因子标识均为 `edge:broom->dustpan`，每臂在两个状态/模态位置保存分布，因此表中各计为 2 个分布条目。其余真实 RLBench 低维示范没有额外实体内部构型字段，且候选边没有在机器人轨迹之外提供通过 LODO 的稳定非冗余识别增益，因此没有强行保留场景因子；节点因子、边因子、同 skill/same mode 的 `r=0,1,2` 最小稳定邻域以及 `r=2` 后删除的机制由专项合成测试覆盖。

### 13 条标准的执行结论

- 12 套模型的 `StateId` 均唯一且完整覆盖；状态数逐臂严格等于各技能 `duration` 总和。
- 每个状态的流均值和协方差均与冻结 DynaMAC/MiDiGaP 数组共享内存，没有重新拟合或复制底层轨迹分布。
- 所有逐状态、逐模态关系先验均归一化。
- 场景因子只来自节点内部字段或局部共同活动的任务实体边，没有枚举场景全对象两两组合。
- 40 个技能边界均有完整 `BoundaryModel`；终止窗口逐一等于当前技能末端 `min(h_b^-, duration)` 个状态，完成目标分布、关系/场景条件及可靠性字段相互一致。
- 所有通过联合关系先验检测和按示范留一支持检查的 external→linked、linked→external 事件均分别生成 LINK 锚点或 UNLINK 解除/合法重入元数据；`link_origin` 只引用已存在的 LINK 锚点。
- 使用显式全 1 精度权重和独立 `mode_index=0` 实际查询全部 2385 个 `StateId`，输出均有限，查询前后的 skill/time/mode cursor 和虚拟帧完全不变。
- 8 个真实任务均通过原始 `reset() -> act()` 固定时钟路径执行一次，并确认 cursor 正常推进。
- 12 套附加模型均完成临时 sidecar 往返，摘要一致，且归档中没有重复保存 stream 数组。
- closed-loop 目录没有重新定义 SE(3)、四元数、流形高斯或 PoE；相关计算继续调用现有 DynaMAC/MiDiGaP 数学实现。

### 验收过程中的异常与处理

- 首轮临时验收脚本对 StoreBottle 错用了通用 `TaskSpec`，触发参考系身份不匹配。核对冻结 `training.json`、checkpoint 和 V4 专用语义配置后，确认冻结模型使用 `bottle/fridge`；改用认证的专用 `TaskSpec` 后左右臂完整通过。该问题只存在于临时验收脚本，没有修改阶段一机制或模型代码。
- SweepDust 是本轮首个产生非零真实场景因子的任务，临时断言误用了不存在的 `FactorId.left/right` 字段。按数据类实际的 `source/target` 字段修正只读断言后重跑通过；没有修改业务实现。

### 自动化校验与阶段结论

- 阶段一专项测试：`19 passed in 51.68s`。
- 全仓回归：`449 passed, 4 skipped in 108.64s`；4 项为仓库条件性跳过。
- `git diff --check` 通过；阶段一目录的临时 `__pycache__` 已清理。
- 本轮没有启动训练、RLBench 仿真评测或回放，没有修改或删除冻结 V4 数据、模型和正式产物。

阶段一的 13 条验收标准全部通过，可以收口；按分阶段约定，等待用户确认后再进入阶段二。

## 2026-08-25：离线 LINK/UNLINK 物理语义修复

### 问题与处理

- 原事件检测把“跨示范相对位姿协方差小”直接视为 linked，HandOver 中高度可重复但未连接的接近轨迹因此产生多次假 LINK/UNLINK。
- 保留 DynaMAC Eq. (5) 的跨示范 GMSD 作为主先验；新增逐示范相邻状态共动检查，只在末端有足够运动时单向降低 `P(linked)`。末端运动不足时事件证据设为中性 `0.5`，不发起关系转移，也不把已有 linked 强制改成 external。
- 共动判据同时使用绝对残差和相对比例：末端加权平方运动量至少 `3e-7`，相对位姿残差不超过 `1e-5`，且不超过末端运动量的 `10%`。比例条件用于排除“缓慢但可重复的接近”。
- 夹爪命令不进入关系先验，不能单独创建 LINK/UNLINK；它只用于否决不完整的事件模板。LINK 锚点必须包含正常示范的夹爪闭合，UNLINK 必须位于最近打开命令之后。锚点窗口冻结为 16 个离散状态，覆盖 SweepDust 中“闭合后经过短技能才出现明显共动”的正常模板。
- 原有 `linked≥0.7` / `external≤0.3` 迟滞、前后至少 2 状态、5 条示范至少 4 折的 LODO 一致性和 ±2 状态位置容差均保留。

### 真实 V4 只读重建

| 任务/机械臂 | 状态 | 边界 | 场景因子 | LINK | UNLINK | link_origin |
|---|---:|---:|---:|---:|---:|---:|
| stack_wine | 181 | 3 | 0 | 1 | 0 | 96 |
| place_cups | 187 | 4 | 0 | 1 | 1 | 108 |
| open_microwave | 168 | 2 | 0 | 1 | 0 | 52 |
| wipe_desk | 255 | 3 | 0 | 1 | 0 | 180 |
| handover_item/left | 262 | 6 | 0 | 1 | 1 | 140 |
| handover_item/right | 262 | 6 | 0 | 0 | 0 | 0 |
| lift_tray/left | 116 | 2 | 0 | 2 | 0 | 59 |
| lift_tray/right | 116 | 2 | 0 | 2 | 0 | 58 |
| store_bottle/left | 275 | 2 | 0 | 1 | 1 | 156 |
| store_bottle/right | 275 | 2 | 0 | 0 | 0 | 0 |
| sweep_to_dustpan/left | 144 | 4 | 2 | 1 | 0 | 55 |
| sweep_to_dustpan/right | 144 | 4 | 2 | 0 | 0 | 0 |
| **合计** | **2385** | **40** | **4** | **11** | **3** | **904** |

HandOver 左臂事件收敛为 `LINK(item0) → UNLINK(item0)`；右臂闭合后的正常示范没有足够同步运动来通过同一物理判据，因此不仅凭夹爪闭合发布 LINK；这表示“离线证据不足”，不表示任务物理上右臂不会接物。LiftTray 仍保留 `item` 和 `tray` 两个任务数据候选，本轮未改其候选实体范围；每臂的 2 个 LINK 仍分别对应这两个参考系。

### 校验

- 阶段一专项测试扩展为 21 项，新增“可重复接近不得产生 LINK”、“无动作激励不转移”、“共动后 LINK、脱离后 UNLINK”和夹爪模板完整性覆盖；`21 passed`。
- 8 个冻结 V4 任务、12 套机械臂完整构建 `StateNode` / 场景因子 / `BoundaryModel` / 关系事件均成功，只读脚本未写入模型或正式结果。
- 全仓回归：`451 passed, 4 skipped in 118.63s`。
- Black、Python 编译检查和 `git diff --check` 通过；未启动训练、RLBench 仿真评测或回放。

## 2026-08-25：增加主动关系验证规划

### 机制决定

- 顶层执行模式扩展为 `TASK / VERIFY_LINK / RECOVERY`；该执行模式与 StateId 及 DynaMAC/MiDiGaP 轨迹 mode 分离。
- 当后续必要 linked 关系仅因动作激励不足而为 Unknown 时，阶段三正常执行或阶段四入口守卫生成 `RelationVerificationRequest`；不把不可观直接当作关系失配。
- `VERIFY_LINK` 冻结正常任务时钟和进度提交，保持夹爪命令和末端朝向，沿最近实际抓取接近轨迹的反方向低速移动；关系滤波继续更新。
- 反向探测在关系稳定判定、达到 $t_{\max}$ 或安全约束触发时停止；$t_{\max}$ 只限制外移阶段，随后仍在安全可行时沿原路径返回。不引入逐级扩大幅度机制。
- 返回完成后无论当前结果是 linked、external 还是 Unknown，均统一切回 `TASK`，由现有正常执行、入口守卫和恢复触发逻辑继续处理。
- 记录同一关系事件的验证尝试状态，防止 `TASK → VERIFY_LINK → TASK` 立即循环；关系状态、技能状态或新抓取事件变化后才允许重新触发。

### 阶段边界

- 《新方法代码开发计划》不新增 Unknown 原因输出字段；后续直接联合使用阶段二已有的 `RuntimeFeatures` 可见性、跟踪可靠性、动作激励和 Unknown 决策。阶段三、四补充验证请求，阶段五定义完整 `VERIFY_LINK` 执行，并同步阶段六集成、诊断与验收条款。
- 阶段一代码不修改。验证方向依赖运行时最近的实际接近轨迹，不应依赖可能因离线证据不足而不存在的 LINK 锚点。本轮未跨阶段实现在线观测、关系滤波、执行模式或控制动作。

## 2026-08-25：阶段一增加 LINK_PENDING 离线事件候选

### 口径与实现

- 上一节“阶段一代码不修改”是当时只规划在线验证动作的阶段边界；后续确认离线模型还必须保留未决 LINK 候选，因此本轮对阶段一作对应补充。
- 关系概率空间仍严格只有 external/linked；在线 `Unknown` 仍是单个控制周期的决策状态。新增 `LinkPendingCandidate` 是按 arm—frame—StateId—mode—occurrence 持久化的离线事件候选，不是第三种关系状态。
- 正式 LINK 仍必须通过 GMSD、有效运动下的共动检查、迟滞/稳定窗口、按示范留一一致性和夹爪模板完整性。Pending 只在 GMSD LINK 假设和全部示范闭合事件对齐、LODO 候选位置稳定，但闭合后最短稳定窗口完全没有可观测运动时生成。闭合当帧的接近运动不作为“闭合后”证据。
- 若同一闭合事件在 `link_anchor_horizon` 内稍后已经获得充分共动证据并生成正式 LINK，则删除中间 Pending，避免对已确认关系重复发起在线验证。
- Pending 不生成正式 `LinkRecoveryAnchor`、不传播 `link_origin`、不作为 `LocalCompletionModel` 中已满足的必要关系，也不在离线阶段执行 `VERIFY_LINK`。为避免本次事件在线确认后缺少恢复依据，Pending 使用与正式 LINK 相同的拟合口径保存未激活的事件前短局部轨迹、协方差和夹爪命令；只有在线稳定验证为 linked 后，后续阶段才可在当前 episode 激活该模板并建立运行时来源。
- `ClosedLoopTaskModel` 增加 `link_pending_events`、摘要计数及 sidecar schema v3 往返序列化；没有增加配置参数，继续复用现有事件支持率、对齐容差、最短稳定窗口和 LINK 锚点窗口。

### 异常与修正

- 首轮真实 HandOver 重建时，右臂仍无 Pending，而左臂多出一个 Pending。检查全示范和五个留一折后发现：右臂的 GMSD 候选起点会随留一示范从最后技能局部索引 1 前移到上一技能，但五条示范的真实闭合事件都对齐在最后技能局部索引 0。因此 Pending 改为由“GMSD LINK 假设窗口附近的跨示范闭合事件”定位，夹爪仍只能定位已有运动学假设，不能单独创建 Pending。
- 左臂多出的 Pending 是真实 LINK 获得共动证据前的闭合时刻。加入“同一锚点窗口内已有 LODO 正式 LINK 则不保留 Pending”后消除，没有降低正式 LINK 判据。

### 定向真实数据结果

| 任务/机械臂 | LINK | LINK_PENDING | UNLINK | link_origin |
|---|---:|---:|---:|---:|
| handover_item/left | 1 | 0 | 1 | 140 |
| handover_item/right | 0 | 1 | 0 | 0 |

右臂 Pending 为 `right:item0:k6:m0:link_pending:0`，候选状态为 `StateId=(6,0)`，五条示范的对齐局部索引均为 0，支持率为 1.0。它没有产生正式 LINK 锚点或来源映射。

### Pending 恢复模板补充

- `LinkPendingCandidate` 增加 `context_state`、`local_means`、`local_covariances` 和 `gripper_commands`。正式 LINK 与 Pending 复用同一个事件前轨迹拟合函数，避免恢复轨迹口径分叉。
- 这些数组只是未激活模板，不改变 Pending 的离线语义：不建立正式 `LinkRecoveryAnchor`，不传播 `link_origin`，不进入边界完成条件。
- 计划中明确：若后续在线主动验证稳定得到 linked，只在当前 episode 激活对应 occurrence 的模板并建立运行时 `link_origin`；external 或 Unknown 均不激活，也不回写离线模型。
- 真实 V4 HandOver 定向只读重建中，右臂仍为 LINK=0、LINK_PENDING=1、UNLINK=0、离线 `link_origin`=0；该 Pending 保存 16 步事件前模板，形状分别为均值 `[16,7]`、协方差 `[16,6,6]`、夹爪命令 `[16,1]`。
- 更新后的阶段一专项测试为 `22 passed in 67.34s`，全仓回归为 `452 passed, 4 skipped in 121.11s`；sidecar 往返测试逐数组核验 Pending 模板。

### 校验

- 阶段一专项测试：`22 passed in 60.28s`；新增无激励 Pending、不生成正式事件/来源、稍后正式 LINK 抑制重复 Pending 和 sidecar v3 往返覆盖。
- 全仓回归：`452 passed, 4 skipped in 120.21s`。
- 本轮只对冻结 V4 `bimanual_handover_item` 左右臂做了定向只读重建，没有重跑全部 8 个 V4 任务统计，未启动训练、仿真评测或回放，也未写入正式模型或结果产物。

## 2026-08-25：LINK 与 Pending 模板增加最近 UNLINK 屏障

### 最终实现

- `link_anchor_horizon=16` 继续作为 LINK 与 Pending 局部轨迹的固定最大长度，单位为对齐后的离散进度状态。
- 对当前机械臂的每个 LINK 或 Pending 事件，在所有可恢复参考系的稳定关系序列中查找事件前最近一次 linked→external。模板起点取固定窗口起点和该 UNLINK 下一状态中的较晚者；不存在先前 UNLINK 时保持原窗口。
- 截断后不向更早状态回填，轨迹仍允许跨技能边界，但不能跨过同一机械臂上一段已经结束的连接。正式 LINK 与 Pending 复用同一起点计算和局部轨迹拟合函数。
- 该规则只处理已经结束的旧连接；事件发生时仍持续的其他 linked 关系不会被误作屏障。

### 校验结果

- 新增正式 LINK 与 Pending 的跨参考系屏障测试：先对 `target` 完成 LINK→UNLINK，再对 `object` 建立 LINK 或 Pending，后一个模板均从 UNLINK 的下一状态开始，由原本最多16步截为4步且不回填。
- 阶段一专项测试：`24 passed in 66.27s`。
- 真实 V4 HandOver 定向只读重建保持左臂 LINK=1、Pending=0、UNLINK=1、`link_origin`=140，右臂 LINK=0、Pending=1、UNLINK=0、`link_origin`=0。当前左右模板窗口内均无更早 UNLINK，因此仍为16步。
- 全仓回归：`454 passed, 4 skipped in 127.64s`。
- 未启动训练、仿真评测或回放，未写入冻结数据、模型或正式结果。

## 2026-08-25：阶段二在线进度评分消除高斯峰值偏置

### 发现与确认

- 初版候选状态评分把完整高斯对数密度直接写入进度后验：$-\frac12[d_M^2+\log|\Sigma|+D\log(2\pi)]$。在少样本静止状态的协方差接近正则化下限时，$\log|\Sigma|$ 会让极窄分布仅凭峰值高度压过名义进度先验。
- HandOver 右臂正常示范第0条的静止协作段暴露了该问题：修复前平均绝对状态偏差为 47.628，最大落后 122 个状态，出现 40 个 `NO_PLAUSIBLE_STATE`。分项检查显示旧边界与后续静止状态的马氏兼容度均约为 0.996～0.999，但完整密度仍持续偏向旧边界，因此不是关系阈值或静止时 `Unknown` 语义造成。

### 修复口径

- 在线进度校正的机器人局部轨迹和连续高斯场景因子统一改用峰值归一化支持度：$\log\widetilde p(x\mid s)=-\frac12d_M^2(x,s)$。协方差仍通过精度矩阵决定偏离惩罚，但不同状态不再因协方差行列式较小而自动获得更高峰值。
- 多模态未显式选定时，分别计算各模态的峰值归一化支持，再按该状态可用模态先验作 log-sum-exp 混合；不把不同模态的完整密度峰值重新引入在线得分。
- `CandidateScore.explanation_log_score` 只组合机器人归一化支持、场景归一化支持和原有离散关系兼容度。关系滤波观测似然、external/linked 兼容度以及后续动作 PoE 均未修改。
- 完整原始 `log p` 仅作为审计信息保留。每个机器人帧和场景因子的每个有效模态均保存原始对数密度、峰值归一化对数支持、马氏距离平方、协方差 `logdet`、维数和模态权重，可直接复原完整高斯评分。

### 定向验证

- 新增单元测试确认：旧状态即使具有更高的原始密度峰值，只要峰值归一化支持相同，进度后验仍按动作后名义先验选择正常后继；同时逐项校验审计分解能够还原完整 `log p`。
- StackWine 五条正常 V4 示范只读 sidecar 回放继续通过。
- HandOver 右臂第0条正常 V4 示范修复后平均绝对状态偏差为 0.295，最大偏差为 2，精确对齐率为 73.18%，`NO_PLAUSIBLE_STATE=0`；已将该静止段问题加入持久回归测试。
- 阶段一专项 24 项与阶段二单元 12 项均通过；加入 HandOver 持久回归后，真实数据回放测试为 `2 passed in 60.86s`，当前相关测试合计 38 项。未启动训练或 RLBench 仿真，未写入冻结模型及正式结果。

### 阶段二整体回归收口

- 逐句对照阶段二 2.1～2.8 和12项验收标准，确认在线关系滤波、候选评分、进度后验、候选扩展和单周期更新均停留在旁路估计范围，没有实现阶段三动态角色/任务时钟、阶段四守卫计算或阶段五恢复控制。
- 静态类型检查发现 `runtime_observation.py` 的循环变量复用和 `runtime_features.py` 的可空对象收窄不明确；只修正变量命名和显式断言，不改变运行行为。仓库已有其他模块的 mypy 历史错误未纳入本阶段扩改。
- 直接执行 `pytest -q` 时，启动脚本未把仓库根目录加入模块搜索路径，导致 `integrations` 在收集阶段不可导入；使用项目一直可用的 `python -m pytest -q` 后全仓结果为 `468 passed, 4 skipped in 184.57s`。没有为测试启动器问题修改包结构或业务代码。
- 阶段二 README、公共导出、配置默认值和最终阶段记录已同步；Black、Python 编译及 `git diff --check` 通过。

## 2026-08-25：阶段三动态流角色与闭环正常执行控制

### 开发实现

- 新增动态流角色、运行期精度加权 PoE、三变量闭环游标、统一执行控制器和持续失配累计模块。
- 按确认口径，正常推进在旧引用 $s$、`nominal=estimated=succ(s)` 时直接提交到 `estimated_state`，不再额外取一次后继；跨技能候选始终留给阶段四守卫。
- `DEFER` 对当前 mode 已选关键物理关系执行 HOLD，并保留上一可信执行权重；没有可信历史时权重为0。未选参考系 Unknown 不阻断当前任务。
- 阶段三只生成 `RelationRecoveryIntent` 和 `RelationVerificationRequest`，没有实现阶段五的恢复或探测移动。

### 发现与修正

- StackWine 的 `success_sensor` 在进入新技能后前两周期因运动激励不足为 Unknown；如果只在流被选中时记录历史，它首次成为唯一执行流时会没有可信权重，导致动作不可用。关系滤波本来持续读取全部物理参考系，因此角色路由改为缓存所有已建模物理关系最近一次可靠 external/linked 权重，但仍只给当前 mode 已选流分配角色和参与 PoE。真实回放确认新流进入 DEFER 时可以沿用进入技能前的可靠 external 正权重。
- 首版真实回放测试曾要求 StackWine 必须出现 `MONITOR`。检查任务选流后确认正常轨迹中没有“期望 linked 且仍被当前 mode 选中”的流，这是任务模型的合法结果；删除该任务特定断言，四种角色语义继续由独立单元场景覆盖。
- 默认角色提交路径曾在同一周期重复写入相同可信权重。结果无数值差异，但为保持历史状态只有一个提交入口，删除前一次冗余写入，统一通过 `FrameRoleRouter.commit()` 保存。

### 验收

- 阶段三单元与真实数据测试：`13 passed`。
- 阶段二与阶段三联合定向回归：`27 passed in 73.12s`。
- 全仓回归：`481 passed, 4 skipped in 201.86s`。
- Black、Python 编译、阶段三定向 mypy 和 `git diff --check` 通过；环境未安装 Ruff，因此没有把 Ruff 作为本轮验收项。
- 未启动训练或 RLBench 仿真，真实 V4 验收只读加载 StackWine 正常示范和冻结模型，没有写入数据、模型或正式结果。

## 2026-08-25：阶段二—三组件级 A/B 补充评测

### 评测建设

- 新增 `evaluations/phase23_component_ab/`，使用冻结 V4 的 8 个任务、12 套机械臂模型和每任务 5 条正常示范，构造确定性时间扰动和关系反事实扰动。
- 时间 A/B 对比 DynaMAC 一观测一状态的固定时钟与阶段二进度后验；关系 A/B 对比固定流掩码与阶段三动态角色、权重归零和推进阻断。
- 输出保存实际配置副本、运行环境、源码/模型/示范哈希、逐试验 CSV、逐任务聚合、压缩逐周期轨迹、JSON/CSV 摘要、SVG 图表及 `SHA256SUMS`。输出采用不覆盖和暂存目录原子改名。

### 协议调整记录

- v1 预先使用 8 状态关系扰动窗口；正式运行后审计发现冻结模型的已选 linked 片段最长只有 4 个状态，因而无法构造断连条件。v1 结果完整保留，没有覆盖或删除。
- v2 只根据上述离线支持范围将扰动窗缩短为 2 状态，没有根据检测结果调阈值。冒烟时还发现候选器多要求了一个“扰动后未来帧”，使技能末端合法窗口被误排除；删除该非必要条件后，窗口可以恰好结束在技能最后状态。

### 正式 v2 结果

- 覆盖 8 个任务、12 套机械臂模型和 5 条示范索引，生成 300 组进度试验、106 组关系试验和 9234 条逐周期审计记录。
- 卡顿扰动下 StateId MAE 从 3.0955 降至 1.5215，降低 50.85%，新方法胜率 80.00%；跳帧下从 1.9843 降至 1.4663，降低 26.11%，胜率 60.83%。
- 正常同步回放中，完美固定时钟 MAE 为 0，在线估计 MAE 为 0.8210；StoreBottle 左臂 98% 以上周期为 `NO_PLAUSIBLE_STATE`，StoreBottle 右臂、WipeDesk 和 PlaceCups 在跳帧场景未得到稳定收益。
- 50 组伪连接扰动全部转为 `RECOVER`、将原固定掩码活跃专家权重置零并阻断推进；50 组对照恢复误报为 0。
- 只有 3 组断连试验通过 linked 支持范围和运动量条件，均未检测。逐周期轨迹显示进度后验尚未进入技能末端 linked 期望段，关系决策为 external 或 Unknown，因而没有产生可靠关系失配意图。
- `results/v1` 与 `results/v2` 的 `SHA256SUMS` 均逐项验证通过。本轮未启动训练或 RLBench/CoppeliaSim，未修改冻结示范、模型或 V4 正式结果。

### 阶段结论

该评测可以作为论文组件实验草稿，但必须保留结论边界：它为卡顿/跳帧下的聚合收益和伪连接下的动态流角色增益提供了可复现证据，但也证明当前进度估计仍有任务间异质性，而断连检测尚未获得正向验证。不应把本结果表述为独立测试集泛化、恢复成功率或完整任务成功率。

### 最终校验

- 阶段二、三联合定向回归：`27 passed in 73.24s`。
- 全仓回归：`481 passed, 4 skipped in 195.60s`。
- 评测器及阶段三代码通过 Black、Python 编译、定向 mypy 和 `git diff --check`。
- v1 的 11 个结果文件和 v2 的 12 个结果文件均逐项通过 `SHA256SUMS`；评测器临时 `__pycache__` 已清理。

## 2026-08-25：修复离线关系期望连续性并完成正式断链 A/B

### 问题确认

- 复查 v2 断链轨迹后确认，“进度后验尚未进入 linked 期望段”并不等于任务物理上没有连接。原实现把每个状态的跨示范 GMSD 结果直接当作独立部署期望，正式 LINK 后仍可能因局部协方差抖动退回 external，并过早清除 `link_origin`。
- v2 候选器又额外要求 linked 参考系仍是当前 mode 的已选动作流，导致只命中少量技能末端逐状态 linked 脉冲，而没有严格验证它是否来自正式 LINK 事件。该负结果的样本语义不够可靠。
- 首轮事件连续修复曾把正式 linked/external 写成 one-hot，使离线先验过强，在线断链观测无法推翻；同时边界守卫把软关系概率与“全部示范一致性”混为同一个0.8门槛，软化后错误删除合法边界关系条件。

### 最终修复

- 原始 `demo_relation_scores` 和逐状态 GMSD 保留审计；正式 LINK 到对应正式 UNLINK 之间使用事件状态机保持部署关系期望和 `link_origin` 连续。Pending 与孤立低协方差脉冲不传播。
- 事件一致状态写回为 `[P(external),P(linked)] = [0.3,0.7]` 或 `[0.7,0.3]`，不使用 one-hot。这样先验表达正常示范期望，但阶段二动作条件化观测仍可在本次执行断链时推翻它。
- 边界关系总体支持阈值与稳定关系阈值统一为0.7；全部正常留一折一致性仍独立要求5/5，不用提高软概率替代跨示范一致性。
- 阶段三继续监控当前进度具有正式 `link_origin` 的关系，即使该参考系当前已不再是动作专家。未选正式关系只能产生零权重 `MONITOR/RECOVER/DEFER`，标记 `selected_offline=False`，不进入 `execution_weights`，因此不会重新启用 Eq.6 已关闭的 PoE 流。
- v3 断链候选必须让整个扰动窗口落在同一个正式 `link_origin` 事件内；伪连接候选仍限定为当前已选 external 动作流。

### 验证结果

- 合成回归确认正式事件能覆盖逐状态抖动、使用软先验，并允许在线断链证据产生稳定 `linked→external`；确认未选正式关系能触发监控/恢复但不产生执行权重。
- 阶段一、二、三相关专项：`51 passed`；全仓：`483 passed, 4 skipped in 199.71s`；Black、Python 编译和 `git diff --check` 通过。
- 正式 v3 覆盖8个任务、12套机械臂、每任务5条正常示范，共300组进度试验、190组关系试验和11890条逐周期记录。
- 45组正式断链扰动检测率100%、平均阻断率98.70%，45组同位置正常对照恢复误报为0；50组伪连接扰动检测率和活跃专家抑制率均为100%，50组对照恢复误报为0。
- 时间扰动结果保持：跳帧 MAE 降低26.16%，卡顿 MAE 降低50.39%；正常同步仍有0.8198状态的平均估计偏差，StoreBottle/WipeDesk 等任务异质性没有被本轮关系修复掩盖。
- 最终结果保存在 `evaluations/phase23_component_ab/results/v3`，目录内 `SHA256SUMS` 全部通过，当时的环境文件和源码哈希与留档提交 `077985c2e` 一致。后续阶段二实现若有修复，应另建结果版本复跑，不覆盖 v3。

## 2026-08-26：阶段一至三留档并启动阶段四正常边界就绪性检查

### 整理与远程留档

- 清理仓库内可再生的 `__pycache__`、`.pytest_cache` 和 `.mypy_cache`；保留阶段二—三组件评测 v1/v2/v3，因为它们分别记录协议问题定位和正式 v3 结果，不属于冗余临时文件。
- 留档前全仓回归为 `483 passed, 4 skipped in 221.59s`，Python 编译通过，v1/v2/v3 的 `SHA256SUMS` 全部通过。
- 提交 `077985c2e feat: complete closed-loop phases 2-3 and component eval` 已推送到远程 `dev/yushuang`，本地与远程分支指向同一提交。

### 正常边界检查器

- 新增 `evaluations/phase4_boundary_readiness/`，只读取冻结模型和五条正常示范，保存逐边界、逐条件、逐状态压缩轨迹、环境/输入哈希、中文报告和校验和。
- 每个新技能假设上一边界已经正常提交，将进度初始化到技能首状态，同时继续携带关系后验；该口径避免某个早期边界的滞后把所有后续边界误报为失败。
- 检查不实现入口守卫，不选择 `theta_local`、关系阈值或 `H`，不注入故障，也不把 `NO_PLAUSIBLE_STATE` 时由名义先验保留的 `P_end` 当成有效完成证据。

### 四元数反号缺陷及修复

- 首次优先回放中，StoreBottle 左臂终止窗口出现80%～100%的 `NO_PLAUSIBLE_STATE`，WipeDesk 也出现正常末端假阴性。分项评分显示 bottle/fridge/sponge 的旋转支持约为 `-4.93e6`，对应物理等价四元数 `q/-q` 被当成接近半周的切空间残差。
- 新增 `pose_log_nearest()`：只在闭环在线观测匹配和相邻周期残差中，将观测四元数对齐到模型均值或上一姿态的同一半球，再调用原 TAPAS `pose_log_world()`。没有修改原函数、冻结 DynaMAC 拟合或 baseline 执行路径。
- `StateEvaluator` 的机器人轨迹/场景高斯、`FactorDistribution` 的边界目标/场景支持以及 `RuntimeFeatureBuilder` 的实际运动和关系残差统一使用该符号不变口径。
- 新增测试确认 q/-q 不改变运行运动残差、机器人状态支持、场景/边界分布兼容度或完整对数似然。DynaMAC、阶段一、阶段二和真实回放相关定向回归共 `111 passed in 171.08s`。

### 修复后的边界结果与未决项

- StoreBottle 左右臂全部4个边界和 WipeDesk 全部3个边界的最终进度均为5/5有效，最小 `P_end=1.0`，最终目标条件全部可观测并落在正常支持域。
- 全任务40个边界中，9个具备当前严格的 `theta_local/H/guard` 检查输入，25个具备 `theta_local` 输入但一次通过式示范没有至少两个连续最终就绪样本，3个仍有末端进度问题，2个有本臂关系输入问题，1个只有边界关系输入问题。
- 仍有三类需要确认后再改动的机制：HandOver 左臂与 LiftTray 右臂的长技能正常进度累计滞后；SweepDust 右臂一个正常末端解释分数略低于统一阈值；多个边界的关系后验已经约0.98～0.999支持目标关系，但最终静止周期因没有新运动激励而重新输出 Unknown。
- 已验证单纯扩大候选半径无效；把提前质量扩散到更远状态会增加正常误差；增强正常后继先验只能改善平均误差，不能使 HandOver 5/5进入末端。因此未自行调参或跨机制加入“目标分布驱动进度跳转”。
- 当前正式检查结果保存在 `evaluations/phase4_boundary_readiness/results/v1`，目录校验和全部通过。入口守卫、多臂事务、`theta_local` 和 `H` 尚未实现或标定，等待对上述机制口径确认。

### 已确认关系判定的静止期连续性

- 用户确认：已经由有效运动证据确认的关系，在参考系继续可见、跟踪可靠且二元后验仍以足够置信度支持相同状态时，不应只因当前周期静止而退回 Unknown；不可见或不可靠仍必须 Unknown。
- `RelationFilter` 接收上一稳定决策。当前周期激励不足时，仅允许满足上述条件的同状态决策保持，并继续标记 `informative=False`；初始静止和未确认 Pending 没有稳定历史，仍为 Unknown。
- 定向测试确认 linked 静止保持、无历史静止 Unknown、不可见强制 Unknown，阶段二与阶段三定向回归共27项通过。
- 新建 `results/v2` 重放全部正常边界，没有覆盖 v1。此前2个本臂关系输入阻塞和1个守卫关系输入阻塞全部消失。
- 关系保持也使此前被末端 Unknown 跳过的物理流与关系兼容项继续参与进度评分，进而暴露3个额外正常末端问题；当前共6个 `blocked_progress_endpoint`，包括 StoreBottle 左臂 `1→2`。这说明 v1 的部分进度通过结果偏乐观，不能通过重新忽略已确认关系来掩盖。

## 2026-08-26：阶段二—三跨阶段闭环复核与阶段四就绪性收口

### 架构复核和修复

- 通读《技术路线报告》和《新方法代码开发计划》后，确认阶段二与阶段三是同一在线闭环的估计与执行两部分，不能按彼此隔离的机制处理。上一周期真正用于查询动作的 `reference_state` 现在只锚定动作后正常/提前进度质量，未完成质量仍保留上一周期 `beta`；离线固定示范回放使用真正产生当前观测的上一示范 StateId/动作，不把未作用于环境的控制查询当成因果输入。
- 四元数 `q/-q` 的同一物理姿态在闭环匹配和运动残差中统一到最近半球，消除 StoreBottle/WipeDesk 的伪旋转大残差；原 DynaMAC 拟合、PoE 和 baseline 执行路径不变。
- 已由有效运动确认的关系只能在可见、跟踪可靠且后验持续支持同一方向时跨静止周期保持；不可见、不可靠或后验冲突会清除稳定判定，避免无新运动证据时“复活”旧关系。
- 离线关系部署期望改为严格依据正式 LINK/UNLINK 事件和 Pending 来源协调；无正式事件、无 Pending 来源的孤立低协方差脉冲只留在 `demo_relation_scores` 审计，不再进入在线关系兼容度。SweepDust 右臂末端 `left_ee` 的该类脉冲因此从伪 linked 期望恢复为软 external 期望 `[0.7,0.3]`。

### 评测与阈值结论

- 阶段二—三组件 A/B 按最新机制重跑为 `results/v6`：跳帧 MAE 降低90.91%，卡顿 MAE 降低94.86%；45组正式断连和50组伪连接扰动检测率均100%，正常对照检测误报均0。
- 阶段四就绪性正式 `results/v6` 保持生产最低解释度 `0.001`。StoreBottle 左右臂全4个边界和 WipeDesk 全3个边界的五条示范最终输入全部有效；40个边界中只剩 SweepDust 右臂 `1→2` 的一条正常示范因 `0.0009270 < 0.001` 为 `NO_PLAUSIBLE_STATE`。
- 正常数据 `0.0009` 灵敏度对照能放行该终点，但在 StoreBottle 左臂新增了一个正常中间状态误差；且该正常示范已参与拟合，不是独立标定集。因此不将 `0.0009` 或更早的 `0.0005` 写入正式配置，阈值后续只能用未参与拟合的正常轨迹或真正按示范留一重建模型标定。
- `H` 尚未标定：一次通过式对齐示范不能表示真实控制频率下末端后验的连续保持时间。

### 最终校验

- 全仓回归：`491 passed, 4 skipped in 207.42s`。
- 所有变更 Python 文件通过 Black 检查和 `compileall`，`git diff --check` 通过。
- `evaluations/phase23_component_ab/results/v6`、`evaluations/phase4_boundary_readiness/results/v6` 和 `results/sensitivity_9e-4` 均保存实际配置、输入/源码哈希和 `SHA256SUMS`。
- 项目整理后只保留上述最新正式结果和有效灵敏度对照；过期的组件评测 `v1–v5`、边界检查 `v1–v4`、`sensitivity_5e-4`、旧评测配置和可再生缓存已移入系统回收站，旧的已跟踪结果仍可从 Git 历史恢复。
- 阶段四入口守卫、多臂事务、跨界提交、`theta_local` 与 `H` 仍未实现或最终标定。

## 2026-08-26：统一绝对解释度的关系支持量纲

### 问题定位

- SweepDust 右臂 `1→2` 的第2条正常示范最终 StateId、`P_end=1` 和目标支持均正确。连续几何支持为 `0.00132545`，但与 `[0.7,0.3]` 软关系先验的原始内积最大只能接近 `0.7`，因此联合绝对解释度被缩放为 `0.00092704`。
- 这不是进度对齐错误，而是将“软离散先验的最大可达内积”与“连续高斯相对自身峰值的支持度”相乘时量纲不统一。

### 最终修复

- 原始关系兼容度 `q·pi` 仍原样参与进度后验，不改变状态排序、关系滤波、动态角色或 PoE。
- 只在 `NO_PLAUSIBLE_STATE` 使用的单一绝对解释度中，将关系支持改为 `(q·pi)/max(pi)`，使每个软先验的最佳匹配上限统一为1。原始内积、峰值归一支持和逐关系分项同时保留用于审计。
- 正式最低解释度保持 `0.001`，没有增加额外门控，也没有用故障数据调参。

### 重放结果

- SweepDust 该正常终点的峰值归一关系支持为 `0.99916`，联合绝对解释度为 `0.00132434`，在门限不变时恢复为有效状态。
- 阶段四就绪性 `results/v7` 中40个边界、共200个正常最终端点全部有效，关系与场景输入阻塞为0。
- 阶段二—三 A/B `results/v7` 保持原有结论：跳帧 MAE 降低 `90.91%`，卡顿 MAE 降低 `94.93%`；正式断连和伪连接扰动检测率均为100%，正常对照恢复误报为0。
- 全仓回归为 `492 passed, 4 skipped in 211.84s`；Black、Python 编译和 `git diff --check` 通过，v7 两组正式结果的 `SHA256SUMS` 验证通过。

## 2026-08-26：阶段四入口守卫、事务提交与正常参数标定

### 开发实现

- 新增 `boundary_runtime.py`、`entry_guard.py`、`transition_transaction.py` 和 `bimanual_controller.py`，实现本地完成度、边界关系/场景逻辑与、连续确认、转换请求、独立异步提交和事务组原子提交。
- 本地完成严格采用 `P_end × L_goal × C_own-rel`：进度使用整个终止窗口，目标使用当前 mode 的最终状态分布，本臂多项必要关系以最小兼容度不可补偿聚合。Unknown 或不可靠关系只使本周期未完成，不直接上报恢复异常。
- 所有机械臂必须基于同一个 pre-action tick 完成守卫评估；转换候选批次先全部校验，再修改任一执行游标。等待事务时保持现有引用状态，阶段三动作查询仍正常运行。
- 必要 linked 关系命中 Pending 且仅因运动激励不足而 Unknown 时，阶段四继续阻断边界并生成 `RelationVerificationRequest`，没有跨阶段实现探测动作。

### 开发中发现并修正

- 首次原始控制周期试跑时，本地目标匹配为0。检查发现 RLBench 原始 pickle 不保存 DynaMAC 运行期派生的 `virtual_skill_*` 固定帧；对齐数据则包含这些帧。标定回放按 DynaMAC 语义从每个技能首个末端位姿重建并累计携带虚拟帧后，最终目标支持恢复正常。该修正只补齐运行观测，没有让虚拟帧参与关系滤波、LINK/UNLINK 或场景因子。
- 末端窗口概率浮点求和偶尔出现 `1+epsilon`，触发运行记录的 `[0,1]` 不变量。对概率、关系内积和最终乘积只做数值范围裁剪，没有改变公式或阈值。
- 首次标定试运行只验证了参数表。为避免“表格正确但真实计数实现有偏差”，正式运行器增加第二遍完整回放，把最终配置重新送入实际 `EntryGuard`，直接检查严格不等式和连续周期状态。
- 首次结果目录 `results/v1` 在正式复核完善后移入系统回收站；最终只保留 `results/v2`，旧目录如需可从回收站恢复。

### 正常标定与正式复核

- 新增 `evaluations/phase4_boundary_calibration/`，使用8个任务、12套机械臂模型和每任务5条正常示范的原始控制周期序列；控制周期依据 RLBench `_DT=0.05s`，每个边界附加2秒正常末端保持回放。
- `theta_local` 只使用正常末端支持下界和正常边界前支持上界；无法用单阈值完全分离时，不叠加新门控，而由既有 `H` 消除正常边界前短脉冲。
- 40/40 个边界完成标定；28个天然可分，12个存在至多单周期提前脉冲。39个边界得到 `H=2`，WipeDesk `1→2` 因最长3周期脉冲得到 `H=4`。
- 最终配置真实复核覆盖200个边界×正常示范：终止窗口前提前放行0次，末端保持后最终放行200/200，最晚在第4个控制周期形成许可。
- 正式运行配置保存于 `configs/closed_loop_boundary/`，详细参数、压缩控制周期轨迹、逐试验放行结果、环境信息和校验和保存于 `evaluations/phase4_boundary_calibration/results/v2`。
- 额外重建四个双臂任务检查事务元数据，当前冻结模型没有任何 `transaction_group`。因此真实正常回放没有联合提交样本；联合原子提交和独立异步推进分别由合成双臂单元场景验证，后续不能把这部分测试误写为真实任务的联合事务实验结果。

### 阶段验收

- 阶段四专项测试9项通过；全仓使用 `python -m pytest -q` 回归为 `501 passed, 4 skipped in 227.77s`。
- 直接调用 Anaconda `pytest` 入口曾因仓库根目录未进入模块搜索路径产生25项 `integrations` 收集错误；改用项目一致的 `python -m pytest` 后全部正常。这是命令入口差异，不是实现异常。
- 阶段四代码与评测器通过 Black、Python编译和 `git diff --check`；正式 `results/v2/SHA256SUMS` 逐项通过，运行配置与结果目录副本完全一致。

## 2026-08-26：联合边界识别覆盖修复

### 问题与机制修正

- 复核发现旧事务标记只能识别“双方共同维持已经写入边界模型的 linked 关系”或同一硬场景条件，不能识别 LiftTray 这种物理连接在边界附近建立、但因需要后续共同运动而在目标技能中才得到正式 LINK 确认的协作边界。
- 事务依赖签名新增跨边界正式 LINK 证据：LINK 局部轨迹上下文必须从源技能开始，首次确认的连续 linked 区间必须位于目标技能，连续长度不低于正式事件最短驻留，并且事件必须得到全部正常示范支持。
- `LINK_PENDING` 单独不能建立事务组；只观察对侧机械臂关系的守卫也不再被当成本臂参与共同物理连接，避免把单向等待错误提升为原子事务。
- 代码开发计划、技术路线报告、闭环模块说明和阶段开发记录同步更新为当前最终口径。

### 真实模型与回放结果

- 重建四个双臂任务后，LiftTray 只生成一组联合边界：`left:1→2 + right:1→2`，共享跨边界 LINK 证据为 `item/tray`；`0→1` 没有被仅凭时间同步误标。
- HandOver、StoreBottle 和 SweepDust 均未形成对称共同硬依赖，继续保持无事务组的异步或定向条件路径。
- 阶段四正式标定更新为 `evaluations/phase4_boundary_calibration/results/v3`：40/40 边界完成标定，200/200 边界×正常示范通过最终入口守卫，LiftTray 联合事务成员在5/5条正常示范中共同就绪，所有 `SHA256SUMS` 通过；运行配置与 `configs/closed_loop_boundary/` 一致。
- 阶段一事务识别与阶段四守卫专项回归共37项通过；全仓回归为 `503 passed, 4 skipped in 254.94s`。

## 2026-08-26：真实 LiftTray 联合事务集成验证

- 新增 `evaluations/phase4_transaction_integration/`，直接加载真实 LiftTray V4 双臂模型、正式阶段四运行配置和5条正常示范，贯通 `BeliefUpdater → ClosedLoopExecutionController → MultiArmBoundaryController → TransitionTransactionCoordinator`。
- 为覆盖真实任务中难以稳定复现的异步就绪顺序，只在前两个终端保持周期把右臂进度后验控制为同技能非末端状态；几何观测、关系后验、场景状态、左臂信念及后两个周期的右臂信念仍来自正常示范。
- 每条示范第2周期均出现左臂已满足 `H=2`、右臂尚未许可；事务组保持，两臂引用状态都停在 `k1:t17`，没有单臂提交。
- 右臂恢复真实终端信念并连续满足 `H=2` 后，5/5条示范均在同一 pre-action tick 把两臂原子提交到 `k2:t0`；20个验证周期中的部分提交次数为0。
- 逐周期表、逐示范摘要、正式配置副本、环境信息和校验和保存在 `evaluations/phase4_transaction_integration/results/v1`，`SHA256SUMS` 全部通过。该验证不等同于完整 RLBench 在线动作执行成功率。

## 2026-08-26：阶段五主动关系验证、统一恢复与任务重入

### GitHub 推送修复

- 阶段四留档提交为 `639d2c6b2 feat: complete phase 4 boundary guards and transactions`。首次推送失败并非 GitHub 网页或仓库不可用，而是仓库 Git 配置仍使用失效的旧 IPv6 HTTP 代理，覆盖了当前 shell 可用代理。
- 将仓库级 `http.proxy` 修正为 `http://127.0.0.1:17890` 后推送成功；fetch 后本地 `dev/yushuang` 与 `origin/dev/yushuang` ahead/behind 均为0，远端指向同一提交。

### 阶段五实现

- 新增独立 `ExecutionMode={TASK,VERIFY_LINK,RECOVERY}`、非 TASK 冻结进度更新、固定反向主动验证、episode 级 Pending 激活、事件锚点解析、通用 LINK/UNLINK、关系目标排序、完整状态重入和结构化失败。
- VERIFY_LINK 使用最近实际 TASK 轨迹方向，不使用命令方向或自适应扩大；`t_max` 只限制探测阶段，安全允许时仍按实际路径返回。尝试签名防止同一事件在 TASK/VERIFY_LINK 间循环。
- LINK 按参考系当前位姿重新实例化事件级短轨迹并放宽协方差；UNLINK 执行打开、脱离和 external 验证；多个目标统一先 UNLINK 后 LINK。
- 重入只使用当前机器人、场景和关系解释度，不恢复旧时钟。跨技能只允许通过阶段四守卫放行的相邻下一技能，状态选择、beta 重置、引用状态设置和返回 TASK 原子提交。

### 开发中发现并修正

- 首轮测试把1 mm探测位移与2 mm返回容差组合，却期待单独返回动作；控制器正确判定已到入口。测试将返回容差收紧，只验证“超时后仍执行真实返回”的机制，不修改生产默认值。
- 首轮 LINK/UNLINK 测试在第一个恢复尚处于重入阶段时复用同一控制器启动第二个恢复；修正为两个独立恢复 episode，保留控制器禁止覆盖未完成恢复的约束。
- 审查发现恢复验证曾把“任意方向的有效关系证据”都记作已确认。例如先观测到 informative external，之后仅凭 non-informative linked 保持值也可能完成 LINK。现改为只有目标方向本身获得有效信息后才可完成 LINK/UNLINK。
- 原管理器在重入状态被选中后先切回 TASK，再依赖调用者另行重置 beta 和 reference_state，存在状态提交空隙。现将选择、信念重置、执行游标重置和模式切换原子化。
- 全任务重入候选包含旧技能或跨多技能状态时，旧实现会尝试构造非法 `BoundaryId`。现同技能正常比较，相邻下一技能必须有守卫许可，后退和跨多技能候选显式拒绝。
- 全闭环测试首次用裸 `pytest` 收集时因仓库根目录未进入 `PYTHONPATH` 而无法导入 `integrations`；补齐 `$PWD:$PWD/source` 后84项全部通过，这属于测试入口环境差异，不是代码失败。

### 验收

- 阶段五专项测试11项通过，阶段一至五闭环回归84项通过；最终全仓回归为 `514 passed, 4 skipped in 253.33s`。
- 新增 `evaluations/phase5_recovery_acceptance/`，用冻结 V4 的五条正常示范重建8个任务、12套机械臂模型并审计恢复元数据接口。
- 正式结果覆盖11个 LINK、7个 Pending、3个 UNLINK、904个状态级 `link_origin`、176个 LINK 路点和8个 UNLINK 合法重入状态，全部通过；`results/v1/SHA256SUMS` 验证通过。
- 该验收不等同于在线故障恢复成功率；阶段六完成 RLBench 顶层策略接入后再进行闭环仿真故障评测。

## 2026-08-26：阶段五受控行为 A/B 验证

### 验证设计

- 新增 `evaluations/phase5_behavior_ab/`，直接加载冻结 V4 任务模型和五条正常示范场景，以确定性理想执行器提供已知 linked/external 物理结果，不另造简化任务模型。
- Pending 验证覆盖7个真实候选、5条示范和两种物理结果，共70条；关系恢复覆盖11个正式 LINK 和3个正式 UNLINK的五条示范，共70条，并另设70条执行器完全不响应的有界失败反例。
- 重入同时覆盖“旧时钟超前、需要后退恢复”和“旧时钟落后、需要向前定位”，共240条；另用50条试验检查无入口许可时阻断跨技能候选、许可后允许相邻技能候选。
- 对照分别为静止被动等待、不执行关系恢复动作和恢复旧时钟；实验组调用正式 `BeliefUpdater.update_frozen`、`ClosedLoopRecoveryManager` 和 `ReentrySelector`。

### 结果与边界

- 被动等待无法消除缺少运动激励造成的 Unknown，关系判定率0%；反向主动验证对 linked/external 两种结果均正确，判定率100%。进度冻结、反向方向、入口朝向/夹爪保持、按实际路径返回、同上下文重复抑制和上下文变化后重新允许均为100%。
- 无恢复动作的关系恢复率0%，统一 LINK/UNLINK恢复率100%；执行器不响应时有界终止和结构化失败均为100%，没有无限循环或误报成功。
- 旧时钟重入 StateId MAE 为37.0000，完整状态重入为1.7292，降低35.2708；96.25%的试验优于旧时钟，状态选择率99.58%，选中后的 beta/reference_state/模式原子提交正确率100%。
- 1/240条 Handover 正常重入因机器人兼容度0.002195低于当前保守门限0.01而被拒绝；未在本轮验证中擅自降低生产门限。精确 StateId 命中率为75%，其余非精确结果多为局部相邻或静态弱区分状态，因此结果证明显著正向贡献，但不等于无误的完整任务恢复。
- 首轮结果统计曾把返回误差要求写成绝对零，导致4条1 mm返回误差在生产2 mm容差内仍被误报失败；最终评测改为读取正式 `return_position_tolerance`，未修改控制器逻辑。最终 v3 的全部结果文件通过 `SHA256SUMS`，阶段五专项测试11项、阶段一至五闭环回归84项全部通过。

### 正常重入阈值收口

- 后续按正常回放正式标定 `minimum_robot_compatibility`。检查发现 A/B 脚本原先直接实例化默认 `ReentryConfig`，没有显式传入 `closed_loop_recovery.json`；旧默认值与配置同为0.01，因此旧结果未改变，但标定后存在配置和评测漂移风险。现统一显式传递配置，并由默认值往返测试约束。
- 正常回放中最低正确状态机器人兼容度为0.00219484；正式阈值取0.001，仍保留联合解释度、场景兼容度和关系兼容度检查，不删除机器人证据门控，也不使用故障数据调参。
- 最终 v5 在同一次240条正常回放中并行比较参考阈值0.01和正式阈值0.001：选择239→240、精确选择180→181、错误选择59→59、MAE 1.7292→0.6375。Handover 左臂第3条示范 `k2:t44` 正常假阴性消失，没有增加错误重入。
- 最终阶段五 A/B 的 Pending 判定和统一恢复仍为100%，重入优于旧时钟比例96.67%、状态选择率100%、原子提交正确率100%；正式报告、逐试验表、阈值标定表、运行配置和 `SHA256SUMS` 保存在 `evaluations/phase5_behavior_ab/results/v5`。更新后阶段一至五闭环回归84项通过。
- 阶段五冻结前全仓回归为 `514 passed, 4 skipped in 251.91s`；Black、Python编译、`git diff --check` 和两组正式结果 `SHA256SUMS` 均通过后再提交留档。

## 2026-08-26：阶段六集成中的动作目标与边界进度语义修复

### 问题定位

- 首次真实 StackWine 闭环试跑发现，阶段二把上一周期实际查询的 `reference_state` 再解释成动作前源状态，将0.65正常完成质量放到其后继。DynaMAC `query_state(reference_state)` 实际直接生成该状态的目标位姿，因此正常观测被人为错开一个状态；技能内部可被相邻轨迹部分掩盖，边界入口则会排除真正发送的首状态并触发正常误恢复。
- 边界同周期捕获的新 `virtual_skill_*` 已加入动作观测，但流角色仍读取边界前信念中的旧可见性快照，使新虚拟帧在首条入口 PoE 中权重为0、下一周期突然变为1。该缺口会让入口动作定义发生非任务语义跳变。
- 仅把正常质量移回动作目标后，已提交边界前的源技能后验仍可能凭相似末端几何压过新技能入口，导致控制器永久保持入口动作但进度退回旧技能。这说明事务此前只原子修改执行游标，没有同步提交下一周期进度拓扑。

### 通用修复

- 统一规定 `reference_state` 是实际发送的动作目标：0.20未完成质量保留同技能上一周期 `beta`，0.65正常质量落在动作目标本身，0.15提前质量落在其直接后继；初始化或动作被环境拒绝时不产生动作后推进。
- `TransitionTransactionCoordinator` 在完整批次校验后，同时提交执行游标和 `BeliefUpdater` 的下一周期进度入口投影。当前周期已经返回的源技能信念继续作为审计，关系后验、场景观测和 mode 不重置；联合事务全部成员同步投影，等待事务和外部恢复重入成员不被提前修改。
- 新捕获的虚拟技能帧是内部固定参考，不依赖外部 tracker 延迟；边界同周期入口查询显式将已捕获虚拟帧作为可靠输入，但不重新运行关系或进度推断。
- 固定示范回放统一传入真正产生当前观测的示范动作目标 StateId，不再机械使用上一帧编号。技术路线报告、代码开发计划、模块说明和阶段最终记录同步更新。

### 当前验证

- 动作目标、动作拒绝、单臂异步边界、双臂联合事务、外部重入委托、同周期虚拟帧和跨阶段回放定向测试共57项通过；完整 closed-loop 回归88项通过。
- 相同密封初始状态的 StackWine 在修复前出现500周期超时；修复后188周期成功，三个边界正常提交，全程188周期均保持 TASK，没有触发 VERIFY_LINK、RECOVERY 或状态重入，且无 InvalidAction。
- 该修改没有降低解释度、关系失配或恢复门限，也没有加入任务名称、状态编号或 StackWine 专用分支。后续仍需完成其他单臂/双臂正常回放和正式阶段六故障消融，当前不提前宣称阶段六验收完成。

## 2026-08-27：低置信进度的控制等价聚合

### 问题与实现

- 阶段二在静态或弱可辨识轨迹段会把相邻状态的后验分散标记为 `LOW_CONFIDENCE`；阶段三旧实现不区分这些状态是否产生相同动作，统一 HOLD。Handover 左臂因此在交接前出现正常假阻断，而右臂继续推进。
- 新增 `ControlEquivalenceAssessment`，只在原始状态为 `LOW_CONFIDENCE` 时评估同技能局部连续候选。候选必须具有相同的离线流选择、动态角色、期望/实际关系语义、执行权重、阻断/恢复/验证请求和夹爪命令，当前观测下的 PoE 位姿目标还必须两两达到已有动作完成兼容度阈值。
- 将每个控制等价类内的进度后验质量相加，并对等价类质量重新计算归一化熵；聚合置信度与类别熵继续使用阶段二的原 `minimum_confidence` 和 `maximum_normalized_entropy`。通过时只取消 `low_progress_confidence` 这一项阻断，原始逐状态后验、MAP、`LOW_CONFIDENCE` 标签和分项诊断全部不改。
- `NO_PLAUSIBLE_STATE`、动作未完成、关系 Unknown/可靠失配、无正权重动作、不同夹爪、不同角色、局部索引有缺口和跨技能候选均不能借该机制放行。顶层诊断新增等价状态集合、聚合置信度、类别熵、类数和最小动作兼容度。

### 验证与新发现

- 阶段三新增4组定向测试：相邻等价动作安全推进；位姿不同继续 HOLD；夹爪不同继续 HOLD；跨技能即使动作相同也继续 HOLD。阶段三执行控制25项、完整闭环101项全部通过；最终全仓回归为 `533 passed, 4 skipped in 262.51s`。
- 使用相同 V4 模型、封存 seed `2608000000` 和同一 RLBench 控制协议重跑 Handover 正常样本。7个低置信周期中6个形成有效控制等价类，原始标签保持可审计；左臂第5技能从旧实现停在 `t6` 改为推进到 `t10`，说明该机制确实消除了对应假阻断。
- 该样本仍未完成：右臂比左臂早5周期进入第5技能，并在 `t17` 提前作用于交接物体，随后左臂对 `item0` 的在线关系由 linked 变为 external，按现有机制正确进入关系恢复。当前 Handover 无 `transaction_group` 符合开发计划，因为其边界没有对称共同硬依赖；不能仅凭时间同步将其改成联合事务。
- 残余问题属于双臂同一协作技能内部的相位协调，而不是本次控制等价聚合失效。现有计划尚未定义通用技能内同步机制，因此没有加入任务名、固定时差、放宽关系阈值或强制事务等特殊处理，待确认通用口径后再继续。
- 本次关键机制已同步更新《技术路线报告》《新方法代码开发计划》、闭环模块 README 和《阶段开发记录》。阶段六仍标记为开发中，不将本次诊断表述为完整 Handover 成功或最终验收。
- 最终静态类型检查同时收口本次可空关系分布和阶段六原有局部变量类型推断问题，仅调整类型收窄与变量命名，不改变运行逻辑；`execution_controller.py + policy.py` 定向 mypy 无错误，阶段三/阶段六定向回归28项通过，Black 与 `git diff --check` 通过。

## 2026-08-27：统一边界条件补全双臂单向等待

### 机制修正

- 没有新增“关系转移驱动的单向协作守卫”。双臂离线构建只在既有 `BoundaryModel.scene_conditions` 中补全跨臂候选：若一臂的本地完成目标由对侧末端参考系表达，并且双方正常示范边界稳定对应，则把同一规范化双臂末端相对构型加入对侧边界候选。
- 候选仍执行普通边界场景条件的全部正常示范留一覆盖、统一马氏兼容度阈值和可靠性统计；在线仍只经过 `EntryGuard`。没有加入对侧进度门控、固定时差、任务名称分支、新阈值或第二种在线守卫。
- 双臂自身末端位姿只在联合快照的运行观测中补齐，冻结 DynaMAC 输入字段和基础动作模型不变。单向条件只更新依赖方的 `affected_arms`，不会仅凭时间同步生成 `transaction_group`。

### 验证与残余问题

- Handover 右臂 `4→5` 自动保留 `edge:left_ee→right_ee` 边界条件，全部5条正常示范的留一支持检查通过。封存正常 seed `2608000000` 中，该条件让右臂在边界等待3周期，左右进入第5技能的间隔由5周期降为1周期，之后同步推进至 `k5:t17`。
- 双臂末端边作为逐状态候选时只在部分状态具有稳定正增益；停留的 `t17` 上左右臂留一增益均为负，因此未把它强行扩展到整个技能。
- 阶段一边界、阶段四入口守卫和阶段六集成回归42项通过，重命名后的定向回归3项通过。
- 同一正常样本的后续失败不再是边界等待：接触发生后左臂—物体关系由 linked 转为 external，现有 LINK 恢复从事件前锚点首路点开始并立即发送打开夹爪命令，导致未被右臂接稳的物体掉落。该问题涉及动作完成与恢复安全语义，未在本轮擅自修改，等待确认通用方案。

## 2026-08-27：动作到达判定收口

### 机制修正

- 删除“上一命令与当前末端的完整高斯相容度不足时独立 HOLD”的重复进度门控。该相容度受状态协方差尺度影响，与已经完成几何观测、关系和场景联合校正的进度后验叠加时，会对正常状态产生第二次且量纲不一致的拒绝。
- 动作到达现统一交由阶段二的可信进度后验判断：估计仍在当前引用之前时 HOLD，已到达当前引用时查询直接后继，已提前到达直接后继时提交该后继。reset 周期仍 HOLD，关系、无可用流、入口守卫和 `NO_PLAUSIBLE_STATE` 等原门控不变。
- 命令跟踪相容度和马氏距离仍完整保存在逐周期诊断中，只不再直接改写进度决策。控制等价阈值更名为 `minimum_action_equivalence_compatibility`，并兼容读取旧 bundle 的 `minimum_command_completion_compatibility`。

### 验证与剩余问题

- 阶段三专项与阶段六跨层定向29项通过；阶段一至六、RLBench 核心及直接评测相关回归为 `165 passed, 1 skipped in 211.81s`，最终全仓回归为 `535 passed, 4 skipped in 274.25s`，Black 和 `git diff --check` 通过。
- 用同一封存 Handover seed `2608000000` 重跑真实 RLBench。右臂已从旧的 `k5:t17` 推进到 `k5:t18`，证明本次删除的是真实假阻断，而不是用放宽进度解释度门限跳过问题。
- 同一回放随后仍失败：右臂到达 `k5:t17/t18` 时，在线关系滤波将左臂—物体从 linked 判为 external，随后触发 LINK 恢复并最终因无合法重入状态进入结构化失败。只读模型审计确认：左臂第5技能所有状态均为 soft linked 期望和闭合夹爪，正式 UNLINK 位于第6技能；旧的命令硬门控版本也在两臂同步到 `k5:t17` 时出现同一关系跳变。因此本轮只能确认它与动作到达口径修改无关，尚不能在没有 RLBench attachment 真值的情况下确认是真实断连还是接触段关系滤波假阴性。未擅自加入任务特例或放宽关系阈值。

## 2026-08-27：Unknown 周期的连续轨迹证据修正

- 阶段二原实现把离散 `Unknown` 决策同时当作了连续位姿证据的零权重门控。当参考系可观测、可靠且二元后验仍主要支持 external，但静止期因缺少新运动激励而无法稳定离散化时，这会让进度评分与同周期仍参与 PoE 的 `DEFER` 流使用不同的位姿证据，导致策略自己的动作目标被误判为无法解释。
- 轨迹权重修正为 $a_{f,t}v_{f,t}q_t^f(\mathrm{external})$：可观测、可靠的物理流直接使用归一化二元后验的 external 概率连续降权；不可观测或不可靠时仍为零。离散关系兼容度、动态角色、PoE、边界守卫和事务处理不变。
- 新增定向回归覆盖 `Unknown + q(external)>0` 、不可观测和零可靠性对照；阶段二、三、六定向回归54项与相关完整回归170项通过。
- 重建8个封存闭环模型包并逐个验证可加载。同一封存 LiftTray 第1号正常样本在107周期内成功，0次 IK 失败、0次 `NO_PLAUSIBLE_STATE`、0次异常恢复；`1→2` 联合事务 HOLD 17周期等待双臂同时就绪后原子提交。LiftTray 第0号样本在闭环策略中仍遇到大量 IK 不可达；该样本在冻结 V4 基线中同样失败并有40次无效动作，因此保留为共享执行器困难，不用任务特例或守卫放宽规避。

## 2026-08-27：LINK_PENDING 软先验与失败抓取恢复闭环

- 真实 Handover 审计发现右臂 `k6:t0` 已有5/5正常示范支持的 `LINK_PENDING`，但部署先验仍为 soft external。根因是候选检测允许闭合当帧缺少事后运动证据，先验写回却再次要求该帧原始 linked 概率达到0.7，并在失败后提前消费 occurrence；同一 arm-frame 存在更早正式事件时还会完全跳过 Pending 覆盖。
- Pending 写回改为独立软期望覆盖层：候选状态直接写 `[0.3,0.7]`，其后只在原始跨示范协方差继续支持 linked 的连续区间保持；不建立正式锚点或离线 `link_origin`，并能与更早的正式 LINK/UNLINK 共存。
- 跨臂定向守卫仍默认只等待；只有关系所属机械臂已经到达匹配 Pending、必要条件要求 linked 且在线稳定判定 external 时，才向该机械臂生成 LINK 恢复意图。恢复可临时使用同一 Pending 轨迹，但成功确认 linked 前不激活运行时来源；确认成功后才建立 episode `link_origin`。
- 阶段一/四/五定向56项、阶段三/六集成34项通过。重建并检查全部7个真实 Pending：候选状态均为 soft linked、支持率均为1.0、正式离线来源仍为空；受影响的 Handover、LiftTray 与 SweepDust 闭环模型已更新。

## 2026-08-27：正式 LINK 自然确认区间与顶层辅助模式冻结修正

### 正式 LINK 自然确认

- Handover 真实诊断暴露固定 `formal_link_confirmation_states=5` 会过早耗尽确认宽限：若关系后验尚未从事件前 external 持续性中恢复，控制器在第6个状态 HOLD，反而消除后续自然携带运动，形成“需要运动才能确认、却因未确认而不再运动”的闭环死锁。
- 取消全局固定状态数。正式 LINK 直接使用阶段一为该 occurrence 学到的完整 `linked_entry_states` 连续区间；确认前相应流保持零权重 `DEFER` 并允许自然运动，可靠且信息充分的 external 反证仍立即进入恢复，已确认事件不能重复使用宽限。
- `formal_link_confirmation_states` 从正式配置和8个闭环模型包删除；加载器只为已有旧包保留忽略式兼容，不再把旧值带入运行配置或重新输出。

### 多臂辅助模式全局冻结

- 正确加载 TRAC-IK 的首次成功 Handover 诊断中，右臂 VERIFY_LINK 外移/返回期间左臂仍按 TASK 更新。共享物体的探测运动使左臂进度从边界末端回退并重走局部状态，而且左臂守卫在右臂返回完成前就消费了 linked 结果。这违反计划中“非 TASK 冻结正常任务进度和入口事务、返回 TASK 后再消费结果”的顶层模式语义。
- 修正为任一机械臂处于 `VERIFY_LINK/RECOVERY` 时，全系统冻结正常进度更新、`reference_state` 提交和正常入口事务；辅助臂继续动作，其他机械臂使用冻结信念并闭环伺服各自当前参考目标。恢复臂自身的合法重入评估仍可运行。
- 新增真实双臂模型集成回归，确认辅助模式下 TASK 对臂无执行提交、使用 `frozen_progress`、不产生正常边界请求；阶段三至六相关64项全部通过，8个模型包均能按新配置加载。

### 真实 Handover 诊断

- 两次初始诊断漏将本地 `pytracik` 扩展目录加入 Python 搜索路径，586次主求解均在求解器创建阶段失败，右臂由伪逆后备执行至 `k5:t17` 后进入 `NO_PLAUSIBLE_STATE`。该启动环境降级未作为机制结果，相关无效过程文件随后清理。
- 补齐扩展路径后，第一轮340周期成功：680次主IK请求中522次由 TRAC-IK 成功求解；右臂进入 VERIFY_LINK，linked 后验由0.062升至0.945，完成外移、原路返回并切回 TASK。该轮验证了真实物理探测与返回链，但尚未包含随后加入的全局冻结。
- 加入全局冻结后的复跑在286周期成功，TRAC-IK 创建失败为0。右臂从第272周期进入 VERIFY_LINK；从下一周期起，左臂始终保持 `reference_state=estimated_state=k5:t21`，执行提交为0、正常边界记录为0，控制源统一为 `auxiliary_peer_frozen_task_target`。RLBench 在验证外移阶段已判任务成功，因此该轮没有继续到返回；返回与重复触发抑制继续由阶段五组件验收覆盖。
- 两轮运行使用与正式包完全相同的 `left.npz/right.npz`，但为隔离本次关系机制诊断，临时把 Handover 边界 `left:1→2`、`left:5→6`、`right:5→6` 的 `local_score_threshold` 从正式值 `0.007981804575222582/0.21224434030410647/0.16980192402029892` 改为 `0.003/0.1/0.003`。旧诊断包中的 `formal_link_confirmation_states=200` 已被新加载器忽略，不影响本轮新语义。
- 上述均为封存 seed 的单样本机制诊断，不是正式配置下的成功率评测，不替代后续正常全任务回归与正式故障消融。诊断配置差异、模型哈希和结果索引已与诊断结果共同保存。
- 最终全仓回归为 `569 passed, 4 skipped in 292.89s`；Black、Python 编译和 `git diff --check` 通过。

## 2026-08-28：LiftTray 困难正常样本的正确 TRAC-IK 复核

- 使用封存 `rlbench_eval_v2` 第1个 LiftTray 样本（seed `2608000000`）、正式 V4 数值模型、当前闭环侧车和 `stage6_converged_cartesian_feedback_v4` 重跑单样本诊断。启动前验证固定 bounded TRAC-IK 的版本、ABI、API和本地扩展哈希；最终 `trac_ik_distance_factory_failures=0`。
- 样本在400周期上限时仍失败：23个动作完整执行，377个周期提交原关节保持。TRAC-IK 共800次请求中422次成功、378次耗尽；连续伪逆378次全部失败，碰撞感知采样只成功1次。
- 执行器每周期按右臂、左臂顺序在物理步前原子准备。全400周期均进入左臂求解，说明右臂每周期都至少由某一级获得候选；377个无效周期都在随后的左臂求解中耗尽。左臂从 `k0:t20` 开始间歇失败，最终在 `k0:t25` 持续失败；右臂后续 `NO_PLAUSIBLE_STATE` 是双臂原子动作长期未执行的下游结果。现有汇总计数器未按臂拆分唯一的采样成功，因此只归因耗尽臂，不虚构单臂求解器精确计数。
- 冻结 V4 同 seed 也失败，116步中有40次无效动作；200个 static 样本中10个失败样本均有32至40次无效动作。这说明它是基线共享的执行困难布局，但不能免除阶段六改进通用执行器的责任。
- 汇总与15 MB逐周期轨迹保存于 `integrations/rlbench/results/diagnostics/phase6_normal_lift_seed0_correct_trac_v5.json` 和 `phase6_normal_lift_seed0_correct_trac_trace_v5`；诊断报告及 SHA256 保存于 `evaluations/phase6_rlbench_integration/diagnostics/lift_tray_seed0_ik_diagnosis.md`。

## 2026-08-28：LiftTray 全部 V4 失败 seed 的执行器复核

- 从封存 `rlbench_eval_v2` 中按原索引只读选取冻结 V4 的10个 LiftTray 失败 seed；新增诊断子集入口只切片既有计划，不重新生成样本，并把结果明确标记为 `formal_result=false`、`paper_comparable=false`。
- 10/10 样本在当前闭环策略和正确加载的 bounded TRAC-IK 下均于400周期上限时失败；所有样本 `trac_ik_distance_factory_failures=0`，排除本地扩展未加载。
- 总计4000周期只完整执行261个双臂动作，3739个周期提交原关节保持。执行器按右臂、左臂顺序原子准备目标，所有无效周期都在右臂已有候选后由左臂求解耗尽；首次失败集中于左臂第0技能 `t20..t28`。
- TRAC-IK 总计3744次耗尽，碰撞感知采样只恢复5次。现有路径备选受10 cm位移门限限制，仅3个 seed 共尝试102次且全部失败；其余7个 seed 没有进入路径备选。
- 由于 TRAC-IK 端点层本身不进行环境碰撞检查，这轮结果排除了“只要关闭碰撞就不会无解”的假设。剩余问题是执行器缺少通用的多初值/多关节分支端点搜索、合法候选统一校验和同目标有界重复失败处理，不属于关系、进度、边界事务或恢复机制失败。
- 完整汇总、逐 seed CSV、原始产物 SHA256 和解释边界保存在 `evaluations/phase6_rlbench_integration/diagnostics/`；这些运行是对已知失败样本的定向诊断，不作为新的正常任务成功率估计。

## 2026-08-28：阶段六通用混合末端执行器

### 方法调研与选择

- 对比 RLBench 官方末端位姿规划、TRAC-IK、MoveIt Servo 和 cuRobo。RLBench/ARM 常用的线性路径加采样规划易于接入但不能消除接触段局部不可解；TRAC-IK 对关节限位和局部极小值更稳健，但官方明确不处理 mesh 自碰撞；MoveIt Servo 的连续笛卡尔反馈、奇异性和限位处理最符合本问题；cuRobo 的批量碰撞 IK 与轨迹优化适合作为未来 GPU 后端，但当前 RLBench Python/CoppeliaSim 进程没有同步的 cuRobo 碰撞世界和依赖环境。
- 最终没有迁移 ROS/MoveIt 或立即引入 cuRobo，而是在 RLBench benchmark 适配层实现同类的混合闭环：保留现有实时 Panda 运动链和 bounded TRAC-IK，用物理反馈和有界局部 SE(3) 延续补齐单次端点求解的脆弱性。该选择不改阶段一至五算法和策略目标。

### 最终实现

- 新配置为 `stage6_hybrid_cartesian_executor_v5` / `rlbench-stage6-hybrid-cartesian-continuation-v7`。执行顺序为 bounded TRAC-IK Distance、连续当前种子伪逆、5 mm / 2° 的局部 SE(3) 延续与四级回退、碰撞感知优先且必要时放宽的采样 IK、碰撞感知/放宽线性路径，以及仅对远目标使用的碰撞感知 RRTConnect。
- 每个内部子段执行后重新观测真实末端并重建当前运动链；最多96段。子段只是同一原始策略目标的执行分解，不能作为新的 `reference_state` 提交。原始目标进入2 mm / 1°才完成；严格0.5 mm / 0.25°残差继续保留审计。
- 双臂在共享物理步前准备全部候选；物理进展要求至少一臂改善且没有任何一臂退化。执行结果区分 `reached/progressed/stopped` 和进展前的全链耗尽，避免把合法但物理停滞的命令误报成 IK 无解。
- 碰撞放宽只位于碰撞感知采样/线性路径之后，并完整记录使用次数；它是 RLBench 接触场景的有界后备，不宣称为真实机器人安全策略。近目标不再调用可能阻塞数分钟的非线性 RRTConnect。
- 试验中否决了两个无效方向：单纯扩大关节搜索窗口没有改善；先使用比控制接受范围严格10倍的 TRAC 再放宽会额外增加约41秒求解时间且不改善任务结果。最终数值求解与控制采用统一2 mm / 1°口径。

### 真实验证与边界

- 已知困难 LiftTray seed `2608000000` 在旧执行器的400周期中有377次 IK 无效动作。混合执行器复跑到131周期时 IK 无效动作降为0；895次 TRAC 请求中352次成功，失败部分由124次伪逆成功和190次小步延续接续，没有发生全链耗尽。样本最终因抓取/关系后的左臂 `no_legal_reentry_state` 结束，因此只确认执行器瓶颈解除，不宣称任务成功。
- 正常 LiftTray seed `2608000001` 在108周期成功，IK 无效动作为0；368次 TRAC 请求中211次成功，其余157次由伪逆接续，确认正常样本没有执行器回归。
- 执行器专项49项全部通过；最终全仓回归为 `577 passed, 4 skipped in 292.66s`。Black、Python 编译、`git diff --check` 和最终产物 SHA256 全部通过。
- 调研、实现、两个真实样本结果与解释边界保存在 `evaluations/phase6_rlbench_integration/diagnostics/HYBRID_EXECUTOR_RESULTS.md`；最终 JSON/JSONL 和 `HYBRID_EXECUTOR_SHA256SUMS` 保留。full-joint、continuation、backoff 和 hybrid v4至v6 的过程结果及大体积轨迹已移入系统回收站，可恢复但不再留在项目中。
- 《新方法代码开发计划》、闭环 README 和《阶段开发记录》已同步更新。阶段六仍处于开发中；下一步是正常全任务回归、困难 LiftTray 抓取/关系重入诊断及正式故障消融，不能用执行器改进代替这些验收。

## 2026-08-28：恢复动作与合法重入协方差口径统一

- 真实 OpenMicrowave 正常回归暴露了阶段五组合链缺口：事件级 LINK 锚点和主动反向探测能够重新获得稳定 linked 关系，但恢复后机器人轨迹重入仍使用理想正常示范的原始窄协方差，导致合法状态被 `robot_incompatible` 和 `insufficient_joint_explanation` 拒绝。
- 对同一真实恢复后快照复算，旧口径的最佳合法状态为 `k2:t41`，机器人兼容度 `2.2437355440241426e-11`、联合解释度 `2.116504998440506e-11`；关系兼容度已为 `0.6603066492636587`，失败并非关系未恢复。
- 修复只在 `RECOVERY -> REENTRY` 的机器人轨迹评分中复用现有恢复协方差放宽量：`Sigma_reentry = Sigma + lambda_rec I`。正常进度、场景因子、关系兼容度、关系滤波、动作 PoE、合法候选和入口守卫保持不变。
- 新口径在同一真实快照上仍选择 `k2:t41`，机器人兼容度提高为 `0.6456458718389564`、联合解释度为 `0.6090346603498499`，通过全部既有门限。阶段二、五、六相关46项测试通过；同一封存 OpenMicrowave 正常样本另一次真实执行在171周期成功，0个无效动作和0次全 IK 链耗尽，确认正常 TASK 路径没有回归。该次成功运行未触发恢复，因此恢复分支的直接证据严格限定为旧真实快照复算与确定性定向测试。

## 2026-08-28：Handover 在线推进与跨臂边界闭环复核

- 阶段六执行器更新为 `stage6_hybrid_cartesian_executor_v8` / `rlbench-stage6-hybrid-cartesian-continuation-v10`：优先使用与当前 CoppeliaSim 状态一致的伪逆候选，失败后才进入 bounded TRAC-IK，再使用笛卡尔小步延续、采样和路径后备。取消“合法命令物理停滞后立即换另一求解器重试同一目标”，避免不同关节解分支在相邻周期震荡；评测协议字符串和最终阶段记录同步更新。
- Handover 真实回放暴露阶段三执行缺口：`LOW_CONFIDENCE` 已通过控制等价聚合，但 MAP 状态比 `reference_state` 落后一个等价状态时，旧决策仍无条件 HOLD。修复后，仅当二者位于同一个已接受的同技能控制等价类时，允许把当前引用按控制等价完成并推进一个直接后继；不修改原始后验、不跨技能、不降低关系或入口门限。阶段三和执行器定向67项测试通过。
- 修复后正式模型回放到500周期，右臂由旧阻塞点 `k1:t4` 推进到 `k4:t25`，未出现无效动作或全求解链耗尽；随后被右臂 `4→5` 的规范跨臂末端场景守卫阻断。审计发现左臂已提前越过其对应边界并改变了该相对几何，使右臂到达时无法再满足从正常同步边界学到的条件。
- 为验证原因，临时模型只把左右 `4→5` 放入同一事务组，不修改正式模型或构建器。A/B 回放中双方从第188周期等待，到第294周期共同满足后原子提交，证明该事务能够消除当前跨臂场景条件的异步破坏。之后任务在右臂 `k5:t17/18` 的接触前目标处出现新的物理执行/动作协调问题，因此本试验只证明边界死锁原因和候选修复有效，不宣称 Handover 已完整成功。
- 正式事务识别尚未改动。当前开发计划只把“全部正常示范同步且双方具有对称共同硬依赖”归为联合事务，而这次证据说明还需决定：当一侧本地完成所依赖的跨臂硬构型正是另一侧入口守卫，且先分别提交会破坏该条件时，是否也应视为同一个共享就绪事务。该项涉及计划机制扩展，等待确认后再更新构建器、正式模型和完整回归。

## 2026-08-28：高置信单状态落后的控制等价补全

- 复核上一节逐周期记录后，确认右臂在 `k1:t4` 的主要阻塞不是 `LOW_CONFIDENCE`，而是连续87个周期左右的高置信 `BACKWARD_REALIGNMENT`：MAP 为 `k1:t3`，当前引用为紧邻的 `k1:t4`。旧控制等价入口只接受 `LOW_CONFIDENCE`，因此即使两状态的流、角色、关系语义、夹爪和 PoE 动作完全等价，也从未执行比较。上一节把这一现象概括成低置信落后并不准确，本节结论取代该表述。
- 控制等价评估现同时接受 `LOW_CONFIDENCE` 和高置信 `BACKWARD_REALIGNMENT`。后者只有在 MAP 恰为当前引用的同技能直接前驱、二者属于包含 MAP 的已接受等价类时，才可把当前引用按控制等价完成；落后两个或更多状态仍 HOLD。原始后验、MAP、进度标签和逐状态评分不改，每周期仍最多推进一个同技能直接后继，关系阻断、入口守卫和跨技能限制不变。
- 新增等价单状态落后安全推进、位姿不同 HOLD、夹爪不同 HOLD和等价多状态落后仍 HOLD测试；阶段三与阶段六相关100项通过，最终全仓回归为 `591 passed, 4 skipped in 297.33s`，Black、Python 编译和 `git diff --check` 通过。
- 使用同一封存正常 Handover seed `2608000000` 和正式模型回放。旧 v11 中右臂进入技能2为 tick 188；新 v12 在 tick 80 的唯一一次 `BACKWARD_REALIGNMENT` 上记录 `control_equivalent_backward_realignment` 并安全推进，tick 128 进入技能2。原先 `current_target_incomplete` 由89周期降为0，未发生跨技能等价提交或无效动作，证明本次修复对目标缺口有效。
- v12 整体仍在1000周期上限失败，原因是另一项既有跨臂边界协调问题：右臂到达 `4→5` 时，本地完成已通过，但 `edge:left_ee->right_ee` 兼容度约 `1.94e-4`，低于正常示范阈值 `7.47e-4`。该构型在左臂刚越过对应边界时约为 `9.16e-3`，随后左臂进入下一保持技能、右臂尚未到达边界期间下降。这个结果不能反推控制等价修复无效，也不能据此直接放宽场景阈值；是否需要改变定向等待或事务归类属于独立机制取舍，正式构建器和事务规则仍未改动。

## 2026-08-28：阶段六执行精度与硬场景守卫闭环

- 对 Handover 右臂 `4→5` 做了四组正常样本 A/B。冻结 v12 的 0.5 mm / 0.25° 执行口径中，五条离线示范终止窗口和策略目标均通过 `edge:left_ee→right_ee` 守卫，但真实执行残差使在线兼容度长期低于 `7.465250633204206e-4`。没有降低守卫阈值，也没有把执行误差伪装成观测协方差。
- 否决 0.25 mm / 0.05°：位置过严使动作长期只能部分执行，Handover 后续进入关系恢复并结构化失败。0.5 mm / 0.1° 虽能通过该边界，但在后续 VERIFY_LINK 累计140周期并于1000周期超时。两者只作为正常诊断对照，不进入正式实现。
- 最终 RLBench 适配层统一为 0.5 mm / 0.05°，同时用于 bounded TRAC 请求、执行后物理完成和控制接受；外部运动链 2 mm / 1° 只保留为 FK 对齐审计。配置身份更新为 `stage6_hybrid_cartesian_executor_v10` / `rlbench-stage6-hybrid-cartesian-continuation-v12`，不读取任务名、StateId 或边界身份。
- 同一 Handover seed 在 tick 211/212 的守卫兼容度分别为 `0.0017090541/0.0062398352`，连续满足后于 tick 212 放行，并在494周期成功；右臂共29周期进入 VERIFY_LINK。正常 LiftTray seed 1 和 StackWine seed 0 分别在106和168周期成功。三条真实回归均为0 InvalidAction、0全IK链耗尽、0控制预算耗尽。
- 全仓回归为 `591 passed, 4 skipped in 296.03s`。本轮只收口 benchmark 执行精度与已有硬守卫的接口，不修改阶段一至五算法；阶段六仍需完整正常任务回归和正式故障消融后才能验收。

## 2026-08-28：对侧末端执行依赖的情况通用筛选

- 针对 SweepDust 技能0中对侧末端与 broom/dustpan 被共同选中的问题，复核 Eq.6、五折按示范留一动作预测、四个真实双臂任务的关系事件图和跨臂相对几何。确认 Eq.6 与普通 LODO 都可能继承正常示范中的任务根平移和双臂同步，不能单独证明因果执行依赖；这不是 SweepDust 专用数据解析错误。
- 新增逐 `(skill, mode)` 的对侧末端执行依赖筛选。只有本臂活动 `link_origin` 与对臂 LINK/`LINK_PENDING` 指向同一共享/交接实体，或没有普通物理任务参考且对侧末端是稳定定向的唯一非虚拟几何目标时才保留。实现不读取任务名、不增加配置白名单、在线门控或固定时间同步。
- 真实结果为：StoreBottle left/skill1、LiftTray left/skill0、SweepDust 双臂/skill0 删除冗余对侧末端；Handover left/skill3、4 因 `item0` 交接路径保留。合成正反例确认 peer-only 定向目标保留，恒定同步但无关系目标时删除。
- 筛选只修改闭环 sidecar 的逐模态动作/进度流和对应本地完成目标，冻结 DynaMAC V4 不改写。sidecar 升级为 schema v4 并持久化筛选后的 `selected_frames/mode_selected_frames`；schema v3 保持只读兼容。四个双臂运行模型已重建，旧模型完整移至 `/tmp/closed_loop_peer_dependency_backup_20260828` 以便恢复。
- 回放同时暴露两个既有通用接口偏差：跨臂场景边无法从 arm-local 专用 `ee_pose` 解析本臂末端；阶段四标定器错误地用守卫放行状态筛除 `theta_local/H` 的本地正样本。前者通过 `RuntimeFeatures.ee_pose` 统一解析，后者改为本地标定与守卫决策分离；没有放宽关系或场景阈值。
- 五条正常示范、四个双臂任务的重新标定为28/28边界成功，140/140边界×示范决策正确；Handover left `5→6` 的5/5为本地完成后正确等待右臂 linked，LiftTray联合事务5/5。故障数据使用0。阶段一与四定向回归47项、阶段一至六全部核心闭环132项、RLBench直接评测适配59项（另1项跳过）通过。
- 复现表、LODO诊断、正反例矩阵、逐周期压缩轨迹、标定和决策结果保存在 `evaluations/peer_execution_dependency/`，并提供 SHA256 校验和。《技术路线报告》《新方法代码开发计划》、闭环 README 与《阶段开发记录》已同步更新。

## 2026-08-29：阶段六正式回归前留档审计

- 清理了2个阶段六评测脚本 Python 缓存和4个已失效的诊断锁文件；所有原始诊断结果、压缩逐周期轨迹、正式摘要和模型均保留。
- 扩大静态检查范围后，修正了闭环模块中仅涉及注解和类型收窄的问题，不改变任何运行机制；35个闭环源文件和4个 RLBench 适配文件的 mypy 检查通过。
- 同步了 LiftTray 诊断报告更新后的 SHA256，`peer_execution_dependency`、混合执行器、执行精度和 LiftTray 失败 seed 复核的产物清单全部通过校验。
- 留档前全仓回归为 `596 passed, 4 skipped in 342.47s`；Black、Python 编译和 `git diff --check` 通过。这一结果确认当前代码可作为阶段六正式正常任务回归的起点，不提前代表阶段六已验收。

## 2026-08-29：联合目标 PoE、执行器停滞升级与主动验证窗口

### 发现与修复

- 阶段四本地完成的最终目标项此前组合多个参考系边缘支持，会对同一个世界末端几何误差重复计分。改为把当前 mode 的全部最终目标流先变换到世界坐标系，用动作生成相同的精度加权 PoE 构造单一联合末端目标，再对真实末端位姿做一次峰值归一马氏评分。全部40个边界只用正常示范重新标定为 v4：200/200 个边界×示范决策符合正常语义，边界前提前放行0次；WipeDesk `1→2` 的连续周期由旧口径 H=4 修正为 H=3，其余39个边界为 H=2。
- 通用 RLBench 执行器升级为 `stage6_hybrid_cartesian_executor_v11` / `rlbench-stage6-hybrid-cartesian-continuation-v13`。当合法关节目标实际执行后对同一个笛卡尔目标没有改善时，按伪逆、bounded TRAC-IK、小步延续、采样、路径的顺序做一次有界求解器升级；备用层一旦产生真实进展就立刻返回闭环策略重新观测，不隐藏多次策略推进，也没有任务名、seed 或 StateId 特例。
- Handover 首轮 v11 回归暴露主动验证的单周期假阴性：进入 `VERIFY_LINK` 的第一份观测还包含上一条 TASK 动作，却可能被当作探测证据；稳定判定又直接消费单周期关系结果，短暂 external 会过早结束验证并触发破坏性 LINK 恢复。
- 主动验证现只累计验证指令真实发出后的末端—参考系成对响应。至少3个可靠响应且累计运动达到原最小值后，使用统一0.85阈值的窗口共动残差比给出 linked/external 响应方向；只有它与原二元关系滤波器当前有效非 Unknown 决策一致时才稳定结束。该统计不创建第三种关系状态、不替代关系后验，也不放宽关系阈值。

### 验证与结论边界

- 主动验证专项17项通过；阶段二至六、入口守卫、恢复、执行器和正常诊断入口的跨阶段定向回归146项通过，耗时132.93秒；最终全仓回归为 `600 passed, 4 skipped in 316.59s`。
- 重建全部8个闭环 sidecar，并确认当前边界 v4 与主动验证 `3/0.85` 参数均被写入。
- 当前正式 sidecar 下，封存正常 Handover seed `2608000000`、WipeDesk seed `2608000199` 和 LiftTray seed `2608000001` 分别在301、240、106个策略周期成功；三者均为0 InvalidAction、0全IK链耗尽、0控制预算耗尽。
- Handover 中单帧/短窗口 external 没有再立即触发错误恢复。不过该样本的关系滤波与窗口方向仍发生过切换，且 benchmark 在后续短验证之后成功，因此本轮只证明已阻断发现的假恢复链和三个正常样本未回归，不把它表述为完整关系扰动恢复或阶段六最终验收。
- 代码口径已同步到《技术路线报告》《新方法代码开发计划》、闭环 README 和《阶段开发记录》；完整结果索引与 SHA256 记录在 `evaluations/phase6_rlbench_integration/diagnostics/GOAL_POE_EXECUTOR_V11_VERIFY_WINDOW_RESULTS.md`。下一步仍是完整正常任务矩阵，再进行冻结配置下的故障/扰动消融。

## 2026-08-29：执行器 v12 关节连续性修复与八任务正常门控

### 发现与修复

- 扩展正常任务门控时，StoreBottle seed `2608000000` 在 v11 的 tick 102→103 选择了末端近似相同但关节构型不连续的采样逆解：关节向量 L2 跳变4.178553 rad，单关节最大跳变2.887153 rad；瓶子被撞离原位，随后以 `no_legal_reentry_state` 结束。旧 v10 同 seed 成功，且两者在 tick 102 前动作一致，因而问题定位在 v11 新增的同目标停滞升级采样入口。
- 执行器升级为 `stage6_hybrid_cartesian_executor_v12` / `rlbench-stage6-hybrid-cartesian-continuation-v14`。只对物理停滞升级中的采样候选施加与 bounded TRAC-IK 相同的关节连续性约束：单关节不超过0.35 rad、关节向量 L2不超过0.50 rad；普通初始全局采样后备保留。不读取任务名、seed、StateId 或目标物体。
- StoreBottle 同 seed 在 v12 中274周期成功、0 InvalidAction，最大 L2/单关节变化为0.499987/0.347337 rad；定向执行器回归37/37通过。

### 八任务门控

- 当前 v12 下 OpenMicrowave、PlaceCups、StackWine、WipeDesk、Handover、LiftTray、StoreBottle、SweepDust 各一个封存正常样本全部成功，策略周期分别为163、187、167、240、782、106、274、141，均为0 InvalidAction；全部已选关节候选满足连续性上限。
- 八个正常样本均未进入破坏性恢复。Handover 的 VERIFY_LINK 窗口最终给出 linked 响应方向，但 benchmark 在探测期间即成功，因此不把本轮写成“验证完整返回”的真实任务证据；LiftTray 的真实联合事务正常等待并提交。
- 本轮只是八任务首轮正常门控，不是多 seed 成功率或故障消融。详细分项、原始路径和 SHA256 写入 `evaluations/phase6_rlbench_integration/diagnostics/EXECUTOR_V12_NORMAL_TASK_GATE_RESULTS.md`。
- 最终 Black、Python 编译和差异检查通过；全仓回归为 `601 passed, 4 skipped in 320.28s`，0失败。

## 2026-08-29：阶段四 v5、恢复目标终止与正常 N=3 门控

### 发现与修复

- 阶段四旧配置把 `H` 统一限制为至少0.10秒，这不是由每个边界的正常反例产生。v5改为逐边界取“正常进度进入末端窗口前最长连续误就绪段 + 1”，再验证正常末端保持能够持续满足；30个边界为 `H=1`、9个为 `H=2`、WipeDesk `1→2` 为 `H=3`。40/40边界完成标定，200/200边界×示范通过，提前放行0次。
- LiftTray 扩展回归发现：目标关系已经由可靠运动证据稳定满足后，恢复控制器仍继续追逐离线 LINK 路点并可能超时。修复为“关系是目标、锚点是手段”；稳定目标关系即可结束关系目标并进入重入。若主动探测已产生外移，仍必须完成原路返回。该规则不读取任务或事件编号。
- StoreBottle 索引2在距目标约6.4 cm处物理停滞。旧执行顺序让碰撞放宽直线先于碰撞感知绕行，且近目标从不使用RRT。执行器升级为 `stage6_hybrid_cartesian_executor_v14` / `rlbench-stage6-hybrid-cartesian-continuation-v16`：首次近目标仍只用快速本地层；同一目标经真实反馈确认停滞并耗尽本地层级后，先尝试碰撞感知RRTConnect，最后才用碰撞放宽直线。执行器不读取任务名、seed、StateId或物体身份。
- 正常诊断入口新增 `policy_type=dynamac|closed_loop_multistream` 对照选项，默认仍为闭环策略；封存索引、模型和评测协议不重新生成。成功后继续策略执行仍是明确的诊断专用选项，正式成功口径默认为0个额外周期。

### 验证与结论边界

- 执行器定向38项通过；阶段一至六及RLBench相关回归487项通过、4项跳过、0失败，用时268.51秒；最终全仓回归611项通过、4项跳过、0失败，用时319.75秒。
- 当前代码在8任务×封存索引0、1、2上得到23/24成功（95.83%）、5407个策略周期、0 `InvalidAction`。Handover、LiftTray、StoreBottle、SweepDust、OpenMicrowave、PlaceCups、StackWine均为3/3，WipeDesk为2/3。
- WipeDesk索引1在策略完整结束后仍有随机污点未清除；冻结DynaMAC V4在同一封存索引也失败并产生9次非法动作。该样本按失败保留，作为当前低维输入不包含污点分布、固定擦拭轨迹无法保证覆盖的共享能力边界，不作为闭环方法成功或优越性证据。
- StoreBottle三条合计44次近目标RRT尝试、5次非线性路径成功，3/3完成且0非法动作；全部8任务合计166次近目标RRT尝试、6次成功，证明新分支在真实物理链中发挥作用。
- Handover诊断性成功后续执行确认：右臂稳定linked并返回TASK后左臂才越过释放边界，随后策略完成。该续跑不改变正式benchmark成功口径。
- 原始结果、逐周期诊断、口径和SHA256记录在 `evaluations/phase6_rlbench_integration/diagnostics/NORMAL_N3_CURRENT_GATE_RESULTS.md`。阶段六仍未最终验收；下一步是在冻结当前实现后执行同集完整基线与时间停滞、关系失配、抓取失败、意外掉落的故障/扰动消融。

## 2026-08-29：阶段六动作事务、移动目标恢复与受控故障 pilot

### 执行语义修复

- 真实故障回归发现，执行器一次只完成绝对策略目标的物理前缀时，旧适配链仍可能把当前策略周期作为已完成提交，导致正常进度、恢复路点或固定时钟提前消费，夹爪也可能在末端尚未就位时执行。统一改为 `reached / progressed / stopped` 三类结果：只有 `reached` 消费绝对目标；后两者只保留已经发生的物理世界变化，策略状态恢复到同一动作事务，下一周期重新观测后继续同一目标。
- 夹爪命令延迟到对应机械臂位姿 `reached` 后执行；双臂命令要求两臂均到达后原子执行两侧夹爪。外层策略接受口径固定为 1 mm / 0.1°，bounded TRAC 请求与执行后物理完成保持 0.5 mm / 0.05°，2 mm / 1° 只作外部运动链审计。
- 执行配置更新为 `stage6_hybrid_cartesian_executor_v16` / `rlbench-stage6-hybrid-cartesian-continuation-v18`。新协议下重新生成全部8个闭环模型包，确保配置和模型 provenance 一致。

### 意外掉落根因与最终修复

- 严格动作事务暴露出旧成功回合并未完整执行恢复目标，不能再作为有效证据；最终动作语义下意外掉落最初为0/3，失败集中在 `link_waypoint_timeout`。
- 逐周期诊断显示掉落瓶子仍在桌面滑动和旋转，而 LINK 控制每周期按其当前位姿重新实例化锚点，机械臂实际在追逐不断移动的目标，最终在锚点附近耗尽 IK/路径后备。
- 曾试验位置/姿态解耦 IK 与解耦采样。单元测试成立，但真实探针仍失败，成功探针也没有使用这些分支，因此全部撤除，不把无实际增益的执行器复杂度留入正式实现。
- 最终通用修复为：参考系可观测时先连续稳定3周期，再锁定本次 LINK 恢复的单一世界位姿；预抓取/开夹爪阶段若对象相对锁定位姿移动超过1 cm或10°，重置路点并重新等待稳定；夹爪闭合后对象共动不触发重启。该机制不读取任务名、StateId 或故障类型。
- 修复后的两个真实探针均成功，其中第二个在移除所有解耦 IK 实验代码后仍成功，确认有效原因是稳定锚点而非执行器特例。

### 最终组件级 pilot

- 正常 StackWine 封存索引0、1、2为3/3成功，分别177、172、179个策略周期；528个周期全部为 `TASK/task_poe`，辅助模式误触发为0。
- 四类受控故障均使用同一 `fault_protocol.json` 和同三条封存样本。时间停滞下四种方法最终均3/3，但固定时钟在12周期停滞中推进12个状态，闭环方法只推进5/4/4个后冻结并在运动恢复后重新对齐。
- 首次抓取失败、关系失配和意外掉落中，DynaMAC V4、仅闭环进度、闭环进度＋动态角色均为0/3；完整技术路线均为3/3。
- 完整方法真实动作计数确认恢复链实际执行：抓取失败为稳定等待6、LINK锚点48、主动探测60、返回27；关系失配为8/48/60/28并有3次重入对齐；意外掉落为9/64/60/28并有14次重入对齐。
- 最终结果固定为 `final_v21`，机器可读汇总脚本和产物位于 `evaluations/phase6_rlbench_integration/`。该结果是单任务、每格3条的组件级 pilot，不作为论文正式成功率或统计显著性结论；故障样本没有用于标定核心阈值。
- 收口回归结果：阶段五/六定向测试 `142 passed, 1 skipped`；阶段一至六与 RLBench 扩大回归 `509 passed, 4 skipped`；全仓 `633 passed, 4 skipped in 321.03s`。Black、Python 编译、18个正式 JSON 文件解析、`git diff --check` 和最终结果 SHA256 清单全部通过。
- 清理41个过期探针/中间诊断目录和62个旧版回合摘要，移至 `/tmp/essay2608-phase6-obsolete.huVmtO` 作为可恢复临时归档；正式目录只保留 `final_v21`。39条最终逐周期轨迹以确定性 gzip 无损压缩，诊断体积由约213 MB降至约17 MB；17个最终回合摘要另存入版本可追踪的 `results/v1/raw_results`。

## 2026-08-29：阶段六正式评测预注册与8路资源预检

- 在正式结果产生前新增 `evaluations/phase6_formal_evaluation/`。协议固定正常8任务×4方法×200条和故障8任务×4故障×4方法×50条，共160个单元、12800回合；故障 episode 固定为封存索引0至49，不能根据结果更换。
- 四种消融方法共用阶段六 v16执行器。统计口径预先固定为 Wilson 95%区间、exact McNemar、Holm校正、任务宏平均成对 bootstrap 10000次，以及完整方法相对 DynaMAC V4 的5个百分点正常非劣效界限。故障数据仍禁止用于任何核心参数标定。
- 8卡预检确认8张 RTX 4090 可见；调度器根据 NUMA 将128个逻辑 CPU 全部分割到8个互不重叠的 lane，限制 BLAS 线程池，并保存 GPU、CPU、命令、提交、模型与数据身份。当前算法拟合、IK和低维仿真主要是 CPU 工作负载，不能仅凭 `CUDA_VISIBLE_DEVICES` 宣称 GPU 加速。
- 在临时诊断路径对同提交、StackWine 封存 episode 0做渲染准入 A/B：surfaceless EGL 与 Xvfb 均成功，但控制周期分别为178和177，未达到严格轨迹等价。正式协议因此固定 Xvfb 软件 OpenGL；两组临时探针已移至 `/tmp/essay2608-phase6-render-probes-20260829`，不进入正式结果。
- 全部160个单元的只读预检通过：封存集身份为 `rlbench_eval_v2`，V4 release 有效，闭环模型树含28个文件，8个 lane 覆盖128逻辑 CPU；当前状态均为 `PENDING`，尚未启动正式正常或故障矩阵。

## 2026-08-29：执行器物理状态与任务进度解耦

- 正式正常矩阵启动前，PlaceCups episode 0 在严格执行器下长期停在 `k3:t35`。核查发现闭环 worker 把低层 `reached/progressed/stopped` 映射成 `action_executed`，由此间接控制下一周期动作条件进度先验；这使执行器形成了阶段二、三之外的第二套任务进度控制。
- 核心接口改为只记录“任务命令是否实际施加”和“绝对目标是否完成”两个正交事实。已施加命令的引用与下一周期真实观测进入原有 $\bar\beta_t/\beta_t$；执行器状态只作为逐臂物理诊断与绝对目标事务结果，不直接改写进度先验、`StateId` 或 `reference_state`。双臂逐臂状态可区分一臂进展、一臂停滞，但不会据此分别强推任务进度；夹爪仍只在整体物理到达条件满足时原子提交。
- Stage6 执行器同时补齐同一绝对目标的跨周期求解层记忆：无物理改善且耗尽当前层后，下周期从后继层继续；备用层产生真实改善后重置为本地层重新观测。当前身份为 `stage6_hybrid_cartesian_executor_v17` / `rlbench-stage6-hybrid-cartesian-continuation-v19`。
- 定向回归为106项通过、1项跳过；全仓回归为 `646 passed, 4 skipped in 321.84s`，差异检查通过。
- 真实 PlaceCups episode 0 已不再停在 `k3:t35`，而是在第194周期进入最终 `k4:t26`，证明原进度冻结被解除。但其最终末端在首次被 $\beta_t$ 确认时距动作分布均值约4.9 mm、动作相容度0.865，固定1 mm/0.1°执行器完成条件仍拒绝开夹爪；随后反复追均值至500周期上限。该剩余问题属于夹爪提交授权口径，不能与本轮进度解耦混为一项修复，也不能通过任务专用阈值处理。

## 2026-08-29：任务夹爪授权、验证后验写回与路径层物理升级

- 将 TASK 夹爪提交从固定 `1 mm / 0.1° reached` 包络中解耦。当前每臂只在进度估计与 `reference_state` 对齐时获得夹爪授权；非最终技能末端还必须先提交边界，边界提交周期可直接执行入口夹爪。该修改不新增任务进度状态，也不让执行器标签推进 StateId。当时暂为冻结基线保留的 `reached` 顺序在随后正式矩阵中被证明会改变其原固定时钟语义，已由下一节所述通用修复替代。
- 双臂适配器按每臂授权屏蔽或执行夹爪，不再强制把无关的两侧夹爪捆成一个动作。Handover 因而可以先闭合接收臂，同时保持交出臂，后者只在自己的释放边界提交后打开。
- `VERIFY_LINK` 原先虽然得到稳定关系结论，但原路返回产生的短时残差可能在回到 TASK 前降低同一关系后验。现保存稳定周期中原二元关系滤波器的实际后验，并在返回完成时精确提交回同一个在线关系状态；不 one-hot 化、不固定增强置信度、不创建第三种关系状态。
- Handover 一次失败回归还暴露执行器确定性缺口：同一不变目标下碰撞感知线性路径成功生成522次、真实末端却持续停滞，而非线性与碰撞放宽路径调用始终为0。原因是旧代码只在“路径生成失败”时尝试后继路径，未处理“生成成功但物理无效”。现将碰撞感知线性、碰撞感知非线性和碰撞放宽线性拆成可跨周期推进的独立物理反馈层；不读取任务名、seed、StateId 或物体身份。
- 定向回归79项通过；全仓回归为 `651 passed, 4 skipped in 323.72s`。真实 PlaceCups 封存索引0在190周期成功；真实 Handover 封存索引0在245周期首次成功、0 InvalidAction，诊断续跑到270周期时右臂完成 VERIFY_LINK，并以原关系滤波后验 `[0.127675, 0.872325]` 返回 TASK。结果文件为 `integrations/rlbench/results/diagnostics/handover_v21_gripper_q_path_fallback_ep0_h500.json`。
- 正式评测尚未产生结果，因此预注册协议在启动前同步到共用 `stage6_hybrid_cartesian_executor_v17`，实现父提交更新为正式预注册提交 `c1ec93bdee8c513e77f94f0e20e973f8a7b40656`。旧 pilot 的 v16/v18 结果保持原生成身份，不回写或伪装成新执行器结果。

## 2026-08-29：正式矩阵止损——恢复冻结基线时钟与夹爪语义

- 首轮正式正常矩阵中，PlaceCups 封存 episode 0 的 DynaMAC V4 对照跑满1000周期；同一 episode 在冻结 V4 发布结果中应为186周期成功。该差异不是随机样本失败，而是共享执行接口改变了基线语义。
- 根因是阶段六曾让冻结基线在 `progressed/stopped` 时回滚固定时钟，并把夹爪延迟到固定1 mm/0.1°包络 `reached` 后执行。这等价于让低层执行器成为基线的第二套进度控制器，与闭环进度解耦原则相冲突。
- 通用修复为：冻结 DynaMAC 的动作只在事务 abort 时回滚；任何正式提交的策略事务都消费一个原固定时钟状态，主动作由该固定时钟命令显式授权夹爪。执行器三类标签继续保留物理诊断，但不推进、回滚或延迟策略时序。闭环方法的后验进度与边界授权保持不变。
- 事务级定向测试19项通过；相同 PlaceCups episode 0 使用同一阶段六 v17执行器重新运行后恢复为186周期成功、0 `InvalidAction`，与冻结 V4 结果逐项一致。由错误接口生成的部分正式矩阵不纳入结果，清理后重新开始。

## 2026-08-29：正式矩阵二次止损——边界虚拟帧捕获时序

- 正常矩阵运行到 OpenMicrowave 后，冻结 V4 维持接近发布水平，三种闭环消融却稳定退化到约20%至31%。这属于跨方法重复的系统性差异，因此停止该批运行，没有把它当成单次物理随机失败。
- 对同一 episode 的边界逐周期对齐确认：冻结 DynaMAC 在当前周期执行源技能终止连续目标和目标技能入口夹爪，目标技能虚拟帧从下一次 post-action 观测捕获；闭环策略则在边界提交后、桥接动作执行前就用 pre-action 末端位姿建立目标虚拟帧。一次 OpenMicrowave 实测偏差为3.191936 mm，随后把手抓取失败；动态角色与关系 HOLD 是对错误物理结果的正确响应，不是根因。
- 通用修复删除事务提交时的提前虚拟帧捕获。入口夹爪直接读取目标 `StateNode.gripper_commands`，不再为取得夹爪而查询依赖尚未建立虚拟帧的入口连续位姿；下一周期正常观测链自然捕获桥接动作后的真实末端。没有任务名、seed、状态编号分支或新增门控。
- OpenMicrowave 封存正常索引0至9的快速真实门控中，`progress_only`、`progress_dynamic_roles`、`full` 均为9/10；冻结 V4 在相同10条中同样为9/10，四种方法均只在索引8失败。阶段三、四、六、正式运行器和诊断工具定向回归为 `145 passed, 1 skipped`。
- 旧时序产生的半程正式结果仅用于本次根因审计，不参与统计；记录完原因后从项目正式结果目录移出，再以修复后的冻结提交从头运行正常与故障矩阵。

## 2026-08-29：正式矩阵三次止损——关系软先验与绝对重入阈值统一

- 重新启动的正常矩阵中，PlaceCups 前22条里冻结 V4 成功20条，`full` 成功16条；两者结果不同的4个 `full` 失败均为 `NO_PLAUSIBLE_STATE -> no_legal_reentry_state`，因而判定为可重复的系统性偏差，停止本批而没有继续消耗完整矩阵。
- 逐候选审计确认：恢复后候选的机器人峰值归一支持已达0.54–0.69，关系物理方向也正确，但 `[0.7,0.3]/[0.3,0.7]` 软先验与非 one-hot 在线后验的原始内积约0.52–0.59，被直接与0.60固定阈值比较。这与先前已为 `NO_PLAUSIBLE_STATE` 解决的软先验峰值尺度问题同源。
- 实现单一 `relation_state_compatibility`：原始内积先除以离线先验最大分量，再要求在线可靠决策与离线目标具有相同 external/linked 方向；方向相反时分数为0。该分数替换原内积在关系事件候选扩展和重入绝对阈值的用法，不改进度后验、统一解释度、关系滤波、动态角色或 PoE，也不叠加新门控。
- 单元测试显式覆盖“同向软先验原始内积低于阈值仍合法”和“归一后数值较高但方向相反仍拒绝”；相关回归67项通过。
- 使用封存 PlaceCups 索引4、16、18、21重跑真实 RLBench 定向门控，4/4成功。四条都在恢复时选择物理方向正确的 `k4:t24`：原始内积分别为0.5874/0.5850/0.6038/0.6136，统一关系状态兼容度为0.8392/0.8358/0.8625/0.8766；关系方向已反转的 `k4:t25/t26` 分数为0。这证明修复确实作用于原失败路径，而非偶然成功。
- 本批未完成正式数据不参与最终统计；修复和门控留档后将再次从干净结果根目录启动全部12800回合。
