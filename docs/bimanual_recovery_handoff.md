# 双臂关系门控恢复收尾说明

## 当前结论

Phase 6 已实现独立于冻结脚本专家的双臂关系门控恢复监督层，并补齐五类物理工况、四种
对照方法、原始逐步证据、结果评分和只读硬审计。最终冻结开发批次包含 30 条在线/Oracle
恢复试验，硬审计为 `30/30` 通过；完整 Python 测试为 `86 passed`。这些是开发证据，
不是预注册正式结果。

实现提交为 `4214730a1d74bef353bac28629adab82fbe03a87`，源码指纹为
`eff06caaedc64725a69ef148d8408f57bd167c0b0eba0e116ea4ce23d46cf1c9`。正式协议提交
为 `e171cbe6d14f2e3cdfe329b5af5c2050b684ef32`，标签为
`bimanual-recovery-protocol-v1`。

## 正式 v1 的状态

2026-08-04 按协议启动了四方法 × 五工况 × 十 seed 的 200 条正式矩阵。用户随后要求
立即收尾，controller 在完成 95 对 JSON/NPZ 后被明确中止。目录中没有 `summary.json`，
剩余 105 条不存在，也没有运行正式硬审计。因此：

- v1 是“中止且不完整”，既不是通过结果，也不是完整负结果；
- 已生成的 95 条不得与开发结果或后续正式结果拼接；
- 不得补跑剩余条目后继续沿用 v1 的“唯一一次正式运行”表述；
- `outputs/bimanual_recovery/formal_v1` 只保留为本地中止证据，不纳入 Git；
- 论文目前只能引用开发阶段机制证据，不能报告双臂恢复正式成功率。

## 后续恢复方式

若继续正式验收，应新建 v2 协议和输出目录，重新冻结身份，并改用未参与标定、开发、
Phase 5 正式评测及本次中止运行的全新 seed。不得复用 `8700–8709` 作为“未见 seed”。
v2 应完整一次运行、生成 summary，再由 `audit_bimanual_recovery_results.py` 从 NPZ 独立
重算全部 200 条后，才允许写正式报告。

即使后续恢复正式通过，它也只证明冻结脚本专家上的关系触发式恢复，不等于已训练两个
并发的论文忠实双臂 DynaMAC。完整双臂学习仍应单独预注册数据、基线、训练和未见任务评测。
