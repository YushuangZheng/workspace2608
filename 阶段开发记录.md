# 阶段开发记录

## 文档用途

本文档只记录《新方法代码开发计划》各阶段最终保留的最新实现，不记录排查、试错和修复过程。

维护规则：

- 每个阶段完成并验收后，在对应章节记录最终代码、模型语义、配置和验收结论；
- 同一阶段机制发生更新时，直接删除或覆盖旧口径，不并列保留过期方案；
- 未进入当前阶段范围的设计只保留在《新方法代码开发计划》中，不在本文件中写成已实现能力；
- 详细开发过程、异常分析和修复历史继续记录在《开发日志》中。

## 当前阶段状态

| 阶段 | 状态 | 最终交付范围 |
|---|---|---|
| 阶段一 | 已完成并验收 | 统一离线任务模型、关系与场景统计、边界模型、关系事件模板、只读状态查询 |
| 阶段二 | 已完成并验收 | 在线观测与关系—进度联合信念推断 |
| 阶段三 | 已完成并验收 | 动态流角色、运行期精度权重与闭环正常执行控制 |
| 阶段四 | 已完成并验收 | 技能入口守卫、多臂事务推进与正常参数标定 |
| 阶段五 | 已完成并验收 | 主动关系验证、统一关系恢复与完整状态重入 |
| 阶段六 | 已完成并验收 | 环境无关顶层策略、RLBench适配、诊断、组件级故障消融和正式评测协议均已完成；正式正常、动态背景与故障矩阵共14400回合完成并通过完整性审计 |

# 阶段一：统一离线任务模型与只读状态查询

## 1. 最终目标

阶段一从拟合 DynaMAC 所使用的同一批正常成功示范中，统一构建后续在线关系滤波、进度滤波、技能入口守卫和恢复模块需要的离线统计。同时将基础策略的“指定状态动作查询”与“任务时钟推进”解耦。

阶段一只负责离线模型、接口和数据持久化，不实现在线关系后验、进度后验、动态角色、在线边界判定、主动验证或恢复控制。

## 2. 统一状态与查询语义

任务进度状态唯一表示为：

```python
StateId = (skill_index, local_index)
```

mode 是同一进度状态内部的轨迹混合分量，不进入 `StateId`。每只机械臂的状态节点数量严格等于所有技能 `duration` 之和，不乘以 mode 数量。

每个状态保存前驱、后继、技能末端和跨技能后继信息。`DynaMAC.query_state()` 与 `StateQueryAdapter.query_state()` 可以查询任意指定状态和独立 `mode_index`，并允许传入逐流精度权重：

- 权重乘在专家精度上；
- 权重为零等价于移除该专家；
- 显式权重表中未列出的流权重为零；
- 离线 Eq.6 未选择的流不能被运行权重重新启用；
- 查询不改变技能索引、时间索引、mode 路径或虚拟帧状态。

原始 `DynaMAC.act()` 复用同一查询计算后，继续按照冻结 baseline 语义推进游标，因此原有执行路径保持可用。

## 3. ClosedLoopTaskModel

每只机械臂构建一个与基础 DynaMAC checkpoint 指纹绑定的 `ClosedLoopTaskModel`。模型主要包含：

```text
states
skill_states
boundaries
link_anchors
link_pending_events
unlink_events
link_origins
```

每个 `StateNode` 保存：

- 全部 mode 的先验；
- 原有多流轨迹均值 `[M,7]` 和协方差 `[M,6,6]`；
- 各参考系的关系连接分数及 `[P(external), P(linked)]`；
- 每个 mode 的已选参考系和夹爪命令；
- 通过筛选的稀疏场景因子分布。

附加模型使用 sidecar schema v3 保存新增统计，不重复保存基础 DynaMAC 中已有的大规模流参数。加载时校验基础策略指纹和流选择语义，防止离线任务模型绑定到错误 checkpoint。

## 4. 离线关系先验

关系概率空间严格只有：

```text
external / linked
```

对每个 arm-frame、进度状态和内部 mode：

1. 使用末端相对于参考系的跨示范局部位姿分布及 GMSD 生成主关系先验；
2. 在存在充分末端运动时，计算相邻状态的相对运动残差和共动支持；
3. 共动证据只用于排除特殊的稳定但未连接假阳性，不能单独创建 LINK；
4. 缺少运动激励时不把缺证据强解释为 external；
5. 最终保存归一化的 `P(external)` 和 `P(linked)`。

正式事件与逐状态原始证据分层保存：GMSD/协方差分数留在 `demo_relation_scores` 供审计；正式 LINK 到正式 UNLINK 之间由事件状态机校正部署关系的连续性，避免局部低协方差抖动把物理连接错误改回 external。校正后的 `demo_relation_priors` 仍是可推翻的软先验，正式 linked/external 分别保存为 `[0.3,0.7]` / `[0.7,0.3]`，不使用 one-hot。没有正式事件或 Pending 来源的孤立低协方差 linked 脉冲不进入部署关系期望；Pending 只在共同候选区间内保留未确认 linked 假设，不传播 `link_origin`。

虚拟技能帧不进入关系状态空间，也不参与 LINK、UNLINK 或关系失配。

## 5. 技能条件化稀疏场景因子

候选因子只保留两种数学形式：

- 节点因子：单个活动实体确实存在的内部、可变构型；
- 边因子：两个任务相关活动实体之间的相对构型。

普通刚体不强制建立节点因子。机械臂—对象 linked/external、末端相对参考系的技能轨迹、对侧机械臂进度均不作为场景因子重复建模。

### 5.1 对侧末端执行依赖

Eq.6 选中的对侧末端先作为候选。逐 `(skill, mode)` 仅在以下情况保留为连续动作/进度流：

- 本臂活动 `link_origin` 与对臂正式 LINK 或 `LINK_PENDING` 指向同一共享/交接实体；
- 没有普通物理任务参考系，对侧末端是唯一非虚拟目标，且具有稳定非零定向相对几何。

若普通任务实体已经解释动作且不存在上述关系路径，则把只由同步相关性选中的对侧末端从闭环 sidecar 的 PoE、进度轨迹和本地完成目标删除；冻结 V4 不改写。纯同步不生成边界依赖或事务，真正的对臂就绪继续由统一关系/场景守卫表达。筛选完全基于关系图、参考系类型和几何证据，不读取任务名。筛选结果与审计原因保存在 `builder_config.peer_execution_dependencies`，sidecar schema v4 逐状态持久化最终 `selected_frames/mode_selected_frames`；schema v3 仅保留向后只读加载。

逐进度候选范围由当前技能实际使用的参考系、相同 skill 和 mode 内的局部共同激活实体，以及任务接口提供的直接结构绑定产生，不依赖人工实体对白名单，也不生成全对象两两组合。局部邻域半径依次检查 `r=0,1,2`，选择通过跨示范支持率和稳定性检查的最小半径；`r=2` 仍不稳定则删除，且邻域不跨技能边界。

逐状态因子采用按示范留一的前向筛选：机器人局部轨迹是识别基线，不加入名义时间先验或关系兼容度；候选因子必须在绝大多数正常示范上可观测、分布稳定，并可重复提升局部进度识别间隔。与机器人轨迹或关系变量重复的信息通过候选定义和排除规则删除。

## 6. BoundaryModel

每个技能边界均保存一个离线 `BoundaryModel`，其边界前窗口只包含当前技能末端的 `h_b^-` 个状态。

### 6.1 LocalCompletionModel

本地完成模型保存：

- 整个技能末端状态窗口；
- 当前技能最终状态在全部正常示范上的目标分布；
- 当前技能完成时由本臂直接建立或解除、且必须满足的关系集合；
- 每个必要关系在成功终止窗口中的目标关系分布。

阶段一不计算在线 `LocalDone`，也不标定本地完成阈值和连续确认周期。

### 6.2 边界关系条件

候选只来自下一技能所有 affected arms 的 selected frames。每个候选从边界前终止窗口拟合关系支持分布，选择最大支持关系，并要求总体支持、观测可用率和全部正常示范留一折一致。随后删除已由本臂或同一事务中其他机械臂 LocalCompletionModel 保证的条件，以及完全重复条件。

### 6.3 边界场景条件

边界候选由下一技能参考系、边界关系实体及其直接结构关联实体限定，只在当前边界前短终止窗口中拟合，不扩散到当前技能的所有进度状态。硬场景条件必须通过全部正常示范留一覆盖检查，并保存最终支持分布和马氏兼容度阈值。

双臂构建还会补全同一候选库：若一臂经过执行依赖筛选后保留的 `LocalCompletionModel` 最终目标以对侧末端为参考，且双方正常示范边界稳定对应，则把同一双臂末端相对构型作为对侧边界的普通 `scene_condition` 候选。它继续使用统一的全部示范留一覆盖、马氏阈值和在线 `EntryGuard`，不建立独立协作守卫、对侧进度条件或固定时差。单向候选只阻断依赖方；双方共享同一硬条件时才继续进入事务判定。跨臂场景边从两臂各自的专用 `ee_pose` 解析，不依赖 RLBench 额外复制本臂末端参考系字段。

### 6.4 双臂事务元数据

只有左右边界在全部正常示范中稳定同步，并且具有对称的共同硬依赖时，才分配同一个 `transaction_group`。共同硬依赖包括：双方各自与同一实体保持必须成立的 linked 关系；双方对同一实体的正式 LINK 局部轨迹跨越该边界、首次连续确认位于目标技能且获得全部正常示范支持；或双方具有相同硬场景条件。`linked_entry_states` 可因因果闭爪前缀从源技能开始，事务判定使用目标技能内的正式运动学确认状态和最短持续证据，不以区间第一个状态所属技能作判据。只观察对臂状态的单向守卫不表示本臂参与共同物理连接，`LINK_PENDING` 也不能单独创建事务组。独立边界、单向等待边界和只有时间同步的边界保持 `None`。阶段一只保存 affected arms 和事务标识，不执行在线原子提交。

## 7. LINK、LINK_PENDING 与 UNLINK 离线事件

正式事件首先由同一 mode 的全部正常示范共同拟合关系先验，再在完整任务关系序列上使用迟滞阈值和前后稳定窗口检测候选。按示范留一只检查候选能否由其余示范稳定重现；通过后使用全部正常示范拟合最终事件数据。夹爪命令只校验运动学事件的抓取或释放模板，不能单独创建事件。

正式 LINK 与 Pending 的事件前轨迹最大长度均为16个对齐后的离散状态。对当前机械臂的事件终点，先在所有可恢复参考系中查找此前最近一次稳定 linked→external；模板起点取“固定窗口起点”和“该 UNLINK 下一状态”中较晚者。没有先前 UNLINK 时使用原固定窗口；发生截断后不向更早状态补齐。轨迹可以跨技能边界，但不会跨过同一机械臂上一段已经结束的连接，因此不会把上一段抓取或释放混入当前模板。

### 7.1 正式 LINK

充分运动证据确认 external→linked 后，保存事件级 `LinkRecoveryAnchor`：

- 事件前短局部轨迹均值和协方差；
- 夹爪命令；
- 上下文状态；
- 从因果闭爪到首次稳定运动学确认的有限 `linked_entry_states`；长期持有来源另由 `link_origin` 保存。

事件前轨迹允许跨回上一技能。正式 LINK 建立后到对应 UNLINK 前，离线 `link_origin` 和软 linked 关系期望按正常示范路径跨技能和 mode 传播，使后续恢复能够追溯正确的事件级锚点；只有正式 UNLINK 才终止该传播。

### 7.2 LINK_PENDING

当跨示范 GMSD、闭合模板和事件位置一致支持 LINK 假设，但闭合后最短稳定窗口完全缺少有效运动激励，且不存在运动反证时，保存 `LinkPendingCandidate`。

Pending 不是第三种关系概率状态，也不等同于在线 `Unknown`。它使用与正式 LINK 相同的拟合函数保存尚未激活的事件前局部轨迹、协方差和夹爪命令，但：

- 不进入正式 `link_anchors`；
- 不传播离线 `link_origin`；
- 不进入 LocalCompletionModel 的已完成必要关系；
- 不在阶段一执行主动验证。

候选闭合状态本身以及其后原始跨示范协方差继续支持 linked 的连续区间，统一保存可推翻的 `[0.3,0.7]` soft linked 期望；闭合当帧不要求已经具有事后运动激励。该覆盖可与同一 arm-frame 更早的正式事件共存，但不建立离线来源。若同一闭合事件随后在锚点窗口内获得正式 LINK，则只保留正式 LINK，不重复保留 Pending。

### 7.3 UNLINK

充分证据确认 linked→external 后，保存 `UnlinkEventMetadata`：

- 正常关系解除状态；
- 解除后的合法任务重入状态；
- 可选的局部脱离目标；
- 跨示范事件支持信息。

## 8. 最终代码与配置

阶段一最终文件：

```text
source/policy/closed_loop/
├── __init__.py
├── README.md
├── state_index.py
├── task_model.py
├── task_model_builder.py
├── scene_factors.py
├── boundary_model.py
├── relation_events.py
└── query_adapter.py

tests/closed_loop/test_phase1_task_model.py
configs/closed_loop_task_model.json
```

基础策略查询改造位于 `source/policy/dynamac.py`；RLBench 任务实体和结构字段接口位于 `integrations/rlbench/rlbench_dynamac/core/task_specs.py`；公共导出位于 `source/policy/__init__.py`。

## 9. 验收结论

阶段一开发计划中的13项验收标准均有实现和自动化测试覆盖：

- 阶段一专项测试：`26 passed`；
- 当前全仓回归：`492 passed, 4 skipped`；
- Python 格式检查和 `git diff --check` 通过；
- sidecar 保存、加载、基础模型绑定和 Pending 数组往返通过；
- 真实 V4 HandOver 定向只读重建确认右臂保留一个支持率为1.0的 `LINK_PENDING`，并保存16步未激活轨迹模板，同时不生成正式 LINK 或离线 `link_origin`；
- 未启动训练、仿真评测或回放，未改写冻结 V4 模型、数据和正式结果。

阶段一最终状态：完成并通过验收，可以在当前离线模型和查询接口基础上进入阶段二开发。

## 10. 当前明确保留的实现口径

- RLBench `LiftTray` 的任务接口同时提供 `item` 和 `tray`，当前任务角色配置将二者都视为可恢复关系参考系，因此每只机械臂可以分别对两个参考系形成离线 LINK 候选。该候选范围按已确认决定暂时保留，尚未收窄为只对 tray 建模。
- 阶段一验收证明离线模型、接口、统计构建和持久化符合当前开发计划，不等价于已经完成后续在线滤波、控制、主动验证、恢复或仿真成功率验收。

# 阶段二：统一在线观测与关系—进度联合信念推断

## 1. 最终目标与阶段边界

阶段二在阶段一离线任务模型之上实现固定的旁路状态估计链：

```text
动作后名义进度先验
→ 在线 external/linked 关系后验
→ 在线进度后验
```

本阶段只估计并输出 `ClosedLoopBelief`，不改变 DynaMAC 动作、不推进或冻结任务时钟、不分配动态流角色、不在线评估入口守卫，也不生成主动验证或恢复动作。跨技能候选只接受调用者提供的已放行 `BoundaryId`；阶段四以后负责产生该许可。

## 2. 统一运行观测与特征

`RuntimeObservation` 统一保存：

- 当前 tick、末端世界位姿和全部参考系世界位姿；
- 当前夹爪状态；
- 上一命令目标位姿和上一实际末端位姿；
- 逐参考系可见性与跟踪可靠性；
- 场景节点因子需要的实体内部构型字段。

RLBench 低维仿真真值没有显式提供可见性或跟踪协方差时，已有位姿默认 `visible=True`、`reliability=1`，但字段和门控完整保留。实体内部构型仅在任务接口实际提供时读取，不为普通刚体构造伪节点状态。

`RuntimeFeatureBuilder` 每周期统一计算一次实际末端运动、上一命令目标运动、参考系世界运动、末端—参考系相对位姿、相邻周期相对运动残差、夹爪变化、动作激励、命令响应一致性和逐关系信息权重。旋转残差先从基线半角表示换算为物理角度，再按 `0.1 m/rad` 折算到共同运动尺度。

在线 tracker 可用 `q` 或 `-q` 表示同一物理姿态。闭环运行特征在计算相邻周期运动前，先把当前四元数对齐到上一姿态的同一半球；这只消除表示符号跳变，不改变实际位置、朝向或冻结 DynaMAC 的连续 S3 拟合语义。

关系决策具有受约束的时间连续性：已经由有效运动证据确认的 external/linked，在参考系继续可见、跟踪可靠且二元后验仍以足够置信度支持相同状态时，可跨越暂时缺少激励的静止周期保持判定，同时保留 `informative=False` 以表明本周期没有新增运动证据。初始静止、未确认 Pending、不可见、不可靠或后验冲突均不得继承旧判定；一旦因这些原因回到 Unknown，稳定判定缓存同时失效，重新可见后不得在无新运动证据时复活旧判定。

关系信息权重要求参考系前后周期均可见且可靠，同时机器人存在实际动作响应。静止、缺少上一帧、不可见或跟踪不可靠时不会把近零相对残差当作 linked 证据。

## 3. 动作后名义进度先验

`ProgressPriorBuilder` 从上一周期 `beta` 构造 `bar_beta`。默认转移质量为：

```text
未完全完成/原状态：0.20
实际动作目标：      0.65
目标的直接后继：    0.15
```

局部候选默认覆盖名义状态前1、后2个状态。技能末端在边界未放行时停留于当前技能末端；边界已由外部许可时只进入下一技能首状态，不在一次转移中连续跨越多个技能。

统一执行链中，上一周期实际查询的 `reference_state` 是 DynaMAC 动作的目标状态：0.65 正常质量落在该目标本身，0.15提前质量落在其直接后继；0.20未完全完成质量仍按上一周期 `beta` 分布保留，不用 one-hot 动作锚点取代同技能后验。固定示范回放使用真正产生当前观测的示范动作目标 StateId；未施加到环境的控制器查询只作审计，不能作为该离线观测的因果输入。

边界守卫放行并提交事务后，执行游标和下一周期内部进度后验原子进入下一技能首状态；当前周期返回的源技能信念继续作为审计记录，关系后验、场景观测和 mode 不重置。联合事务只有全部成员同时放行时才同步投影，正常执行不能回退穿过已提交边界。提交周期保持 DynaMAC 的混合桥接动作：连续位姿仍取源技能终止状态，夹爪直接取已提交入口 `StateNode`。目标技能固定虚拟帧不在 pre-action 快照中提前建立，而由桥接动作执行后的下一次真实观测捕获，再作为可靠内部参考参与入口技能查询。

## 4. 在线关系滤波

`RelationFilter` 对每个物理 arm-frame 关系只维护：

```text
[P(external), P(linked)]
```

单周期更新依次组合：

1. external/linked 持久转移先验；
2. 由 `bar_beta` 加权的阶段一示范关系先验；
3. 由当前相对运动残差、实际末端运动、上一命令响应和夹爪弱上下文得到的动作条件化观测似然。

第2、3项作为同一个观测更新整体由逐关系信息权重调温；信息权重为0时只执行持久性预测，不在 HOLD/静止周期反复注入示范先验。动作条件化似然最终按平移/旋转的实际动作幅值分别计算有限分量似然，每个分量先混入状态无关的小概率离群项，再以动作幅值为权重进行混合。该口径将“哪一个运动分量当前提供可靠响应”作为被边缘化变量，不使用会让单个接触抖动分量否决共动证据的几何乘积。它没有增加第三种关系状态、任务特例或新门控；单分量激励与原公式一致，持续的全分量失配仍输出 external。

夹爪只在存在运动信息时提供弱似然偏置，不能单独决定 linked。`Unknown` 是二元后验之上的决策状态，不是第三种关系概率；动作激励不足、不可见、跟踪不可靠、后验熵过高或最大概率不足时输出 `Unknown`，同时保留归一化二元后验供以后继续更新。若当前参考系仍可见可靠，最近一次由有效动作响应确认的 external/linked 离散方向可以在无信息周期继续保留，而软后验仍按持久性模型演化；新的反向有效证据会通过原二元滤波正常覆盖该记忆。

`RelationChange` 只报告前后均为稳定二元判定的 external↔linked 变化；中间的 Unknown 不被误报为 LINK/UNLINK。

## 5. 候选状态统一评分

`StateEvaluator` 对每个候选状态分别计算并保存：

- 机器人局部轨迹支持；
- 稀疏场景状态支持；
- 当前关系后验与示范关系先验的兼容度。

物理轨迹流的参与权重为可观测指示、跟踪可靠性和 `P(external)` 的乘积。`Unknown` 作为二元关系后验之上的离散决策，不再删除可靠的连续位姿证据；关系不确定性由 `P(external)` 软降权表达。linked 流因此不会凭极小协方差独占进度判断，不可观测或不可靠流仍为零权重。离散关系兼容度、动态角色和边界守卫的 `Unknown` 门控不变。虚拟技能帧只作为固定执行参考系，按可靠性参与轨迹匹配，不进入关系滤波。

机器人轨迹和连续高斯场景因子在线使用峰值归一化支持：

$$
\widetilde p(x\mid s)
=
\frac{p(x\mid s)}{p(\mu_s\mid s)}
=
\exp\left(-\frac12d_M^2(x,s)\right).
$$

这保留协方差对偏离方向和尺度的约束，但删除不同状态之间由高斯峰值高度产生的 `logdet` 偏置。未显式选择 mode 时，先在各可用 mode 内计算归一化支持，再按该状态 mode 先验混合。

当前实际执行路径固定每个技能的轨迹 mode。对该情形，多条对象中心流的局部峰值不一定能由一个世界末端位姿同时达到，因此机器人绝对支持进一步减去同一参考系变换与 PoE 所定义的联合可达峰值。该修正复用基础策略的变换和 PoE，实现量相对于真正可实现的多流最优状态归一；不建立第二套 SE(3) 或动作融合路径。

事件感知候选扩展和恢复重入的机器人绝对阈值统一作用于该联合可达峰值归一支持。逐流支持几何平均 `robot_compatibility` 只保留作审计，不再作为另一道准入阈值；候选扩展0.01和重入0.001的数值不变，但现统一表示 $[0,1]$ 联合可达峰值尺度。

完整高斯 `log p` 不参与进度后验，但仍逐候选、逐帧/场景因子、逐 mode 保存以下审计项：

```text
raw_log_likelihood
normalized_log_support
mahalanobis_squared
covariance_log_determinant
dimension
mode_weight
```

external/linked 原始关系兼容度继续计算当前 `q_t` 与离线关系先验的内积，并原样参与进度后验。为使 `NO_PLAUSIBLE_STATE` 的单一绝对门限不受 `[0.7,0.3]` 等软先验自身峰值上限影响，`normalized_explanation_score` 中的每个关系重叠额外除以当前离线先验的最大分量。该峰值尺度支持仅用于绝对可解释性，原始内积、正确/错误关系的相对比例及所有在线关系机制不变。

在线机器人轨迹、连续场景因子和边界目标分布在计算姿态残差前，同样先将观测四元数对齐到分布均值的同一半球。因此物理等价的 `q/-q` 得到完全相同的马氏距离和支持度；原 `pose_log_world` 及冻结 baseline 的拟合、PoE 和执行路径不变。

## 6. 进度后验与状态输出

`ProgressFilter` 在 log-space 中实现：

$$
\beta_t(s)
\propto
\bar\beta_t(s)
\widetilde L_t^{\mathrm{robot}}(s)
[\widetilde L_t^{\mathrm{state}}(s)]^{\lambda_x}
[C_t^{\mathrm{rel}}(s)]^{\lambda_r}.
$$

`ProgressEstimate` 保存先验、后验、名义状态、估计状态、置信度、熵、最佳解释支持度和以下状态：

```text
ALIGNED
FORWARD_REALIGNMENT
BACKWARD_REALIGNMENT
LOW_CONFIDENCE
NO_PLAUSIBLE_STATE
```

当全部候选均低于最低解释支持度时，后验保留动作后名义先验并报告 `NO_PLAUSIBLE_STATE`，不把全部近零似然强行归一化为虚假高置信结果。本阶段不据此冻结任务时钟；阶段三读取该状态后再决定 HOLD、REALIGN 或 ADVANCE。

## 7. 关系事件感知候选扩展与单周期更新

默认进度推断只检查局部候选。当出现可靠 external↔linked 变化且局部候选均无法解释观测时，`BeliefUpdater` 按时间顺序查询未来关系兼容状态段；只保留机器人轨迹、关系和状态要求的场景因子全部达到支持阈值的状态。未放行技能边界不能跨越。

该扩展允许动作提前完成后对齐到最近的未来正常状态，同时要求真实机器人和场景状态共同匹配，因此搬运中掉落不会只凭 external 关系跳到未来释放状态。

每个递增 tick 严格只执行一次：

```text
progress prior
→ relation posterior
→ progress posterior
```

重复或倒序 tick 直接拒绝。`ClosedLoopBelief` 保存共享运行特征、全部关系估计、进度估计、关系变化、局部候选、扩展候选和所有候选的分项评分。

## 8. 最终代码与配置

阶段二新增文件：

```text
source/policy/closed_loop/
├── runtime_observation.py
├── runtime_features.py
├── progress_prior.py
├── relation_filter.py
├── state_evaluator.py
├── progress_filter.py
└── belief_updater.py

tests/closed_loop/
├── test_phase2_belief_inference.py
└── test_phase2_rlbench_replay.py

configs/closed_loop_belief.json
```

配置文件与 `BeliefUpdaterConfig` 默认值逐字段往返校验；阶段二模块通过 `essay2608.policy.closed_loop` 和 `essay2608.policy` 公共导出。

## 9. 验收结果

开发计划中阶段二12项验收标准均有自动化覆盖：

- 同步共动使 linked 后验上升，机械臂单独运动使 external 后验上升；
- 双方静止、不可见、跟踪不可靠或缺少上一帧时输出 Unknown；
- 名义先验集中于实际动作目标，并严格服从边界事务；
- 进度后验支持向前和向后校正；
- linked 轨迹流按 `P(external)` 抑制；
- 正常提前释放能够扩展到未来匹配段，搬运中掉落不能仅凭关系跳到释放段；
- 单周期更新顺序和逐候选高斯/关系分项审计通过；
- StackWine 五条正常 V4 示范旁路回放通过；
- 四元数 `q/-q` 等价性、上一周期真实动作锚点、静止期已确认关系保持及可见性断点均有定向回归；
- 阶段二专项单元与真实数据回放共18项随全仓回归通过；
- 当前全仓回归：`492 passed, 4 skipped in 211.84s`；
- Black、Python 编译和 `git diff --check` 通过。

未启动训练或 RLBench 仿真，没有改写冻结 V4 数据、模型或正式结果。阶段二实现停留在旁路信念推断边界并已通过确认；阶段三只读取其输出，不反向改变阶段二的单周期更新语义。

# 阶段三：动态流角色与闭环正常执行控制

## 1. 最终目标与阶段边界

阶段三将阶段二输出的 `ClosedLoopBelief` 接入正常动作生成和技能内任务时间控制，建立：

```text
关系—进度信念
→ 动态流角色
→ 运行期精度权重
→ HOLD / REALIGN / ADVANCE
→ reference_state 动作查询
```

本阶段只实现 `TASK` 模式下的正常执行控制。跨技能入口守卫和多臂事务属于阶段四；`VERIFY_LINK` 移动、LINK/UNLINK 恢复及任务重入属于阶段五。本阶段可以输出恢复意图和主动关系验证请求，但不执行相应动作。

## 2. 动态流角色

`FrameRoleRouter` 为当前 `StateId`、当前轨迹 mode 中已经通过离线任务参数选择的流分配动作角色；此外，当前进度具有正式 `link_origin` 的物理关系即使对应专家已不再被当前 mode 选择，也继续作为零动作权重关系条件接受 `MONITOR/RECOVER/DEFER` 路由。普通未选流不参与本周期动作，也不会因其关系 Unknown 阻断推进；虚拟技能帧保持固定 `EXECUTE` 参考系，不进入 external/linked 关系空间。

物理流的控制期望关系按进度后验加权：

$$
\pi_{f,t}^{\mathrm{ctrl}}(z)
=
\sum_s\beta_t(s)\pi_{f,s}^{\mathrm{demo}}(z).
$$

当最大期望关系概率达到 `0.7` 时，形成 external 或 linked 期望，否则期望关系为 Unknown。最终路由为：

| 期望关系 | 在线关系 | 角色 | 正常执行作用 |
|---|---|---|---|
| external | external | `EXECUTE` | 以连续正权重参与 PoE |
| linked | linked | `MONITOR` | PoE 权重为0，关系滤波继续观测 |
| 可靠 external/linked | 可靠相反关系 | `RECOVER` | 权重为0、阻断推进、输出恢复意图 |
| 期望方向模糊 | 稳定 external/linked | `DEFER` | 保持安全权重，不误报失配，不单独阻断进度 |
| 期望方向明确 | 关键关系 Unknown | `DEFER` | 通常阻断推进并保持上一可信执行权重 |

`DEFER` 不把 Unknown 强制解释成 external 或 linked。若之前可靠 external，则保留当时的连续执行权重；若之前可靠 linked，则保留0。唯一的无历史启动例外是：示范期望和二元后验都明确支持 external，参考系可见可靠时，以可靠性与 $q(\mathrm{external})$ 的乘积安全降权执行，用自然动作产生关系激励；角色仍为 `DEFER`，不写入 external 判定。关系滤波持续观测全部建模物理参考系，路由器缓存未选流的稳定关系权重，使新技能首次选中某一流时可以读取此前可信证据。正式 linked 的未选关系可阻断或产生恢复意图，但始终标记 `selected_offline=False`、执行权重为0且不进入动作查询，不能借监控重新启用 PoE 专家。

正式 LINK 的在线确认不使用固定状态数宽限。每个 occurrence 直接复用阶段一学习的完整 `linked_entry_states` 连续区间；该有限区间从全部对齐正常示范均已发出因果闭爪开始，到首次稳定运动学 LINK 证据满足最短持续要求时结束。正式 LINK 后持续到 UNLINK 的来源由 `link_origins` 单独表示，不能扩大确认宽限。确认区间只有运行轨迹从事件直接前驱沿任务拓扑进入时才激活；reset、恢复重入或任意索引跳转不能取得窗口。直接前驱上的 external/Unknown 不得阻断建立 LINK 所必需的直接后继命令；进入窗口后允许抓取、夹爪闭合和初始自然携带产生证据，相应流保持零权重 `DEFER`。当前查询状态领先 beta 一个周期时，属于该 occurrence 的物理参考系即使未被 Eq.6 选中也进入零权重关系监控，正式事件直接提供 linked 期望而不恢复 PoE 权重。入口末状态在 beta 尚未到达时继续完成自然动作；若该末状态本周期已出现可用、指向 linked 的运动学响应，但持续后验尚未达稳定阈值，只允许一个直接拓扑后继作为最后确认动作。该动作不直接确认 linked，不连续向后外推；下一周期未由原关系滤波器稳定确认，则结束自然确认区间。此时若出现新的、信息充分且可靠的 external 运动响应，才直接进入普通 LINK 恢复；若参考系仍可见可靠，但本周期因激励不足而无法稳定判定，则对同一 occurrence 生成 `VERIFY_LINK`，不得把事件前遗留的 external 决策当作本次抓取失败的新证据。beta 确认末状态且不满足该有限条件后结束宽限；同一事件一旦确认 linked，后续断连不得重复使用窗口。配置和模型包只保存该事件区间语义，额外字段由严格配置检查拒绝。

## 3. 运行期精度权重

`EXECUTE` 物理流使用：

$$
w_{f,t}
=
v_{f,t}q_t^f(\mathrm{external}),
$$

虚拟流使用其观测可靠性；`MONITOR/RECOVER` 权重为0；`DEFER` 使用上述可信历史规则。`WeightedPoEExecutor` 将权重传入现有 DynaMAC 指定状态查询：

$$
\widetilde\Lambda_{f,t}=w_{f,t}\Lambda_{f,t}.
$$

权重只缩放协方差对应的精度，不修改专家均值。显式全1权重与冻结 PoE 数值一致，零权重等价于移除专家。离线 Eq.6 未选择的模态流不能被运行权重重新启用；所有当前执行权重为0时返回 `action=None` 并 HOLD，不采用无审计回退。

## 4. 闭环任务游标与统一提交

`ClosedLoopCursor` 明确分离：

```text
nominal_state   动作后先验认为应该达到的状态
estimated_state 当前观测推断的实际状态
reference_state 当前真正用于查询动作的状态
```

`ClosedLoopExecutionController` 是正常执行进度变更的唯一提交入口。角色路由和加权 PoE 查询均不能直接修改游标，同一 tick 不能重复提交。

- `HOLD`：当前目标未完成、控制相关的进度低置信/无可解释状态、关键关系 Unknown、可靠关系失配、后继未就绪或候选越过技能边界时保持旧引用，并继续查询旧 `reference_state` 进行伺服；
- `REALIGN`：后验可信且估计状态与名义状态不一致，或出现可信的非直接后继索引修正时，直接把引用改到 `estimated_state`；不依次反向执行中间状态，也不推进基础 DynaMAC 时钟；
- `ADVANCE`：若可信进度已到达当前 `reference_state`，则查询并提交它的同技能直接后继；若观测已可信地提前到达旧引用的直接后继，则提交该后继。每周期最多提交一条拓扑边，不会二次计算后继。

若拟议新引用的角色阻断或没有正权重执行流，控制器撤销该提交并继续查询旧引用。跨技能状态统一 HOLD 并标记 `skill_boundary_requires_guard`，等待阶段四放行。

离散动作目标是否到达由同一进度后验判定：可信估计仍在当前引用之前时 HOLD，已到达当前引用或其直接后继时再按拓扑推进。若 beta 已确认当前引用，但观测中的夹爪还没有体现当前 `StateNode` 的开合命令，则本周期保持同一引用并提交该离散命令；下一周期仍由原 beta、关系和边界逻辑决定是否推进。该动作完整性检查不读取 `reached/progressed/stopped`，不会形成第二套进度控制。上一命令与末端的连续高斯相容度字段仍保留计算和诊断，但不作为另一个进度硬门控。

阶段二逐状态后验仍原样输出 `LOW_CONFIDENCE` 或高置信 `BACKWARD_REALIGNMENT`。阶段三执行器对这两类局部离散差异构造控制等价类：候选必须位于同一技能的连续局部状态，当前 mode 已选流、动态角色、期望/实际关系方向、执行权重、阻断/恢复/验证语义及夹爪命令一致，并且当前观测下的 PoE 位姿目标两两达到 `minimum_action_equivalence_compatibility`。包含 MAP 状态的等价类概率质量作为聚合控制置信度，所有类质量计算归一化类别熵；两者继续使用阶段二原置信度和熵阈值。`LOW_CONFIDENCE` 通过时解除该项阻断；高置信 `BACKWARD_REALIGNMENT` 还要求 MAP 是当前引用的同技能直接前驱且二者同属已接受类，才把当前引用按控制等价完成。两者都只推进一个同技能直接后继。该处理不修改原始后验、MAP、状态标签或诊断，也不覆盖 `NO_PLAUSIBLE_STATE`、高置信多状态落后、未共同通过等价检查的落后状态、关系阻断、无动作流和技能入口守卫。

## 5. 正常状态失配累计

`MismatchTracker` 独立累计：

- `NO_PLAUSIBLE_STATE`；
- `RELATION_MISMATCH`；
- `PERSISTENT_HOLD`；
- `STALLED_PROGRESS`。

默认连续周期阈值分别为 `3 / 3 / 20 / 20`。短暂异常只更新计数，不发布事件；达到阈值后只发布一次 `MismatchEvent`，条件恢复后计数和发布门闩复位。本阶段的事件只包含状态、持续周期、相关参考系及恢复意图，不调用任何恢复轨迹。

## 6. 未决 LINK 主动验证请求

`RelationVerificationRequest` 只在以下条件同时满足时生成：

- 请求必须引用唯一的阶段一 LINK occurrence：要么是当前上下文命中的 `LinkPendingCandidate`，要么是本次执行已因果穿过、自然确认区间已结束但仍未稳定判定的正式 LINK；
- Pending 必须在后续正常状态或下一边界中明确被需要；正式 LINK 必须仍是当前状态的有效 `link_origin`；
- 当前关系没有稳定判定为 linked，且本周期没有新的、信息充分的 external 反证；
- 当前和上一周期都具有该参考系观测，配对跟踪可靠性不低于阈值；
- 关系信息权重低于阈值，即未决的原因确实是当前动作激励不足。

终止 Pending 若后续没有 linked 必要性，不生成请求。阶段二 `RuntimeFeatures` 使用 `frame_pair_available` 和 `paired_tracking_reliability` 复用现有观测信息；没有新增 Unknown 原因枚举。阶段三只输出带 `event_id` 和关系上下文状态的请求，不执行探测移动、不直接改写关系结论，也不回写离线模型。

## 7. 最终代码与配置

阶段三新增：

```text
source/policy/closed_loop/
├── frame_roles.py
├── weighted_poe.py
├── execution_cursor.py
├── execution_controller.py
└── mismatch.py

tests/closed_loop/
├── test_phase3_execution_control.py
└── test_phase3_rlbench_replay.py

configs/closed_loop_execution.json
```

阶段二的 `runtime_features.py` 保留当前/上一观测的成对可用性和成对可靠性输出，并使用物理等价四元数的同半球代表元计算相邻周期残差；关系与进度更新公式不变。阶段三类型均通过 `essay2608.policy.closed_loop` 和 `essay2608.policy` 公共导出。

## 8. 验收结果

开发计划中阶段三14项验收标准均有自动化覆盖：

- 四种角色及其 PoE 参与、监测、阻断和意图语义通过；
- Unknown 单帧不会抹除可信历史权重，无历史时不会自行启用专家；
- 全1/零精度权重分别与冻结 PoE一致/等价于移除专家，专家均值不变；
- HOLD 持续查询当前引用，REALIGN 只作索引跳转，ADVANCE 只提交一个直接后继；
- 关系失配阻断推进，持续证据达到门槛后才产生失配事件；
- 路由器不改任务游标，统一控制器拒绝同 tick 重复提交；
- Pending 在可见可靠但激励不足且后续确需 linked 时生成验证请求，终止无依赖 Pending 不生成；
- 控制等价聚合的正反例覆盖 `LOW_CONFIDENCE` 与单状态 `BACKWARD_REALIGNMENT` 的相邻等价动作安全推进、不同位姿继续 HOLD、不同夹爪继续 HOLD、高置信多状态落后继续 HOLD及跨技能禁止聚合；原始进度标签不被改写；
- StackWine V4 正常示范只读回放中无恢复误报、全部状态查询动作可用，并观测到新选流 Unknown 时复用此前可信正权重；
- 阶段三执行控制专项25项通过；加入阶段六调用链后的闭环全阶段回归101项通过；最终全仓回归为 `533 passed, 4 skipped in 262.51s`；
- Black、Python 编译、阶段三定向 mypy 和 `git diff --check` 通过。

阶段三算法仍严格停止在正常执行、请求和意图输出边界；RLBench 观测适配、策略事务和仿真调用属于阶段六。控制等价聚合没有改写冻结 V4 数据、模型或正式结果。

## 9. 组件级 A/B 补充评测

在冻结 V4 的 8 个任务、12 套机械臂模型和每任务 5 条正常示范上运行确定性离线反事实组件 A/B。当前正式 `results/v7` 包含 300 组进度试验、190 组关系试验和 11890 条逐周期记录。

- 跳帧扰动下，StateId MAE 从 `1.9843` 降至 `0.1804`，降低 `90.91%`；卡顿扰动下从 `3.0955` 降至 `0.1569`，降低 `94.93%`。两类扰动的240组试验中新方法胜率均为100%。
- 同步正常回放中，固定时钟按构造 MAE 为0，在线估计 MAE 为 `0.0636`，表示获得时间失步鲁棒性的同时保留了很小的同步代价。
- 45组正式断连扰动检测率100%、平均阻断率99.81%；45组正常对照检测误报0，仅有0.93%保守阻断。
- 50组伪连接扰动检测率和阻断率均为100%；50组正常对照检测误报和阻断率均为0。
- 断连候选严格限定于正式 LINK 的 `link_origin`；Pending 和孤立低协方差脉冲不被当作已确认关系。故障结果没有用于调整在线阈值。

该结果支持时间失步下的进度收益，以及伪连接和正式断连下的动态关系角色增益；不证明独立数据泛化、主动验证/恢复成功率或完整任务成功率。配置、源码/模型/示范哈希、逐试验数据、逐周期轨迹和校验和保存于 `evaluations/phase23_component_ab/results/v7`。

# 阶段四：对象中心技能入口守卫与多臂事务推进

## 1. 最终目标与阶段边界

阶段四在阶段二、三共享的在线信念和正常执行链之后，完成：

```text
同一 pre-action 场景快照
→ 各机械臂本地完成度
→ 边界关系与场景条件
→ 各臂 TransitionRequest
→ 独立或事务式统一提交
```

本阶段只评估并提交正常技能边界。必要 linked 关系命中 `LinkPendingCandidate` 时可以输出 `RelationVerificationRequest`，但 `VERIFY_LINK` 探测移动、关系恢复和任务重入仍属于阶段五。

## 2. 本臂局部完成度

每只机械臂只使用自身的进度后验、最终目标状态和本臂必要关系，计算：

$$
R_{i,b,t}^{\mathrm{local}}
=P_{i,b,t}^{\mathrm{end}}
L_{i,b,t}^{\mathrm{goal}}
C_{i,b,t}^{\mathrm{own-rel}}.
$$

- $P^{\mathrm{end}}$ 对阶段一保存的整个终止窗口求进度后验和；
- $L^{\mathrm{goal}}$ 使用当前 mode 最终状态全部目标流的联合峰值归一高斯支持；
- 每项本臂关系兼容度为在线 $q_t$ 与离线终止关系分布的内积，多项取最小值，空集合为1；
- 任一本臂必要关系 Unknown、不可见或跟踪不可靠时，本周期证据不可用、`LocalDone=False`，但不直接产生异常；
- 只有 $R^{\mathrm{local}}>\theta_{i,b}^{\mathrm{local}}$ 连续满足 $H_{i,b}$ 个控制周期才得到 `LocalDone=True`。

## 3. 边界入口条件

`EntryGuard` 直接读取当前控制周期的 `ClosedLoopBelief`：

- 关系守卫检查目标关系后验严格大于对应阈值，且关系必须可观测、可靠、非 Unknown；
- 场景守卫从当前 `RuntimeFeatures` 读取节点构型或实体间相对构型，同时严格检查观测可靠性和阶段一保存的边界兼容度阈值；
- 本地完成、全部关系条件和全部场景条件使用逻辑与，并分别保留兼容度、可靠性、原始满足状态、连续周期和阻断原因；
- 必要 linked 关系只有在命中 Pending、可见可靠且信息权重不足时才附带验证请求，边界仍保持阻断；
- 配置缺少某个边界的正常标定项时失败关闭，不使用隐式通用阈值代替 `theta_local/H`。

## 4. 多臂统一评估与提交

`MultiArmBoundaryController` 要求所有机械臂信念来自同一递增 tick。每个周期先完成所有边界评估，再由 `TransitionTransactionCoordinator` 提交：

- 无事务组的独立边界按各自许可异步推进；
- 同一事务组必须收到全部成员请求且全部许可，才在同一周期原子提交；
- 完整候选批次先全部校验，再修改任何控制器游标；
- 跨界把 `reference_state` 和下一周期内部进度后验原子投影到下一技能首状态；当前周期已经返回的 `nominal_state/estimated_state` 仍保留源技能真实估计作为审计，关系后验不重置；
- `TransitionCommitResult.permitted_boundaries` 明确提供给下一周期阶段二，使进度先验和候选扩展只越过已提交边界；
- 未放行或等待事务时保持当前引用状态，阶段三仍查询当前状态动作并使用最新参考系位姿动态伺服。

## 5. 正常数据标定

逐任务正式运行配置位于 `configs/closed_loop_boundary/`。标定只使用冻结 V4 的五条正常训练示范，不使用故障数据：

1. 直接回放原始 RLBench 控制周期观测，不用对齐后的离散状态数代替时间；
2. 控制周期读取自 `RLBench/rlbench/task_environment.py::_DT=0.05s`；
3. 在每个正常边界终点冻结当前场景并继续运行同一关系—进度更新链，形成2秒末端保持回放；
4. `theta_local/H` 只读取本地完成分数，不把边界关系/场景守卫是否已放行作为本地正样本筛选条件；若正常边界前最高分低于正常末端最低分，`theta_local` 取二者中点；若有重叠，取末端最低分的0.8，并由既有连续确认消除短暂提前脉冲；
5. 每个边界的 `H` 取正常进度进入末端窗口前最长连续误就绪段加一，并由正常末端保持验证能够持续满足；不附加与边界数据无关的统一最短秒数；
6. 最终再把正式参数送入真实 `EntryGuard` 对全部正常数据做第二遍决策复核：守卫已满足时必须放行；可观测的单向跨臂条件尚未满足时必须继续等待。

当前30个边界标定为 `H=1`，9个边界为 `H=2`；WipeDesk `1→2` 的正常边界前最长存在2周期短脉冲，因此为 `H=3`。

## 6. 最终代码、配置与评测

```text
source/policy/closed_loop/
├── boundary_runtime.py
├── entry_guard.py
├── transition_transaction.py
└── bimanual_controller.py

configs/closed_loop_boundary/
└── <task>.json

tests/closed_loop/test_phase4_boundary_guard.py

evaluations/phase4_boundary_calibration/
├── config.json
├── run.py
└── results/v5/

evaluations/phase4_transaction_integration/
├── config.json
├── run.py
└── results/v1/
```

开发前输入就绪性检查继续保存在 `evaluations/phase4_boundary_readiness/results/v7`。当前40个边界统一使用 `evaluations/phase4_boundary_calibration/results/v5` 的运行配置；该版本以当前 mode 全部最终目标流在世界坐标系中的精度加权 PoE 构造单一联合末端目标，并只对真实末端位姿评分一次，同时按逐边界正常误就绪段标定 `H`。对侧末端执行依赖筛选的输入证据保存在 `evaluations/peer_execution_dependency/results/v1`，真实 LiftTray 联合事务的逐周期集成结果保存在 `evaluations/phase4_transaction_integration/results/v1`。

## 7. 验收结论

- 40/40 个边界完成 `theta_local/H` 正常数据标定；
- 当前40个边界中30个边界前/正常末端支持天然可分，10个由同一连续确认机制消除短暂提前脉冲，没有增加额外门控；
- 200/200 个“边界×正常示范”通过最终 `EntryGuard` 运行配置决策复核；进入末端窗口前提前放行0次，正常末端保持后200次得到与正常边界语义一致的放行或定向等待结果；
- StoreBottle 左右臂4个边界和 WipeDesk 3个边界全部通过；SweepDust 右臂 `1→2` 保持正式解释度门限0.001并正常放行；
- 单元测试覆盖未完成阻断、Unknown/不可靠阻断、稳定周期、跨臂关系、Pending请求、同快照约束、独立异步推进、联合事务提交和等待期间继续动作查询；
- LiftTray 的 `left:1→2` 与 `right:1→2` 由双方共同建立跨边界 LINK 的全示范证据形成一个真实 `transaction_group`；其两侧入口守卫在5/5条正常示范中共同就绪。HandOver、StoreBottle 和 SweepDust 未满足对称共同硬依赖，继续保持异步或定向条件路径；
- 真实 LiftTray 集成回放对右臂施加两周期受控进度延迟：5/5条示范中左臂先许可时双方游标均保持，20个周期部分提交为0；右臂恢复真实终端信念并满足 `H=2` 后，5/5条示范都经 `MultiArmBoundaryController → TransitionTransactionCoordinator` 在同一 tick 原子提交到技能2；
- 阶段四专项测试9项通过，阶段一事务识别新增2项定向测试，全仓回归为 `503 passed, 4 skipped in 254.94s`；Black、Python编译、`git diff --check` 和正式结果 `SHA256SUMS` 通过；

# 阶段五：主动关系验证、统一关系恢复与任务重入

## 1. 阶段目标与边界

阶段五在阶段二的在线关系—进度信念、阶段三的验证请求/恢复意图和阶段四的边界请求之上，实现独立的：

```text
ExecutionMode = TASK / VERIFY_LINK / RECOVERY
```

该模式不属于 `StateId`，也不属于 DynaMAC/MiDiGaP 轨迹 mode。阶段五提供完整的组件控制和状态提交接口，但不提前实现阶段六的 RLBench 顶层策略包装、观测适配和完整仿真闭环。

## 2. 非 TASK 信念更新

`BeliefUpdater.update_frozen()` 专用于 `VERIFY_LINK/RECOVERY` 观测：

- 继续用真实末端与参考系运动构建 `RuntimeFeatures`；
- 继续更新二元关系后验和稳定关系变化；
- 冻结正常进度后验、名义进度、估计进度和正常任务引用；
- 状态解释分项仅作为诊断或后续重入输入，不提交正常进度。

由此恢复动作不会被错误解释为任务正常前进，但恢复期间得到的真实关系证据仍能进入原关系滤波器。

非 TASK 是整个多臂任务的顶层模式：任一机械臂处于 `VERIFY_LINK/RECOVERY` 时，其他 TASK 机械臂也使用 `update_frozen()`，不提交进度、不运行正常入口事务，只查询并伺服各自已经提交的冻结参考目标。辅助臂自身的恢复重入评估仍可执行；所有辅助动作返回 TASK 后，正常进度和入口守卫才恢复。

## 3. VERIFY_LINK

`RelationVerificationController` 只消费引用确切 LINK occurrence 的必要 linked 请求。请求可来自当前 `LinkPendingCandidate`，也可来自本次执行已因果穿过、自然确认区间已结束但因当前激励不足而仍未稳定判定的正式 LINK。正式 LINK 若已经出现新的、信息充分且指向 external 的运动证据，直接进入普通 LINK 恢复，不由主动验证掩盖。其他统一进入条件为关系尚未稳定判定 linked、参考系当前/上一周期成对可用、跟踪可靠且信息权重不足，同一事件上下文尚未验证过，且最近 `TASK` 实际末端轨迹能估计非零接近方向。最后一项是原固定反向探测模板的可定义前提：轨迹静止、样本不足或净位移近似为零时，本周期保持 `TASK/HOLD`，不进入辅助模式、不抛出策略异常；它不增加新关系状态、新阈值或任务分支。

执行模板固定为：

```text
记录入口位姿与夹爪命令
→ 从最近 TASK 实际末端轨迹估计接近方向
→ 保持朝向和夹爪，沿反方向匀速探测
→ 稳定 linked/external、探测 t_max 或安全约束触发时停止外移
→ 按实际探测路径倒序返回
→ 统一回到 TASK
```

稳定判定必须同时具有实际探测运动和关系滤波有效信息，离线先验或已发送目标不能提前结束验证。刚进入 `VERIFY_LINK` 时属于上一条 TASK 指令的运动不计为探测响应；只累计验证指令发出后，在下一控制周期实际观测到的末端—参考系成对响应。窗口至少包含3个可靠响应且累计平移达到 `minimum_probe_motion` 后，计算共动残差比

$$
\rho_{i,f}^{\mathrm{verify}}
=
\sqrt{\frac{\sum_\ell\lVert\Delta p_{f,\ell}-\Delta p_{e,\ell}\rVert^2}{\sum_\ell\lVert\Delta p_{e,\ell}\rVert^2}}.
$$

当前统一阈值为0.85：不高于阈值支持 linked，高于阈值支持 external。该窗口只提供跨周期物理响应方向，不创建第三种关系状态，也不替代原二元关系滤波；仅当窗口方向与同一滤波器当前非 Unknown、有效信息决策一致时才结束探测。`t_max` 只限制外移；超时后仍返回。返回路径不安全时由安全控制优先，并输出结构化失败。同一 LINK occurrence 的尝试签名由“关系状态、任务状态、抓取事件”组成，签名不变时禁止立即重复触发。

Pending 稳定验证为 linked 后，仅在当前 episode 的 `EpisodeLinkAnchorRegistry` 中激活候选模板并建立运行时来源；不会修改 `ClosedLoopTaskModel`、离线 `link_origin` 或正式事件。正式 LINK 的离线来源已经存在，其验证结果只更新同一在线二元关系后验，不重复激活来源。external/Unknown 不直接激活 Pending。若必要 linked 守卫在关系所属机械臂已经到达同一 Pending occurrence 后稳定得到 external，则向该机械臂生成 LINK 恢复意图，并临时使用候选轨迹修复本次未建立的连接；修复后获得有效 linked 证据才激活运行时来源。对臂尚未到达 occurrence 时，跨臂定向守卫仍只等待。

## 4. LINK/UNLINK 关系恢复

`RelationGoalPlanner` 把可靠二元关系失配转换为：

- 期望 linked、实际 external：`LINK(f)`；
- 期望 external、实际 linked：`UNLINK(f)`。

同一机械臂有多个目标时，全部 UNLINK 在 LINK 之前执行，保证夹爪资源先释放。

LINK 通过当前状态的正式事件级 `link_origin`、当前 episode 已验证的 Pending 来源，或同一已到达但验证为 external 的 Pending 临时修复模板解析锚点。临时模板不等于已确认关系。执行前先等待参考系连续稳定并锁定本次世界位姿，完整局部轨迹只按该锁定位姿实例化一次；预抓取/开夹爪阶段若参考系显著移动，则废弃旧实例并回到稳定等待，夹爪闭合后允许对象随末端运动。协方差使用 `Sigma_rec = Sigma + lambda_rec I`，随后执行重新接近、预抓取、夹爪命令和 linked 验证；只有最后一种来源恢复成功后才被提升为 episode 运行时来源。

恢复轨迹是达到目标关系的动作模板，不是关系之外的额外完成目标。一旦当前目标关系已经
由可靠运动证据稳定判定，当前关系目标立即完成并进入后续目标或重入，不再机械追逐剩余
LINK/UNLINK 路点。唯一例外是主动 LINK 探测已经让机械臂离开起点：即使探测中已经得到
稳定关系，也必须先完成既定原路返回，再结束该辅助模式。

UNLINK 从对应 `UnlinkEventMetadata` 解析正常解除事件，统一执行打开夹爪、局部脱离和 external 验证；有学习到的局部脱离目标时按当前参考系重新实例化，否则使用通用径向脱离。只有“当前决策等于目标关系且该目标方向获得过有效运动证据”才算关系恢复成功，不能把先前支持反方向的证据当成确认。

路点等待、关系验证、单目标尝试、恢复总周期和重入等待均有显式上限；超限或观测/安全不可用时返回 `RecoveryFailure`，不会无限循环。恢复动作完全由独立控制器产生，不查询或重新启用正常任务 PoE。

## 5. 完整状态重入

关系目标完成后，`ReentrySelector` 只在事件提供的合法重入状态中，使用当前机器人轨迹、稀疏场景和关系状态共同评分。正常进度评分保持原始示范协方差；恢复后的重入机器人轨迹评分复用关系恢复已经采用的 `lambda_rec`，即 `Sigma_reentry=Sigma+lambda_rec I`。机器人绝对准入使用同一 PoE 的联合可达峰值归一支持，逐流兼容度只作审计。该口径不放宽场景、关系、候选范围、跨技能守卫或既有阈值，也不写回离线模型。由无可解释状态触发且没有关系目标时，可以在全任务状态中重新搜索，但只允许：

- 当前技能内部重入；
- 经阶段四入口守卫明确放行的相邻下一技能重入。

后退或跨多技能跳转没有对应入口事务，直接视为非法。选择成功后，管理器原子执行进度后验 one-hot 重置、执行控制器 `reference_state` 重置和 `RECOVERY→TASK`；不会先返回 TASK 再依赖外部补做状态重置，也不会恢复故障发生前的旧时钟索引。

当某个合法候选的场景和关系条件已成立，但机器人轨迹尚未达到正式重入阈值时，对齐动作使用该候选自身的关系期望和流角色。该条件化信念只是一次只读动作查询，真实进度后验、`StateId`、`reference_state`、mode、角色历史和任务时钟全部继续冻结；只有随后通过原完整重入阈值才原子提交。这修复了“选中候选后反而被故障旧状态的关系语义卡住”的接口矛盾，不增加状态、候选、阈值或任务分支。

## 6. 最终代码、配置与验收

```text
source/policy/closed_loop/
├── relation_verification.py
├── recovery.py
├── link_anchors.py
├── unlink_metadata.py
├── relation_goals.py
└── reentry.py

configs/closed_loop_recovery.json
tests/closed_loop/test_phase5_recovery.py

evaluations/phase5_recovery_acceptance/
├── config.json
├── run.py
└── results/v1/

evaluations/phase5_behavior_ab/
├── config.json
├── run.py
└── results/v5/
```

- 阶段五组件验收11项全部通过；
- 阶段一至五闭环回归84项全部通过；
- 最终全仓回归为 `514 passed, 4 skipped in 251.91s`；
- 真实冻结 V4 元数据验收覆盖8个任务、12套机械臂模型：11个正式 LINK 锚点、7个 Pending、3个 UNLINK、904个状态级 `link_origin`、176个实例化 LINK 路点和8个 UNLINK 合法重入状态全部通过；
- 正式结果配置、逐模型统计、环境信息、报告和 `SHA256SUMS` 保存在 `evaluations/phase5_recovery_acceptance/results/v1`；
- 受控行为 A/B 覆盖70条 Pending 验证、70条 LINK/UNLINK 恢复、70条无响应执行器、240条双向任务重入和50条跨技能许可试验：主动验证和统一恢复分别由被动/无动作对照的0%提高到100%，反向探测、进度冻结、原路返回、重复触发抑制和有界失败不变量全部100%通过；
- 完整状态重入相对旧时钟将 StateId MAE 从37.0000降至0.6375，96.67%的试验优于旧时钟；状态选择率100%，选中后的进度后验、执行引用和 `RECOVERY→TASK` 原子提交正确率100%，无许可跨技能候选阻断率100%；
- 重入机器人兼容度只使用240条正常回放从0.01标定为0.001，最低正确状态兼容度为0.00219484；状态选择239→240、精确选择180→181、错误选择保持59→59。精确状态识别率75.42%，其余非精确结果主要为局部相邻或静态弱区分状态，因此不将组件结果夸大为无误的完整环境恢复；最终 A/B 配置、逐试验表、标定对照、报告和 `SHA256SUMS` 保存在 `evaluations/phase5_behavior_ab/results/v5`；
- 该阶段只证明恢复机制、接口和真实事件元数据闭环成立，不把它表述为完整 RLBench 在线故障恢复成功率；完整策略集成和仿真故障评测属于阶段六。

# 阶段六：完整策略集成、诊断与组件级仿真验收

## 1. 当前最终保留的集成结构

核心算法与 benchmark 适配保持分层：

```text
source/policy/closed_loop/
├── policy.py
├── config.py
├── state.py
├── diagnostics.py
└── serialization.py

integrations/rlbench/rlbench_closed_loop/
├── observation_adapter.py
└── policy_server.py

integrations/rlbench/rlbench_dynamac/core/
├── runtime.py          # RLBench 通用混合末端执行器
└── trac_ik.py          # 实时运动链 bounded TRAC-IK
```

`ClosedLoopMultiStreamPolicy` 不依赖 RLBench，按同一 pre-action 快照完成阶段二至五的信念、动态角色、正常控制、边界事务、主动验证/恢复和重入。`act()` 只准备完整周期；环境接受动作后由 `commit()` 提交，拒绝或异常时由 `abort()` 回滚全部阶段状态，禁止部分提交。

阶段二轨迹证据权重统一为 $a_{f,t}v_{f,t}q_t^f(\mathrm{external})$。
可观测、可靠参考系在离散关系决策为 `Unknown` 时仍按二元后验软降权提供
连续位姿证据；不可观测或不可靠时仍为零。这不改变关系兼容度、动态角色、
边界守卫或 PoE 的已定义 `Unknown` 处理。

RLBench 层只负责低维真值到统一观测的适配、双臂联合快照、策略进程协议、动作格式和 benchmark 执行器。`direct_evaluate` 以 `policy_type=closed_loop_multistream` 复用同一评测集和结果 schema，并保留 `policy_type=dynamac` 的冻结 V4 执行器对照路径；闭环策略使用独立、可替换的通用混合末端执行器，不反向改变阶段一至五算法。

正式评测只读取 `evaluations/phase6_formal_evaluation/protocol.json` 中的当前活动协议。该文件只保存正式矩阵、封存样本、故障、统计、资源方案、阶段六基础/闭环模型根，以及 `stage6_hybrid_cartesian_executor_v19` / `rlbench-stage6-hybrid-cartesian-continuation-v23` 唯一运行身份；加载器会拒绝历史修订分区、未知分区和执行器身份不一致的配置。无效审计过程只保留在开发日志与 Git 历史中，不进入可执行协议。

WipeDesk 保留 RLBench 原始的单条连续 `CartesianPath` 和公共任务的单一 `sponge` 低维参考系。五条正常示范的时间切分使用与任务名无关的末端几何方向反转证据，再按归一化时间位置建立跨示范对应；不从灰尘成功标签、任务名、人工路点或新增任务字段生成切分。当前五条示范均得到8个对应边界、9个技能和合计253个进度状态。折返点仅用于离线对齐，运行时任务和成功条件保持上游 RLBench 定义。

纠正模型使用通用 `dynamac-direct-static-training-v1` 训练清单。它与标准 V3 使用完全相同的五示范学习链和公共 `WipeDesk.get_low_dim_state=[sponge]` 输入，只不携带与旧分段长度绑定的 V3 动态干预锚点。静态场景统一使用无动态干预语义；平滑动态场景则用当前加载 checkpoint 的技能时长、Eq. (5) 可用窗口和 Eq. (6) 已选参考流认证冻结交互前相位，没有 WipeDesk 任务分支或策略专属字段。匹配的闭环 sidecar 保存253个状态、8个边界和1个由跨示范运动学证据确认的 sponge LINK 锚点；边界标定仅使用五条正常示范并通过40/40个边界×示范实例。

## 2. 当前已接通的跨阶段语义

- `reference_state` 是上一周期实际发送的动作目标；动作后名义质量落在该目标本身及其合法邻域，不把动作目标再误解释为动作前源状态；
- 当前离散动作目标是否到达由 `beta` 与 `reference_state` 的统一拓扑口径判定；上一命令连续几何相容度仅保留执行审计，不重复阻断进度；
- 边界事务同时提交执行游标与下一周期进度入口投影，关系后验、场景观测和 mode 不重置；
- 边界提交周期执行源技能最终连续位姿目标和入口状态夹爪命令。若离线正式 LINK/LINK_PENDING 与该边界相交、源引用和 beta MAP 已进入终止窗口且其他守卫满足，边界事务可先准备目标入口的开到闭关系建立动作；准备不提交 StateId、beta、LocalDone 或虚拟帧，源连续轨迹继续推进，释放仍只能在正式提交后执行。对 Pending，真实观测闭爪后直接路由边界保存的同一 occurrence 进入主动验证，不要求先提交目标 StateId，也不会在夹爪实际闭合前探测。正式 LINK 跨界时，本周期仍提交源末端流角色和连续动作，但会在因果游标切换前预览入口，仅合并该入口的关系事件生命周期，使后续有限自然确认区间不会被源端快照覆盖；
- 目标技能虚拟帧由上述桥接动作后的下一次真实观测捕获，再作为可靠内部固定参考进入入口技能查询；不得用桥接动作前的末端位姿提前锚定；
- `VERIFY_LINK/RECOVERY` 周期继续更新关系后验并冻结正常进度，返回 TASK 后按阶段五规则重入；
- `VERIFY_LINK` 冻结 beta/StateId 时，仅为请求指定的关系使用确切事件上下文：Pending 使用其候选状态，正式 LINK 使用自然确认区间末状态；其他关系仍由冻结 beta 混合，真实滤波公式、阈值和稳定判定不变；
- 任一机械臂处于 `VERIFY_LINK/RECOVERY` 时冻结全系统的正常进度与正常入口事务，其他机械臂只伺服当前冻结参考目标；
- LINK 恢复先等待参考系连续稳定并锁定本次锚点；预抓取阶段参考系显著移动时废弃旧实例并重新等待，闭合后允许对象随末端运动；
- 已施加任务命令的引用与下一周期真实观测进入既有进度模型；执行器 `reached/progressed/stopped` 不直接改写进度先验、`StateId` 或 `reference_state`，`progressed/stopped` 也不标记闭环绝对目标完成；TASK 夹爪由每臂进度对齐和边界事务授权。若当前引用已被 beta 确认而该节点夹爪命令尚未体现在观测中，则保持引用并先提交离散动作，防止连续游标推进跳过抓取/释放；该检查不读取执行器标签。冻结 DynaMAC 每个正式提交的动作事务仍消费一个原固定时钟状态并显式授权对应夹爪值，不由执行器标签推进、回滚或延迟；只有无任务授权来源的非 TASK 辅助命令保留位姿 `reached` 后提交的顺序。动作事务只使用 `primary_action_status` 与 `primary_action_applied`：正常主动作默认为 `primary_action_applied=true`；若主动作在产生有效物理进展前失败而关节保持成功，则提交 `primary_action_status=stopped` 与 `primary_action_applied=false`；
- `VERIFY_LINK` 既可处理 Pending，也可处理自然确认区间耗尽后因低激励未决的正式 LINK；新的信息充分 external 运动证据仍直接进入恢复。验证稳定确认后保存原关系滤波器当时的二元后验，并在完成原路返回时精确写回同一在线 $q_t$；不转成 one-hot、不固定增强置信度、不新增关系状态；
- 边界准备的 Pending 若验证为 external，恢复意图携带该边上确切的事件标识，并使用同一 occurrence 的未激活临时轨迹模板；在恢复真正确认 linked 前不激活 `link_origin`；
- 每个 tick 保存观测、原始进度/关系后验、候选分项、动态角色、PoE、边界、恢复、动作和控制等价性评估。

## 3. 控制等价低置信处理

阶段六调用同一阶段三 `ControlEquivalenceAssessment`，不在 RLBench 适配器中复制或特判。原始 `LOW_CONFIDENCE/BACKWARD_REALIGNMENT`、逐状态后验和动作等价诊断全部保留；不同位姿、不同夹爪、高置信多状态落后和跨技能反例均保持 HOLD。动作等价阈值唯一命名为 `minimum_action_equivalence_compatibility`，其他配置字段由严格检查拒绝。

## 4. RLBench 通用混合末端执行器

闭环策略当前使用 `stage6_hybrid_cartesian_executor_v19` / `rlbench-stage6-hybrid-cartesian-continuation-v23`。每个原始绝对末端目标先由 CoppeliaSim 当前关节种子伪逆求解；未产生合法候选时调用实时 Panda 运动链上的 bounded TRAC-IK Distance，再依次进入有界 SE(3) 小步延续、碰撞感知优先且必要时放宽的采样 IK和碰撞感知线性路径。远目标可直接使用碰撞感知 RRTConnect；近目标首次求解不调用全局规划，只有同一目标已经由物理反馈确认停滞并耗尽本地求解层级后，才先尝试碰撞感知 RRTConnect。若其后的碰撞放宽线性路径仍失败或没有产生真实改善，最后使用共享预算约束下的碰撞放宽 RRTConnect。路径生成成功但真实末端仍无改善时同样升级，四个路径族都是可跨周期推进的独立后备层。若某层对同一目标耗尽且无物理改善，则把后继求解层保存到下一周期；任一备用解产生真实进展后重置为本地层并立即把 `progressed` 返回，由下一闭环周期重新观测和求解。升级采样仍须通过0.35 rad单关节/0.50 rad关节向量 L2连续性约束，所有层耗尽才报告停滞。执行器只在当前有界物理执行段内拥有规划路径；完整路径由 PyRep 正常释放运动句柄，提前返回时则在交还闭环观测前显式释放尚未完成的 RML 句柄。该规则只收口仿真资源所有权，不改变任何任务状态或门控。

小步延续的默认上限为 5 mm / 2°，按 `1, 0.5, 0.25, 0.125` 回退；每段物理执行后重新读取真实末端位姿并重建求解。内部子目标不提交为 `reference_state`。bounded TRAC 请求与执行后物理完成使用 0.5 mm / 0.05°，外层策略目标接受使用 1 mm / 0.1°；只有原始目标进入外层包络才返回 `reached`，真实改善但未到达返回 `progressed`，合法命令停止改善返回 `stopped`。2 mm / 1° 只保留为外部运动链 FK 对齐审计上限。只要任务命令已经施加，其引用和下一周期真实观测就进入既有 $\bar\beta_t/\beta_t$；三个执行器标签不直接改写进度先验、`StateId` 或 `reference_state`。`progressed/stopped` 不提交绝对目标完成。TASK 夹爪由当前状态或边界事务授权；关系建立闭爪可按上述边动作准备协议先执行，但不推进任务状态，源节点开爪标签也不会撤销已准备动作。接收臂和交出臂可以按各自授权先后动作，无关夹爪不强制原子捆绑。双臂位姿命令仍先全部准备再推进共享物理时钟，并以“至少一臂改善且无臂退化”的不可补偿口径确认物理进展。

单个原始策略目标共享64个原始物理步预算。笛卡尔小步延续不再另有更小的8段上限：每个成功延续段至少消耗一个物理步，所以原始预算本身已是唯一且有界的限制。这避免执行器把“内部小步数用完”误报成“原始策略目标已可提交”，从而在固定时钟基线中累积物理轨迹滞后。

冻结 DynaMAC 对照不读取闭环进度与边界授权。其固定策略时钟在动作事务正式提交后推进一次，主动作同时提交该状态已经产生的夹爪命令；`progressed/stopped` 仅被记录为执行器物理结果，不能使固定时钟重试同一状态或延迟夹爪。这样四种正式方法共享同一 IK 执行器，但不会让执行器改变冻结基线的时间语义。

碰撞放宽只作为 RLBench 接触场景的有界后备；确认近目标停滞后，碰撞感知非线性绕行先于碰撞放宽直线，放宽直线仍无效时才进入最终放宽非线性路径。执行器不读取任务名、seed、技能编号或固定 StateId，不修改闭环策略目标。更换 benchmark 时可替换该层而不改阶段一至五核心实现。

## 5. 当前最终保留的验收结果

- 阶段一至五的组件与回放验收结果继续保存在各阶段独立评测目录；阶段六不复制或改写这些结果。
- 当前 v19/v23 定向物理验证使用公共 sponge 接口、253状态纠正模型和封存样本；完整方法单回合成功，确认新基础模型、闭环 sidecar、边界标定和真实仿真执行链能够贯通。该结果只作启动门控，不作为成功率统计。
- 关系建立边界准备、动态角色、统一事务及执行器物理标签的当前实现由紧凑自动化回归覆盖；正式消融中边界准备只随 `relation_scene_boundary_guards` 在完整方法启用。
- 旧执行器生成的组件 pilot、逐周期诊断和预正式门控已从当前项目交付中删除；其修复过程只存在于开发日志与 Git 历史，不参与当前结果汇总。
- 上述定向运行只证明当前模型和执行链可以启动正式矩阵，不是论文正式成功率。论文统计只读取当前协议完整生成的正式矩阵。

## 6. 正式评测协议与资源调度

- evaluations/phase6_formal_evaluation/ 保存唯一活动的 protocol.json、中文协议说明、单元运行器、48路可恢复调度器、并行准入记录、自动统计器和专项测试。
- 正常静态矩阵固定为8任务×4方法×200条封存 episode，共6400回合；平滑动态无故障对照固定为8任务×4方法×封存索引0至49，共1600回合；故障矩阵在同一批平滑动态背景上固定为8任务×4故障×4方法×相同50条，共6400回合，总计14400回合。Handover 五种 variation 在动态与故障子集中各10条。任何结果产生后均不得换 seed、换 episode、换场景计划或修改故障配置。
- DynaMAC V4、仅闭环进度、闭环进度＋动态角色和完整方法统一使用 stage6_hybrid_cartesian_executor_v19 / rlbench-stage6-hybrid-cartesian-continuation-v23。正常成功率、故障 intention-to-treat 成功率、故障触发率、条件恢复率、Wilson 区间、exact McNemar、Holm 校正和任务宏平均成对 bootstrap 均已在运行前写入协议。
- 调度器完成封存评测集、V4 release、闭环模型文件、协议哈希、Python运行时、8张RTX 4090与128逻辑CPU预检。48路准入中48/48 worker生成合法结果，无致命日志、锁或端口/Xvfb冲突；正式动态与故障矩阵均拆成连续 episode 分片，由同一48槽全局队列动态补位。
- CoppeliaSim 正式后端固定为验证过的 Xvfb 软件 OpenGL。GPU身份按round-robin隔离，但不把 CUDA_VISIBLE_DEVICES 误报为GPU已承担软件渲染。
- 正式协议加载器只接受当前顶层分区，并要求活动身份与共享执行器完全一致；历史修订分区、未知分区和身份混用均在启动前失败。此前无效批次全部排除且不留在正式结果根。
- 正常矩阵按最终最小受影响重跑口径冻结：WipeDesk四种方法和PlaceCups两个闭环消融单元重新生成，其余26个不受最终改动影响的完整单元以路径、原提交、协议哈希和文件SHA256逐项认证。32个单元共同构成同一正式正常矩阵；保留清单只服务内部复现，不改变结果定义。

## 7. 关系状态绝对兼容口径

进度后验继续使用在线二元关系后验与离线软先验的原始内积 $q_t\cdot\pi_s^{\mathrm{demo}}$，因此正常状态排序和概率推断未改变。需要通过固定绝对阈值的事件感知候选扩展和恢复后重入，改用单一 `relation_state_compatibility`：

- 先将原始内积除以离线软先验最大分量，使同向完全匹配的可达上限为1；
- 若在线可靠决策与非持平离线先验的 external/linked 方向相反，该候选分数为0；
- 多个关系中任一方向相反不得由其他关系补偿。

这替换了原始内积直接与绝对阈值比较的口径，不叠加新门控，也不改关系滤波、动态角色、正常进度后验、PoE、合法候选集或入口守卫。原始内积、峰值归一支持和方向约束后分数同时保留供诊断。

最终口径进一步区分“正式关系方向”和“待验证关系假设”：仅由 `LINK_PENDING` 写入 soft linked、且尚无正式或 episode `link_origin` 的状态不施加硬方向删除，继续以峰值归一重叠提供软解释；入口所需 Pending 仍由守卫与 `VERIFY_LINK` 确认。关系恢复一旦完成，重入则显式要求本次全部 LINK/UNLINK 目标仍由在线决策保持，防止软 Pending 语义覆盖刚恢复的物理结果。

正式矩阵的 PlaceCups 早期正常回合暴露了该偏差：接触偏差使4条 `full` 回合进入合法重入评估，正确关系方向的候选原始内积仅为0.585–0.614，被0.60阈值拒绝。修正后同一物理方向的关系状态兼容度为0.836–0.877，4条回合均选择 `k4:t24` 并成功；几何上可匹配但关系方向已相反的 `k4:t25/t26` 仍被拒绝。定向自动化回归为67项全部通过。

## 8. 正式正常矩阵与最终专项审查

正式正常矩阵已完成8任务×4方法×200条封存episode，共32个单元、6400回合。跨任务宏平均成功率为：冻结DynaMAC V4 89.75%、仅闭环进度84.38%、闭环进度加动态角色83.56%、完整方法92.13%。完整方法相对冻结基线提高2.38个百分点，成对bootstrap 95%区间为[+0.31,+4.94]个百分点，通过预注册的5个百分点非劣效判定。

PlaceCups的两个消融分别为62.5%和61.5%，完整方法为98.0%。消融失败全部耗尽1000周期且无非法动作；两个消融明确关闭关系/场景入口守卫、主动验证和恢复，因此在末端状态没有达到绝对支持度时按定义HOLD。完整方法分别挽救71/75和73/77个对应失败样本，证明这组差值体现被消融模块的作用，而不是共享执行器或模型损坏。不得通过放宽进度阈值、盲目推进或向消融泄漏恢复能力来提高该数字。

WipeDesk四种方法为68.5%、70.0%、72.0%和72.5%。46条episode四种方法共同失败，其中40条均完整执行策略；冻结基线的63条失败全部执行完253个状态且非法动作数为0。该任务公共低维接口只有sponge位姿，而每回合随机生成50个未进入策略输入的灰尘点；固定示范学习轨迹不能针对本回合外缘灰尘自适应改变覆盖范围。完整方法相对基线净挽救8条，没有显示系统性路线退化。该上限属于当前benchmark可观测性与示范覆盖范围，不通过任务专属字段或技术路线门控修补。

动态/故障协议最终采用“平滑动态是共同样本背景”的统一口径。每个故障回合先完成封存 A/B 计划中与目标交互相关的10周期平滑移动，随后才开放原有物理谓词；注入器不读取策略内部状态。抓取失败按邻近传感器实际检测到动态任务实体的闭爪区间计 occurrence，不让自由空间 close-valued 命令提前消费事件。关系失配和意外掉落统一接受连续稳定 attachment 或同一动态实体的闭爪 maintained contact，复用预注册稳定周期；接触型关系必须实际观测到夹爪打开才算释放生效。该口径覆盖注册抓取物和托盘把手等接触约束对象，不读取任务名。故障释放是外生物理干预，在触发的唯一夹爪动作上临时越过策略夹爪授权屏蔽，避免共享执行器把故障命令消除；该操作不改变策略状态、后验或后续授权。另设同一前50条 episode 的1600回合无故障动态对照，使每类故障能够与同任务、同方法、同 episode 的动态干净结果配对。WipeDesk 等重训模型的动态触发不复用旧分段锚点，而是用当前加载 checkpoint 的技能时长、可用窗口和已选参考流认证同一个冻结交互前相位。

平滑动态无故障矩阵完成32个单元、1600回合，四种方法的跨任务宏平均依次为93.50%、84.75%、84.50%和90.50%。完整方法相对冻结基线为-3.00个百分点，成对bootstrap 95%区间为[-7.00,+0.75]个百分点；它仍保持90.5%的绝对成功率，但该50条/任务子集本身不能证明动态条件下5个百分点非劣效。

四类故障矩阵完成128个单元、6400回合。按每种方法1600个预注册故障回合合并，冻结DynaMAC V4、仅闭环进度、闭环进度加动态角色和完整方法分别为38.81%、33.69%、33.56%和68.63%，完整方法相对冻结基线提高29.81个百分点。按故障族，完整方法相对冻结基线的跨任务宏平均差值为：时间停滞+31.75、抓取失败+69.25、关系失配+15.50、意外掉落+2.75个百分点；对应成对bootstrap区间完整保存在机器可读统计中。

最后一次Handover完整方法最小受影响重跑同时替换正常200回合、动态50回合和四类故障各50回合。正常成功率由修复前的无效中间结果收口为91.0%，高于同任务冻结基线87.5%；真实正常回合确认“正式LINK自然确认区间耗尽但运动激励不足”会进入现有`VERIFY_LINK`，完成反向探测、原路返回、稳定linked写回并继续TASK。实现没有新增关系状态、阈值或任务分支。

Handover关系失配最终为1/50。46个实际触发回合均在故障当周期产生后验报警，说明残余瓶颈不在关系漏检；该协议会解除悬空物体连接并额外平移4 cm，随后重力使物体在下一观测时总位移达到43.3至56.3 cm，超出少量正常示范锚点可可靠重抓取的范围。失败主要记录为LINK锚点执行超时，其次为验证返回超时或无合法重入。该结果作为当前“只回到正常示范支持域”恢复能力的残余边界保留，不通过Handover专用轨迹、放宽门限或故障数据调参改写技术路线。

最终深度审计覆盖192个正式单元、14400个回合和14400次固定运动计划身份检查，结果为通过、0项协议违规、0次故障注入器改写策略状态。1600个动态背景回合全部完成预定移动；四类故障分别记录1501、1338、1600和1339次物理触发，未触发或触发前终止的回合仍按intention-to-treat保留在固定分母中。

机器可读逐单元统计、Wilson区间、同episode成对检验、Holm校正、跨任务bootstrap、完整审计和校验和位于`evaluations/phase6_formal_evaluation/results/v2/`；正常专项审查位于`evaluations/phase6_formal_evaluation/NORMAL_AUDIT.md`。`results/v1/`旧汇总已从当前项目清除，避免与最终Handover结果混用。
