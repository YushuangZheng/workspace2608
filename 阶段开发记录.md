# 阶段开发记录

## 文档用途

本文档只记录《新方法代码开发计划》各阶段最终保留的最新实现，不记录排查、试错和修复过程。

维护规则：

- 每个阶段完成并验收后，在对应章节记录最终代码、模型语义、配置和验收结论；
- 同一阶段机制发生更新时，直接删除或覆盖旧口径，不并列保留过期方案；
- 未进入当前阶段范围的设计只保留在《新方法代码开发计划》中，不在本文件中写成已实现能力；
- 详细开发过程、异常分析和修复历史继续记录在《开发日志》中。

## 当前阶段状态

| 阶段 | 状态 | 最终交付范围 |
|---|---|---|
| 阶段一 | 已完成并验收 | 统一离线任务模型、关系与场景统计、边界模型、关系事件模板、只读状态查询 |
| 阶段二 | 已完成并验收 | 在线观测与关系—进度联合信念推断 |
| 阶段三 | 开发完成，待确认验收 | 动态流角色、运行期精度权重与闭环正常执行控制 |
| 阶段四 | 未开始 | 技能入口守卫与多臂事务推进 |
| 阶段五 | 未开始 | 主动关系验证、关系恢复与任务重入 |
| 阶段六 | 未开始 | 完整策略集成、诊断与最终验收 |

# 阶段一：统一离线任务模型与只读状态查询

## 1. 最终目标

阶段一从拟合 DynaMAC 所使用的同一批正常成功示范中，统一构建后续在线关系滤波、进度滤波、技能入口守卫和恢复模块需要的离线统计。同时将基础策略的“指定状态动作查询”与“任务时钟推进”解耦。

阶段一只负责离线模型、接口和数据持久化，不实现在线关系后验、进度后验、动态角色、在线边界判定、主动验证或恢复控制。

## 2. 统一状态与查询语义

任务进度状态唯一表示为：

```python
StateId = (skill_index, local_index)
```

mode 是同一进度状态内部的轨迹混合分量，不进入 `StateId`。每只机械臂的状态节点数量严格等于所有技能 `duration` 之和，不乘以 mode 数量。

每个状态保存前驱、后继、技能末端和跨技能后继信息。`DynaMAC.query_state()` 与 `StateQueryAdapter.query_state()` 可以查询任意指定状态和独立 `mode_index`，并允许传入逐流精度权重：

- 权重乘在专家精度上；
- 权重为零等价于移除该专家；
- 显式权重表中未列出的流权重为零；
- 离线 Eq.6 未选择的流不能被运行权重重新启用；
- 查询不改变技能索引、时间索引、mode 路径或虚拟帧状态。

原始 `DynaMAC.act()` 复用同一查询计算后，继续按照冻结 baseline 语义推进游标，因此原有执行路径保持可用。

## 3. ClosedLoopTaskModel

每只机械臂构建一个与基础 DynaMAC checkpoint 指纹绑定的 `ClosedLoopTaskModel`。模型主要包含：

```text
states
skill_states
boundaries
link_anchors
link_pending_events
unlink_events
link_origins
```

每个 `StateNode` 保存：

- 全部 mode 的先验；
- 原有多流轨迹均值 `[M,7]` 和协方差 `[M,6,6]`；
- 各参考系的关系连接分数及 `[P(external), P(linked)]`；
- 每个 mode 的已选参考系和夹爪命令；
- 通过筛选的稀疏场景因子分布。

附加模型使用 sidecar schema v3 保存新增统计，不重复保存基础 DynaMAC 中已有的大规模流参数。加载时校验基础策略指纹和流选择语义，防止离线任务模型绑定到错误 checkpoint。

## 4. 离线关系先验

关系概率空间严格只有：

```text
external / linked
```

对每个 arm-frame、进度状态和内部 mode：

1. 使用末端相对于参考系的跨示范局部位姿分布及 GMSD 生成主关系先验；
2. 在存在充分末端运动时，计算相邻状态的相对运动残差和共动支持；
3. 共动证据只用于排除特殊的稳定但未连接假阳性，不能单独创建 LINK；
4. 缺少运动激励时不把缺证据强解释为 external；
5. 最终保存归一化的 `P(external)` 和 `P(linked)`。

正式事件与逐状态原始证据分层保存：GMSD/协方差分数留在 `demo_relation_scores` 供审计；正式 LINK 到正式 UNLINK 之间由事件状态机校正部署关系的连续性，避免局部低协方差抖动把物理连接错误改回 external。校正后的 `demo_relation_priors` 仍是可推翻的软先验，正式 linked/external 分别保存为 `[0.3,0.7]` / `[0.7,0.3]`，不使用 one-hot。Pending 不参与该传播。

虚拟技能帧不进入关系状态空间，也不参与 LINK、UNLINK 或关系失配。

## 5. 技能条件化稀疏场景因子

候选因子只保留两种数学形式：

- 节点因子：单个活动实体确实存在的内部、可变构型；
- 边因子：两个任务相关活动实体之间的相对构型。

普通刚体不强制建立节点因子。机械臂—对象 linked/external、末端相对参考系的技能轨迹、对侧机械臂进度均不作为场景因子重复建模。

逐进度候选范围由当前技能实际使用的参考系、相同 skill 和 mode 内的局部共同激活实体，以及任务接口提供的直接结构绑定产生，不依赖人工实体对白名单，也不生成全对象两两组合。局部邻域半径依次检查 `r=0,1,2`，选择通过跨示范支持率和稳定性检查的最小半径；`r=2` 仍不稳定则删除，且邻域不跨技能边界。

逐状态因子采用按示范留一的前向筛选：机器人局部轨迹是识别基线，不加入名义时间先验或关系兼容度；候选因子必须在绝大多数正常示范上可观测、分布稳定，并可重复提升局部进度识别间隔。与机器人轨迹或关系变量重复的信息通过候选定义和排除规则删除。

## 6. BoundaryModel

每个技能边界均保存一个离线 `BoundaryModel`，其边界前窗口只包含当前技能末端的 `h_b^-` 个状态。

### 6.1 LocalCompletionModel

本地完成模型保存：

- 整个技能末端状态窗口；
- 当前技能最终状态在全部正常示范上的目标分布；
- 当前技能完成时由本臂直接建立或解除、且必须满足的关系集合；
- 每个必要关系在成功终止窗口中的目标关系分布。

阶段一不计算在线 `LocalDone`，也不标定本地完成阈值和连续确认周期。

### 6.2 边界关系条件

候选只来自下一技能所有 affected arms 的 selected frames。每个候选从边界前终止窗口拟合关系支持分布，选择最大支持关系，并要求总体支持、观测可用率和全部正常示范留一折一致。随后删除已由本臂或同一事务中其他机械臂 LocalCompletionModel 保证的条件，以及完全重复条件。

### 6.3 边界场景条件

边界候选由下一技能参考系、边界关系实体及其直接结构关联实体限定，只在当前边界前短终止窗口中拟合，不扩散到当前技能的所有进度状态。硬场景条件必须通过全部正常示范留一覆盖检查，并保存最终支持分布和马氏兼容度阈值。

### 6.4 双臂事务元数据

只有左右边界在全部正常示范中稳定同步，并且共享同一必须保持的 linked 物理实体或相同硬场景条件时，才分配同一个 `transaction_group`。独立边界和单向等待边界保持 `None`。阶段一只保存 affected arms 和事务标识，不执行在线原子提交。

## 7. LINK、LINK_PENDING 与 UNLINK 离线事件

正式事件首先由同一 mode 的全部正常示范共同拟合关系先验，再在完整任务关系序列上使用迟滞阈值和前后稳定窗口检测候选。按示范留一只检查候选能否由其余示范稳定重现；通过后使用全部正常示范拟合最终事件数据。夹爪命令只校验运动学事件的抓取或释放模板，不能单独创建事件。

正式 LINK 与 Pending 的事件前轨迹最大长度均为16个对齐后的离散状态。对当前机械臂的事件终点，先在所有可恢复参考系中查找此前最近一次稳定 linked→external；模板起点取“固定窗口起点”和“该 UNLINK 下一状态”中较晚者。没有先前 UNLINK 时使用原固定窗口；发生截断后不向更早状态补齐。轨迹可以跨技能边界，但不会跨过同一机械臂上一段已经结束的连接，因此不会把上一段抓取或释放混入当前模板。

### 7.1 正式 LINK

充分运动证据确认 external→linked 后，保存事件级 `LinkRecoveryAnchor`：

- 事件前短局部轨迹均值和协方差；
- 夹爪命令；
- 上下文状态；
- 关系建立后的合法 linked 状态。

事件前轨迹允许跨回上一技能。正式 LINK 建立后到对应 UNLINK 前，离线 `link_origin` 和软 linked 关系期望按正常示范路径跨技能和 mode 传播，使后续恢复能够追溯正确的事件级锚点；只有正式 UNLINK 才终止该传播。

### 7.2 LINK_PENDING

当跨示范 GMSD、闭合模板和事件位置一致支持 LINK 假设，但闭合后最短稳定窗口完全缺少有效运动激励，且不存在运动反证时，保存 `LinkPendingCandidate`。

Pending 不是第三种关系概率状态，也不等同于在线 `Unknown`。它使用与正式 LINK 相同的拟合函数保存尚未激活的事件前局部轨迹、协方差和夹爪命令，但：

- 不进入正式 `link_anchors`；
- 不传播离线 `link_origin`；
- 不进入 LocalCompletionModel 的已完成必要关系；
- 不在阶段一执行主动验证。

若同一闭合事件随后在锚点窗口内获得正式 LINK，则只保留正式 LINK，不重复保留 Pending。后续阶段只有在本次在线执行稳定验证为 linked 后，才允许为当前 episode 激活该模板并建立运行时来源；这部分尚未实现。

### 7.3 UNLINK

充分证据确认 linked→external 后，保存 `UnlinkEventMetadata`：

- 正常关系解除状态；
- 解除后的合法任务重入状态；
- 可选的局部脱离目标；
- 跨示范事件支持信息。

## 8. 最终代码与配置

阶段一最终文件：

```text
source/policy/closed_loop/
├── __init__.py
├── README.md
├── state_index.py
├── task_model.py
├── task_model_builder.py
├── scene_factors.py
├── boundary_model.py
├── relation_events.py
└── query_adapter.py

tests/closed_loop/test_phase1_task_model.py
configs/closed_loop_task_model.json
```

基础策略查询改造位于 `source/policy/dynamac.py`；RLBench 任务实体和结构字段接口位于 `integrations/rlbench/rlbench_dynamac/core/task_specs.py`；公共导出位于 `source/policy/__init__.py`。

## 9. 验收结论

阶段一开发计划中的13项验收标准均有实现和自动化测试覆盖：

- 阶段一专项测试：`25 passed`；
- 当前全仓回归：`483 passed, 4 skipped`；
- Python 格式检查和 `git diff --check` 通过；
- sidecar 保存、加载、基础模型绑定和 Pending 数组往返通过；
- 真实 V4 HandOver 定向只读重建确认右臂保留一个支持率为1.0的 `LINK_PENDING`，并保存16步未激活轨迹模板，同时不生成正式 LINK 或离线 `link_origin`；
- 未启动训练、仿真评测或回放，未改写冻结 V4 模型、数据和正式结果。

阶段一最终状态：完成并通过验收，可以在当前离线模型和查询接口基础上进入阶段二开发。

## 10. 当前明确保留的实现口径

- RLBench `LiftTray` 的任务接口同时提供 `item` 和 `tray`，当前任务角色配置将二者都视为可恢复关系参考系，因此每只机械臂可以分别对两个参考系形成离线 LINK 候选。该候选范围按已确认决定暂时保留，尚未收窄为只对 tray 建模。
- 阶段一验收证明离线模型、接口、统计构建和持久化符合当前开发计划，不等价于已经完成后续在线滤波、控制、主动验证、恢复或仿真成功率验收。

# 阶段二：统一在线观测与关系—进度联合信念推断

## 1. 最终目标与阶段边界

阶段二在阶段一离线任务模型之上实现固定的旁路状态估计链：

```text
动作后名义进度先验
→ 在线 external/linked 关系后验
→ 在线进度后验
```

本阶段只估计并输出 `ClosedLoopBelief`，不改变 DynaMAC 动作、不推进或冻结任务时钟、不分配动态流角色、不在线评估入口守卫，也不生成主动验证或恢复动作。跨技能候选只接受调用者提供的已放行 `BoundaryId`；阶段四以后负责产生该许可。

## 2. 统一运行观测与特征

`RuntimeObservation` 统一保存：

- 当前 tick、末端世界位姿和全部参考系世界位姿；
- 当前夹爪状态；
- 上一命令目标位姿和上一实际末端位姿；
- 逐参考系可见性与跟踪可靠性；
- 场景节点因子需要的实体内部构型字段。

RLBench 低维仿真真值没有显式提供可见性或跟踪协方差时，已有位姿默认 `visible=True`、`reliability=1`，但字段和门控完整保留。实体内部构型仅在任务接口实际提供时读取，不为普通刚体构造伪节点状态。

`RuntimeFeatureBuilder` 每周期统一计算一次实际末端运动、上一命令目标运动、参考系世界运动、末端—参考系相对位姿、相邻周期相对运动残差、夹爪变化、动作激励、命令响应一致性和逐关系信息权重。旋转残差先从基线半角表示换算为物理角度，再按 `0.1 m/rad` 折算到共同运动尺度。

关系信息权重要求参考系前后周期均可见且可靠，同时机器人存在实际动作响应。静止、缺少上一帧、不可见或跟踪不可靠时不会把近零相对残差当作 linked 证据。

## 3. 动作后名义进度先验

`ProgressPriorBuilder` 从上一周期 `beta` 构造 `bar_beta`。默认转移质量为：

```text
未完全完成/原状态：0.20
正常后继：          0.65
稍微提前完成：      0.15
```

局部候选默认覆盖名义状态前1、后2个状态。技能末端在边界未放行时停留于当前技能末端；边界已由外部许可时只进入下一技能首状态，不在一次转移中连续跨越多个技能。

## 4. 在线关系滤波

`RelationFilter` 对每个物理 arm-frame 关系只维护：

```text
[P(external), P(linked)]
```

单周期更新依次组合：

1. external/linked 持久转移先验；
2. 由 `bar_beta` 加权的阶段一示范关系先验；
3. 由当前相对运动残差、实际末端运动、上一命令响应和夹爪弱上下文得到的动作条件化观测似然。

夹爪只在存在运动信息时提供弱似然偏置，不能单独决定 linked。`Unknown` 是二元后验之上的决策状态，不是第三种关系概率；动作激励不足、不可见、跟踪不可靠、后验熵过高或最大概率不足时输出 `Unknown`，同时保留归一化二元后验供以后继续更新。

`RelationChange` 只报告前后均为稳定二元判定的 external↔linked 变化；中间的 Unknown 不被误报为 LINK/UNLINK。

## 5. 候选状态统一评分

`StateEvaluator` 对每个候选状态分别计算并保存：

- 机器人局部轨迹支持；
- 稀疏场景状态支持；
- 当前关系后验与示范关系先验的兼容度。

物理轨迹流的参与权重为可见性、跟踪可靠性、非 Unknown 门控和 `P(external)` 的乘积；linked 流因此不会凭极小协方差独占进度判断。虚拟技能帧只作为固定执行参考系，按可靠性参与轨迹匹配，不进入关系滤波。

机器人轨迹和连续高斯场景因子在线使用峰值归一化支持：

$$
\widetilde p(x\mid s)
=
\frac{p(x\mid s)}{p(\mu_s\mid s)}
=
\exp\left(-\frac12d_M^2(x,s)\right).
$$

这保留协方差对偏离方向和尺度的约束，但删除不同状态之间由高斯峰值高度产生的 `logdet` 偏置。未显式选择 mode 时，先在各可用 mode 内计算归一化支持，再按该状态 mode 先验混合。

完整高斯 `log p` 不参与进度后验，但仍逐候选、逐帧/场景因子、逐 mode 保存以下审计项：

```text
raw_log_likelihood
normalized_log_support
mahalanobis_squared
covariance_log_determinant
dimension
mode_weight
```

external/linked 关系兼容度继续计算当前 `q_t` 与离线关系先验的内积，不采用连续高斯归一化口径。

## 6. 进度后验与状态输出

`ProgressFilter` 在 log-space 中实现：

$$
\beta_t(s)
\propto
\bar\beta_t(s)
\widetilde L_t^{\mathrm{robot}}(s)
[\widetilde L_t^{\mathrm{state}}(s)]^{\lambda_x}
[C_t^{\mathrm{rel}}(s)]^{\lambda_r}.
$$

`ProgressEstimate` 保存先验、后验、名义状态、估计状态、置信度、熵、最佳解释支持度和以下状态：

```text
ALIGNED
FORWARD_REALIGNMENT
BACKWARD_REALIGNMENT
LOW_CONFIDENCE
NO_PLAUSIBLE_STATE
```

当全部候选均低于最低解释支持度时，后验保留动作后名义先验并报告 `NO_PLAUSIBLE_STATE`，不把全部近零似然强行归一化为虚假高置信结果。本阶段不据此冻结任务时钟；阶段三读取该状态后再决定 HOLD、REALIGN 或 ADVANCE。

## 7. 关系事件感知候选扩展与单周期更新

默认进度推断只检查局部候选。当出现可靠 external↔linked 变化且局部候选均无法解释观测时，`BeliefUpdater` 按时间顺序查询未来关系兼容状态段；只保留机器人轨迹、关系和状态要求的场景因子全部达到支持阈值的状态。未放行技能边界不能跨越。

该扩展允许动作提前完成后对齐到最近的未来正常状态，同时要求真实机器人和场景状态共同匹配，因此搬运中掉落不会只凭 external 关系跳到未来释放状态。

每个递增 tick 严格只执行一次：

```text
progress prior
→ relation posterior
→ progress posterior
```

重复或倒序 tick 直接拒绝。`ClosedLoopBelief` 保存共享运行特征、全部关系估计、进度估计、关系变化、局部候选、扩展候选和所有候选的分项评分。

## 8. 最终代码与配置

阶段二新增文件：

```text
source/policy/closed_loop/
├── runtime_observation.py
├── runtime_features.py
├── progress_prior.py
├── relation_filter.py
├── state_evaluator.py
├── progress_filter.py
└── belief_updater.py

tests/closed_loop/
├── test_phase2_belief_inference.py
└── test_phase2_rlbench_replay.py

configs/closed_loop_belief.json
```

配置文件与 `BeliefUpdaterConfig` 默认值逐字段往返校验；阶段二模块通过 `essay2608.policy.closed_loop` 和 `essay2608.policy` 公共导出。

## 9. 验收结果

开发计划中阶段二12项验收标准均有自动化覆盖：

- 同步共动使 linked 后验上升，机械臂单独运动使 external 后验上升；
- 双方静止、不可见、跟踪不可靠或缺少上一帧时输出 Unknown；
- 名义先验集中于正常后继，并严格服从边界许可；
- 进度后验支持向前和向后校正；
- linked 轨迹流按 `P(external)` 抑制；
- 正常提前释放能够扩展到未来匹配段，搬运中掉落不能仅凭关系跳到释放段；
- 单周期更新顺序和逐候选高斯/关系分项审计通过；
- StackWine 五条正常 V4 示范旁路回放通过；
- HandOver 右臂正常示范第0条旁路回放平均绝对状态偏差为 `0.295`、最大偏差为 `2`、精确对齐率为 `73.18%`，且 `NO_PLAUSIBLE_STATE=0`；
- 阶段二单元测试12项、真实数据回放2项均通过；
- 全仓回归：`468 passed, 4 skipped in 184.57s`；
- Black、Python 编译和 `git diff --check` 通过。

未启动训练或 RLBench 仿真，没有改写冻结 V4 数据、模型或正式结果。阶段二实现停留在旁路信念推断边界并已通过确认；阶段三只读取其输出，不反向改变阶段二的单周期更新语义。

# 阶段三：动态流角色与闭环正常执行控制

## 1. 最终目标与阶段边界

阶段三将阶段二输出的 `ClosedLoopBelief` 接入正常动作生成和技能内任务时间控制，建立：

```text
关系—进度信念
→ 动态流角色
→ 运行期精度权重
→ HOLD / REALIGN / ADVANCE
→ reference_state 动作查询
```

本阶段只实现 `TASK` 模式下的正常执行控制。跨技能入口守卫和多臂事务属于阶段四；`VERIFY_LINK` 移动、LINK/UNLINK 恢复及任务重入属于阶段五。本阶段可以输出恢复意图和主动关系验证请求，但不执行相应动作。

## 2. 动态流角色

`FrameRoleRouter` 为当前 `StateId`、当前轨迹 mode 中已经通过离线任务参数选择的流分配动作角色；此外，当前进度具有正式 `link_origin` 的物理关系即使对应专家已不再被当前 mode 选择，也继续作为零动作权重关系条件接受 `MONITOR/RECOVER/DEFER` 路由。普通未选流不参与本周期动作，也不会因其关系 Unknown 阻断推进；虚拟技能帧保持固定 `EXECUTE` 参考系，不进入 external/linked 关系空间。

物理流的控制期望关系按进度后验加权：

$$
\pi_{f,t}^{\mathrm{ctrl}}(z)
=
\sum_s\beta_t(s)\pi_{f,s}^{\mathrm{demo}}(z).
$$

当最大期望关系概率达到 `0.7` 时，形成 external 或 linked 期望，否则期望关系为 Unknown。最终路由为：

| 期望关系 | 在线关系 | 角色 | 正常执行作用 |
|---|---|---|---|
| external | external | `EXECUTE` | 以连续正权重参与 PoE |
| linked | linked | `MONITOR` | PoE 权重为0，关系滤波继续观测 |
| 可靠 external/linked | 可靠相反关系 | `RECOVER` | 权重为0、阻断推进、输出恢复意图 |
| 任一关键关系 Unknown | 任意 | `DEFER` | 阻断推进并保持上一可信执行权重 |

`DEFER` 不把 Unknown 强制解释成 external 或 linked。若已选流没有可信历史，执行权重为0；若之前可靠 external，则保留当时的连续执行权重；若之前可靠 linked，则保留0。关系滤波持续观测全部建模物理参考系，路由器缓存未选流的稳定关系权重，使新技能首次选中某一流时可以读取此前可信证据。正式 linked 的未选关系可阻断或产生恢复意图，但始终标记 `selected_offline=False`、执行权重为0且不进入动作查询，不能借监控重新启用 PoE 专家。

## 3. 运行期精度权重

`EXECUTE` 物理流使用：

$$
w_{f,t}
=
v_{f,t}q_t^f(\mathrm{external}),
$$

虚拟流使用其观测可靠性；`MONITOR/RECOVER` 权重为0；`DEFER` 使用上述可信历史规则。`WeightedPoEExecutor` 将权重传入现有 DynaMAC 指定状态查询：

$$
\widetilde\Lambda_{f,t}=w_{f,t}\Lambda_{f,t}.
$$

权重只缩放协方差对应的精度，不修改专家均值。显式全1权重与冻结 PoE 数值一致，零权重等价于移除专家。离线 Eq.6 未选择的模态流不能被运行权重重新启用；所有当前执行权重为0时返回 `action=None` 并 HOLD，不采用无审计回退。

## 4. 闭环任务游标与统一提交

`ClosedLoopCursor` 明确分离：

```text
nominal_state   动作后先验认为应该达到的状态
estimated_state 当前观测推断的实际状态
reference_state 当前真正用于查询动作的状态
```

`ClosedLoopExecutionController` 是正常执行进度变更的唯一提交入口。角色路由和加权 PoE 查询均不能直接修改游标，同一 tick 不能重复提交。

- `HOLD`：当前目标未完成、进度低置信/无可解释状态、关键关系 Unknown、可靠关系失配、后继未就绪或候选越过技能边界时保持旧引用，并继续查询旧 `reference_state` 进行伺服；
- `REALIGN`：后验可信且估计状态与名义状态不一致，或出现可信的非直接后继索引修正时，直接把引用改到 `estimated_state`；不依次反向执行中间状态，也不推进基础 DynaMAC 时钟；
- `ADVANCE`：当旧引用为 $s$，且 `nominal_state == estimated_state == succ(s)`，同时该状态是同技能直接后继时，把引用提交为这个已经估计到的后继；不再对 `estimated_state` 额外执行一次 `succ()`，每周期最多推进一次。

若拟议新引用的角色阻断或没有正权重执行流，控制器撤销该提交并继续查询旧引用。跨技能状态统一 HOLD 并标记 `skill_boundary_requires_guard`，等待阶段四放行。

## 5. 正常状态失配累计

`MismatchTracker` 独立累计：

- `NO_PLAUSIBLE_STATE`；
- `RELATION_MISMATCH`；
- `PERSISTENT_HOLD`；
- `STALLED_PROGRESS`。

默认连续周期阈值分别为 `3 / 3 / 20 / 20`。短暂异常只更新计数，不发布事件；达到阈值后只发布一次 `MismatchEvent`，条件恢复后计数和发布门闩复位。本阶段的事件只包含状态、持续周期、相关参考系及恢复意图，不调用任何恢复轨迹。

## 6. LINK_PENDING 主动验证请求

`RelationVerificationRequest` 只在以下条件同时满足时生成：

- 当前路由/估计事件上下文命中阶段一持久化的 `LinkPendingCandidate`，且 mode 一致；
- 该物理参考系是当前状态、当前 mode 的离线已选流；
- 后续正常状态或当前技能的下一边界明确要求该关系为 linked；
- 当前在线关系决策为 Unknown；
- 当前和上一周期都具有该参考系观测，配对跟踪可靠性不低于阈值；
- 关系信息权重低于阈值，即当前 Unknown 确实对应动作激励不足。

终止 Pending 若后续没有 linked 必要性，不生成请求。阶段二 `RuntimeFeatures` 增加 `frame_pair_available` 和 `paired_tracking_reliability` 以复用已有观测信息；没有新增 Unknown 原因枚举。阶段三只输出请求并保持正常推进，不执行探测移动、不激活 Pending 模板，也不回写离线模型。

## 7. 最终代码与配置

阶段三新增：

```text
source/policy/closed_loop/
├── frame_roles.py
├── weighted_poe.py
├── execution_cursor.py
├── execution_controller.py
└── mismatch.py

tests/closed_loop/
├── test_phase3_execution_control.py
└── test_phase3_rlbench_replay.py

configs/closed_loop_execution.json
```

阶段二的 `runtime_features.py` 只增加当前/上一观测的成对可用性和成对可靠性输出，没有改变关系或进度更新公式。阶段三类型均通过 `essay2608.policy.closed_loop` 和 `essay2608.policy` 公共导出。

## 8. 验收结果

开发计划中阶段三13项验收标准均有自动化覆盖：

- 四种角色及其 PoE 参与、监测、阻断和意图语义通过；
- Unknown 单帧不会抹除可信历史权重，无历史时不会自行启用专家；
- 全1/零精度权重分别与冻结 PoE一致/等价于移除专家，专家均值不变；
- HOLD 持续查询当前引用，REALIGN 只作索引跳转，ADVANCE 只提交一个直接后继；
- 关系失配阻断推进，持续证据达到门槛后才产生失配事件；
- 路由器不改任务游标，统一控制器拒绝同 tick 重复提交；
- Pending 在可见可靠但激励不足且后续确需 linked 时生成验证请求，终止无依赖 Pending 不生成；
- StackWine V4 正常示范只读回放中无恢复误报、全部状态查询动作可用，并观测到新选流 Unknown 时复用此前可信正权重；
- 阶段三专项测试14项全部通过；
- 阶段一、二、三相关专项共51项全部通过；
- 全仓回归：`483 passed, 4 skipped in 199.71s`；
- Black、Python 编译、阶段三定向 mypy 和 `git diff --check` 通过。

未启动训练或 RLBench 仿真，没有改写冻结 V4 数据、模型或正式结果。阶段三实现严格停止在正常执行、请求和意图输出边界，等待确认后再进入阶段四。

## 9. 组件级 A/B 补充评测

在冻结 V4 的 8 个任务、12 套机械臂模型和每任务 5 条正常示范上运行确定性离线反事实组件 A/B。当前正式 v3 包含 300 组进度试验、190 组关系试验和 11890 条逐周期记录。

- 卡顿扰动下，进度 StateId MAE 相对固定时钟降低 50.39%；跳帧扰动下降低 26.16%。
- 同步正常回放中，固定时钟 MAE 为0，在线估计 MAE 为0.8198；StoreBottle 左臂仍有高比例 `NO_PLAUSIBLE_STATE`，StoreBottle 右臂和 WipeDesk 仍显示任务特定偏差。
- 50 组伪连接扰动全部被动态角色检测、将原活跃专家权重置0并阻断推进；50 组对照无恢复误报。
- 断连候选严格限定在正式 `link_origin` 区间，排除 Pending 和孤立逐状态 linked 脉冲。45 组断连扰动全部被检测，平均推进阻断率98.70%；45组对照无恢复误报。
- 正式 linked 关系在后续技能中即使对应动作专家已未选中，仍作为零权重关系条件接受监控；这不会重新启用 Eq.6 已关闭的 PoE 流。断连方向证明的是持久关系保护，伪连接方向同时证明活跃专家抑制。
- 正常关系对照仍有少量保守 `DEFER/HOLD`：断连对照平均阻断率4.07%，伪连接对照1.00%，但均未形成 `RECOVER` 误报。

v1/v2 的断连候选基于“当前已选流的逐状态 linked 证据”，没有要求正式事件来源，因此其3组断连负结果不再作为最终机制结论；结果文件仍完整保留用于审计。v3 采用事件连续的软先验和正式来源区间，未使用故障结果调节在线阈值。

该结果支持时间失步下的总体进度收益，以及伪连接和正式断连下的动态关系角色增益；仍不支持独立测试集泛化、实际恢复成功率或完整任务成功率。完整协议、逐试验数据、逐周期轨迹、图表、环境/哈希信息及结论边界保存于 `evaluations/phase23_component_ab/`。
